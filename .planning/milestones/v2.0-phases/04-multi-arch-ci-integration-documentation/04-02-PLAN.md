---
phase: 04-multi-arch-ci-integration-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - argus/DOCS.md
autonomous: true
requirements: [DOCS-01]
must_haves:
  truths:
    - "argus/DOCS.md exists in the add-on folder (same dir as config.yaml) so it renders in the HA Documentation tab"
    - "DOCS.md documents installing Argus from a custom repository in the HA add-on store"
    - "DOCS.md documents the Mosquitto broker add-on as a prerequisite and explains the add-on fails loudly without it"
    - "DOCS.md has a configuration reference covering every config.yaml schema field"
    - "DOCS.md has a troubleshooting section pointing to the Argus health binary_sensor entity"
    - "argus/icon.png is present (verified, NOT regenerated) — DOCS-01 icon requirement already satisfied"
    - "[UAT — live, NOT executor-verifiable] DOCS.md and icon.png render correctly in the HA add-on Documentation tab on a live HA OS"
    - "[UAT — live, NOT executor-verifiable] An anomaly appears in HA as a binary_sensor + score sensor within 2 seconds of state_changed on a live HA OS install (end-to-end)"
  artifacts:
    - path: "argus/DOCS.md"
      provides: "HA add-on documentation: install, config reference, Mosquitto prereq, troubleshooting"
      contains: "## Prerequisites"
      min_lines: 60
    - path: "argus/icon.png"
      provides: "Add-on store icon (pre-existing — verify only)"
  key_links:
    - from: "argus/DOCS.md"
      to: "argus/config.yaml"
      via: "configuration reference documents every schema field"
      pattern: "detector_endpoint"
    - from: "argus/DOCS.md"
      to: "binary_sensor.argus_addon_health"
      via: "troubleshooting points to the Argus health entity"
      pattern: "argus_addon_health"
---

<objective>
Write `argus/DOCS.md` — the English documentation rendered in the HA add-on Documentation tab —
covering install from a custom repository, the Mosquitto prerequisite, a full configuration
reference for every schema field, and a troubleshooting section pointing at the Argus health entity.
Verify the pre-existing `argus/icon.png` (do NOT regenerate).

Purpose: DOCS-01. The icon half of DOCS-01 is already satisfied (icon.png present); this plan
delivers the DOCS.md half. The live render check and the 2-second end-to-end anomaly test are
Phase 4 UAT items, not executor tasks.

Output: `argus/DOCS.md`.
</objective>

<execution_context>
@$HOME/.claude-work-work/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-work-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-arch-ci-integration-documentation/04-RESEARCH.md
@.planning/phases/04-multi-arch-ci-integration-documentation/04-CONTEXT.md

<read_first>
- argus/config.yaml — the authoritative `schema:` block. EVERY field documented in DOCS.md must match a key here (entities, include_patterns, exclude_patterns, influx_url, influx_token, influx_org, influx_bucket, influx_measurement, influx_value_field, detector_endpoint, batch_interval_minutes, nightly_fit_hour, log_level). Also note defaults from the `options:` block.
- argus/translations/en.yaml — human-readable name + description per field; reuse this wording (do not contradict it).
</read_first>

<interfaces>
<!-- Authoritative config schema from argus/config.yaml. DOCS.md config reference must cover all of these. -->

Schema fields (config.yaml `schema:`) with defaults (config.yaml `options:`):
- entities: list(str) — default []
- include_patterns: list(str) — default []
- exclude_patterns: list(str) — default []
- influx_url: url? (optional; empty disables batch path)
- influx_token: password? (optional)
- influx_org: str? (optional)
- influx_bucket: str? (optional)
- influx_measurement: str — default "homeassistant"
- influx_value_field: str — default "value"
- detector_endpoint: str? (optional; empty = bundled local detector)
- batch_interval_minutes: int(1,1440) — default 10
- nightly_fit_hour: int(0,23) — default 2
- log_level: list(debug|info|warning) — default "info"

Health entity (from Phase 3, SUMMARY 03-03): friendly name "Argus — status",
binary_sensor with object_id/unique_id `argus_addon_health`, device_class problem,
OFF = healthy, ON = problem. Troubleshooting must reference it. NOTE the actual entity
id in HA Developer Tools → States will be `binary_sensor.argus_addon_health`.

Supervisor facts (config.yaml): `services: [mqtt:need]` (MQTT credentials auto-fetched;
add-on fails loudly with no MQTT service), `homeassistant_api: true` (no HA URL/token entered
by the user). Repo URL: https://github.com/krzyl2/argus.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write DOCS.md — intro, prerequisites, installation</name>
  <files>argus/DOCS.md</files>
  <read_first>
    - argus/config.yaml (services: mqtt:need, url field)
    - .planning/phases/04-multi-arch-ci-integration-documentation/04-RESEARCH.md "DOCS.md Conventions" section (location, section structure, Mosquitto prerequisite)
  </read_first>
  <action>
    Create `argus/DOCS.md` (English, standard Markdown only — HA Documentation tab does not support JSX).

    `# Argus Anomaly Detection` title + a 2-3 sentence intro: streaming + batch anomaly detection
    for HA environmental sensors; surfaces anomalies back into HA as auto-created binary_sensor
    (flag) and sensor (score) entities via MQTT discovery; self-hosted, no cloud.

    `## Prerequisites` section (RESEARCH "Mosquitto Add-on Prerequisite"):
      - The official **Mosquitto broker** add-on (slug `core_mosquitto`) must be installed and
        running BEFORE starting Argus.
      - Explain why: the add-on declares `services: [mqtt:need]` and fetches MQTT host/user/password
        automatically from the Supervisor. If no MQTT service is registered, the Supervisor fails
        Argus at start (exit non-zero) — the user must never enter MQTT credentials manually.
      - Note that HA authentication is automatic via the Supervisor (`homeassistant_api: true`) —
        no HA URL or long-lived token is required.
      - Note InfluxDB is optional (only needed for the batch detection path).

    `## Installation` section (numbered steps, RESEARCH section structure):
      1. HA → Settings → Add-ons → Add-on Store → ⋮ (top right) → Repositories → paste
         `https://github.com/krzyl2/argus` → Add.
      2. Find "Argus Anomaly Detection" in the store and click Install.
      3. Open the Configuration tab and set monitored entities / InfluxDB (see Configuration below).
      4. Start the add-on. On first start it logs the numeric HA sensors it discovered so you can copy entity_ids.

    Leave the Configuration and Troubleshooting sections for Task 2 (write a placeholder
    `## Configuration` and `## Troubleshooting` heading so the file structure is in place, or
    simply stop here and let Task 2 append — either is fine, but keep headings consistent).
  </action>
  <verify>
    <automated>grep -q "## Prerequisites" argus/DOCS.md && grep -q "## Installation" argus/DOCS.md && grep -q "core_mosquitto" argus/DOCS.md && grep -qi "krzyl2/argus" argus/DOCS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    DOCS.md exists at argus/DOCS.md with a title + intro, a `## Prerequisites` section naming the
    Mosquitto broker add-on (`core_mosquitto`) and explaining the mqtt:need fail-loud behavior plus
    automatic HA auth, and a `## Installation` section with the custom-repository steps including the
    repo URL.
  </acceptance_criteria>
  <done>Intro, Prerequisites (Mosquitto), and Installation sections present and accurate.</done>
</task>

<task type="auto">
  <name>Task 2: Write DOCS.md — configuration reference + troubleshooting; verify icon.png</name>
  <files>argus/DOCS.md</files>
  <read_first>
    - argus/config.yaml (schema + options defaults — authoritative)
    - argus/translations/en.yaml (field descriptions)
  </read_first>
  <action>
    Append to `argus/DOCS.md`.

    `## Configuration` section — one H3 (`### \`field\``) per schema field, in this order, each with a
    one-paragraph description, accepted values/type, and **Default:** line. Cover ALL fields (match
    config.yaml exactly — see <interfaces>): entities, include_patterns, exclude_patterns, influx_url,
    influx_token, influx_org, influx_bucket, influx_measurement, influx_value_field, detector_endpoint,
    batch_interval_minutes, nightly_fit_hour, log_level. Reuse the wording from translations/en.yaml.
    Call out the optional/disabling semantics explicitly:
      - influx_url empty → batch path disabled, no error (UICFG-02).
      - detector_endpoint empty → bundled local detector runs; set to `https://host:50051` for a remote detector (UICFG-03).
      - entities vs include_patterns/exclude_patterns as alternative selection methods (UICFG-06).
    For `log_level` use the table style from RESEARCH "Configuration Reference Format" (debug/info/warning).

    `## Troubleshooting` section:
      - Point to the Argus health entity: check `binary_sensor.argus_addon_health` ("Argus — status")
        in Developer Tools → States. OFF = healthy; ON = a problem (detector not SERVING, HA
        disconnected, or MQTT disconnected).
      - "No entities appear": confirm Mosquitto is installed/running and that monitored entity_ids
        are numeric sensors (copy them from the first-start discovery log).
      - "Add-on won't start": check that the Mosquitto broker add-on is installed (mqtt:need).
      - Where to read logs: the add-on Log tab.

    `## Support` section: link to GitHub issues at https://github.com/krzyl2/argus/issues.

    Then VERIFY (do not regenerate) that `argus/icon.png` exists — DOCS-01's icon requirement is
    already satisfied. Do not create or modify icon.png.
  </action>
  <verify>
    <automated>test -f argus/icon.png && grep -q "## Configuration" argus/DOCS.md && grep -q "argus_addon_health" argus/DOCS.md && for f in entities include_patterns exclude_patterns influx_url influx_token influx_org influx_bucket influx_measurement influx_value_field detector_endpoint batch_interval_minutes nightly_fit_hour log_level; do grep -q "$f" argus/DOCS.md || { echo "MISSING field: $f"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    DOCS.md has a `## Configuration` section documenting all 13 schema fields with defaults and
    optional/disabling semantics, a `## Troubleshooting` section referencing
    `binary_sensor.argus_addon_health`, and a `## Support` section. `argus/icon.png` confirmed present
    (unchanged).
  </acceptance_criteria>
  <done>Full configuration reference (all fields), troubleshooting (health entity), and support sections present; icon.png verified present and untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → user actions | DOCS.md instructs the operator on credential-free setup; wrong guidance could lead a user to enter secrets manually |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Information Disclosure | Manual MQTT/HA credential entry | mitigate | DOCS.md explicitly states MQTT and HA credentials are auto-fetched and must NOT be entered manually, preventing a user from pasting secrets into config. |
| T-04-06 | Repudiation | Stale config reference | accept | DOCS.md config reference is sourced directly from config.yaml schema; drift risk accepted (single maintainer, schema changes will update docs in the same PR). |
</threat_model>

<verification>
- argus/DOCS.md exists with sections: intro, `## Prerequisites`, `## Installation`, `## Configuration`, `## Troubleshooting`, `## Support`.
- Every config.yaml schema field appears in the configuration reference.
- Mosquitto prerequisite (`core_mosquitto`, mqtt:need fail-loud) documented.
- Troubleshooting references `binary_sensor.argus_addon_health`.
- argus/icon.png present and unchanged (not regenerated).
</verification>

<success_criteria>
DOCS-01 automatable surface complete: argus/DOCS.md ships install-from-custom-repo steps, the
Mosquitto prerequisite, a full configuration reference, and troubleshooting via the Argus health
entity; icon.png confirmed present. Live render in the HA Documentation tab and the end-to-end
2-second anomaly test remain Phase 4 UAT items.
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-arch-ci-integration-documentation/04-02-SUMMARY.md`
</output>
