---
phase: 01-add-on-skeleton-config-gen
plan: 03
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - argus/Dockerfile
  - deploy/image-facts.sh
autonomous: true
requirements: [ADDON-03, ADDON-05]
user_setup: []

must_haves:
  truths:
    - "argus/Dockerfile uses ARG BUILD_FROM defaulting to ghcr.io/home-assistant/base-debian:bookworm (Debian, never Alpine)"
    - "The Dockerfile installs the detector requirements with pip --prefer-binary and installs pyyaml so gen-entities.py can run in the image"
    - "The Dockerfile installs darts/torch nowhere — no extras that pull PyTorch (ADDON-05)"
    - "The Dockerfile sets S6_BEHAVIOUR_IF_STAGE2_FAILS=2 and adds no CMD/ENTRYPOINT (base image owns /init)"
    - "deploy/image-facts.sh asserts no torch in the built image, glibc linkage, and compressed size < 2 GB"
  artifacts:
    - path: "argus/Dockerfile"
      provides: "Single-arch-parametric add-on image build recipe"
      contains: "ghcr.io/home-assistant/base-debian:bookworm"
    - path: "deploy/image-facts.sh"
      provides: "Reusable image-fact gate (static Dockerfile checks + built-image torch/size/glibc assertions)"
      contains: "import torch"
  key_links:
    - from: "argus/Dockerfile"
      to: "argus/rootfs/ (plan 02 scripts)"
      via: "COPY argus/rootfs/ / brings cont-init.d + gen-entities.py into the image"
      pattern: "COPY argus/rootfs"
    - from: "argus/Dockerfile"
      to: "detector/requirements.txt"
      via: "COPY + pip install --prefer-binary"
      pattern: "requirements.txt"
---

<objective>
Author the single add-on `Dockerfile` on the Debian bookworm base (folding the existing deploy/ orchestrator + detector recipes into one image) and a reusable `image-facts.sh` gate that proves the image is Debian/glibc, torch-free, and under 2 GB. No services are wired yet (Phase 3); this plan only proves the image builds and meets the ADDON-03/ADDON-05 facts.

Purpose: ADDON-03 (Debian bookworm base, never Alpine), ADDON-05 (no PyTorch, < 2 GB compressed).
Output: argus/Dockerfile, deploy/image-facts.sh.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-add-on-skeleton-config-gen/01-RESEARCH.md
@.planning/phases/01-add-on-skeleton-config-gen/01-CONTEXT.md
@detector/requirements.txt
@deploy/Dockerfile.orchestrator
@deploy/Dockerfile.detector
</context>

<artifacts_produced>
- `argus/Dockerfile` — `ARG BUILD_FROM=ghcr.io/home-assistant/base-debian:bookworm`; `FROM ${BUILD_FROM}`; `ENV S6_BEHAVIOUR_IF_STAGE2_FAILS=2`; .NET 8 runtime install (Microsoft Debian 12 feed); python3.12 + pip; `pip install --prefer-binary -r detector/requirements.txt` + `pip install pyyaml`; `COPY orchestrator/publish/ /opt/argus/orchestrator/`; `COPY detector/ /opt/argus/detector/`; `COPY argus/rootfs/ /`; io.hass LABELs; no CMD/ENTRYPOINT.
- `deploy/image-facts.sh` — modes: `static` (grep-assert Dockerfile directives) and `image <tag>` (build-time facts: `python3 -c "import torch"` must fail, compressed size < 2 GB, glibc linkage of orchestrator dll when present). Reused by Phase 4 CI.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: argus/Dockerfile on Debian bookworm base</name>
  <files>argus/Dockerfile</files>
  <read_first>
    - .planning/phases/01-add-on-skeleton-config-gen/01-RESEARCH.md (section "Dockerfile Skeleton" + "Notes for planner")
    - deploy/Dockerfile.orchestrator, deploy/Dockerfile.detector (existing recipes to fold)
    - detector/requirements.txt (no darts/torch present — confirm; pyyaml is absent and must be added)
  </read_first>
  <action>
    Create `argus/Dockerfile` matching the research skeleton. `ARG BUILD_FROM=ghcr.io/home-assistant/base-debian:bookworm` then `FROM ${BUILD_FROM}` (ADDON-03 — Debian, never Alpine; the composable GHA build-image action substitutes the arch-specific base). `ENV S6_BEHAVIOUR_IF_STAGE2_FAILS=2` (exit on service crash, not silent loop).

    Install .NET 8 runtime (not SDK) from the Microsoft Debian 12 package feed (packages-microsoft-prod.deb → dotnet-runtime-8.0), clean apt lists. Install python3.12, python3-pip, python3.12-venv. Then `COPY detector/requirements.txt /tmp/requirements.txt` and `pip3 install --no-cache-dir --prefer-binary -r /tmp/requirements.txt` (--prefer-binary is mandatory for aarch64 to avoid scipy/statsmodels source builds). Add a separate `pip3 install --no-cache-dir pyyaml` line — PyYAML is NOT in detector/requirements.txt but gen-entities.py imports it (note this with a comment explaining why the explicit install exists).

    `COPY orchestrator/publish/ /opt/argus/orchestrator/`, `COPY detector/ /opt/argus/detector/`, `COPY argus/rootfs/ /` (brings plan 02's cont-init.d + gen-entities.py into the image). Add the io.hass LABEL block (name, description, arch from ARG BUILD_ARCH, type addon, version from ARG BUILD_VERSION). Do NOT add CMD or ENTRYPOINT — the base image owns `/init`. Do NOT add any `darts[...]` extras and do NOT install torch (ADDON-05).
  </action>
  <verify>
    <automated>python -c "s=open('argus/Dockerfile').read(); assert 'ghcr.io/home-assistant/base-debian:bookworm' in s; assert 'S6_BEHAVIOUR_IF_STAGE2_FAILS=2' in s; assert '--prefer-binary' in s; assert 'pyyaml' in s.lower(); assert 'COPY argus/rootfs' in s; assert 'dotnet-runtime-8.0' in s; import re; assert not re.search(r'(?im)^\\s*(CMD|ENTRYPOINT)\\b', s), 'no CMD/ENTRYPOINT allowed'; assert 'darts[' not in s and 'torch' not in s.lower(); assert 'alpine' not in s.lower(); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Dockerfile references base-debian:bookworm, sets S6_BEHAVIOUR_IF_STAGE2_FAILS=2, installs dotnet-runtime-8.0, installs deps with --prefer-binary, installs pyyaml, COPYs argus/rootfs/.
    - No `CMD`/`ENTRYPOINT` line; no `torch`, no `darts[`, no `alpine`.
    - Assertion command prints OK and exits 0.
  </acceptance_criteria>
  <done>argus/Dockerfile is a Debian-bookworm, torch-free, s6-fail-loud build recipe that folds in the detector deps + pyyaml and copies the rootfs.</done>
</task>

<task type="auto">
  <name>Task 2: image-facts.sh static + built-image gate</name>
  <files>deploy/image-facts.sh</files>
  <read_first>
    - argus/Dockerfile (just authored — the directives the static mode asserts)
    - .planning/phases/01-add-on-skeleton-config-gen/01-RESEARCH.md (sections "Image fact assertions" + Open Question 2 on torch)
  </read_first>
  <action>
    Create `deploy/image-facts.sh` (`set -euo pipefail`) with two modes selected by the first arg.

    `static` (always runnable, no Docker): grep-assert argus/Dockerfile contains base-debian:bookworm, S6_BEHAVIOUR_IF_STAGE2_FAILS=2, --prefer-binary, pyyaml, no torch, no `darts[`, no CMD/ENTRYPOINT. Exit non-zero with a clear message on any failure.

    `image <tag>`: run the built-image facts (the real ADDON-05 gate, reused by Phase 4 CI). Assert `docker run --rm <tag> python3 -c "import torch"` FAILS (torch absent → command exits non-zero → that is the pass condition; fail the script loudly if import succeeds). Compute compressed size from `docker manifest inspect <tag>` layer sizes (or `docker image inspect` size as a local fallback) and assert < 2 GB. If `/opt/argus/orchestrator/Argus.Orchestrator.dll` exists in the image, run `ldd` on it and assert glibc linkage (linux-vdso/libc present); if the dll is absent (Phase 1 has no real orchestrator publish output yet), print a clear SKIPPED notice for the glibc check rather than failing — the full glibc assertion runs in Phase 4 CI with real publish output.

    For Phase 1 local verification the orchestrator publish dir may not exist; document at the top of the script that callers may create a throwaway `orchestrator/publish/` stub so `docker build` succeeds, and that the torch/size assertions are valid even with a stub (they exercise the python layer, not the orchestrator binary). Make `static` the default mode when no arg is given.
  </action>
  <verify>
    <automated>bash -n deploy/image-facts.sh && bash deploy/image-facts.sh static && echo GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n deploy/image-facts.sh` reports no syntax errors.
    - `bash deploy/image-facts.sh static` exits 0 against the authored Dockerfile and prints a pass indicator; GATE_OK echoes.
    - `image <tag>` mode encodes the torch-absent, size<2GB, and conditional-glibc assertions (verified by reading the script; full build runs in CI / locally when Docker is present).
  </acceptance_criteria>
  <done>deploy/image-facts.sh passes static mode against the Dockerfile and carries the built-image ADDON-05 assertions for CI reuse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public package feeds → image | Microsoft .NET feed, PyPI (pip), base image layers are pulled into the shipped artifact |
| Build args → image labels | BUILD_FROM / BUILD_ARCH / BUILD_VERSION injected by the composable GHA action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-08 | Tampering | base image substitution via ARG BUILD_FROM | mitigate | Default pinned to ghcr.io/home-assistant/base-debian:bookworm (official HA org); static gate asserts the default is the HA Debian base, never Alpine |
| T-1-09 | Denial of Service | torch pulled transitively inflates image past 2 GB | mitigate | No darts extras; image-facts.sh asserts `import torch` fails and compressed size < 2 GB (ADDON-05) |
| T-1-SC | Tampering | pip/apt installs (dotnet-runtime-8.0, detector requirements, pyyaml) | mitigate | .NET runtime from Microsoft's signed Debian feed; detector/requirements.txt is pre-vetted (shipped in v1.0); pyyaml is a top-tier BSD-licensed PyPI package. All versions resolved by --prefer-binary against PyPI; image-facts gate confirms no torch slipped in |
</threat_model>

<verification>
- `bash -n deploy/image-facts.sh` + `bash deploy/image-facts.sh static` pass.
- Static Dockerfile assertions pass.
- Live/CI (Docker present, optional stub orchestrator/publish): `docker build -f argus/Dockerfile -t argus:test .` succeeds; `bash deploy/image-facts.sh image argus:test` confirms no torch and size < 2 GB.
</verification>

<success_criteria>
- ADDON-03: image built on ghcr.io/home-assistant/base-debian:bookworm (Debian, never Alpine).
- ADDON-05: no PyTorch in the image; compressed size < 2 GB; gate enforces both.
- All automated checks pass.
</success_criteria>

<output>
Create `.planning/phases/01-add-on-skeleton-config-gen/01-03-SUMMARY.md` when done.
</output>
