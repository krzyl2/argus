---
phase: 03-process-supervision-runtime-integration
plan: 03
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - orchestrator/Argus.Orchestrator/Mqtt/DiscoveryPublisher.cs
  - orchestrator/Argus.Orchestrator/Mqtt/HealthEvaluator.cs
  - orchestrator/Argus.Orchestrator/Health/ArgusHealthSignals.cs
  - orchestrator/Argus.Orchestrator/Workers/HealthPublisherWorker.cs
  - orchestrator/Argus.Orchestrator/Mqtt/MqttConnection.cs
  - orchestrator/Argus.Orchestrator/Ha/NetDaemonHaEventSource.cs
  - orchestrator/Argus.Orchestrator/Logging/LogEvents.cs
  - orchestrator/Argus.Orchestrator/Program.cs
  - orchestrator/Argus.Orchestrator.Tests/HealthEntityTests.cs
  - orchestrator/Argus.Orchestrator.Tests/StartupSensorLogTests.cs
autonomous: true
requirements: [HEALTH-01, UICFG-05]
tags: [home-assistant-addon, mqtt-discovery, health-entity, startup-log]

must_haves:
  truths:
    - "Argus publishes a single composite health binary_sensor (device_class problem) to HA via MQTT discovery"
    - "Health is ON (problem) unless detector is SERVING AND HA is connected AND MQTT is connected, in which case OFF (healthy)"
    - "The health entity is grouped under a dedicated Argus device with a stable unique_id and the Polish friendly name 'Argus — status'"
    - "On the first successful HA connect the orchestrator logs each discovered numeric sensor not already in the entities config, plus a total count"
  artifacts:
    - path: "orchestrator/Argus.Orchestrator/Mqtt/HealthEvaluator.cs"
      provides: "pure composite-health → ON/OFF mapping"
      exports: ["HealthEvaluator"]
    - path: "orchestrator/Argus.Orchestrator/Workers/HealthPublisherWorker.cs"
      provides: "publishes health discovery once then periodic state"
      exports: ["HealthPublisherWorker"]
    - path: "orchestrator/Argus.Orchestrator/Health/ArgusHealthSignals.cs"
      provides: "shared HA-connected liveness flag"
      exports: ["ArgusHealthSignals"]
    - path: "orchestrator/Argus.Orchestrator.Tests/HealthEntityTests.cs"
      provides: "evaluator + discovery payload coverage"
    - path: "orchestrator/Argus.Orchestrator.Tests/StartupSensorLogTests.cs"
      provides: "discovered-sensor filtering coverage (UICFG-05)"
  key_links:
    - from: "orchestrator/Argus.Orchestrator/Workers/HealthPublisherWorker.cs"
      to: "orchestrator/Argus.Orchestrator/Detection/DetectionGateway.cs"
      via: "polls grpc.health.v1 Check for the SERVING component of composite health"
      pattern: "HealthClient"
    - from: "orchestrator/Argus.Orchestrator/Ha/NetDaemonHaEventSource.cs"
      to: "orchestrator/Argus.Orchestrator/Health/ArgusHealthSignals.cs"
      via: "sets HaConnected on connect and clears it on disconnect"
      pattern: "HaConnected"
---

<objective>
Publish an Argus self-health entity to HA and log discovered numeric sensors at startup. The health entity is a single composite `binary_sensor` (device_class `problem`, ON = problem/unavailable, OFF = healthy) reflecting detector-SERVING AND HA-connected AND MQTT-connected, grouped under a dedicated "Argus" MQTT device with the Polish friendly name "Argus — status". The startup log lists numeric HA sensors not already configured so the user can copy entity_ids (mitigating the missing entity picker).

Purpose: HEALTH-01 (monitor the add-on itself) and UICFG-05 (startup discovered-sensors log), both per the locked decisions in 03-CONTEXT.md. Reuse the existing MQTTnet discovery/state stack and the existing get_states snapshot — do not re-invent.
Output: A composite-health evaluator + discovery payload, a publisher worker, a shared HA-connected signal, the startup sensor log in the existing HA event source, DI wiring, and unit tests (all runnable on the dev box).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-process-supervision-runtime-integration/03-CONTEXT.md

# Reuse the existing MQTT discovery/state stack + HA event source
@orchestrator/Argus.Orchestrator/Mqtt/DiscoveryPublisher.cs
@orchestrator/Argus.Orchestrator/Mqtt/MqttConnection.cs
@orchestrator/Argus.Orchestrator/Mqtt/UniqueId.cs
@orchestrator/Argus.Orchestrator/Ha/NetDaemonHaEventSource.cs
@orchestrator/Argus.Orchestrator/Detection/DetectionGateway.cs
@orchestrator/Argus.Orchestrator/Workers/MqttPublisherWorker.cs
@orchestrator/Argus.Orchestrator/Program.cs
@orchestrator/Argus.Orchestrator/Logging/LogEvents.cs
@orchestrator/Argus.Orchestrator.Tests/DiscoveryPayloadTests.cs
@orchestrator/Argus.Orchestrator.Tests/HaEventFilterTests.cs
</context>

<environment_note>
The evaluator, discovery payload, and startup-sensor filtering are pure functions — fully unit-testable on the dev box (dotnet test). The worker's live behavior (health entity appearing in HA, state flipping when the detector drops) needs a live HA OS and is a human-verify item. Depends on 03-02 because both edit Program.cs and MqttConnection.cs; run after it.
</environment_note>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Composite health evaluator + health discovery payload</name>
  <files>orchestrator/Argus.Orchestrator/Mqtt/HealthEvaluator.cs, orchestrator/Argus.Orchestrator/Mqtt/DiscoveryPublisher.cs, orchestrator/Argus.Orchestrator.Tests/HealthEntityTests.cs</files>
  <behavior>
    - Evaluate(detectorServing:true, haConnected:true, mqttConnected:true) → "OFF" (healthy).
    - Any one of the three false → "ON" (problem).
    - The health discovery payload has device_class "problem", payload_on "ON", payload_off "OFF", a stable unique_id equal to object_id, a dedicated Argus device, and name "Argus — status".
  </behavior>
  <action>
Create `HealthEvaluator` with a pure static `string Evaluate(bool detectorServing, bool haConnected, bool mqttConnected)` returning "OFF" when all three are true (healthy) and "ON" otherwise (problem) — ON means problem for device_class problem per the 03-CONTEXT decision. Keep it free of I/O so it is fully unit-testable.

Add `BuildHealthBinarySensorConfig()` to the existing static `DiscoveryPublisher` (mirror the shape of `BuildBinarySensorConfig` but for the composite add-on health, not a per-sensor entity). Use a fixed device slug (e.g. `argus_addon`) and a stable `unique_id` = `object_id` = `argus_addon_health` (object_id set to prevent HA mangling, matching the existing D-14 pattern). state_topic `argus/addon/health/state`; payload_on "ON"; payload_off "OFF"; device_class "problem"; availability = the bridge availability topic (reuse `MqttConnection.BridgeAvailabilityTopic`). Set `name` to the Polish friendly name "Argus — status" (D8). The device block groups it under a dedicated Argus device: identifiers `["argus_addon"]`, name "Argus", manufacturer/model consistent with the existing publisher constants. Serialize with the existing JsonOptions.

Create `HealthEntityTests`: assert the evaluator truth table (all-true → OFF; each single-false → ON); parse the health discovery JSON and assert device_class problem, unique_id == object_id == argus_addon_health, name "Argus — status", payload_on/off, state_topic, and the device identifiers. Follow the JsonDocument assertion style in `DiscoveryPayloadTests`.
  </action>
  <verify>
    <automated>dotnet test orchestrator/Argus.Orchestrator.Tests/Argus.Orchestrator.Tests.csproj --filter "FullyQualifiedName~HealthEntity" -v q</automated>
  </verify>
  <done>Evaluator returns OFF only when all three signals are true; health discovery payload is a problem binary_sensor named "Argus — status" under a dedicated Argus device with stable unique_id == object_id.</done>
</task>

<task type="auto">
  <name>Task 2: HA-connected signal + MqttConnection.IsConnected + HealthPublisherWorker + DI</name>
  <files>orchestrator/Argus.Orchestrator/Health/ArgusHealthSignals.cs, orchestrator/Argus.Orchestrator/Mqtt/MqttConnection.cs, orchestrator/Argus.Orchestrator/Ha/NetDaemonHaEventSource.cs, orchestrator/Argus.Orchestrator/Workers/HealthPublisherWorker.cs, orchestrator/Argus.Orchestrator/Logging/LogEvents.cs, orchestrator/Argus.Orchestrator/Program.cs</files>
  <action>
Create `ArgusHealthSignals` (namespace Argus.Orchestrator.Health) — a singleton holding a `volatile bool HaConnected` (default false). This is the minimal shared liveness signal for the HA component of composite health.

Expose `public bool IsConnected => _client.IsConnected;` on `MqttConnection` (the underlying IMqttClient already tracks connection state) — one-line addition, no behavior change. This is the MQTT component of composite health.

In `NetDaemonHaEventSource`, inject `ArgusHealthSignals` and set `HaConnected = true` immediately after a successful `ConnectAsync` (right where the "Connected to HA WebSocket" log is emitted) and set it back to false in the catch/backoff path when the connection is lost. Surgical: do not alter the reconnect/backoff logic or TryMap.

Create `HealthPublisherWorker` (BackgroundService, namespace Argus.Orchestrator.Workers). It depends on MqttConnection, DetectionGateway, ArgusHealthSignals, and ILogger. On start: wait briefly for MQTT to be connected (poll IsConnected with a short delay), publish the health discovery config once via `mqtt.PublishAsync("homeassistant/binary_sensor/argus_addon_health/config", DiscoveryPublisher.BuildHealthBinarySensorConfig(), retain:true, ct)`. Then loop on a fixed interval (~15s): determine detectorServing by calling `DetectionGateway.HealthClient.CheckAsync` with a short deadline (catch → false), read `signals.HaConnected` and `mqtt.IsConnected`, compute the payload via `HealthEvaluator.Evaluate(...)`, and publish it to `argus/addon/health/state` with retain:true. Respect cancellation. Log the published composite state with a new LogEvents id (allocate a fresh 60xx Health block, e.g. `HealthEntityPublished = new(6001, ...)`) — log only the boolean components, no secrets.

In `Program.cs`: register `ArgusHealthSignals` as a singleton, and register `HealthPublisherWorker` as a hosted service. Place the registration near the existing MQTT stack registrations. Do not modify the existing DI for other services beyond adding these and the constructor injection of ArgusHealthSignals into NetDaemonHaEventSource (the existing `AddSingleton&lt;IHaEventSource, NetDaemonHaEventSource&gt;` resolves the new dependency automatically once it is registered).
  </action>
  <verify>
    <automated>dotnet build orchestrator/Argus.Orchestrator/Argus.Orchestrator.csproj -v q</automated>
    <human-check>On a live HA OS: confirm an "Argus — status" binary_sensor appears via MQTT discovery after startup and reads OK (OFF) when healthy; stop the detector and confirm it flips to problem (ON) — HEALTH-01.</human-check>
  </verify>
  <done>ArgusHealthSignals tracks HA connectivity; MqttConnection exposes IsConnected; HealthPublisherWorker publishes discovery once then periodic composite state; DI registers both; orchestrator builds.</done>
</task>

<task type="auto">
  <name>Task 3: Startup discovered-sensors log (UICFG-05)</name>
  <files>orchestrator/Argus.Orchestrator/Ha/NetDaemonHaEventSource.cs, orchestrator/Argus.Orchestrator.Tests/StartupSensorLogTests.cs</files>
  <action>
Add a startup discovery log to `NetDaemonHaEventSource`. On the FIRST successful HA connect only (guard with the existing isFirstConnection flag), call `GetStatesAsync` (the source already uses this on reconnect — reuse the same call) and, before/at detection start, log at INFO one line per numeric sensor that is NOT already in the configured entities set, each line carrying the entity_id and its last numeric value, followed by a single total-count line. Extract the selection into a pure static helper `internal static IReadOnlyList&lt;(string EntityId, double Value)&gt; SelectDiscoverableSensors(states, configuredEntities)` so it is unit-testable without a live HA connection — a state counts as a numeric sensor when its value parses as a double (invariant culture, same rule as TryMap) and its entity_id is not in the configured set. Use a new LogEvents id (e.g. `DiscoveredSensorsLogged` in the 30xx HA block) for the lines. Do not change the existing reconnect snapshot behavior; this is additive on first connect.

Create `StartupSensorLogTests`: feed a synthetic list of states (mix of numeric/non-numeric values and configured/unconfigured entity_ids) to `SelectDiscoverableSensors` and assert it returns exactly the unconfigured numeric sensors with their parsed values, excludes configured ones, and excludes non-numeric (unavailable/unknown) states. Follow the static-helper test style in `HaEventFilterTests`.
  </action>
  <verify>
    <automated>dotnet test orchestrator/Argus.Orchestrator.Tests/Argus.Orchestrator.Tests.csproj --filter "FullyQualifiedName~StartupSensorLog" -v q</automated>
    <human-check>On a live HA OS: confirm the add-on log lists discovered numeric sensors (entity_id + value) and a total count at startup before detection begins — UICFG-05.</human-check>
  </verify>
  <done>SelectDiscoverableSensors returns only unconfigured numeric sensors with parsed values; first-connect startup log emits one line per such sensor plus a total count; reconnect behavior unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator → MQTT (HA) | health entity discovery + state published to the broker HA reads from |
| HA states → log | get_states snapshot enumerated to emit the startup discovery log |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Information Disclosure | health entity / startup log leaking sensitive detail | accept | health state is a single boolean; the startup log lists only entity_ids and numeric values the user already owns in HA |
| T-03-08 | Denial of Service | health poll tight loop hammering the detector | mitigate | fixed ~15s interval with a short Check deadline; failures caught and treated as not-SERVING, no tight retry |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | no new package installs — reuses MQTTnet, gRPC health client, and NetDaemon already present |
</threat_model>

<verification>
- `dotnet test --filter "FullyQualifiedName~HealthEntity"` passes (evaluator truth table + health discovery payload).
- `dotnet test --filter "FullyQualifiedName~StartupSensorLog"` passes (discoverable-sensor selection).
- `dotnet build` succeeds with the new worker, signal, and DI wiring.
- Live HA checks (entity appears, flips on detector loss, startup log present) deferred per environment_note.
</verification>

<success_criteria>
- HEALTH-01: a single composite "Argus — status" problem binary_sensor is published via MQTT discovery and reflects detector-SERVING AND HA-connected AND MQTT-connected.
- UICFG-05: the orchestrator logs discovered numeric sensors (entity_id + value) not already configured, plus a total count, on first HA connect.
</success_criteria>

<output>
Create `.planning/phases/03-process-supervision-runtime-integration/03-03-SUMMARY.md` when done.
</output>
