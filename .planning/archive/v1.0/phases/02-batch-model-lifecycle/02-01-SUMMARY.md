---
phase: 02-batch-model-lifecycle
plan: "01"
subsystem: infra
tags: [grpc, protobuf, python-stubs, dotnet, grpcio-tools]

# Dependency graph
requires:
  - phase: 01-foundations-streaming
    provides: "argus.proto with ScoreStream + Fit RPCs; Python grpcio-tools stub generation; .NET Grpc.Tools build"
provides:
  - "argus.proto extended with ScoreBatch, SaveModel, LoadModel RPCs and 6 new messages"
  - "Python stubs regenerated (argus_pb2.py, argus_pb2_grpc.py) via gen_proto.py"
  - ".NET stubs auto-regenerated by Grpc.Tools on build (ArgusGrpc.cs)"
  - "Full test suite clean: 30 Python + 59 .NET passing (3 pre-existing .NET failures confirmed unchanged)"
affects: [02-02, 02-03, 02-04, 02-05, 02-06, batch-scheduler, model-lifecycle, influx-reader]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Proto extension: add messages after FitResponse block; add RPCs after Fit in DetectorService"
    - "Python stubs are gitignored generated artifacts; gen_proto.py regenerates on test session start"
    - "version=0 semantics: LoadModelRequest.version=0 means load latest"

key-files:
  created: []
  modified:
    - proto/argus.proto
    - detector/argus_detector/servicer.py

key-decisions:
  - "Python pb2 stub files are gitignored (generated artifacts); committed via gen_proto.py at test/build time"
  - "LoadModelRequest.version=0 means load latest (per CONTEXT.md locked decision)"
  - "ScoreBatch is read-only; SaveModel/LoadModel are internal restart-resilience plumbing, not user-facing"

patterns-established:
  - "Proto extension pattern: append new messages after existing FitResponse; extend service block without breaking ScoreStream/Fit field numbers"

requirements-completed: [INFRA-02, MDL-02]

# Metrics
duration: 15min
completed: 2026-06-10
---

# Phase 2 Plan 01: Proto Extensions - ScoreBatch + Model Lifecycle RPCs

**argus.proto extended with ScoreBatch, SaveModel, LoadModel RPCs and 6 new proto messages; .NET and Python stubs regenerated and verified**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-06-10T17:00:00Z
- **Completed:** 2026-06-10T17:13:40Z
- **Tasks:** 3
- **Files modified:** 2

## Accomplishments

- Extended `proto/argus.proto` with ScoreBatchRequest/Response, SaveModelRequest/Response, LoadModelRequest/Response (6 messages) and ScoreBatch, SaveModel, LoadModel RPCs (3 methods) — existing ScoreStream/Fit unmodified
- Regenerated Python stubs via `gen_proto.py`; all new stub methods confirmed in `argus_pb2_grpc.py`
- `dotnet build` succeeded (Grpc.Tools auto-regenerated `ArgusGrpc.cs` with 14 ScoreBatch occurrences)
- Full test suites: 30/30 Python passing, 59/62 .NET passing (3 pre-existing failures confirmed unchanged by testing at prior commit)

## Task Commits

1. **Task 1: Extend argus.proto** - `7789ea8` (feat)
2. **Task 2: Regenerate Python stubs + verify .NET build** - covered by `7789ea8` (stubs are gitignored; build verification required no commit)
3. **Task 3: Run full test suite** - regression fix `d56b1f0` (fix — Rule 1 deviation)

## Files Created/Modified

- `proto/argus.proto` - Added 6 new messages and 3 new RPCs to DetectorService
- `detector/argus_detector/servicer.py` - Fixed Windows-1252 em dash encoding (pre-existing bug; Rule 1)

## Decisions Made

- Python stub files (`*_pb2.py`, `*_pb2_grpc.py`) are gitignored per `.gitignore` — generated at test/build time by `gen_proto.py`; this is the correct pattern, no change needed
- Task 2 does not require a separate commit since the proto change (Task 1) is the source artifact and stubs are not tracked
- Pre-existing .NET test failures (3 of 62): `BinarySensorPayload_AvailabilityTopicIsBridgeLevel`, `BinarySensorPayload_PayloadAvailableOnline`, `ScoreStreamPipelineTests.RunAsync_CompleteAsyncCalledBeforeReadTaskAwaited` — confirmed present before this plan; not caused by proto extension; deferred to scope owner

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed Windows-1252 encoded em dash in servicer.py**
- **Found during:** Task 3 (full test suite run)
- **Issue:** `detector/argus_detector/servicer.py` line 45 had byte `0x97` (Windows-1252 em dash `—`) in a string literal; Python's UTF-8 parser raised `SyntaxError` causing 6 test errors in `test_server_boot.py` and `test_health.py`
- **Fix:** Replaced byte `0x97` with ASCII hyphen-minus `-` using binary rewrite
- **Files modified:** `detector/argus_detector/servicer.py`
- **Verification:** `python -m pytest tests/ -v` shows 30/30 passed (up from 24 passed, 6 errors)
- **Committed in:** `d56b1f0`

---

**Total deviations:** 1 auto-fixed (Rule 1 - encoding bug)
**Impact on plan:** Necessary correctness fix. No scope creep. Bug was introduced in Phase 1 WR-04 commit.

## Issues Encountered

- Python stubs are gitignored — `git status` appeared clean after regen. Verified by checking `.gitignore` and `git ls-files`; pattern is intentional and correct (stubs generated from proto at build/test time).
- 3 pre-existing .NET test failures confirmed by re-running tests at the commit before any changes. Out of scope for this plan.

## Next Phase Readiness

- All Phase 2 plans (02-02 through 02-06) can now use ScoreBatch, SaveModel, LoadModel RPCs as their implementation contracts
- `.NET DetectionGateway` can add typed calls for new RPCs after `using Argus.Detector.V1;` (stubs in `ArgusGrpc.cs`)
- Python servicer can implement `ScoreBatch`, `SaveModel`, `LoadModel` methods on `DetectorServicer`
- No blockers

## Self-Check: PASSED

- `proto/argus.proto` exists and contains ScoreBatchRequest (grep count: 6)
- `.NET stubs` contain ScoreBatch (grep count: 14 in `ArgusGrpc.cs`)
- Python stubs contain ScoreBatch (grep count: 13 in `argus_pb2_grpc.py`)
- Commits: `7789ea8` (proto extension), `d56b1f0` (encoding fix) — both verified in git log

---
*Phase: 02-batch-model-lifecycle*
*Completed: 2026-06-10*
