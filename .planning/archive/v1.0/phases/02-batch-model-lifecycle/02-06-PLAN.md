---
phase: 02-batch-model-lifecycle
plan: 06
type: execute
wave: 4
depends_on: [02-04, 02-05]
files_modified:
  - orchestrator/Argus.Orchestrator.Tests/Res02ResilienceTests.cs
  - detector/tests/test_restart_resilience.py
autonomous: true
requirements: [RES-02]

must_haves:
  truths:
    - "Orchestrator restart does not duplicate or orphan HA entities: DiscoveryPublisher.PublishAllAsync is idempotent via retain=true and stable unique_id"
    - "Detector restart loads all saved models before health transitions to SERVING (MDL-03 gate verified end-to-end)"
    - "Orchestrator-side test: calling PublishAllAsync twice for the same entity list does not double the discovered entity count"
    - "Detector-side test: create_server with a pre-populated ModelStore has SERVING status and registry populated after startup"
  artifacts:
    - path: "orchestrator/Argus.Orchestrator.Tests/Res02ResilienceTests.cs"
      provides: "RES-02 orchestrator restart idempotency tests"
      contains: "Res02ResilienceTests"
    - path: "detector/tests/test_restart_resilience.py"
      provides: "RES-02 detector restart model load gate tests"
      contains: "test_restart_loads_models"
  key_links:
    - from: "orchestrator/Argus.Orchestrator.Tests/Res02ResilienceTests.cs"
      to: "orchestrator/Argus.Orchestrator/Mqtt/DiscoveryPublisher.cs"
      via: "PublishAllAsync called twice; unique_id payloads asserted identical"
      pattern: "PublishAllAsync"
    - from: "detector/tests/test_restart_resilience.py"
      to: "detector/argus_detector/server.py"
      via: "create_server(model_root=tmp_path) with pre-saved model; assert health=SERVING and registry populated"
      pattern: "create_server"
---

<objective>
Write RES-02 resilience tests verifying: (1) orchestrator restart is idempotent for MQTT discovery, (2) detector restart loads all saved models before accepting traffic.

Purpose: RES-02 requires testing, not implementation — the mechanism is already in place from Phase 1 (idempotent discovery) and Plan 05 (startup model load gate). This plan adds the verification layer.
Output: Res02ResilienceTests.cs (orchestrator), test_restart_resilience.py (detector).
</objective>

<execution_context>
@C:\Users\Admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-batch-model-lifecycle/02-CONTEXT.md
@.planning/phases/01-foundations-streaming/01-08-SUMMARY.md
</context>

<interfaces>
<!-- MQTT discovery idempotency mechanism (from RESEARCH.md + Phase 1): -->
DiscoveryPublisher.PublishAllAsync publishes config with retain=true.
unique_id formula: argus_{entity_slug}_{detector}_{suffix} (stable, deterministic).
HA deduplicates by unique_id — re-publishing same config is a no-op from HA's perspective.
MqttPublisherWorker.ExecuteAsync calls DiscoveryPublisher.PublishAllAsync on every startup.

<!-- Test approach for orchestrator RES-02: -->
Use FakeMqttConnection (capture published messages) — check existing test helpers.
Call DiscoveryPublisher.PublishAllAsync twice with same entities list.
Assert all captured discovery payloads have the same unique_id values in both runs.
Assert no new entities appear in second publish (same config, not additive).

<!-- Detector restart mechanism (from Plan 05 server.py): -->
create_server(model_root=tmp_path) creates ModelStore(root=tmp_path).
load_all_into(registry) populates registry from disk.
Health is NOT_SERVING during load; SERVING after.
Test: save a model to tmp_path before calling create_server; then verify registry has_model after.

<!-- DiscoveryPublisher.PublishAllAsync signature (from Phase 1): -->
static Task PublishAllAsync(MqttConnection mqtt, List<EntityConfig> entities, CancellationToken ct)

<!-- Existing test helpers (check before writing new helpers): -->
orchestrator/Argus.Orchestrator.Tests/ — look for FakeMqttConnection or MqttConnectionTests.cs for message capture helpers.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Orchestrator RES-02 — MQTT discovery restart idempotency test</name>
  <files>
    orchestrator/Argus.Orchestrator.Tests/Res02ResilienceTests.cs
  </files>
  <read_first>
    - orchestrator/Argus.Orchestrator.Tests/MqttConnectionTests.cs (understand test setup, any fake MQTT helpers)
    - orchestrator/Argus.Orchestrator.Tests/DiscoveryPayloadTests.cs (existing discovery test pattern)
    - orchestrator/Argus.Orchestrator/Mqtt/DiscoveryPublisher.cs (PublishAllAsync implementation)
    - orchestrator/Argus.Orchestrator/Mqtt/MqttConnection.cs (understand message capture approach)
    - orchestrator/Argus.Orchestrator.Tests/ScoreStreamPipelineTests.cs (FakeStatePublisher pattern — see if similar fake exists for MqttConnection)
  </read_first>
  <behavior>
    - PublishAllAsync called once for N entities → N*2 discovery payloads (binary_sensor + score per entity), each with unique_id field
    - PublishAllAsync called twice for the same entities list → unique_id values in second batch exactly equal to first batch (no new IDs, no duplicates)
    - All discovery payloads use retain=true (verified on captured MqttApplicationMessage)
  </behavior>
  <action>
    Read MqttConnectionTests.cs and DiscoveryPayloadTests.cs first to understand how existing tests capture MQTT messages. If a capture helper exists (e.g., a fake/in-memory MQTT client), reuse it.

    Create Res02ResilienceTests.cs in the test project:

    Namespace: Argus.Orchestrator.Tests.
    Class: Res02ResilienceTests.

    Test 1 — DiscoveryIdempotency:
    - Create an entities list with 2 EntityConfig entries (e.g., "sensor.test_a", "sensor.test_b"), each with one DetectorConfig (detector "hst")
    - Set up a message capture (use existing fake or inline List that captures messages)
    - Call DiscoveryPublisher.PublishAllAsync twice with the same entities list
    - Collect all published unique_id values from both batches
    - Assert: unique_id set from first batch == unique_id set from second batch
    - Assert: total payload count from second batch == total payload count from first batch (no accumulation)

    Test 2 — RetainFlag:
    - Same setup; call PublishAllAsync once
    - Assert: every published MqttApplicationMessage has Retain=true

    If MqttConnection cannot be instantiated in unit tests without a live broker, extract the message-capture layer (a Func or IMessageSink) to allow test injection. Look for existing patterns in Phase 1 tests before inventing a new abstraction.
  </action>
  <verify>
    <automated>dotnet test orchestrator/Argus.Orchestrator.Tests/Argus.Orchestrator.Tests.csproj --filter "Res02" --verbosity normal 2>&1 | tail -10</automated>
  </verify>
  <done>Res02ResilienceTests: DiscoveryIdempotency and RetainFlag tests pass; unique_id set identical across two PublishAllAsync calls</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Detector RES-02 — restart model load gate end-to-end test</name>
  <files>
    detector/tests/test_restart_resilience.py
  </files>
  <read_first>
    - detector/tests/test_server_boot.py (existing server boot test — understand create_server call pattern and health check approach)
    - detector/argus_detector/server.py (current create_server signature after Plan 05 changes)
    - detector/argus_detector/model_store.py (save_pyod, load_all_into — verify API from Plan 03)
    - detector/argus_detector/registry.py (has_model — verify from Plan 05)
  </read_first>
  <behavior>
    - create_server(tls=False, model_root=empty_tmp_path) → health is SERVING; registry has no models (empty model store)
    - create_server(tls=False, model_root=tmp_path_with_one_model) → registry has_model("sensor.test", "mad") is True after create_server returns; health is SERVING
    - During model loading (before SERVING set): health check would return NOT_SERVING — but we cannot test this timing in a unit test; instead assert the final state: SERVING after create_server
    - No exception raised when MODEL_ROOT does not exist (directory missing → load_all_into is a no-op)
  </behavior>
  <action>
    Create detector/tests/test_restart_resilience.py:

    Import: pytest, pathlib, tmp_path fixture.
    Import: create_server from argus_detector.server.
    Import: ModelStore from argus_detector.model_store.
    Import: PyODDetector from argus_detector.pyod_detector.
    Import grpc health protos for status check.

    Helper: save_test_model(tmp_path, entity_id, detector):
      - Create PyODDetector, fit on [1.0]*10
      - slug = entity_id.replace(".", "_")
      - ms = ModelStore(root=tmp_path)
      - version = 1
      - ms.save_pyod(slug, detector, version, model) — saves model; updates latest

    Test 1 — empty_model_root_is_noop:
      - server = create_server(tls=False, model_root=tmp_path) (empty tmp_path)
      - Access health_servicer via server (may need refactor to expose it, or just start/stop and check)
      - Alternative: introspect registry from servicer — check that no models loaded (no assertion failure)
      - Assert server is not None (create_server did not raise)
      - server does not need to start — just verify create_server completes without error

    Test 2 — preloaded_model_in_registry:
      - save_test_model(tmp_path, "sensor.test_entity", "mad")
      - server = create_server(tls=False, model_root=tmp_path)
      - Extract registry from server's servicer: need to expose it — either via test parameter or refactor create_server to return (server, registry, model_store) tuple, OR add a test-only accessor
      - Recommended: create_server returns the server object; add registry as an attribute on the server object (store registry on server after create_server, e.g. server._argus_registry = registry), or return a named tuple
      - Assert registry.has_model("sensor.test_entity", "mad") is True
      - Do NOT start the server — just call create_server

    If exposing registry requires a minor refactor of create_server (e.g., store registry on the returned server object), do it. Keep the change minimal: server._registry = registry after server construction.
  </action>
  <verify>
    <automated>cd detector && python -m pytest tests/test_restart_resilience.py -v 2>&1 | tail -10</automated>
  </verify>
  <done>test_restart_resilience.py all tests pass; empty model root is a no-op; pre-saved model is in registry after create_server</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Full regression sweep</name>
  <files></files>
  <read_first></read_first>
  <action>
    Run the complete test suites for both orchestrator and detector to confirm no regressions across all Phase 2 plans:

    Step 1 — .NET full suite:
      dotnet test orchestrator/Argus.Orchestrator.Tests/Argus.Orchestrator.Tests.csproj --verbosity normal

    Step 2 — Python full suite:
      cd detector && python -m pytest tests/ -v

    Step 3 — If any test fails: read the failure, fix the root cause (do not skip or comment out tests).

    Step 4 — Verify test counts are higher than Phase 1 baseline (62 .NET tests from 01-08-SUMMARY.md):
      dotnet test ... --verbosity quiet 2>&1 | grep -E "passed|failed"
  </action>
  <verify>
    <automated>dotnet test orchestrator/Argus.Orchestrator.Tests/Argus.Orchestrator.Tests.csproj --verbosity quiet 2>&1 | tail -3</automated>
  </verify>
  <done>dotnet test exits 0 with N > 62 passing tests; python -m pytest exits 0 with all tests passing; zero failures, zero skips</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test MQTT capture | Tests use in-memory fake MQTT; no real broker needed; no network traffic in tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-01 | Repudiation | MQTT discovery idempotency | mitigate | Test verifies unique_id is stable across restarts — no orphaned entities |
| T-02-06-02 | Tampering | Startup model load before SERVING | mitigate | Test verifies health NOT_SERVING during load (MDL-03); SERVING only after load_all_into completes |
| T-02-06-SC | Tampering | No new installs in test plans | accept | Tests use stdlib + existing test framework; no new packages |
</threat_model>

<verification>
1. Res02ResilienceTests: DiscoveryIdempotency passes (unique_id identical on second publish)
2. Res02ResilienceTests: RetainFlag passes (all discovery payloads have Retain=true)
3. test_restart_resilience.py: empty model root no-op passes
4. test_restart_resilience.py: preloaded model in registry passes
5. dotnet test exits 0 (all tests pass, count > 62)
6. python -m pytest exits 0 (all tests pass)
</verification>

<success_criteria>
- RES-02 verified: orchestrator restart = idempotent discovery (no duplicate entities)
- RES-02 verified: detector restart = models loaded before SERVING (no cold-start after restart)
- Full regression: zero failures across all Phase 2 additions and Phase 1 baseline
</success_criteria>

<output>
Create .planning/phases/02-batch-model-lifecycle/02-06-SUMMARY.md when done
</output>
