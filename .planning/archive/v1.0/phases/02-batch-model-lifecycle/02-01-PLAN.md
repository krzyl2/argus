---
phase: 02-batch-model-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - proto/argus.proto
  - detector/argus_detector/proto/argus_pb2.py
  - detector/argus_detector/proto/argus_pb2_grpc.py
autonomous: true
requirements: [INFRA-02, MDL-02]

must_haves:
  truths:
    - "argus.proto contains ScoreBatch, SaveModel, LoadModel RPC declarations"
    - ".NET stubs compile against the extended proto (dotnet build exits 0)"
    - "Python stubs contain ScoreBatchStub, SaveModelStub, LoadModelStub callable methods"
    - "Existing ScoreStream and Fit RPCs are unmodified and still functional"
  artifacts:
    - path: "proto/argus.proto"
      provides: "Extended proto contract with 3 new RPCs and 6 new messages"
      contains: "ScoreBatchRequest"
    - path: "detector/argus_detector/proto/argus_pb2.py"
      provides: "Python proto message classes"
      contains: "ScoreBatchRequest"
    - path: "detector/argus_detector/proto/argus_pb2_grpc.py"
      provides: "Python gRPC stub classes"
      contains: "ScoreBatch"
  key_links:
    - from: "proto/argus.proto"
      to: "orchestrator/Argus.Orchestrator/obj/Debug/net8.0/ArgusGrpc.cs"
      via: "Grpc.Tools MSBuild auto-regen on dotnet build"
      pattern: "ScoreBatch"
    - from: "proto/argus.proto"
      to: "detector/argus_detector/proto/argus_pb2_grpc.py"
      via: "grpcio-tools protoc in gen_proto.py"
      pattern: "ScoreBatch"
---

<objective>
Extend argus.proto with ScoreBatch, SaveModel, and LoadModel RPCs and their request/response messages. Regenerate Python stubs. Verify .NET stubs auto-regenerate on build.

Purpose: All Phase 2 implementation (both orchestrator and detector) depends on these new gRPC message types and service methods. Proto must be done first.
Output: Extended proto file, regenerated Python stubs, verified .NET build.
</objective>

<execution_context>
@C:\Users\Admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-batch-model-lifecycle/02-CONTEXT.md
@.planning/phases/02-batch-model-lifecycle/02-RESEARCH.md
</context>

<interfaces>
<!-- Current proto/argus.proto content (read before editing): -->

Existing messages: Point, Verdict, FitRequest, FitResponse
Existing service: DetectorService { ScoreStream(stream Point) returns (stream Verdict); Fit(FitRequest) returns (FitResponse); }
Package: argus.v1
csharp_namespace: Argus.Detector.V1
Imports: google/protobuf/timestamp.proto, google/protobuf/wrappers.proto

New messages to add (from CONTEXT.md locked decisions):
  ScoreBatchRequest  { string entity_id=1; string detector=2; map<string,string> params=3; repeated Point window=4; }
  ScoreBatchResponse { repeated Verdict verdicts=1; bool ok=2; string error=3; }
  SaveModelRequest   { string entity_id=1; string detector=2; int32 version=3; bytes model_bytes=4; }
  SaveModelResponse  { bool ok=1; string error=2; }
  LoadModelRequest   { string entity_id=1; string detector=2; int32 version=3; }
  LoadModelResponse  { bool ok=1; bytes model_bytes=2; string error=3; }

New RPCs to add to DetectorService:
  rpc ScoreBatch(ScoreBatchRequest) returns (ScoreBatchResponse);
  rpc SaveModel(SaveModelRequest) returns (SaveModelResponse);
  rpc LoadModel(LoadModelRequest) returns (LoadModelResponse);

LoadModelRequest.version=0 semantics: 0 = load latest version (per CONTEXT.md locked decisions).

Python stub regen command (from detector/ directory):
  python -m grpc_tools.protoc -I../proto --python_out=argus_detector/proto --grpc_python_out=argus_detector/proto ../proto/argus.proto

Python stubs live at: detector/argus_detector/proto/argus_pb2.py and argus_pb2_grpc.py
.NET stubs: auto-regenerated by Grpc.Tools MSBuild on next dotnet build (no manual step).
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend argus.proto with batch/model messages and RPCs</name>
  <files>proto/argus.proto</files>
  <read_first>
    - proto/argus.proto (current content — read before editing)
    - .planning/phases/02-batch-model-lifecycle/02-RESEARCH.md (gRPC Contract section, lines 554-608)
    - .planning/phases/02-batch-model-lifecycle/02-CONTEXT.md (Proto Extensions decisions, lines 40-48)
  </read_first>
  <action>
    Add six new messages after the FitResponse message block, then extend the DetectorService service block with three new RPCs.

    New messages (add in this order, after FitResponse):
    - ScoreBatchRequest: entity_id (string,1), detector (string,2), params (map string string,3), window (repeated Point,4)
    - ScoreBatchResponse: verdicts (repeated Verdict,1), ok (bool,2), error (string,3)
    - SaveModelRequest: entity_id (string,1), detector (string,2), version (int32,3), model_bytes (bytes,4)
    - SaveModelResponse: ok (bool,1), error (string,2)
    - LoadModelRequest: entity_id (string,1), detector (string,2), version (int32,3) — version=0 means load latest
    - LoadModelResponse: ok (bool,1), model_bytes (bytes,2), error (string,3)

    Extend DetectorService (add after the Fit RPC):
    - rpc ScoreBatch(ScoreBatchRequest) returns (ScoreBatchResponse);
    - rpc SaveModel(SaveModelRequest) returns (SaveModelResponse);
    - rpc LoadModel(LoadModelRequest) returns (LoadModelResponse);

    Do not modify existing messages (Point, Verdict, FitRequest, FitResponse) or existing RPCs (ScoreStream, Fit). Adding new RPCs to an existing service is non-breaking per RESEARCH.md.
  </action>
  <verify>
    <automated>grep -c "ScoreBatchRequest\|SaveModelRequest\|LoadModelRequest" proto/argus.proto</automated>
  </verify>
  <done>proto/argus.proto contains all 6 new message definitions and 3 new RPC declarations; grep count == 6 (3 message defs + 3 RPC params)</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Regenerate Python stubs and verify .NET build</name>
  <files>
    detector/argus_detector/proto/argus_pb2.py
    detector/argus_detector/proto/argus_pb2_grpc.py
  </files>
  <read_first>
    - detector/scripts/gen_proto.py (existing proto generation script — check if it can be reused)
    - detector/argus_detector/proto/argus_pb2_grpc.py (current content — verify ScoreStream + Fit still present after regen)
  </read_first>
  <action>
    Step 1 — Regenerate Python stubs from the detector/ directory:
      python -m grpc_tools.protoc -I../proto --python_out=argus_detector/proto --grpc_python_out=argus_detector/proto ../proto/argus.proto

    Step 2 — Verify the generated argus_pb2_grpc.py contains the new stub methods:
      grep "ScoreBatch\|SaveModel\|LoadModel" detector/argus_detector/proto/argus_pb2_grpc.py

    Step 3 — Verify existing methods still present:
      grep "ScoreStream\|Fit" detector/argus_detector/proto/argus_pb2_grpc.py

    Step 4 — Run dotnet build to trigger Grpc.Tools MSBuild auto-regen of .NET stubs:
      dotnet build orchestrator/Argus.Orchestrator/Argus.Orchestrator.csproj

    Step 5 — Verify .NET build succeeds (exit 0) and new method exists in generated stub:
      grep "ScoreBatch" orchestrator/Argus.Orchestrator/obj/Debug/net8.0/ArgusGrpc.cs

    If gen_proto.py exists and wraps protoc, prefer running it instead of the raw protoc command — but verify it uses --grpc_python_out before using it.
  </action>
  <verify>
    <automated>grep -c "ScoreBatch" detector/argus_detector/proto/argus_pb2_grpc.py && dotnet build orchestrator/Argus.Orchestrator/Argus.Orchestrator.csproj -v q 2>&1 | tail -5</automated>
  </verify>
  <done>Python argus_pb2_grpc.py contains ScoreBatch, SaveModel, LoadModel stub classes; dotnet build exits 0; existing test suite still passes (dotnet test exits 0)</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run full test suite to confirm no regressions</name>
  <files></files>
  <read_first>
    - detector/tests/test_proto_codegen.py (existing proto codegen test — may need update for new methods)
  </read_first>
  <action>
    Run the .NET test suite:
      dotnet test orchestrator/Argus.Orchestrator.Tests/Argus.Orchestrator.Tests.csproj

    Run the Python test suite:
      cd detector && python -m pytest tests/ -v

    If test_proto_codegen.py imports specific message types, it may need the new message names added to its assertions. Read it first — only update if existing assertions would fail due to regen overwriting the stubs.

    Do not add new tests here — this task is regression verification only.
  </action>
  <verify>
    <automated>dotnet test orchestrator/Argus.Orchestrator.Tests/Argus.Orchestrator.Tests.csproj --verbosity quiet 2>&1 | tail -3</automated>
  </verify>
  <done>dotnet test exits 0 (all N tests pass); python -m pytest exits 0 (all Python tests pass); no regressions from proto extension</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proto contract → .NET stubs | Grpc.Tools generates typed C# from proto; wrong field numbers break wire format silently |
| proto contract → Python stubs | grpcio-tools generates Python; stale stubs cause UNIMPLEMENTED gRPC errors at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | proto field numbering | mitigate | Never reuse field numbers; new fields always get next available number (1-based, no gaps) |
| T-02-01-02 | Repudiation | SaveModelRequest.model_bytes | accept | Model bytes are opaque; integrity verified by version.json sidecar on load; no PII |
| T-02-01-SC | Tampering | grpcio-tools install | accept | grpcio-tools already in requirements.txt from Phase 1; no new install |
</threat_model>

<verification>
1. proto/argus.proto contains ScoreBatchRequest, ScoreBatchResponse, SaveModelRequest, SaveModelResponse, LoadModelRequest, LoadModelResponse message definitions
2. proto/argus.proto DetectorService block contains ScoreBatch, SaveModel, LoadModel RPC declarations
3. detector/argus_detector/proto/argus_pb2_grpc.py contains new stub method names
4. dotnet build exits 0
5. dotnet test exits 0 (all existing tests pass)
6. python -m pytest detector/tests/ exits 0
</verification>

<success_criteria>
- proto/argus.proto: 6 new messages + 3 new RPCs, existing messages/RPCs unmodified
- Python stubs regenerated, contain new message classes and stub methods
- dotnet build 0 errors (Grpc.Tools auto-regenerated .NET stubs)
- All prior tests (62 .NET + Python) still pass
</success_criteria>

<output>
Create .planning/phases/02-batch-model-lifecycle/02-01-SUMMARY.md when done
</output>
