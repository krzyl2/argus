---
phase: 01-foundations-streaming
plan: 05
type: execute
wave: 4
depends_on: [04]
files_modified:
  - orchestrator/Argus.Orchestrator/Ha/IHaEventSource.cs
  - orchestrator/Argus.Orchestrator/Ha/HaReading.cs
  - orchestrator/Argus.Orchestrator/Ha/NetDaemonHaEventSource.cs
  - orchestrator/Argus.Orchestrator/Ha/ReconnectCooldown.cs
  - orchestrator/Argus.Orchestrator/Workers/HaListenerWorker.cs
  - orchestrator/Argus.Orchestrator/Program.cs
  - orchestrator/Argus.Orchestrator.Tests/ReconnectCooldownTests.cs
  - orchestrator/Argus.Orchestrator.Tests/HaEventFilterTests.cs
autonomous: true
requirements: [STRM-01, STRM-02]
requirements_addressed: [STRM-01, STRM-02]

must_haves:
  truths:
    - "Orchestrator authenticates to HA WebSocket and subscribes to state_changed"
    - "Only entities in entities.yaml are forwarded; others are dropped"
    - "WebSocket reconnects with exponential backoff (1s→2s→4s→8s→max 60s)"
    - "On reconnect, orchestrator calls get_states (not burst replay)"
    - "On reconnect, binary_sensor publication is suppressed for 60 seconds"
  artifacts:
    - path: "orchestrator/Argus.Orchestrator/Ha/NetDaemonHaEventSource.cs"
      provides: "NetDaemon.Client HA WebSocket subscription + reconnect + get_states"
      contains: "NetDaemon"
    - path: "orchestrator/Argus.Orchestrator/Ha/ReconnectCooldown.cs"
      provides: "60s post-reconnect binary_sensor suppression window"
      contains: "60"
    - path: "orchestrator/Argus.Orchestrator/Ha/IHaEventSource.cs"
      provides: "Stream of filtered HaReading events consumed by the streaming pipeline"
  key_links:
    - from: "orchestrator/Argus.Orchestrator/Ha/NetDaemonHaEventSource.cs"
      to: "entities.yaml configured set"
      via: "entity filter"
      pattern: "Contains|HashSet"
    - from: "orchestrator/Argus.Orchestrator/Workers/HaListenerWorker.cs"
      to: "IHaEventSource"
      via: "DI injection + ReadAllAsync"
      pattern: "IHaEventSource"
---

<objective>
Implement the HA WebSocket ingestion layer with NetDaemon.Client 23.46.0: authenticate, subscribe to state_changed, filter to the configured entity set, reconnect with exponential backoff, and on reconnect call get_states (NOT replay the burst) while suppressing binary_sensor publication for 60 seconds.

Purpose: STRM-01/STRM-02 are the front of the streaming pipeline. PITFALL 4 (reconnect burst → false anomaly cascade) is mitigated here via get_states + 60s cooldown (D-07). This plan fills the HaListenerWorker stub from Plan 04 and exposes a clean IHaEventSource the rest of the pipeline (Plan 07) consumes.
Output: NetDaemon HA event source, entity filter, reconnect cooldown, populated HaListenerWorker, Program.cs registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundations-streaming/01-CONTEXT.md
@.planning/phases/01-foundations-streaming/01-RESEARCH.md
@.planning/phases/01-foundations-streaming/01-04-SUMMARY.md

<interfaces>
<!-- From Plan 04: ConnectionSettings (HaUrl, HaToken), EntitiesConfig (entity set), DetectionGateway.WaitForHealthyAsync, HaListenerWorker stub delegates to IHaEventSource -->

NetDaemon.Client 23.46.0 (D-06) — IHomeAssistantRunner / IHomeAssistantConnection:
  - Connect with HaUrl + HaToken (long-lived access token)
  - SubscribeToHomeAssistantEventsAsync → state_changed events
  - get_states via connection.GetServicesAsync/SendCommandAndReturnResponseAsync("get_states")
  - Reconnect backoff per STRM-01: 1s,2s,4s,8s,...,max 60s

Contract this plan EXPORTS (consumed by Plan 07):
```csharp
public record HaReading(string EntityId, double Value, DateTimeOffset LastChanged, bool SuppressBinarySensor);
public interface IHaEventSource {
    IAsyncEnumerable<HaReading> ReadAllAsync(CancellationToken ct);
}
```
SuppressBinarySensor=true while within the 60s post-reconnect cooldown (D-07) and (later) during warm-up.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IHaEventSource contract, HaReading, and ReconnectCooldown (60s suppression)</name>
  <read_first>
    - .planning/phases/01-foundations-streaming/01-CONTEXT.md (D-06, D-07)
    - .planning/phases/01-foundations-streaming/01-RESEARCH.md (STRM-01/STRM-02; PITFALL 4 reconnect burst)
    - orchestrator/Argus.Orchestrator/Config/EntitiesConfig.cs (from Plan 04 — entity set)
  </read_first>
  <files>orchestrator/Argus.Orchestrator/Ha/IHaEventSource.cs, orchestrator/Argus.Orchestrator/Ha/HaReading.cs, orchestrator/Argus.Orchestrator/Ha/ReconnectCooldown.cs, orchestrator/Argus.Orchestrator.Tests/ReconnectCooldownTests.cs</files>
  <behavior>
    - Test: immediately after MarkReconnect(now), IsSuppressed(now) is true and IsSuppressed(now+59s) is true.
    - Test: IsSuppressed(now+61s) is false (60s window elapsed).
    - Test: a fresh ReconnectCooldown with no reconnect recorded returns IsSuppressed=false.
  </behavior>
  <action>
    HaReading.cs: `public record HaReading(string EntityId, double Value, DateTimeOffset LastChanged, bool SuppressBinarySensor);`
    IHaEventSource.cs: interface with `IAsyncEnumerable<HaReading> ReadAllAsync(CancellationToken ct);`
    ReconnectCooldown.cs: tracks last reconnect timestamp; `MarkReconnect(DateTimeOffset now)`; `bool IsSuppressed(DateTimeOffset now)` returns true if `now - lastReconnect < TimeSpan.FromSeconds(60)`. Make the 60s a const `SuppressionWindowSeconds = 60` (D-07). Inject a clock or take `now` as a parameter so it is deterministically testable.
    ReconnectCooldownTests.cs: implement the three <behavior> tests with explicit timestamps (no Thread.Sleep).
  </action>
  <verify>
    <automated>dotnet test orchestrator/Argus.Orchestrator.sln --filter "FullyQualifiedName~ReconnectCooldown"</automated>
  </verify>
  <acceptance_criteria>
    - ReconnectCooldown.cs contains "60"
    - HaReading.cs contains "SuppressBinarySensor"
    - IHaEventSource.cs contains "IAsyncEnumerable<HaReading>"
    - `dotnet test --filter "FullyQualifiedName~ReconnectCooldown"` exits 0
  </acceptance_criteria>
  <done>60s suppression window is deterministic and unit-tested; IHaEventSource/HaReading contract exported for Plan 07.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NetDaemonHaEventSource — auth, subscribe, filter, reconnect, get_states</name>
  <read_first>
    - .planning/phases/01-foundations-streaming/01-RESEARCH.md (NetDaemon.Client 23.46.0 usage; STRM-01 backoff; D-07 get_states)
    - orchestrator/Argus.Orchestrator/Ha/IHaEventSource.cs (Task 1)
    - orchestrator/Argus.Orchestrator/Config/ConnectionSettings.cs (Plan 04 — HaUrl, HaToken)
  </read_first>
  <files>orchestrator/Argus.Orchestrator/Ha/NetDaemonHaEventSource.cs, orchestrator/Argus.Orchestrator.Tests/HaEventFilterTests.cs</files>
  <behavior>
    - Test: given a configured set {sensor.salon_temperatura} and incoming raw state_changed events for sensor.salon_temperatura and sensor.unrelated, the mapped HaReading stream yields exactly the salon event (filter drops unrelated). Test the filter/mapping function in isolation (do not require a live HA).
    - Test: a state_changed with a non-numeric new_state value is dropped (cannot score non-numeric).
  </behavior>
  <action>
    NetDaemonHaEventSource.cs implementing IHaEventSource using NetDaemon.Client 23.46.0 (D-06). Connect with ConnectionSettings.HaUrl + HaToken. Build a `HashSet<string>` of configured entity_ids from EntitiesConfig for O(1) filtering. Subscribe to state_changed; map each event → HaReading (parse new_state.state to double via invariant culture; drop if entity not in set or value non-numeric; set LastChanged from event). Reconnect loop with exponential backoff 1s,2s,4s,8s capped at 60s (STRM-01). On every successful (re)connection AFTER the first: call get_states once (D-07 — snapshot, NOT burst replay), feed current values, call `ReconnectCooldown.MarkReconnect(now)` so SuppressBinarySensor=true for 60s. Each yielded HaReading sets `SuppressBinarySensor = cooldown.IsSuppressed(now)`. Structured-log connect/reconnect/backoff attempts and events/s (OBS-01). Keep the raw-event→HaReading mapping in a static/internal method `TryMap(rawEvent, set, out HaReading)` so it is unit-testable without a live HA.
    HaEventFilterTests.cs: implement the two <behavior> tests against TryMap.
  </action>
  <verify>
    <automated>dotnet test orchestrator/Argus.Orchestrator.sln --filter "FullyQualifiedName~HaEventFilter"</automated>
  </verify>
  <acceptance_criteria>
    - NetDaemonHaEventSource.cs contains "NetDaemon"
    - NetDaemonHaEventSource.cs contains "get_states" (or GetStates)
    - NetDaemonHaEventSource.cs contains "HashSet" (entity filter) and "MarkReconnect"
    - NetDaemonHaEventSource.cs references backoff values (e.g. "60" cap)
    - `dotnet test --filter "FullyQualifiedName~HaEventFilter"` exits 0
  </acceptance_criteria>
  <done>HA WebSocket authenticated, subscribed, filtered to config; reconnect uses get_states + 60s suppression (STRM-01, STRM-02; PITFALL 4 mitigated).</done>
</task>

<task type="auto">
  <name>Task 3: Wire HaListenerWorker to IHaEventSource and register in Program.cs</name>
  <read_first>
    - orchestrator/Argus.Orchestrator/Workers/HaListenerWorker.cs (Plan 04 stub)
    - orchestrator/Argus.Orchestrator/Program.cs (Plan 04)
    - orchestrator/Argus.Orchestrator/Ha/NetDaemonHaEventSource.cs (Task 2)
  </read_first>
  <files>orchestrator/Argus.Orchestrator/Workers/HaListenerWorker.cs, orchestrator/Argus.Orchestrator/Program.cs</files>
  <action>
    Replace the Plan 04 HaListenerWorker stub body: ExecuteAsync first awaits `gateway.WaitForHealthyAsync(stoppingToken)` (INFRA-07 gate stays), then `await foreach (var reading in haEventSource.ReadAllAsync(stoppingToken))` and pushes each HaReading into a bounded `Channel<HaReading>` (or a placeholder consumer) — log each forwarded reading. Add a `// TODO(plan07): forward to ScoreStream + frozen/hysteresis/MQTT` at the consumption point. Keep constructor injecting IHaEventSource + DetectionGateway + ILogger.
    Program.cs: register `builder.Services.AddSingleton<ReconnectCooldown>()` and `builder.Services.AddSingleton<IHaEventSource, NetDaemonHaEventSource>()`. Ensure NetDaemon.Client DI (`AddNetDaemonClient` or manual IHomeAssistantRunner registration per 23.46.0 API) is added. HaListenerWorker registration already exists from Plan 04 — do not duplicate.
  </action>
  <verify>
    <automated>dotnet build orchestrator/Argus.Orchestrator.sln</automated>
  </verify>
  <acceptance_criteria>
    - HaListenerWorker.cs contains "IHaEventSource" and "ReadAllAsync"
    - HaListenerWorker.cs contains "WaitForHealthyAsync"
    - Program.cs contains "AddSingleton<IHaEventSource, NetDaemonHaEventSource>"
    - `dotnet build orchestrator/Argus.Orchestrator.sln` exits 0
  </acceptance_criteria>
  <done>HaListenerWorker consumes the real HA event source after the health gate; DI wired; solution compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HA instance → orchestrator (WebSocket) | Inbound event data; values parsed and validated |
| HA long-lived token | Secret used to authenticate the WebSocket |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | malformed state_changed payload | mitigate | TryMap validates numeric value via invariant-culture parse; non-numeric/unknown dropped (acceptance-tested) |
| T-05-02 | Spoofing | HA endpoint identity | accept | HA URL is on the trusted LAN; token auth; TLS to HA out of scope for Phase 1 (LAN-local) |
| T-05-03 | Denial of Service | reconnect storm | mitigate | Exponential backoff capped at 60s prevents tight reconnect loop (STRM-01) |
| T-05-04 | Tampering | false anomaly cascade on reconnect | mitigate | get_states snapshot (no burst replay) + 60s SuppressBinarySensor window (D-07, PITFALL 4) |
| T-05-05 | Information Disclosure | HA token in logs | mitigate | Token never logged; OBS-01 logs entity_id/value/latency only |
</threat_model>

<verification>
- `dotnet test orchestrator/Argus.Orchestrator.sln` passes (ReconnectCooldown + HaEventFilter)
- `dotnet build` exits 0
- grep confirms get_states, HashSet filter, MarkReconnect, 60s window
</verification>

<success_criteria>
- HA WebSocket authenticated + subscribed + filtered (STRM-01)
- Reconnect uses get_states + 60s binary_sensor suppression, no burst replay (STRM-02, D-07)
- IHaEventSource exported for Plan 07 to consume
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-streaming/01-05-SUMMARY.md`
</output>
