---
phase: 01-foundations-streaming
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - proto/argus.proto
  - proto/buf.yaml
  - .gitignore
  - README.md
  - orchestrator/Argus.Orchestrator.sln
  - orchestrator/Argus.Orchestrator/Argus.Orchestrator.csproj
  - orchestrator/Argus.Orchestrator/Program.cs
  - orchestrator/Argus.Orchestrator.Tests/Argus.Orchestrator.Tests.csproj
  - orchestrator/Argus.Orchestrator.Tests/ProtoCodegenTests.cs
  - detector/pyproject.toml
  - detector/requirements.txt
  - detector/argus_detector/__init__.py
  - detector/argus_detector/proto/__init__.py
  - detector/scripts/gen_proto.py
  - detector/tests/test_proto_codegen.py
  - deploy/.gitkeep
autonomous: true
requirements: [INFRA-01, INFRA-02]
requirements_addressed: [INFRA-01, INFRA-02]

must_haves:
  truths:
    - "proto/argus.proto compiles to .NET stubs at build time via Grpc.Tools"
    - "proto/argus.proto compiles to Python stubs via grpcio-tools"
    - "Verdict.score uses google.protobuf.DoubleValue (not raw double)"
    - "Point.timestamp uses google.protobuf.Timestamp"
    - "Mono-repo has proto/, orchestrator/, detector/, deploy/ directories"
  artifacts:
    - path: "proto/argus.proto"
      provides: "Service + message contract (Point, Verdict, FitRequest, FitResponse, DetectorService)"
      contains: "google.protobuf.DoubleValue"
    - path: "orchestrator/Argus.Orchestrator.sln"
      provides: ".NET solution with worker + tests projects"
    - path: "orchestrator/Argus.Orchestrator/Argus.Orchestrator.csproj"
      provides: "Worker service project with Grpc.Tools proto reference"
      contains: "Grpc.Tools"
    - path: "detector/pyproject.toml"
      provides: "Python package with grpcio/river/pyod deps"
    - path: "detector/scripts/gen_proto.py"
      provides: "grpcio-tools codegen entrypoint"
  key_links:
    - from: "orchestrator/Argus.Orchestrator/Argus.Orchestrator.csproj"
      to: "proto/argus.proto"
      via: "Grpc.Tools Protobuf Include"
      pattern: "Protobuf Include=.*argus\\.proto"
    - from: "detector/scripts/gen_proto.py"
      to: "proto/argus.proto"
      via: "grpc_tools.protoc invocation"
      pattern: "argus\\.proto"
---

<objective>
Scaffold the Argus mono-repo and define the single-source-of-truth gRPC contract, with codegen wired on both sides (.NET via Grpc.Tools, Python via grpcio-tools).

Purpose: Every downstream wave depends on the proto contract and the directory skeleton. The proto must encode the locked wire-format decisions (DoubleValue wrappers, Timestamp, grpc.health.v1) before any server or client code is written.
Output: Mono-repo skeleton (proto/, orchestrator/, detector/, deploy/), finalized argus.proto, .NET solution that generates C# stubs at build, Python codegen script producing Python stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations-streaming/01-CONTEXT.md
@.planning/phases/01-foundations-streaming/01-RESEARCH.md

<interfaces>
<!-- Canonical proto contract from RESEARCH.md Pattern 1. Implement EXACTLY this shape. -->

```protobuf
syntax = "proto3";
package argus.v1;
option csharp_namespace = "Argus.Detector.V1";

import "google/protobuf/timestamp.proto";
import "google/protobuf/wrappers.proto";

message Point {
  string entity_id = 1;
  google.protobuf.DoubleValue value = 2;
  google.protobuf.Timestamp timestamp = 3;
}

message Verdict {
  string entity_id = 1;
  google.protobuf.DoubleValue score = 2;
  google.protobuf.DoubleValue expected = 3;
  google.protobuf.DoubleValue lower = 4;
  google.protobuf.DoubleValue upper = 5;
  bool is_anomaly = 6;
  string detector = 7;
  google.protobuf.Timestamp timestamp = 8;
}

message FitRequest {
  string entity_id = 1;
  string detector = 2;
  map<string, string> params = 3;
  repeated Point window = 4;
}
message FitResponse {
  bool ok = 1;
  string error = 2;
}

service DetectorService {
  rpc ScoreStream(stream Point) returns (stream Verdict);
  rpc Fit(FitRequest) returns (FitResponse);
}
```

Health RPC: do NOT hand-write. .NET uses Grpc.HealthCheck NuGet (grpc.health.v1 stubs bundled); Python uses grpcio-health-checking package (health_pb2 bundled). No Health message in argus.proto.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mono-repo skeleton and finalized argus.proto</name>
  <read_first>
    - .planning/phases/01-foundations-streaming/01-CONTEXT.md (D-01 through D-05, D-19)
    - .planning/phases/01-foundations-streaming/01-RESEARCH.md (Pattern 1, Recommended Project Structure)
  </read_first>
  <files>proto/argus.proto, proto/buf.yaml, .gitignore, README.md, deploy/.gitkeep</files>
  <action>
    Create directory layout per D-19: proto/, orchestrator/, detector/, deploy/.
    Write proto/argus.proto EXACTLY as in the <interfaces> block above: package `argus.v1`, `option csharp_namespace = "Argus.Detector.V1";`, imports `google/protobuf/timestamp.proto` and `google/protobuf/wrappers.proto`. Messages Point, Verdict, FitRequest, FitResponse with the exact field names and numbers shown. Service DetectorService with `rpc ScoreStream(stream Point) returns (stream Verdict);` and `rpc Fit(FitRequest) returns (FitResponse);`. Do NOT add a custom Health message — grpc.health.v1 comes from the health NuGet/PyPI packages (D-03).
    Write proto/buf.yaml with `version: v1` and `name: buf.build/argus/argus` (lint/breaking config optional, but file present documents the contract is canonical).
    Write .gitignore covering: `bin/`, `obj/`, `__pycache__/`, `*.pyc`, `.venv/`, `*_pb2.py`, `*_pb2_grpc.py`, `*_pb2.pyi`, `detector/argus_detector/proto/argus_pb2*.py`, `deploy/certs/*.key`, `deploy/certs/*.crt`, `deploy/certs/*.srl`.
    Write README.md with a "## Repository Layout" section listing the four top-level dirs and a one-line purpose each, plus a "## Build" section documenting `dotnet build orchestrator/Argus.Orchestrator.sln` and `python detector/scripts/gen_proto.py`.
    Write deploy/.gitkeep (empty) so the directory is tracked.
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path proto/argus.proto -Pattern 'google.protobuf.DoubleValue','google.protobuf.Timestamp','package argus.v1','rpc ScoreStream' -Quiet"</automated>
  </verify>
  <acceptance_criteria>
    - File proto/argus.proto exists
    - proto/argus.proto contains "google.protobuf.DoubleValue"
    - proto/argus.proto contains "google.protobuf.Timestamp"
    - proto/argus.proto contains "package argus.v1"
    - proto/argus.proto contains "option csharp_namespace = \"Argus.Detector.V1\""
    - proto/argus.proto contains "rpc ScoreStream(stream Point) returns (stream Verdict)"
    - proto/argus.proto contains "rpc Fit(FitRequest) returns (FitResponse)"
    - proto/argus.proto does NOT contain "message Health" (grep returns no match)
    - Directories proto/, orchestrator/, detector/, deploy/ all exist
    - .gitignore contains "deploy/certs/*.key"
  </acceptance_criteria>
  <done>argus.proto encodes all D-01..D-05 wire decisions; four-directory skeleton exists; .gitignore excludes generated stubs and cert private keys.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire .NET solution with Grpc.Tools codegen</name>
  <read_first>
    - proto/argus.proto (created in Task 1)
    - .planning/phases/01-foundations-streaming/01-RESEARCH.md (Standard Stack: Grpc.Net.Client 2.80.0, Grpc.Tools 2.80.0; D-20)
  </read_first>
  <files>orchestrator/Argus.Orchestrator.sln, orchestrator/Argus.Orchestrator/Argus.Orchestrator.csproj, orchestrator/Argus.Orchestrator/Program.cs, orchestrator/Argus.Orchestrator.Tests/Argus.Orchestrator.Tests.csproj, orchestrator/Argus.Orchestrator.Tests/ProtoCodegenTests.cs</files>
  <behavior>
    - Test: `Argus.Detector.V1.Point` type is constructible and `new Point().Value` is a nullable Google.Protobuf.WellKnownTypes.DoubleValue (proves DoubleValue wrapper generated, not raw double).
    - Test: `Argus.Detector.V1.DetectorService.DetectorServiceClient` type exists (proves service stub generated).
  </behavior>
  <action>
    Create a .NET 8 Worker Service. Argus.Orchestrator.csproj: `<TargetFramework>net8.0</TargetFramework>`, `<OutputType>Exe</OutputType>` is implicit via Worker SDK — use `Microsoft.NET.Sdk.Worker`. Add PackageReferences (exact versions): `NetDaemon.Client` 23.46.0, `MQTTnet` 5.1.0.1559, `Grpc.Net.Client` 2.80.0, `Grpc.Tools` 2.80.0 with `PrivateAssets="All"`, `Google.Protobuf` 3.27.0+, `Grpc.Net.ClientFactory` 2.80.0, `Grpc.HealthCheck` 2.80.0. Add `<Protobuf Include="..\..\proto\argus.proto" GrpcServices="Client" />` with `ProtoRoot` set to `..\..\proto`. (Health stubs come from Grpc.HealthCheck, no extra Protobuf entry.)
    Program.cs: minimal `Host.CreateApplicationBuilder(args)` with `builder.Services.AddHostedService` placeholder removed for now — keep `var builder = Host.CreateApplicationBuilder(args); var host = builder.Build(); host.Run();` so it compiles. Add a `// TODO(wave3)` comment for worker registration.
    Argus.Orchestrator.Tests.csproj: `Microsoft.NET.Sdk`, net8.0, references xunit 2.9.x, Microsoft.NET.Test.Sdk 17.x, and `ProjectReference` to Argus.Orchestrator.csproj.
    ProtoCodegenTests.cs: xunit tests implementing the <behavior> above — assert `typeof(Argus.Detector.V1.Point)` is not null, `new Argus.Detector.V1.Point().Value` accepts a `Google.Protobuf.WellKnownTypes.DoubleValue`, and `typeof(Argus.Detector.V1.DetectorService.DetectorServiceClient)` is not null.
    Create the .sln referencing both projects (`dotnet new sln` then `dotnet sln add`).
  </action>
  <verify>
    <automated>dotnet test orchestrator/Argus.Orchestrator.sln</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build orchestrator/Argus.Orchestrator.sln` exits 0
    - Argus.Orchestrator.csproj contains "Grpc.Tools" and "Version=\"2.80.0\""
    - Argus.Orchestrator.csproj contains "Protobuf Include" referencing argus.proto
    - Argus.Orchestrator.csproj contains "NetDaemon.Client" and "23.46.0"
    - Argus.Orchestrator.csproj contains "MQTTnet" and "5.1.0.1559"
    - `dotnet test orchestrator/Argus.Orchestrator.sln` exits 0 (ProtoCodegenTests pass)
  </acceptance_criteria>
  <done>.NET solution builds; Grpc.Tools generates Argus.Detector.V1.* C# types from argus.proto; codegen tests pass proving DoubleValue wrapper and service client exist.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Python package with grpcio-tools codegen</name>
  <read_first>
    - proto/argus.proto (created in Task 1)
    - .planning/phases/01-foundations-streaming/01-RESEARCH.md (Standard Stack table; D-21 deps list; Installation block)
  </read_first>
  <files>detector/pyproject.toml, detector/requirements.txt, detector/argus_detector/__init__.py, detector/argus_detector/proto/__init__.py, detector/scripts/gen_proto.py, detector/tests/test_proto_codegen.py</files>
  <action>
    requirements.txt (exact, per D-21 / RESEARCH Installation block): `grpcio==1.81.0`, `grpcio-tools==1.81.0`, `grpcio-health-checking==1.81.0`, `river==0.25.0`, `pyod==3.6.0`, `joblib==1.5.3`, `numpy`, `pandas`, `pydantic`. Add `pytest` to a separate dev requirements section or include in pyproject optional-dependencies `dev`.
    pyproject.toml: `[project]` name `argus-detector`, version `0.1.0`, requires-python `>=3.12`, dependencies mirroring requirements.txt runtime entries. `[project.optional-dependencies] dev = ["pytest>=8"]`. Use `[build-system]` with setuptools. Configure package discovery for `argus_detector`.
    detector/argus_detector/__init__.py: empty (or `__version__ = "0.1.0"`).
    detector/argus_detector/proto/__init__.py: empty (generated stubs land here).
    detector/scripts/gen_proto.py: invoke `grpc_tools.protoc` programmatically with args `-I<repo>/proto`, `-I` for well-known types (grpc_tools includes them), `--python_out=detector/argus_detector/proto`, `--grpc_python_out=detector/argus_detector/proto`, `--pyi_out=detector/argus_detector/proto`, target `argus.proto`. After generation, rewrite the `import argus_pb2` line in `argus_pb2_grpc.py` to `from argus_detector.proto import argus_pb2` (known grpcio relative-import issue) OR generate with a package-aware path. Print success and exit 0.
    detector/tests/test_proto_codegen.py: pytest that runs gen_proto first (or asserts stubs present), then `from argus_detector.proto import argus_pb2, argus_pb2_grpc`; asserts `argus_pb2.Point` exists, `argus_pb2.Verdict` exists, and `argus_pb2_grpc.DetectorServiceStub` exists.
  </action>
  <verify>
    <automated>python detector/scripts/gen_proto.py && python -m pytest detector/tests/test_proto_codegen.py -x</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt contains "grpcio==1.81.0"
    - requirements.txt contains "grpcio-tools==1.81.0"
    - requirements.txt contains "river==0.25.0"
    - requirements.txt contains "grpcio-health-checking==1.81.0"
    - `python detector/scripts/gen_proto.py` exits 0
    - File detector/argus_detector/proto/argus_pb2.py exists after gen_proto runs
    - File detector/argus_detector/proto/argus_pb2_grpc.py exists after gen_proto runs
    - `python -m pytest detector/tests/test_proto_codegen.py` exits 0
  </acceptance_criteria>
  <done>Python codegen produces importable argus_pb2/argus_pb2_grpc stubs; test confirms Point/Verdict/DetectorServiceStub are present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → repo | Build tooling and generated code; no runtime network surface in this plan |
| proto contract → both runtimes | Wire format defined here governs all later cross-host messages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | proto/argus.proto field numbers | mitigate | Field numbers fixed and committed; .gitignore excludes generated stubs so contract is the only source of truth (prevents drift between hand-edited stubs and proto) |
| T-01-02 | Information Disclosure | deploy/certs private keys | mitigate | .gitignore excludes `deploy/certs/*.key` and `*.crt` so cert material (added in Plan 03) is never committed |
| T-01-03 | Tampering | supply chain (NuGet/PyPI deps) | accept | All dependency versions pinned exactly (Grpc.Tools 2.80.0, grpcio 1.81.0, etc.); pinning is the ASVS L1 control. No lockfile hashing in Phase 1 (low-value local-only build target) |
</threat_model>

<verification>
- `dotnet build orchestrator/Argus.Orchestrator.sln` exits 0
- `dotnet test orchestrator/Argus.Orchestrator.sln` passes
- `python detector/scripts/gen_proto.py` exits 0
- `python -m pytest detector/tests/test_proto_codegen.py` passes
- All four top-level directories exist
</verification>

<success_criteria>
- proto/argus.proto is the single source of truth, encoding DoubleValue (D-01), Timestamp (D-02), and DetectorService bidi ScoreStream (D-04)
- .NET and Python both generate stubs from the same proto with zero hand-edited stub files committed
- Mono-repo skeleton matches D-19 layout
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-streaming/01-01-SUMMARY.md`
</output>
