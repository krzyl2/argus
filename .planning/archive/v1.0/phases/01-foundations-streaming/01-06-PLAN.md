---
phase: 01-foundations-streaming
plan: 06
type: tdd
wave: 4
depends_on: [02]
files_modified:
  - detector/argus_detector/normalizer.py
  - detector/argus_detector/hst_detector.py
  - detector/argus_detector/registry.py
  - detector/tests/test_hst_detector.py
  - detector/tests/test_registry.py
  - detector/tests/test_score_zero_wire.py
autonomous: true
requirements: [STRM-03, FAULT-01]
requirements_addressed: [STRM-03, FAULT-01]

must_haves:
  truths:
    - "Each incoming Point is scored by River HalfSpaceTrees via per-entity registry"
    - "Per-entity online min-max normalization is applied before HST scoring"
    - "A single spike value produces a high anomaly score after warm-up (FAULT-01 point spike)"
    - "score=0.0 survives the wire as a present DoubleValue (proto3 default-drop mitigated)"
    - "Per-entity HST params (window, n_trees) are overridable from the params map"
  artifacts:
    - path: "detector/argus_detector/hst_detector.py"
      provides: "River HalfSpaceTrees wrapper with online min-max + warm-up tracking"
      contains: "HalfSpaceTrees"
    - path: "detector/argus_detector/normalizer.py"
      provides: "Per-entity online min-max normalization"
    - path: "detector/argus_detector/registry.py"
      provides: "(entity_id, detector) → EntityDetector instance map; real score_one"
      contains: "score_one"
  key_links:
    - from: "detector/argus_detector/servicer.py"
      to: "detector/argus_detector/registry.py"
      via: "registry.score_one in ScoreStream"
      pattern: "score_one"
    - from: "detector/argus_detector/hst_detector.py"
      to: "river.anomaly.HalfSpaceTrees"
      via: "score_one + learn_one"
      pattern: "HalfSpaceTrees"
---

<objective>
Implement the real River Half-Space Trees streaming detector behind the Plan 02 servicer/registry skeleton: per-entity online min-max normalization → HalfSpaceTrees.score_one → return score. Replace the placeholder 0.0 score in DetectorRegistry. Prove a point spike scores high (FAULT-01) and that score=0.0 survives the DoubleValue wire (PITFALL 1).

Purpose: STRM-03 is the detection core. River HST is the locked detector (D-09). Point spike detection (FAULT-01) is the natural output of HST + the orchestrator hysteresis gate. This is a TDD plan because the scoring behavior has clear input→output contracts.
Output: normalizer.py, hst_detector.py, real registry.score_one, full test suite including the score-zero wire test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundations-streaming/01-CONTEXT.md
@.planning/phases/01-foundations-streaming/01-RESEARCH.md
@.planning/phases/01-foundations-streaming/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 02: DetectorServicer.ScoreStream calls registry.score_one(entity_id, value); registry keyed by (entity_id, detector) -->

River 0.25.0 (RESEARCH Code Example) — implement EXACTLY this shape:
```python
from river import anomaly, preprocessing

class EntityDetector:
    def __init__(self, window=250, n_trees=25):
        self._normalizer = preprocessing.MinMaxScaler()
        self._model = anomaly.HalfSpaceTrees(n_trees=n_trees, height=8, window_size=window, seed=42)
        self._n_seen = 0
    def score_one(self, value: float) -> float:
        x = {"value": value}
        x_norm = self._normalizer.learn_one(x).transform_one(x)
        score = self._model.score_one(x_norm)
        self._model.learn_one(x_norm)
        self._n_seen += 1
        return score
    @property
    def is_warmed_up(self) -> bool:
        return self._n_seen >= self._model.window_size
```
Defaults D-09: window=250, n_trees=25. Per-entity overridable via params map (string→string; cast).
</interfaces>
</context>

<feature>
  <name>River HST per-entity streaming scorer</name>
  <files>detector/argus_detector/normalizer.py, detector/argus_detector/hst_detector.py, detector/argus_detector/registry.py</files>
  <behavior>
    RED tests (write first, must fail before implementation):
    - test_hst_detector: feeding a stable series (e.g., 300 readings of ~21.0 with tiny noise) then a single spike (e.g., 99.0) yields a spike score strictly greater than the median of the preceding stable-window scores (FAULT-01 point spike). [The exact threshold is hysteresis's job in the orchestrator; here we only assert spike >> baseline.]
    - test_hst_detector: is_warmed_up is False before window_size readings and True at/after window_size.
    - test_hst_detector: params {window:"50", n_trees:"10"} override defaults (assert the model's window_size == 50).
    - test_registry: registry.score_one("sensor.a", 21.0) and score_one("sensor.b", 21.0) maintain independent per-entity state (different EntityDetector instances; scoring "a" does not advance "b"'s n_seen).
    - test_score_zero_wire: build a Verdict with score=DoubleValue(value=0.0), serialize to bytes, parse back; assert `verdict.HasField("score")` is True and `verdict.score.value == 0.0` (PITFALL 1 — proves DoubleValue wrapper transmits 0.0). [This guards the contract independent of HST.]
  </behavior>
  <implementation>
    GREEN:
    normalizer.py: thin wrapper around `river.preprocessing.MinMaxScaler` exposing `learn_transform(value) -> dict` (or reuse River directly inside EntityDetector — either is fine; keep a module so Plan 2 can swap).
    hst_detector.py: `EntityDetector` exactly per the <interfaces> block. Add `from_params(params: dict[str,str]) -> EntityDetector` that casts window/n_trees from the string params map with D-09 defaults when absent.
    registry.py: replace the Plan 02 placeholder. `DetectorRegistry` holds `dict[tuple[str,str], EntityDetector]`. `score_one(entity_id, value, detector="hst", params=None)` lazily creates the EntityDetector (via from_params) on first sight of (entity_id, detector), then returns `detector.score_one(value)`. Thread-safety: guard the dict with a `threading.Lock` for creation (ScoreStream runs on a thread pool). Expose `is_warmed_up(entity_id, detector)`.
    Confirm servicer.py (Plan 02) already calls registry.score_one — if its signature changed, update the call site (servicer.py is owned by Plan 02; only adjust if the registry signature requires it, and note in SUMMARY).
    REFACTOR: extract any shared param-casting helper; keep score_one hot path allocation-light (STRM-04 latency budget).
  </implementation>
</feature>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — failing tests for HST scoring, registry isolation, and score-zero wire</name>
  <read_first>
    - .planning/phases/01-foundations-streaming/01-RESEARCH.md (River HalfSpaceTrees code example; PITFALL 1 proto3 default-drop; PITFALL 8 warm-up)
    - detector/argus_detector/servicer.py (Plan 02 — score_one call site)
  </read_first>
  <files>detector/tests/test_hst_detector.py, detector/tests/test_registry.py, detector/tests/test_score_zero_wire.py</files>
  <action>Write the five tests in <feature>.behavior. Run them; they MUST fail (EntityDetector/registry not yet implemented). Commit `test(01-06): add failing tests for HST scorer + score-zero wire`.</action>
  <verify>
    <automated>python -m pytest detector/tests/test_hst_detector.py detector/tests/test_registry.py detector/tests/test_score_zero_wire.py 2>&1 | python -c "import sys; d=sys.stdin.read(); print(d); sys.exit(0 if 'failed' in d or 'error' in d.lower() else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - test_hst_detector.py contains "is_warmed_up" and a spike-vs-baseline assertion
    - test_score_zero_wire.py contains "HasField" and "0.0"
    - Running the three test files fails (RED) before implementation
  </acceptance_criteria>
  <done>Five tests exist and fail for the right reason (missing implementation).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — implement normalizer, EntityDetector, and real registry.score_one</name>
  <read_first>
    - detector/tests/test_hst_detector.py (Task 1)
    - .planning/phases/01-foundations-streaming/01-RESEARCH.md (River code example; D-09 defaults)
    - detector/argus_detector/registry.py (Plan 02 placeholder)
  </read_first>
  <files>detector/argus_detector/normalizer.py, detector/argus_detector/hst_detector.py, detector/argus_detector/registry.py</files>
  <action>Implement per the <implementation> section so all five tests pass. Use river 0.25.0 anomaly.HalfSpaceTrees(n_trees, height=8, window_size, seed=42) + preprocessing.MinMaxScaler. Lazy per-(entity,detector) instance creation under a threading.Lock. Commit `feat(01-06): river HST per-entity streaming scorer`.</action>
  <verify>
    <automated>python -m pytest detector/tests/test_hst_detector.py detector/tests/test_registry.py detector/tests/test_score_zero_wire.py -x</automated>
  </verify>
  <acceptance_criteria>
    - hst_detector.py contains "HalfSpaceTrees" and "MinMaxScaler"
    - hst_detector.py contains "window_size" and "n_trees"
    - registry.py contains "score_one" and "Lock"
    - registry.py does NOT contain "TODO(plan06)" (placeholder removed)
    - `python -m pytest detector/tests/test_hst_detector.py detector/tests/test_registry.py detector/tests/test_score_zero_wire.py` exits 0
  </acceptance_criteria>
  <done>HST scorer implemented; spike scores above baseline (FAULT-01); per-entity isolation holds; score=0.0 survives the wire; all RED tests now GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ScoreStream input (Point.value) → detector | Numeric value already validated upstream; scored without side effects |
| per-entity model state | Shared across concurrent ScoreStream threads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | concurrent registry mutation | mitigate | threading.Lock guards lazy EntityDetector creation; per-entity instances isolate state (test_registry asserts isolation) |
| T-06-02 | Denial of Service | unbounded per-entity model growth | accept | HalfSpaceTrees window_size bounds memory; entity set bounded by entities.yaml; no untrusted entity creation path |
| T-06-03 | Tampering | proto3 drops score=0.0 | mitigate | DoubleValue wrapper (D-01); test_score_zero_wire asserts HasField after round-trip (PITFALL 1) |
| T-06-04 | Information Disclosure | input values in logs | accept | Sensor values are non-sensitive environmental readings; logged at debug only |
</threat_model>

<verification>
- `python -m pytest detector/tests/` passes (HST + registry + score-zero wire)
- grep confirms HalfSpaceTrees, MinMaxScaler, Lock present; no remaining placeholder TODO
</verification>

<success_criteria>
- River HST scores each point with per-entity min-max normalization (STRM-03, D-08/D-09)
- Point spike scores above baseline (FAULT-01)
- score=0.0 survives the DoubleValue wire (PITFALL 1 closed)
- Per-entity HST params overridable from the params map (CONF-02)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-streaming/01-06-SUMMARY.md`
</output>
