---
phase: 05-group-detection-core-proto-python-detectors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - proto/argus.proto
  - detector/argus_detector/proto/argus_pb2.py
  - detector/argus_detector/proto/argus_pb2_grpc.py
  - detector/argus_detector/proto/argus_pb2.pyi
  - detector/tests/test_proto_codegen.py
autonomous: true
requirements:
  - GRP-03
  - GRP-05
  - GRP-07
must_haves:
  truths:
    - "A gRPC caller can construct a GroupScoreRequest carrying multiple Series (a real 2D matrix), not a loop of univariate Point calls"
    - "The regenerated Python stubs import the new group messages and RPC stubs without ImportError"
    - "The DetectorService stub advertises ScoreGroupBatch and FitGroup RPCs alongside the existing univariate RPCs"
  artifacts:
    - path: "proto/argus.proto"
      provides: "Series, FeatureContribution, GroupScoreRequest, GroupScoreResponse, FitGroupRequest, FitGroupResponse messages + ScoreGroupBatch/FitGroup RPCs"
      contains: "message Series"
    - path: "detector/argus_detector/proto/argus_pb2.py"
      provides: "Regenerated Python message classes including Series"
    - path: "detector/tests/test_proto_codegen.py"
      provides: "Assertions that new group messages and RPC stubs exist"
  key_links:
    - from: "detector/argus_detector/proto/argus_pb2_grpc.py"
      to: "proto/argus.proto"
      via: "gen_proto.py regeneration of DetectorServiceStub with group RPCs"
      pattern: "ScoreGroupBatch"
  prohibitions:
    - "Do NOT reuse or overload ScoreBatch/ScoreBatchRequest for group scoring (clean seam per D-proto)"
    - "Do NOT change or remove any existing message field number or RPC (additive only)"
    - "Do NOT edit .NET csproj or write .NET consumer code (MSBuild regen is automatic; out of Phase 5 scope)"
---

<objective>
Rozszerz kontrakt proto o warstwę grupową: nowe wiadomości niosące prawdziwą macierz 2D (`repeated Series`) oraz dwa nowe RPC (`ScoreGroupBatch`, `FitGroup`) dodane obok istniejących RPC univariate — bez zmiany istniejących pól. Po edycji zregeneruj stuby Python i rozszerz test codegen o nowe symbole.

Purpose: Kontrakt musi wylądować pierwszy, bo detektory (Wave 2) i handlery servicera (Wave 3) importują wygenerowane `argus_pb2`/`argus_pb2_grpc`. Prawdziwa macierz 2D jest fundamentem success criterion 3 (joint anomaly, którego pętla univariate nie złapie).
Output: zaktualizowany `proto/argus.proto`, zregenerowane stuby Python, rozszerzony `test_proto_codegen.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-group-detection-core-proto-python-detectors/05-CONTEXT.md
@.planning/phases/05-group-detection-core-proto-python-detectors/05-RESEARCH.md
@.planning/phases/05-group-detection-core-proto-python-detectors/05-PATTERNS.md
@proto/argus.proto
@detector/scripts/gen_proto.py
@detector/tests/test_proto_codegen.py
</context>

<artifacts_this_phase_produces>
This plan contributes:
- Proto messages: `Series`, `FeatureContribution`, `GroupScoreRequest`, `GroupScoreResponse`, `FitGroupRequest`, `FitGroupResponse`
- Proto RPCs: `DetectorService.ScoreGroupBatch(GroupScoreRequest) returns (GroupScoreResponse)`, `DetectorService.FitGroup(FitGroupRequest) returns (FitGroupResponse)`
- Regenerated Python stubs exposing all of the above
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extend proto contract with group messages and RPCs</name>
  <files>proto/argus.proto</files>
  <read_first>
    - proto/argus.proto (existing Point/Verdict/ScoreBatchRequest/DetectorService shape; DoubleValue wrapper convention for null-vs-0.0)
    - .planning/phases/05-group-detection-core-proto-python-detectors/05-PATTERNS.md (exact locked message shape, lines 63-104)
  </read_first>
  <action>
    Append to proto/argus.proto (same `argus.v1` package, same `csharp_namespace`, no new file, all additive) the messages Series, FeatureContribution, GroupScoreRequest, GroupScoreResponse, FitGroupRequest, FitGroupResponse using the exact locked shape from 05-PATTERNS.md. Series holds member_id (string, field 1) + repeated double values (field 2) — a real per-member value array on a shared aligned timestamp axis. GroupScoreRequest carries group_id, detector (string; dispatched server-side on the name "peer_divergence"|"ecod"|"copod"|"pca"|"iforest" per RESEARCH Open Question 2 — no separate mode enum), map<string,string> params, repeated Series series. GroupScoreResponse is the union: repeated Verdict per_member (peer-divergence mode), Verdict group_verdict (joint mode), repeated FeatureContribution contributions (ranked; empty for non-attributable detectors), bool ok, string error. FitGroupRequest mirrors GroupScoreRequest fields; FitGroupResponse is bool ok + string error (mirrors FitResponse). Add the two RPCs ScoreGroupBatch and FitGroup to the existing DetectorService block, leaving the five existing RPCs unchanged. Reuse the existing Verdict message for per-member and group scores (do not define a parallel score message). Do NOT alter any existing field number or RPC.
  </action>
  <verify>
    <automated>grep -q "message Series" proto/argus.proto && grep -q "message GroupScoreRequest" proto/argus.proto && grep -q "message FeatureContribution" proto/argus.proto && grep -q "rpc ScoreGroupBatch" proto/argus.proto && grep -q "rpc FitGroup" proto/argus.proto && grep -c "rpc ScoreBatch(ScoreBatchRequest)" proto/argus.proto</automated>
  </verify>
  <done>argus.proto contains all six new messages and both new RPCs inside the unchanged DetectorService block; the five original RPCs and all existing field numbers are intact.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate Python stubs and extend codegen test</name>
  <files>detector/argus_detector/proto/argus_pb2.py, detector/argus_detector/proto/argus_pb2_grpc.py, detector/argus_detector/proto/argus_pb2.pyi, detector/tests/test_proto_codegen.py</files>
  <read_first>
    - detector/scripts/gen_proto.py (regen invocation + relative-import fixup; .NET regen is automatic via MSBuild — no .NET step here, per RESEARCH Pitfall 6)
    - detector/tests/test_proto_codegen.py (existing assertion style; autouse session fixture already re-runs gen_proto.py)
  </read_first>
  <action>
    Run `python detector/scripts/gen_proto.py` from repo root to regenerate argus_pb2.py, argus_pb2_grpc.py, and argus_pb2.pyi with the new group messages and RPC stubs (RESEARCH Pitfall 6: Python regen is manual and MUST run before Python tests touch the new symbols; the .NET side regenerates automatically on dotnet build via the existing Protobuf MSBuild item and needs no action in this phase). Then extend test_proto_codegen.py following the existing hasattr-based assertion style: add tests asserting argus_pb2 has Series, FeatureContribution, GroupScoreRequest, GroupScoreResponse, FitGroupRequest, FitGroupResponse; assert a Series instance round-trips a member_id and a values list (proves field numbers preserved and repeated-double works); and assert argus_pb2_grpc.DetectorServiceStub instances expose ScoreGroupBatch and FitGroup callables (verifies the RPC stubs regenerated). Do not remove or weaken any existing test.
  </action>
  <verify>
    <automated>cd detector && python scripts/gen_proto.py && python -m pytest tests/test_proto_codegen.py -x -q</automated>
  </verify>
  <done>gen_proto.py exits 0; test_proto_codegen.py passes including the new assertions for all six group messages and both RPC stubs; existing codegen tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gRPC caller → DetectorService | Untrusted request payloads (group_id, series arrays) cross here; validation is enforced in the servicer handlers (Plan 05-04), not in the proto layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | proto contract additive edit | mitigate | Additive-only: no existing field number or RPC changed; codegen test asserts existing Point/Verdict/Fit messages still present so a wire-breaking edit fails CI |
| T-05-02 | Information Disclosure | new messages | accept | Same mTLS-protected DetectorService channel (D4); no new field exposes secrets — group_id + numeric sensor values only |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package install in this plan (scikit-learn pin lands in Plan 05-03); no [ASSUMED]/[SUS] package added here |
</threat_model>

<verification>
- `python detector/scripts/gen_proto.py` exits 0 (idempotent regen)
- `python -m pytest detector/tests/test_proto_codegen.py -q` passes
- Existing five RPCs and all existing message field numbers unchanged (additive-only)
</verification>

<success_criteria>
- proto/argus.proto carries a real 2D matrix contract (repeated Series with repeated double values) and the two new group RPCs, additive to the existing DetectorService
- Regenerated Python stubs import the new symbols without error and the codegen test proves it
</success_criteria>

<output>
Create `.planning/phases/05-group-detection-core-proto-python-detectors/05-01-SUMMARY.md` when done
</output>
