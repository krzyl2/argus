---
phase: 05-group-detection-core-proto-python-detectors
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - detector/argus_detector/group/__init__.py
  - detector/argus_detector/group/peer_divergence.py
  - detector/tests/test_peer_divergence.py
autonomous: true
requirements:
  - GRP-03
  - GRP-04
must_haves:
  truths:
    - "Given a (n_timestamps, n_members) matrix with one clear outlier member, peer-divergence flags WHICH member diverges via a robust modified z-score across members per timestamp"
    - "A group with fewer than 3 members produces no verdict (score unavailable / NaN), never a false 'not anomalous' verdict"
    - "When all members report identical values (MAD=0), the detector returns a concrete not-flagged verdict, not NaN and not a divide-by-zero error — distinct from the below-floor no-verdict case"
  artifacts:
    - path: "detector/argus_detector/group/peer_divergence.py"
      provides: "PeerDivergenceDetector — stateless modified z-score scorer with MAD=0 meanAD fallback and min-member floor"
      contains: "class PeerDivergenceDetector"
      min_lines: 40
    - path: "detector/tests/test_peer_divergence.py"
      provides: "Scoring, floor, MAD=0/meanAD edge-case tests using hand-verified fixtures"
      contains: "class TestPeerDivergence"
  key_links:
    - from: "detector/tests/test_peer_divergence.py"
      to: "detector/argus_detector/group/peer_divergence.py"
      via: "imports and exercises PeerDivergenceDetector.score_batch"
      pattern: "PeerDivergenceDetector"
  prohibitions:
    - "Do NOT use ADTK or any PyOD class for peer-divergence — it is hand-rolled numpy per D-peer (PyOD has no cross-member primitive)"
    - "Do NOT give PeerDivergenceDetector a persisted model / joblib bundle — it is stateless (Fit/Save is a no-op, wired in Plan 05-04)"
    - "Do NOT collapse the below-floor no-verdict case and the MAD=0 all-normal case into the same representation (they are semantically distinct per RESEARCH Pitfall 4)"
---

<objective>
Zaimplementuj peer-divergence jako bezstanowy moduł numpy: modified z-score `0.6745·(x − median) / MAD` liczony per-timestamp across members, próg `|z| > 3.5`, floor minimalnej liczby członków = 3 (poniżej → brak werdyktu), oraz guard MAD=0 z fallbackiem na meanAD. Kod formuł skopiuj wprost z 05-RESEARCH.md (zweryfikowany numerycznie) — nie wyprowadzaj stałych ponownie.

Purpose: GRP-03 (który członek grupy odbiega od robustnego konsensusu median/MAD) + GRP-04 (bezpieczna degradacja poniżej floor). Detektor musi być testowalny w izolacji ręcznie konstruowanymi macierzami — to twarde success criterion tej fazy.
Output: `group/peer_divergence.py` + `group/__init__.py` + `test_peer_divergence.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-group-detection-core-proto-python-detectors/05-CONTEXT.md
@.planning/phases/05-group-detection-core-proto-python-detectors/05-RESEARCH.md
@.planning/phases/05-group-detection-core-proto-python-detectors/05-PATTERNS.md
@detector/argus_detector/stl_detector.py
@detector/tests/test_stl_detector.py
@detector/tests/test_pyod_detector.py
</context>

<artifacts_this_phase_produces>
This plan contributes:
- Python module `detector/argus_detector/group/__init__.py` (new package)
- Python class `PeerDivergenceDetector` in `group/peer_divergence.py` with `score_batch(matrix) -> (scores, flags, error)` tuple contract
- Module-level functions `modified_zscore(row)` and `score_group(matrix)` (copied verbatim from 05-RESEARCH.md Code Examples)
- Test file `detector/tests/test_peer_divergence.py`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement stateless PeerDivergenceDetector</name>
  <files>detector/argus_detector/group/__init__.py, detector/argus_detector/group/peer_divergence.py</files>
  <read_first>
    - .planning/phases/05-group-detection-core-proto-python-detectors/05-RESEARCH.md (Code Examples: modified_zscore + score_group functions, pre-verified numerically — copy these constants and logic verbatim)
    - detector/argus_detector/stl_detector.py (stateless-no-fit module pattern, tuple-return contract, docstring style)
    - .planning/phases/05-group-detection-core-proto-python-detectors/05-PATTERNS.md (PeerDivergenceDetector class shape, lines 139-146)
  </read_first>
  <behavior>
    - modified_zscore on a row with one outlier returns a value whose absolute magnitude exceeds 3.5 for the outlier and stays small for the consensus members
    - modified_zscore on a row where MAD=0 but meanAD>0 (e.g. [10,10,10,50]) still flags the outlier using the meanAD constant, no divide-by-zero
    - modified_zscore on an all-identical row returns all zeros (no divergence possible), never NaN
    - score_group on a matrix with fewer than 3 members returns NaN-filled scores signalling no verdict (GRP-04 floor)
    - score_group on a valid matrix returns (scores, flags) both shape (n_timestamps, n_members) with flags = |z| > 3.5
  </behavior>
  <action>
    Create the group package (empty group/__init__.py) and peer_divergence.py. Copy the modified_zscore and score_group functions verbatim from 05-RESEARCH.md Code Examples (they encode the Iglewicz-Hoaglin 0.6745 MAD constant, the 0.7979 meanAD-fallback constant, the _THRESHOLD=3.5 and _MIN_MEMBERS=3 module constants, and the MAD=0 -> meanAD -> all-zeros guard chain — all numerically verified; do not re-derive). Document the 0.7979 meanAD constant source as the Iglewicz-Hoaglin statistics convention in a code comment (RESEARCH Assumptions A2 / Open Question 1 resolved as Claude's discretion). Wrap them in a stateless PeerDivergenceDetector class matching the StlDetector shape: no fit(), no saved model, a score_batch(matrix) method returning a (scores, flags, error) tuple where error is a string and scores/flags are None when n_members < 3 (below-floor no-verdict, GRP-04), mirroring StlDetector's "return an error tuple, do not raise, for the not-enough-data case". Keep the below-floor no-verdict case (error string set, scores None) representationally distinct from the MAD=0 all-normal case (concrete 0.0 scores, no error) per RESEARCH Pitfall 4.
  </action>
  <verify>
    <automated>cd detector && python -c "from argus_detector.group.peer_divergence import PeerDivergenceDetector; d=PeerDivergenceDetector(); s,f,e=d.score_batch([[10.0,10.0]]); print('floor', e is not None and s is None)"</automated>
  </verify>
  <done>PeerDivergenceDetector imports; score_batch on a <3-member matrix returns an error tuple with scores None; on a valid matrix returns per-member/per-timestamp scores and flags; MAD=0 path returns concrete zeros without warning.</done>
</task>

<task type="auto">
  <name>Task 2: Test peer-divergence scoring, floor, and MAD=0 edge cases</name>
  <files>detector/tests/test_peer_divergence.py</files>
  <read_first>
    - detector/tests/test_pyod_detector.py (class-grouped test naming convention)
    - detector/tests/test_stl_detector.py (stateless-detector test shape)
    - .planning/phases/05-group-detection-core-proto-python-detectors/05-RESEARCH.md (Code Examples: MAD=0/meanAD fixture [10,10,10,50] -> z last ~3.19, numerically confirmed — reuse verbatim)
  </read_first>
  <action>
    Create test_peer_divergence.py mirroring the class-grouped style of test_pyod_detector.py. Add TestPeerDivergenceScoring (a matrix where one member is a clear outlier at some timestamp is flagged, consensus members are not), TestPeerDivergenceFloor (a <3-member matrix returns the no-verdict error tuple with scores None — GRP-04), and TestPeerDivergenceEdgeCases (MAD=0 with meanAD>0 flags the outlier using the [10,10,10,50] fixture from RESEARCH with expected z last-element ~3.19; all-identical row returns zeros not NaN; assert no RuntimeWarning is raised on the MAD=0 path). Use the exact hand-verified fixtures from 05-RESEARCH.md Code Examples for the MAD=0/meanAD case — already numerically confirmed, do not re-derive.
  </action>
  <verify>
    <automated>cd detector && python -m pytest tests/test_peer_divergence.py -x -q</automated>
  </verify>
  <done>All peer-divergence tests pass: outlier-member flagged, consensus not flagged, below-floor returns no-verdict, MAD=0 meanAD fallback flags correctly, all-identical returns zeros without warning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller → PeerDivergenceDetector.score_batch | Untrusted matrix shape/values; ragged-matrix and empty-group validation is enforced at the servicer boundary (Plan 05-04), but the detector must not divide-by-zero or emit NaN-as-false internally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Denial of Service | modified z-score on MAD=0 input | mitigate | Explicit MAD==0 guard with meanAD fallback then all-zeros — no divide-by-zero, no NaN/inf propagation (RESEARCH Pitfall 3); asserted by an edge-case test |
| T-05-04 | Tampering | below-floor / degenerate matrix producing a misleading verdict | mitigate | Below-floor (<3 members) returns explicit no-verdict (scores None + error), never a false not-anomalous verdict (GRP-04); kept representationally distinct from MAD=0 all-normal case |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package install in this plan; peer-divergence is hand-rolled numpy (numpy already present), no new dependency |
</threat_model>

<verification>
- `python -m pytest detector/tests/test_peer_divergence.py -q` passes
- No `RuntimeWarning: invalid value encountered in divide` in test output (MAD=0 guard proven)
- Below-floor matrix returns no-verdict distinct from MAD=0 all-normal (both edge cases asserted separately)
</verification>

<success_criteria>
- Peer-divergence correctly identifies which member diverges from the group's robust (median/MAD) consensus
- No verdict emitted below the minimum-member floor of 3
- MAD=0 handled as a concrete no-anomaly result, not an error or NaN
</success_criteria>

<output>
Create `.planning/phases/05-group-detection-core-proto-python-detectors/05-02-SUMMARY.md` when done
</output>
