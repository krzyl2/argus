---
phase: 11-new-standalone-screens-dashboard-algorithms-settings
plan: 03
type: execute
wave: 2
depends_on: [11-02]
files_modified:
  - orchestrator/ui/src/components/DashboardPage.tsx
  - orchestrator/ui/src/state/dashboard.ts
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03]
must_haves:
  truths:
    - "Dashboard shows 4 KPI tiles: Monitored sensors, Groups, Active group detectors, Home Assistant"
    - "Monitored-sensor and group counts are real, fetched from GET /api/sensors and GET /api/groups"
    - "The Home Assistant tile, Recent anomalies section, and System health section each carry a visible in-UI 'mocked' TODO marker"
    - "If /api/sensors or /api/groups fails, the failed KPI values render '—' (never a stale/zero fake) and an error banner shows"
  artifacts:
    - path: "orchestrator/ui/src/components/DashboardPage.tsx"
      provides: "Full Dashboard: KPI row + Recent anomalies (mock) + System health (mock)"
      contains: "argus-dashboard-kpi-row"
      min_lines: 60
    - path: "orchestrator/ui/src/state/dashboard.ts"
      provides: "KPI data signals + loadDashboard() fetching sensors/groups counts"
      contains: "loadDashboard"
  key_links:
    - from: "orchestrator/ui/src/state/dashboard.ts"
      to: "GET /api/sensors + GET /api/groups"
      via: "apiGet('api/sensors') and apiGet('api/groups') to derive KPI counts"
      pattern: "apiGet"
    - from: "orchestrator/ui/src/components/DashboardPage.tsx"
      to: "orchestrator/ui/src/state/dashboard.ts"
      via: "page reads KPI signals and renders KpiTile grid"
      pattern: "KpiTile"
---

<objective>
Flesh out the Dashboard screen (DASH-01/02/03) on the Phase 11-02 skeleton: a 4-tile KPI row with real counts where an existing API supplies them, plus explicitly-marked mock "Recent anomalies" and "System health" sections.

Purpose: Give operators an at-a-glance overview while being scrupulously honest about which data is live vs. mocked — no silent fakes (D-01/D-02/D-03).
Output: A complete `DashboardPage.tsx` and a `state/dashboard.ts` data module.

**Resolved Flagged Conflict #1 (from 11-UI-SPEC):** D-01 locks "Dashboard is frontend-only (no new backend)". `GET /api/sensors` does not expose per-sensor detector counts, so "sum of per-sensor detectors + group count" cannot be computed frontend-only. Honoring the locked no-backend constraint, adopt the UI-SPEC fallback: tile 3 is labelled **"Active group detectors"** with value `groups.length` (each group has exactly one detector) — truthful, no new backend, never a wrong number under a misleading label. Do NOT add a `detectorCount` field to /api/sensors (that would violate D-01's frontend-only constraint).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-new-standalone-screens-dashboard-algorithms-settings/11-CONTEXT.md
@.planning/phases/11-new-standalone-screens-dashboard-algorithms-settings/11-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: KPI row with real + mock tiles (D-01)</name>
  <files>orchestrator/ui/src/state/dashboard.ts, orchestrator/ui/src/components/DashboardPage.tsx</files>
  <read_first>
    - orchestrator/ui/src/state/groups.ts (signals + loader + apiGet pattern to mirror)
    - orchestrator/ui/src/api/client.ts (apiGet — relative path, no leading slash)
    - orchestrator/ui/src/api/types.ts (SensorsResponse.entries[].isTracked, GroupsResponse.groups)
    - orchestrator/ui/src/components/KpiTile.tsx (props: label, value, accent, status, hint — note: passing `status` renders a StatusDot INSTEAD of the value)
    - orchestrator/ui/src/components/Banner.tsx (tone="error" for load failure)
    - .planning/phases/11-new-standalone-screens-dashboard-algorithms-settings/11-UI-SPEC.md "Dashboard" KPI tiles + Copywriting (mock marker + data-load error copy)
  </read_first>
  <action>
    Create `state/dashboard.ts` exporting signals for the KPI inputs (e.g. `trackedCount`, `groupCount`, `loadError`) and an async `loadDashboard()` that calls `apiGet<SensorsResponse>('api/sensors')` and `apiGet<GroupsResponse>('api/groups')`, sets `trackedCount = entries.filter(e => e.isTracked).length` and `groupCount = groups.length`, and on any failure sets `loadError = true` (leaving counts null so the UI renders "—"). Mirror the loader/signals convention in state/groups.ts. Use relative paths (no leading slash) per apiGet's contract.
    In `DashboardPage.tsx`, below the existing `.argus-page-header`, render a `.argus-dashboard-kpi-row` with exactly 4 `KpiTile`s in this order: (1) `label="Monitored sensors"`, value = trackedCount (or "—" on error), `accent`; (2) `label="Groups"`, value = groupCount (or "—"); (3) `label="Active group detectors"`, value = groupCount (or "—"), `hint="group detectors"` — per Resolved Conflict #1; (4) `label="Home Assistant"`, value="Connected", `hint="mocked — no endpoint yet"` and do NOT pass a `status` prop (a StatusDot would falsely claim a verified live state). Call `loadDashboard()` in a `useEffect` on mount. When `loadError` is true, render a `Banner tone="error"` with the UI-SPEC copy ("Couldn't load sensor/group counts. Refresh to try again.").
  </action>
  <acceptance_criteria>
    - state/dashboard.ts exports loadDashboard and derives trackedCount from isTracked and groupCount from groups.length via apiGet.
    - DashboardPage renders exactly 4 KpiTile elements inside .argus-dashboard-kpi-row in the specified order.
    - The Home Assistant tile passes a hint containing "mocked" and passes NO status prop.
    - Failed loads render "—" for numeric tiles and an error Banner (no zero/stale value shown as real).
    - `npm --prefix orchestrator/ui run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix orchestrator/ui run build</automated>
  </verify>
  <done>KPI row shows real monitored/group counts, a truthful "Active group detectors" tile, and a clearly-mocked HA tile; load errors degrade to "—" + error banner.</done>
</task>

<task type="auto">
  <name>Task 2: Recent anomalies + System health mock sections (D-02/D-03)</name>
  <files>orchestrator/ui/src/components/DashboardPage.tsx</files>
  <read_first>
    - orchestrator/ui/src/components/Card.tsx, Banner.tsx, StatusDot.tsx, Badge.tsx (props for row rendering)
    - Argus Design System/ui_kits/admin/Dashboard.jsx (layout reference ONLY — its ARGUS_DATA is mock; do not copy behaviors)
    - Argus Design System/ui_kits/admin/data.js (shape reference for the mock rows)
    - .planning/phases/11-new-standalone-screens-dashboard-algorithms-settings/11-UI-SPEC.md "Recent anomalies" + "System health" (exact mock datasets, severity→status mapping, banner copy)
  </read_first>
  <action>
    Below the KPI row, add a `.argus-dashboard-layout` two-column container. Left column "Recent anomalies" (D-02): `.argus-section-label` "Recent anomalies" → `Banner tone="info"` (no dismiss) with copy "Mocked — no anomaly-history endpoint yet. Showing example data." → a `Card` containing one row per mock anomaly using the EXACT dataset from the UI-SPEC (5 entries, real product identifiers). Each row: `StatusDot` with severity→status mapping (high→error, med→warn, low→idle) + entity/group identifier (mono, ellipsis-truncated) + secondary line ("sensor"/"group" · detector · relative time) + a `Badge` showing the score to 2 decimals (tone error/warn/neutral for high/med/low). Divider between rows, none after the last.
    Right column "System health" (D-03): `.argus-section-label` "System health" → `Banner tone="info"` (no dismiss) with copy "Mocked — no /api/health endpoint yet. Showing example data." → a `Card` with one row per mock health item using the EXACT UI-SPEC dataset (5 items). Each row: `StatusDot` (status per item; do NOT use a `pulse` prop — it does not exist) + label (primary) + detail (secondary). Do NOT build the reference kit's "Quick actions" block (out of scope).
    Define the two mock datasets as local const arrays in the component. Both mock markers must be visibly rendered so no mock is mistaken for a live feed (hard acceptance requirement).
  </action>
  <acceptance_criteria>
    - DashboardPage renders a .argus-section-label "Recent anomalies" and "System health".
    - Both sections render a Banner whose text contains "Mocked".
    - Recent anomalies renders the 5 UI-SPEC mock rows with severity→status mapping; System health renders the 5 UI-SPEC health rows.
    - No "Quick actions" block is present; no `pulse` prop passed to StatusDot.
    - `npm --prefix orchestrator/ui run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix orchestrator/ui run build</automated>
  </verify>
  <done>Both mock sections render with visible TODO/mock banners and the exact UI-SPEC datasets; no silent fakes, no out-of-scope blocks.</done>
</task>

</tasks>

<threat_model>
Not applicable — Dashboard is frontend-only (D-01), adds no HTTP surface, handles no secrets. It consumes existing authorized read endpoints (/api/sensors, /api/groups) via apiGet.
</threat_model>

<verification>
- `npm --prefix orchestrator/ui run build` succeeds.
- Manual: #/dashboard shows 4 KPI tiles (2 real counts, truthful active-detector tile, clearly-mocked HA tile) and two mock sections each with a visible "Mocked" banner, correct in light and dark mode.
- Simulated fetch failure: numeric tiles show "—" and an error banner (never 0-as-real).
</verification>

<success_criteria>
Dashboard renders real monitored-sensor and group counts, a truthful active-group-detectors tile, and explicitly-marked mock HA tile / Recent anomalies / System health sections — matching DASH-01/02/03 and the honesty requirement.
</success_criteria>

## Artifacts this phase produces (Plan 11-03)

- `state/dashboard.ts` — new module: KPI signals + `loadDashboard()`.
- `DashboardPage` body — KPI row + Recent anomalies (mock) + System health (mock).
- No new backend, no new API types, no new CLI flags.

<output>
Create `.planning/phases/11-new-standalone-screens-dashboard-algorithms-settings/11-03-SUMMARY.md` when done.
</output>
