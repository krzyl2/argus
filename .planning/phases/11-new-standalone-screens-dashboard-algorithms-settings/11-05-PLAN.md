---
phase: 11-new-standalone-screens-dashboard-algorithms-settings
plan: 05
type: execute
wave: 2
depends_on: [11-01, 11-02]
files_modified:
  - orchestrator/ui/src/components/SettingsPage.tsx
  - orchestrator/ui/src/state/settings.ts
autonomous: true
requirements: [SET-01]
must_haves:
  truths:
    - "Settings shows three sections: Connections (read-only), Batch & detection (read-only), Appearance (functional)"
    - "Connections + Batch & detection render live values from GET /api/settings, as disabled controls"
    - "Appearance offers a Light/Dark control (no System option) bound to the shared theme signal — toggling it also updates the sidebar toggle live"
    - "If /api/settings fails, an error banner shows and no fabricated config values are displayed"
  artifacts:
    - path: "orchestrator/ui/src/components/SettingsPage.tsx"
      provides: "Full Settings screen: Connections + Batch & detection + Appearance"
      contains: "Appearance"
      min_lines: 60
    - path: "orchestrator/ui/src/state/settings.ts"
      provides: "Settings signal + loadSettings() fetching GET /api/settings"
      contains: "loadSettings"
  key_links:
    - from: "orchestrator/ui/src/state/settings.ts"
      to: "GET /api/settings"
      via: "apiGet('api/settings') returning SettingsResponse"
      pattern: "api/settings"
    - from: "orchestrator/ui/src/components/SettingsPage.tsx"
      to: "orchestrator/ui/src/state/theme.ts"
      via: "Appearance control reads theme signal and calls setTheme"
      pattern: "setTheme"
---

<objective>
Flesh out the Settings screen (SET-01) on the Phase 11-02 skeleton: three sections per the reference kit — read-only Connections and Batch & detection driven by `GET /api/settings` (from Plan 11-01), plus a functional Appearance section wired to the shared theme signal (from Plan 11-02).

Purpose: Show truthful live configuration read-only, and provide the one functional control (theme) as a second surface over the same shared theme state (D-08/D-09).
Output: A complete `SettingsPage.tsx` and a `state/settings.ts` data module.

**Resolved Flagged Conflict #2:** Plan 11-01's endpoint sources `logLevel` from `IConfiguration["Logging:LogLevel:Default"]`, so the "Log level" row is kept (rendered read-only from the endpoint value). If the value is null, render the control disabled showing an em-dash rather than a fabricated level.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-new-standalone-screens-dashboard-algorithms-settings/11-CONTEXT.md
@.planning/phases/11-new-standalone-screens-dashboard-algorithms-settings/11-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings fetch + read-only Connections & Batch sections (D-08)</name>
  <files>orchestrator/ui/src/state/settings.ts, orchestrator/ui/src/components/SettingsPage.tsx</files>
  <read_first>
    - orchestrator/ui/src/state/groups.ts (signals + loader + apiGet convention)
    - orchestrator/ui/src/api/client.ts (apiGet — relative path)
    - orchestrator/ui/src/api/types.ts (SettingsResponse — added by Plan 11-01)
    - orchestrator/ui/src/components/Input.tsx, Select.tsx, Card.tsx, Badge.tsx, Banner.tsx (props; Input/Select accept a `disabled` prop)
    - orchestrator/ui/public/css/argus.css (.argus-param-field__label for visible field labels; .argus-param-grid two-column recipe; .argus-section-label / Settings layout classes from Plan 11-02)
    - .planning/phases/11-new-standalone-screens-dashboard-algorithms-settings/11-UI-SPEC.md "Settings" sections 1 & 2 (exact fields, labels, units, the Connections caption/badge) + Copywriting (settings data-load error)
  </read_first>
  <action>
    Create `state/settings.ts` exporting a `settings` signal (`SettingsResponse | null`), a `loadError` signal, and an async `loadSettings()` calling `apiGet<SettingsResponse>('api/settings')`; on failure set `loadError = true` and leave settings null. Mirror state/groups.ts.
    In `SettingsPage.tsx`, below the existing `.argus-page-header`, call `loadSettings()` in a mount useEffect and render the Settings layout container (max-width 720px, sections gap --space-lg from Plan 11-02 CSS). Section 1 **Connections** (`.argus-section-label` "Connections" → `Card`): a caption row "Home Assistant & MQTT are injected automatically by the Supervisor." + `Badge tone="ok"` "auto" (do NOT show HA URL / MQTT host / any token field — excluded by D-07); a disabled mono `Input` for `detectorEndpoint` with a visible field label "Detector gRPC endpoint (mTLS)" using the `.argus-param-field__label` pattern; a two-column `.argus-param-grid` row of disabled mono `Input`s for "InfluxDB URL (optional)" (`influxUrl`) and "InfluxDB bucket" (`influxBucket`). Section 2 **Batch & detection** (`.argus-section-label` → `Card`): two-column row of disabled numeric `Input`s "Batch interval (minutes)" (`batchIntervalMinutes`) and "Nightly fit hour (UTC)" (`nightlyFitHour`); a "Log level" disabled `Select` (options debug/info/warning) bound to `logLevel` — if `logLevel` is null show the control disabled with no fabricated value. All Connections/Batch controls are `disabled` (read-only — no SaveBar, D-06). When `loadError`, render a `Banner tone="error"` with the UI-SPEC copy and do not render fabricated field values.
  </action>
  <acceptance_criteria>
    - state/settings.ts exports loadSettings fetching api/settings via apiGet into a SettingsResponse signal.
    - SettingsPage renders .argus-section-label "Connections" and "Batch & detection", each in a Card, with all inputs `disabled`.
    - HA URL / MQTT host / token fields are NOT rendered anywhere in SettingsPage.
    - Load failure renders an error Banner and no fabricated values.
    - `npm --prefix orchestrator/ui run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix orchestrator/ui run build</automated>
  </verify>
  <done>Connections + Batch & detection render live, read-only values from /api/settings; no secret fields; errors degrade gracefully.</done>
</task>

<task type="auto">
  <name>Task 2: Functional Appearance section (D-09)</name>
  <files>orchestrator/ui/src/components/SettingsPage.tsx</files>
  <read_first>
    - orchestrator/ui/src/state/theme.ts (theme signal + setTheme — created in Plan 11-02)
    - orchestrator/ui/src/components/Sidebar.tsx (how the sidebar consumes the shared theme signal — the Appearance control is a second surface over the SAME state)
    - orchestrator/ui/public/css/argus.css (.argus-sensitivity-preset-picker family: __options / __option classes to reuse directly in JSX)
    - .planning/phases/11-new-standalone-screens-dashboard-algorithms-settings/11-UI-SPEC.md "Settings" section 3 Appearance + "Required refactor note"
  </read_first>
  <action>
    Add Section 3 **Appearance** to `SettingsPage.tsx` (`.argus-section-label` "Appearance" → `Card`): a "Theme" `.argus-label` caption, then a 2-option Light/Dark radio group reusing the existing `.argus-sensitivity-preset-picker` family classes (`__options`/`__option`) directly in plain JSX — do NOT repurpose the typed `SensitivityPresetPicker.tsx` component (it is hardwired to DetectorCatalogEntry). The current selection reflects `theme.value` from `state/theme.ts`; selecting an option calls `setTheme('light'|'dark')`. No "System" option (D-09 — Light/Dark only). No new theme logic and no new localStorage key — reuse the shared signal so the Sidebar toggle updates live when Appearance changes and vice versa. Changes apply immediately; no SaveBar.
  </action>
  <acceptance_criteria>
    - SettingsPage renders an Appearance section with exactly two options (Light, Dark) and no System option.
    - The control reads theme.value and calls setTheme from state/theme.ts (no local theme useState, no direct localStorage write in SettingsPage).
    - `npm --prefix orchestrator/ui run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix orchestrator/ui run build</automated>
  </verify>
  <done>Appearance offers a Light/Dark control bound to the shared theme signal; switching there updates the sidebar toggle live and persists via the existing mechanism.</done>
</task>

</tasks>

<threat_model>
Not applicable — this plan adds no new HTTP surface. It consumes GET /api/settings (whose secret-redaction + authorization threats are modeled and mitigated in Plan 11-01, T-11-01/T-11-02). The Appearance control persists only a non-sensitive 'light'|'dark' value via the existing shared theme mechanism.
</threat_model>

<verification>
- `npm --prefix orchestrator/ui run build` succeeds.
- Manual: #/settings shows three sections; Connections/Batch render live disabled values from /api/settings with no secret fields; Appearance toggles theme and the sidebar toggle updates in sync; correct in both themes.
- Simulated /api/settings failure: error banner shows, no fabricated config values.
</verification>

<success_criteria>
Settings shows read-only live Connections + Batch & detection from /api/settings and a functional Light/Dark Appearance control sharing state with the sidebar — matching SET-01 and D-08/D-09.
</success_criteria>

## Artifacts this phase produces (Plan 11-05)

- `state/settings.ts` — new module: `settings` signal + `loadSettings()`.
- `SettingsPage` body — Connections (read-only) + Batch & detection (read-only) + Appearance (functional).
- No new backend (consumes 11-01's endpoint), no new API types (reuses SettingsResponse), no new CLI flags.

<output>
Create `.planning/phases/11-new-standalone-screens-dashboard-algorithms-settings/11-05-SUMMARY.md` when done.
</output>
