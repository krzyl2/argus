---
phase: 11-new-standalone-screens-dashboard-algorithms-settings
plan: 04
type: execute
wave: 2
depends_on: [11-02]
files_modified:
  - orchestrator/ui/src/components/AlgorithmsPage.tsx
  - orchestrator/ui/src/state/algorithms.ts
autonomous: true
requirements: [ALGO-07, ALGO-08]
must_haves:
  truths:
    - "Algorithms shows a read-only catalog of exactly the 5 group detectors from GET /api/detectors/catalog, in catalog order"
    - "Each card's 'best for' copy is rendered verbatim from the catalog API, never hardcoded client-side"
    - "Each card shows the Low/Med/High presets and the parameter schema"
    - "There is no SaveBar, no editable controls, and no 'Single sensors' section"
  artifacts:
    - path: "orchestrator/ui/src/components/AlgorithmsPage.tsx"
      provides: "Read-only detector catalog browse (5 cards)"
      contains: "detectors"
      min_lines: 50
    - path: "orchestrator/ui/src/state/algorithms.ts"
      provides: "Catalog signal + loadCatalog() fetching GET /api/detectors/catalog"
      contains: "loadCatalog"
  key_links:
    - from: "orchestrator/ui/src/state/algorithms.ts"
      to: "GET /api/detectors/catalog"
      via: "apiGet('api/detectors/catalog') returning DetectorCatalog"
      pattern: "detectors/catalog"
    - from: "orchestrator/ui/src/components/AlgorithmsPage.tsx"
      to: "orchestrator/ui/src/state/algorithms.ts"
      via: "page maps catalog.detectors to read-only cards"
      pattern: "bestFor"
---

<objective>
Flesh out the Algorithms screen (ALGO-07/08) on the Phase 11-02 skeleton: a read-only browse of the 5 group detectors sourced entirely from `GET /api/detectors/catalog`.

Purpose: Let operators browse detector capabilities (name, "best for…" guidance, presets, param schema) without any editing surface — distinct from the in-flow AlgorithmChooser wizard (D-05).
Output: A complete `AlgorithmsPage.tsx` and a `state/algorithms.ts` data module.

**Rejected by decision (D-04/D-05):** the reference `ui_kits/admin/Algorithms.jsx` is editable (SaveBar + editable params) and has an extra "Single sensors" section. Both are intentionally NOT built — this screen is read-only browse of group detectors only. Use the kit for layout/visual reference only.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-new-standalone-screens-dashboard-algorithms-settings/11-CONTEXT.md
@.planning/phases/11-new-standalone-screens-dashboard-algorithms-settings/11-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Catalog fetch state (ALGO-07)</name>
  <files>orchestrator/ui/src/state/algorithms.ts</files>
  <read_first>
    - orchestrator/ui/src/state/groups.ts (signals + loader + apiGet convention)
    - orchestrator/ui/src/api/client.ts (apiGet — relative path)
    - orchestrator/ui/src/api/types.ts (DetectorCatalog, DetectorCatalogEntry, DetectorPreset, ParamFieldSchema — reuse as-is)
    - orchestrator/ui/src/components/AlgorithmChooser.tsx (how the catalog is already fetched/consumed elsewhere — reuse the same shape, read-only)
  </read_first>
  <action>
    Create `state/algorithms.ts` exporting a `catalog` signal (`DetectorCatalogEntry[]`, initially empty), a `loadError` signal, and an async `loadCatalog()` that calls `apiGet<DetectorCatalog>('api/detectors/catalog')` and sets `catalog.value = response.detectors` (preserving server order — never reorder or filter client-side). On failure set `loadError = true`. Reuse the existing `DetectorCatalog` types from api/types.ts; do not redefine them. Ignore the `guided` field (that belongs to the wizard, D-05).
  </action>
  <acceptance_criteria>
    - state/algorithms.ts exports loadCatalog and a catalog signal typed DetectorCatalogEntry[].
    - loadCatalog fetches api/detectors/catalog via apiGet and assigns response.detectors unmodified.
    - `npm --prefix orchestrator/ui run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix orchestrator/ui run build</automated>
  </verify>
  <done>Catalog loader exists, fetches the endpoint, preserves server order, reuses existing types.</done>
</task>

<task type="auto">
  <name>Task 2: Read-only detector cards (ALGO-08, D-04/D-05)</name>
  <files>orchestrator/ui/src/components/AlgorithmsPage.tsx</files>
  <read_first>
    - orchestrator/ui/src/components/Card.tsx, Badge.tsx, Disclosure.tsx (props for card composition)
    - orchestrator/ui/src/state/algorithms.ts (catalog signal from Task 1)
    - Argus Design System/ui_kits/admin/Algorithms.jsx (layout/visual reference ONLY — its SaveBar/editable params + "Single sensors" section are REJECTED, D-05)
    - .planning/phases/11-new-standalone-screens-dashboard-algorithms-settings/11-UI-SPEC.md "Algorithms" per-card composition (name, best-for verbatim, presets row, param schema disclosure) + Copywriting (do not reuse the kit's editable subtitle)
  </read_first>
  <action>
    In `AlgorithmsPage.tsx`, below the existing `.argus-page-header`, call `loadCatalog()` in a mount `useEffect` and render a grid (`repeat(auto-fill, minmax(280px, 1fr))`, gap --space-md — same recipe as `.argus-algorithm-chooser__grid`) of one `Card` per `catalog` entry (5 cards, catalog order). Per card, read-only, NO editing controls (no SensitivityPreset picker, no Input): detector name (`entry.name`, mono, weight 600) → best-for copy (`entry.bestFor` rendered VERBATIM — never hardcoded/paraphrased client-side, per the Phase 9 anti-pattern lesson) → a "Presets" `.argus-label` caption then one `Badge tone="neutral"` per `entry.presets` entry with content `"{preset.label}: {params as key=value joined by comma, in entry.paramSchema key order}"` → a `Disclosure summary="Parameter schema"` containing one `.argus-catalog-param-row` per `entry.paramSchema` item: key (mono) left, description right formatted `"{type} · {min}–{max} · step {step}"`. If `loadError`, render a `Banner tone="error"`. Do NOT render a SaveBar or a "Single sensors" section.
  </action>
  <acceptance_criteria>
    - AlgorithmsPage renders one Card per catalog entry, mapping entry.bestFor directly into the DOM (no hardcoded detector description strings in the component source).
    - Presets render as Badge pills; param schema renders inside a Disclosure using .argus-catalog-param-row.
    - No SaveBar component and no "Single sensors" section exist in AlgorithmsPage.
    - `npm --prefix orchestrator/ui run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix orchestrator/ui run build</automated>
  </verify>
  <done>Five read-only detector cards render from the catalog with verbatim best-for copy, presets, and param-schema disclosure; no editing surface.</done>
</task>

</tasks>

<threat_model>
Not applicable — frontend-only, no new HTTP surface, no secrets. Consumes the existing authorized read endpoint /api/detectors/catalog via apiGet.
</threat_model>

<verification>
- `npm --prefix orchestrator/ui run build` succeeds.
- Manual: #/algorithms shows 5 detector cards (peer_divergence, ecod, copod, pca, iforest) in catalog order with server-sourced best-for copy, presets, and expandable param schema, correct in both themes; no Save button, no single-sensor section.
</verification>

<success_criteria>
Algorithms is a read-only catalog of the 5 group detectors with server-sourced copy/presets/schema, matching ALGO-07/08 and the read-only D-04/D-05 constraints.
</success_criteria>

## Artifacts this phase produces (Plan 11-04)

- `state/algorithms.ts` — new module: `catalog` signal + `loadCatalog()`.
- `AlgorithmsPage` body — read-only 5-detector catalog cards.
- No new backend, no new API types (reuses DetectorCatalog types), no new CLI flags.

<output>
Create `.planning/phases/11-new-standalone-screens-dashboard-algorithms-settings/11-04-SUMMARY.md` when done.
</output>
