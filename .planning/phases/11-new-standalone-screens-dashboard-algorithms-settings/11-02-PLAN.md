---
phase: 11-new-standalone-screens-dashboard-algorithms-settings
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - orchestrator/ui/src/router.ts
  - orchestrator/ui/src/main.tsx
  - orchestrator/ui/src/components/Sidebar.tsx
  - orchestrator/ui/src/components/Sidebar.test.tsx
  - orchestrator/ui/src/state/theme.ts
  - orchestrator/ui/src/components/DashboardPage.tsx
  - orchestrator/ui/src/components/AlgorithmsPage.tsx
  - orchestrator/ui/src/components/SettingsPage.tsx
  - orchestrator/ui/public/css/argus.css
autonomous: true
requirements: [DASH-01, ALGO-07, SET-01]
must_haves:
  truths:
    - "The 3 previously-disabled sidebar items (Dashboard, Algorithms, Settings) are enabled and navigate to their hash routes"
    - "Navigating to #/dashboard, #/algorithms, #/settings renders the matching page component (titled skeleton) inside AppShell's <main>"
    - "Default landing route is still #/sensors"
    - "Toggling the theme from the Sidebar and from any other surface stays in sync via one shared theme signal"
    - "All new page/section CSS classes exist so wave-2 screen plans do not touch argus.css"
  artifacts:
    - path: "orchestrator/ui/src/state/theme.ts"
      provides: "Shared reactive theme signal + setTheme writing localStorage('argus-theme') and data-theme"
      contains: "signal"
    - path: "orchestrator/ui/src/components/DashboardPage.tsx"
      provides: "Dashboard page component (skeleton: page header only, fleshed out in 11-03)"
      contains: "DashboardPage"
    - path: "orchestrator/ui/src/components/AlgorithmsPage.tsx"
      provides: "Algorithms page component (skeleton, fleshed out in 11-04)"
      contains: "AlgorithmsPage"
    - path: "orchestrator/ui/src/components/SettingsPage.tsx"
      provides: "Settings page component (skeleton, fleshed out in 11-05)"
      contains: "SettingsPage"
    - path: "orchestrator/ui/public/css/argus.css"
      provides: "New page/section composition classes for all 3 screens"
      contains: "argus-page-header"
  key_links:
    - from: "orchestrator/ui/src/main.tsx"
      to: "orchestrator/ui/src/components/DashboardPage.tsx"
      via: "render switch maps route.value '/dashboard' to <DashboardPage />"
      pattern: "DashboardPage"
    - from: "orchestrator/ui/src/components/Sidebar.tsx"
      to: "orchestrator/ui/src/state/theme.ts"
      via: "Sidebar reads/writes the shared theme signal instead of local useState"
      pattern: "theme"
    - from: "orchestrator/ui/src/router.ts"
      to: "location.hash"
      via: "hash routes #/dashboard #/algorithms #/settings drive route signal"
      pattern: "dashboard|algorithms|settings"
---

<objective>
Build the shared frontend foundation the three new screens depend on (D-10 nav/routing + D-09 shared theme state), and pre-create all new CSS classes so the wave-2 screen plans never touch `argus.css` (keeping them parallel).

Purpose: One dependency wave that (a) makes Dashboard/Algorithms/Settings routable and reachable from the sidebar, (b) converts the Sidebar's local theme state into a single shared reactive source so two theme surfaces (Sidebar toggle + Settings Appearance) stay in sync, and (c) lands the page/section CSS recipes.
Output: Extended `router.ts`, `main.tsx` render switch, enabled `Sidebar.tsx` nav + isActive, new `state/theme.ts` signal, three skeleton page components, and new `.argus-*` CSS classes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-new-standalone-screens-dashboard-algorithms-settings/11-CONTEXT.md
@.planning/phases/11-new-standalone-screens-dashboard-algorithms-settings/11-UI-SPEC.md
@.planning/phases/10-design-system-foundation/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable nav + routing for the 3 new screens (D-10)</name>
  <files>orchestrator/ui/src/router.ts, orchestrator/ui/src/main.tsx, orchestrator/ui/src/components/Sidebar.tsx, orchestrator/ui/src/components/Sidebar.test.tsx</files>
  <read_first>
    - orchestrator/ui/src/router.ts (hand-rolled hash router: route signal, normalizeHash, parseGroupId, hashchange listener — NO router library)
    - orchestrator/ui/src/main.tsx (App render switch: currently isGroupsRoute ? GroupsPage : SensorsPage)
    - orchestrator/ui/src/components/Sidebar.tsx (NAV_ITEMS with dashboard/algorithms/settings disabled:true; isActive() only handles sensors/groups)
    - orchestrator/ui/src/components/Sidebar.test.tsx (existing sidebar test expectations to keep green)
    - .planning/phases/11-new-standalone-screens-dashboard-algorithms-settings/11-UI-SPEC.md "Navigation (D-10)" section
  </read_first>
  <action>
    In `Sidebar.tsx` `NAV_ITEMS`: change the `dashboard`, `algorithms`, and `settings` items from `disabled: true` to real `href` values `#/dashboard`, `#/algorithms`, `#/settings` (drop the `disabled` flag on those three). Extend `isActive(item, currentRoute)` with exact-match cases: dashboard → `/dashboard`, algorithms → `/algorithms`, settings → `/settings`. Do not reorder items (order is locked from Phase 10 D-02). Keep sensors as the default landing route — do NOT change `normalizeHash`'s `/sensors` fallback in router.ts (D-10).
    In `router.ts`: the three new routes are static (no `:id` segment) and already flow through the existing `route` signal + `hashchange` listener, so no parser change is needed — add a short comment documenting the 3 new valid static routes so future readers know they are intentional. Do NOT add a router library.
    In `main.tsx`: extend the `App()` render switch to map `route.value` to the new page components — `/dashboard` → `<DashboardPage />`, `/algorithms` → `<AlgorithmsPage />`, `/settings` → `<SettingsPage />`, keeping the existing groups/sensors branches and the sensors default. Import the three page components created in Task 3.
    Update `Sidebar.test.tsx` if any assertion depends on the 3 items being disabled — the three should now render enabled with hrefs and become active on their route.
  </action>
  <acceptance_criteria>
    - Sidebar.tsx NAV_ITEMS entries for dashboard/algorithms/settings have `href` and no `disabled: true`.
    - Sidebar.tsx isActive returns true for each of /dashboard, /algorithms, /settings on the matching route.
    - main.tsx render switch references DashboardPage, AlgorithmsPage, and SettingsPage.
    - router.ts still defaults an empty hash to /sensors (unchanged).
    - `npm --prefix orchestrator/ui test -- --run` passes (Sidebar tests included).
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix orchestrator/ui test -- --run src/components/Sidebar.test.tsx</automated>
  </verify>
  <done>The three sidebar items navigate to their routes; the render switch mounts each page; default route unchanged; sidebar tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Shared theme signal + Sidebar refactor (D-09)</name>
  <files>orchestrator/ui/src/state/theme.ts, orchestrator/ui/src/components/Sidebar.tsx, orchestrator/ui/src/main.tsx</files>
  <read_first>
    - orchestrator/ui/src/components/Sidebar.tsx (current local useState theme + handleThemeToggle writing data-theme + localStorage('argus-theme'))
    - orchestrator/ui/src/main.tsx (existing theme bootstrap reading localStorage('argus-theme') / prefers-color-scheme before render)
    - orchestrator/ui/src/state/groups.ts (existing @preact/signals state-module pattern: exported signals + exported mutator functions)
    - orchestrator/ui/src/router.ts (signal + effect usage reference)
    - .planning/phases/11-new-standalone-screens-dashboard-algorithms-settings/11-UI-SPEC.md "Required refactor note" under Settings Appearance (why one shared source is needed)
  </read_first>
  <action>
    Create `state/theme.ts` exporting a `theme` signal of type `'light' | 'dark'` initialized from the current `document.documentElement` `data-theme` attribute (falling back to `'light'`), and an exported `setTheme(next: 'light' | 'dark')` function that sets `document.documentElement`'s `data-theme` attribute, writes `localStorage('argus-theme', next)`, and assigns `theme.value = next`. This is the single write path for theme — mirror the existing signals-module convention in `state/groups.ts`. Do NOT introduce a new persistence key or a 'system' value (D-09: Light/Dark only, same key/attr as Phase 10).
    Refactor `Sidebar.tsx` to remove the local `useState<Theme>` and its inline `handleThemeToggle` mutation: read `theme.value` from the shared signal and, on toggle, call `setTheme(theme.value === 'dark' ? 'light' : 'dark')`. The toggle button's icon/aria-label derive from `theme.value` so it reflects changes made by other surfaces.
    Leave `main.tsx`'s pre-render bootstrap (which sets `data-theme` from localStorage/prefers-color-scheme before mount) intact — the `theme` signal reads that already-applied attribute on init, so no double logic. Only adjust the bootstrap if needed so the initial attribute is set before `state/theme.ts` reads it.
  </action>
  <acceptance_criteria>
    - state/theme.ts exports a `theme` signal and a `setTheme` function; setTheme writes both data-theme and localStorage('argus-theme').
    - Sidebar.tsx no longer holds theme in useState and calls setTheme from the shared module.
    - No 'system' option and no localStorage key other than 'argus-theme' introduced.
    - `npm --prefix orchestrator/ui run build` type-checks and `npm --prefix orchestrator/ui test -- --run src/components/Sidebar.test.tsx` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix orchestrator/ui run build</automated>
  </verify>
  <done>Theme is a single shared signal written only via setTheme; the Sidebar consumes it; build + sidebar tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Skeleton page components + new CSS classes</name>
  <files>orchestrator/ui/src/components/DashboardPage.tsx, orchestrator/ui/src/components/AlgorithmsPage.tsx, orchestrator/ui/src/components/SettingsPage.tsx, orchestrator/ui/public/css/argus.css</files>
  <read_first>
    - orchestrator/ui/src/components/SensorsPage.tsx (existing page-component shape: exported function, .argus-heading/.argus-body usage)
    - orchestrator/ui/public/css/argus.css (existing recipes to mirror: .argus-param-grid, .argus-filters responsive breakpoints, .argus-algorithm-chooser__grid, .argus-kpi-tile__label, .argus-param-field__label, .argus-sensitivity-preset-picker family, .argus-card)
    - .planning/phases/11-new-standalone-screens-dashboard-algorithms-settings/11-UI-SPEC.md "Screen Specifications" (page header, section label, dashboard grids, catalog param row, settings max-width) + "Spacing Scale" table
  </read_first>
  <action>
    Create three page components as minimal skeletons, each exporting a named function (`DashboardPage`, `AlgorithmsPage`, `SettingsPage`) that renders a `.argus-page-header` with the exact title/subtitle copy from the UI-SPEC Copywriting Contract (Dashboard: "Dashboard" / "System overview for your self-hosted Argus instance."; Algorithms: "Algorithms" / "Browse the detector catalog used for group anomaly detection."; Settings: "Settings" / "Current connection and detection configuration. Appearance can be changed below; other values are read-only."). Wave-2 plans (11-03/04/05) flesh out the body below the header. Do not add feature logic here.
    In `argus.css`, add the new composition classes (mirroring the noted existing recipes, following the `.argus-*` BEM convention, using only Phase 10 tokens — no new pixel values except the two UI-SPEC exceptions): `.argus-page-header` (title + subtitle stack), `.argus-section-label` (12px/600 uppercase caps, recipe generalized from `.argus-kpi-tile__label`), `.argus-dashboard-kpi-row` (grid repeat(4,1fr), gap --space-md, margin-bottom --space-xl; responsive repeat(2,1fr) <900px, repeat(1,1fr) <560px), `.argus-dashboard-layout` (grid 1.4fr 1fr, gap --space-lg, align-items:start; single column <900px), `.argus-catalog-param-row` (flex, justify-content:space-between), and the Settings layout container (max-width 720px, sections gap --space-lg). Match the existing breakpoint convention used by `.argus-filters`/`.argus-param-grid`.
  </action>
  <acceptance_criteria>
    - DashboardPage.tsx, AlgorithmsPage.tsx, SettingsPage.tsx each export their named function and render the exact UI-SPEC title + subtitle strings.
    - argus.css contains selectors `.argus-page-header`, `.argus-section-label`, `.argus-dashboard-kpi-row`, `.argus-dashboard-layout`, and `.argus-catalog-param-row`.
    - `npm --prefix orchestrator/ui run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix orchestrator/ui run build</automated>
  </verify>
  <done>Three titled skeleton pages exist and route correctly; all new CSS classes are present in argus.css; build passes.</done>
</task>

</tasks>

<threat_model>
Not applicable — this plan introduces no new HTTP surface and handles no secrets. It is pure client-side routing, shared UI state, and CSS. Theme state persists only a non-sensitive `'light'|'dark'` value in localStorage under the existing `argus-theme` key (Phase 10 mechanism, unchanged).
</threat_model>

<verification>
- `npm --prefix orchestrator/ui run build` type-checks and builds.
- `npm --prefix orchestrator/ui test -- --run` passes (Sidebar tests updated, still green).
- Manual: clicking each of the 3 sidebar items routes to its titled page in both light and dark mode; default load lands on Sensors.
</verification>

<success_criteria>
All three screens are reachable from the sidebar and render titled skeletons; theme is a single shared signal in sync across surfaces; every new CSS class exists so wave-2 plans touch only their own .tsx file.
</success_criteria>

## Artifacts this phase produces (Plan 11-02)

- `state/theme.ts` — new module: `theme` signal + `setTheme()` mutator.
- `DashboardPage`, `AlgorithmsPage`, `SettingsPage` — new page components (skeletons this plan; bodies added in 11-03/04/05).
- New hash routes `#/dashboard`, `#/algorithms`, `#/settings` wired into `router.ts` + `main.tsx`.
- Enabled sidebar nav items dashboard/algorithms/settings + isActive cases (Sidebar.tsx).
- New CSS classes: `.argus-page-header`, `.argus-section-label`, `.argus-dashboard-kpi-row`, `.argus-dashboard-layout`, `.argus-catalog-param-row`, Settings layout container.
- No new backend, no new CLI flags, no new env vars.

<output>
Create `.planning/phases/11-new-standalone-screens-dashboard-algorithms-settings/11-02-SUMMARY.md` when done.
</output>
