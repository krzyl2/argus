---
phase: 08-group-config-ui-algorithm-chooser
plan: 04
type: execute
wave: 3
depends_on: ["08-03"]
files_modified:
  - orchestrator/ui/src/state/groupEditor.ts
  - orchestrator/ui/src/components/AlgorithmChooser.tsx
  - orchestrator/ui/src/components/GuidedFlowStep.tsx
  - orchestrator/ui/src/components/AlgorithmCard.tsx
  - orchestrator/ui/src/components/SensitivityPresetPicker.tsx
  - orchestrator/ui/src/components/AdvancedParamsDisclosure.tsx
  - orchestrator/ui/src/components/AttributionPanel.tsx
  - orchestrator/ui/src/components/AttributionBar.tsx
  - orchestrator/ui/src/components/AreaSuggestionBanner.tsx
  - orchestrator/ui/src/components/GroupEditorForm.tsx
  - orchestrator/ui/src/components/GroupsPage.tsx
  - orchestrator/ui/src/state/groups.ts
  - orchestrator/ui/src/state/groupEditor.test.ts
  - orchestrator/ui/src/components/AlgorithmChooser.test.tsx
  - orchestrator/ui/src/components/AttributionPanel.test.tsx
autonomous: false
requirements: [ALGO-01, ALGO-02, ALGO-03, ALGO-04, GRP-09, SRCH-03]
must_haves:
  truths:
    - "Selecting Low/Med/High expands to concrete params (from catalog) written to the group's params on save — raw values hidden by default"
    - "The Advanced disclosure reveals the param grid and editing a field overrides that value while the preset radio stays selected with a 'customized' indicator"
    - "Each algorithm card shows a catalog-sourced 'best for…' description"
    - "The guided question pre-selects a detector, visibly labels it as suggested, and one click on any other card overrides with zero friction"
    - "AttributionPanel renders ranked bars for ecod/copod, an honest 'no per-feature attribution' line for pca/iforest, and a no-verdict-yet state — polling api/groups/{id}/status while the edit screen is open"
    - "AreaSuggestionBanner offers area-scoped candidate groups (>=3 ungrouped sensors) that pre-fill the editor on approval; never auto-groups"
  artifacts:
    - path: "orchestrator/ui/src/state/groupEditor.ts"
      provides: "chooserMode/selectedDetector/guidedRecommended signals + answerGuidedQuestion/skipToManual/pickAlgorithmManually mutators"
      contains: "guidedRecommended"
    - path: "orchestrator/ui/src/components/AttributionPanel.tsx"
      provides: "polling attribution panel with 4 states"
    - path: "orchestrator/ui/src/components/AlgorithmChooser.tsx"
      provides: "guided/manual chooser orchestration"
  key_links:
    - from: "orchestrator/ui/src/components/AttributionPanel.tsx"
      to: "orchestrator/ui/src/api/client.ts"
      via: "polling apiGet(`api/groups/${id}/status`) with clearInterval on unmount"
      pattern: "api/groups/\\$\\{"
    - from: "orchestrator/ui/src/components/SensitivityPresetPicker.tsx"
      to: "orchestrator/ui/src/api/types.ts"
      via: "reads DetectorCatalog presets to expand label→params"
      pattern: "preset"
  prohibitions:
    - "NO cosmetic-only preset — the expanded preset params are what gets written to the group config; NO hardcoded client-side preset numbers (source them from api/detectors/catalog)"
    - "NO fabricated attribution ranking for PCA/IForest — honest 'does not provide per-feature attribution' (secondary text, NOT an error state)"
    - "NO opaque/automatic algorithm selection — the guided pick is always visible and one-click overridable"
    - "NO auto-grouping — AreaSuggestionBanner pre-fills for operator review only, never saves"
    - "NO SSE/websocket push for attribution — interval polling only, cleared on route change"
    - "NO charting/icon library — attribution bars are CSS div width %"
    - "NO client-side 'best for…' copy that could drift — source it from the catalog"
    - "NO leading-slash fetch; NO new argus.css tokens"
---

<objective>
Complete the Phase 8 UX: the transparent guided algorithm chooser (ALGO-01/02/03/04), the ranked attribution display (GRP-09), and the area-scoped group suggestions (SRCH-03). These mount into the GroupEditorForm slot and GroupsPage prepared in Plan 08-03, consuming the `/api/detectors/catalog` and `/api/groups/{id}/status` contracts from Plan 08-02.

This is the transparency crux of the phase: presets hide raw params but Advanced reveals+overrides them; the guided pick is always visible and overridable; attribution is honest (real ranking for ecod/copod, explicit "not supported" for pca/iforest). Ships `autonomous: false` — the final human-verify checkpoint covers the live-HA Ingress round-trip for the new screens (carried forward from Phase 7 UI-02).

Purpose: Make ALGO-01..04 + GRP-09 real and transparent in the UI.
Output: Chooser + preset/advanced + attribution + suggestion components, the guided-flow state machine, and a human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-group-config-ui-algorithm-chooser/08-UI-SPEC.md
@.planning/phases/08-group-config-ui-algorithm-chooser/08-RESEARCH.md
@.planning/phases/08-group-config-ui-algorithm-chooser/08-02-SUMMARY.md
@.planning/phases/08-group-config-ui-algorithm-chooser/08-03-SUMMARY.md

@orchestrator/ui/src/state/sensors.ts
@orchestrator/ui/src/state/groups.ts
@orchestrator/ui/src/api/types.ts
@orchestrator/ui/src/api/client.ts
@orchestrator/ui/src/components/DetectorDisclosure.tsx
@orchestrator/ui/src/components/DetectorEntry.tsx
@orchestrator/ui/src/components/DetectorParamGrid.tsx
@orchestrator/ui/src/components/SensorSearchInput.tsx
@orchestrator/ui/src/components/GroupEditorForm.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guided-flow state machine + algorithm chooser (ALGO-01..04)</name>
  <files>orchestrator/ui/src/state/groupEditor.ts, orchestrator/ui/src/components/AlgorithmChooser.tsx, orchestrator/ui/src/components/GuidedFlowStep.tsx, orchestrator/ui/src/components/AlgorithmCard.tsx, orchestrator/ui/src/components/SensitivityPresetPicker.tsx, orchestrator/ui/src/components/AdvancedParamsDisclosure.tsx, orchestrator/ui/src/components/GroupEditorForm.tsx, orchestrator/ui/src/state/groups.ts, orchestrator/ui/src/state/groupEditor.test.ts, orchestrator/ui/src/components/AlgorithmChooser.test.tsx</files>
  <read_first>
    - orchestrator/ui/src/components/DetectorEntry.tsx (select + param sync unit) + DetectorParamGrid.tsx (.argus-param-grid field pattern) + DetectorDisclosure.tsx (native details/summary)
    - orchestrator/ui/src/state/sensors.ts (pure-mutator style; removeDetector zero-friction pattern)
    - 08-RESEARCH.md Pattern 4 (groupEditor.ts full state machine — copy directly) + Pattern 3 (catalog shape)
    - 08-UI-SPEC.md Preset+Advanced Interaction Contract + Guided Flow Contract + Copywriting Contract
    - 08-03-SUMMARY.md (GroupEditorForm chooser slot; DetectorCatalog type; groups draft params signal)
  </read_first>
  <behavior>
    - answerGuidedQuestion('together') sets selectedDetector='ecod', guidedRecommended='ecod', chooserMode='guided-pick-shown'; answerGuidedQuestion('diverges')→peer_divergence
    - pickAlgorithmManually(x) clears guidedRecommended (override drops the 'guided' label) in one synchronous update
    - skipToManual() clears guidedRecommended and sets chooserMode='manual'
    - Selecting a preset expands to that preset's catalog params into the draft; editing an Advanced field overrides only that key and the preset radio stays selected with a "customized" indicator
    - AlgorithmCard renders the catalog bestFor text; a guided-recommended card shows the "Suggested based on your answer…" label
  </behavior>
  <action>
    groupEditor.ts (new): copy RESEARCH Pattern 4 verbatim — chooserMode/selectedDetector/guidedRecommended signals + answerGuidedQuestion/skipToManual/pickAlgorithmManually. Load the catalog once (apiGet('api/detectors/catalog')) into a signal; expose the guided answer→detector map from the catalog's guided block (not hardcoded).
    AlgorithmChooser.tsx: orchestrate guided vs manual (states per UI-SPEC AlgorithmChooser table) — opens in guided mode with a "Skip — choose manually" link always visible; renders GuidedFlowStep or the AlgorithmCard grid; when a detector is selected, renders SensitivityPresetPicker + AdvancedParamsDisclosure below the selected card.
    GuidedFlowStep.tsx: the "What are you monitoring?" question with 2 answers (verbatim copy) calling answerGuidedQuestion.
    AlgorithmCard.tsx: a selectable button/role=radio unit showing name + catalog bestFor + selected border (--color-accent); a guided-recommended variant shows the label; one click calls pickAlgorithmManually (zero friction, no confirm).
    SensitivityPresetPicker.tsx: native radio group Low/Med/High (accent-color var, Med default). Selecting expands the catalog preset params into the groups draft params.
    AdvancedParamsDisclosure.tsx: native details/summary "Advanced — view/override parameters" reusing the .argus-param-grid/.argus-param-field classes; fields from the catalog paramSchema pre-filled with the current expanded values; editing overrides that key in the draft; show the "Med, customized" inline indicator next to the preset radio when >=1 field diverges.
    GroupEditorForm.tsx: mount AlgorithmChooser in its slot; on save, write selectedDetector + expanded/overridden params into the group draft (mode maps peer_divergence vs joint per detector).
    groups.ts: ensure the draft params reflect the expanded preset so saveGroup persists self-contained params.
    Add groupEditor.test.ts (state machine transitions) + AlgorithmChooser.test.tsx (guided pick + override + bestFor render + preset expansion + advanced override indicator).
  </action>
  <verify>
    <automated>cd orchestrator/ui && npx vitest run src/state/groupEditor.test.ts src/components/AlgorithmChooser.test.tsx</automated>
  </verify>
  <done>Guided flow pre-selects + labels + overrides with zero friction; presets expand catalog params (no hardcoded numbers) and get written on save; Advanced reveals+overrides with a customized indicator; bestFor is catalog-sourced; tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Attribution panel + bar (GRP-09) + area suggestion banner (SRCH-03)</name>
  <files>orchestrator/ui/src/components/AttributionPanel.tsx, orchestrator/ui/src/components/AttributionBar.tsx, orchestrator/ui/src/components/AreaSuggestionBanner.tsx, orchestrator/ui/src/components/GroupEditorForm.tsx, orchestrator/ui/src/components/GroupsPage.tsx, orchestrator/ui/src/components/AttributionPanel.test.tsx</files>
  <read_first>
    - orchestrator/ui/src/components/SensorSearchInput.tsx (debounce timer useRef + clearTimeout-on-unmount discipline — reuse for setInterval/clearInterval)
    - orchestrator/ui/src/components/EmptyState.tsx (informational banner pattern)
    - 08-UI-SPEC.md Attribution Display Contract (4 states, sort, accent-only-top-bar) + Sensor Search & Browse Contract (SRCH-03 banner) + Copywriting Contract
    - 08-02-SUMMARY.md (GET /api/groups/{id}/status shape — contributions already sorted server-side, empty for pca/iforest)
  </read_first>
  <behavior>
    - Given a status with contributions, AttributionPanel renders one AttributionBar per contribution in received order (already ranked server-side — do NOT re-sort); top bar uses accent fill, others neutral; bar width = contribution / topContribution * 100%
    - Given a status with empty contributions (pca/iforest), renders "This algorithm does not provide per-feature attribution." as secondary text (NOT error state)
    - Given status null, renders "No anomaly score yet — attribution will appear after the next batch run."
    - The poll interval is cleared on unmount (no leak after leaving #/groups/:id)
    - AreaSuggestionBanner shows for an area with >=3 ungrouped sensors and "Review" navigates to #/groups/new pre-filled; "Not now" dismisses for the session; renders nothing when no suggestion
  </behavior>
  <action>
    AttributionPanel.tsx: only on #/groups/:id (existing groups). Poll apiGet(`api/groups/${id}/status`) on an interval (roughly the batch cadence; a fixed reasonable interval is fine) using the useRef + clearInterval-on-unmount discipline from SensorSearchInput. Render the 4 states per the Attribution Display Contract. Do NOT re-sort contributions (server pre-sorts).
    AttributionBar.tsx: one row — member/feature name (label class) + a CSS div-width bar (% of top contributor) + numeric value; top-rank accent fill, others neutral. No chart lib.
    AreaSuggestionBanner.tsx: computed client-side from the enriched sensor list (areaName) minus sensors already in any group; surface "{N} sensors share area \"{area}\" — group them?" with "Review" (navigate to #/groups/new pre-filling those members via the groups draft) and "Not now" (session-dismiss via a signal, not persisted). Render on #/groups above the list. Never auto-save.
    GroupEditorForm.tsx: mount AttributionPanel for edit mode (:id present).
    GroupsPage.tsx: mount AreaSuggestionBanner on the #/groups list view.
    Add AttributionPanel.test.tsx covering the 4 states + no-resort + interval cleanup.
  </action>
  <verify>
    <automated>cd orchestrator/ui && npx vitest run src/components/AttributionPanel.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Attribution shows ranked bars (ecod/copod), honest no-attribution (pca/iforest), no-verdict, and loading; polling cleans up on unmount; area suggestions pre-fill on approval and never auto-group; typecheck clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live-HA Ingress round-trip verification (human)</name>
  <action>Pause for human verification of the Phase 8 group UX under real HA Ingress — the live "Open Web UI" round-trip that automated tests cannot cover (carried forward from Phase 7 UI-02). Do not proceed until approved.</action>
  <what-built>The full Phase 8 group UX built on the SPA and wired to the group backend: group authoring (list/editor/member picker), transparent guided algorithm chooser (Low/Med/High presets + Advanced override + best-for + guided flow), sensor search by friendly_name, browse by area/domain, area-scoped suggestions, and ranked attribution display. Automated component + endpoint tests pass; this checkpoint is the live-HA Ingress round-trip that only a human can confirm (carried forward from Phase 7 UI-02).</what-built>
  <how-to-verify>
    1. Build + deploy the add-on image (per the deploy process in MEMORY.md), install/update in real HA.
    2. Open the add-on via HA "Open Web UI" (never a direct port).
    3. Navigate to Groups → Create group. Confirm: sensor search matches a Polish friendly_name (not just entity_id); the sensor list browses by HA area with a domain/Ungrouped fallback.
    4. Pick 3+ members, answer the guided "What are you monitoring?" question, confirm the suggested algorithm is visibly labeled and that clicking a different card overrides it in one click.
    5. Select Low/Med/High (raw params hidden), open Advanced, override one value, confirm the "customized" indicator; save and confirm hot-reload (no restart) and that entities: config is untouched.
    6. For an existing joint (ecod/copod) group with a recent verdict, open it and confirm the ranked per-member contribution bars; for a pca/iforest group confirm the honest "no per-feature attribution" message.
    7. On the Groups list, confirm any area-scoped suggestion banner pre-fills (never auto-saves) on "Review".
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. area browse mostly Ungrouped — that would be the documented device-inherited-area fast-follow, not a bug).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (Ingress) | catalog + status reads under Supervisor base path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Information Disclosure | GET /api/groups/{id}/status polling | mitigate | Server returns 200-null for unknown id (08-02); catalog/status expose no secrets |
| T-08-11 | Tampering | Advanced-form param values | mitigate | catalog paramSchema bounds fields client-side; server _cast_float default-fallback (08-01) is the authority |
</threat_model>

<verification>
- `cd orchestrator/ui && npx vitest run` green
- `cd orchestrator/ui && npx tsc --noEmit` clean
- `cd orchestrator/ui && npx vite build` succeeds
- Human-verify checkpoint approved (live-HA Ingress round-trip)
</verification>

<success_criteria>
ALGO-01/02: preset selection hides raw params, Advanced reveals/overrides them, presets genuinely change detection (backed by 08-01). ALGO-03/04: each algorithm shows catalog best-for; guided flow pre-selects + explains + one-click overrides. GRP-09: joint anomalies show ranked per-member/feature contribution (ecod/copod), honest no-attribution for pca/iforest. SRCH-03: area-scoped approve-only suggestions.
</success_criteria>

<output>
Create `.planning/phases/08-group-config-ui-algorithm-chooser/08-04-SUMMARY.md` when done.
</output>
