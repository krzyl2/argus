---
phase: 08-group-config-ui-algorithm-chooser
plan: 03
type: execute
wave: 2
depends_on: ["08-02"]
files_modified:
  - orchestrator/ui/src/api/types.ts
  - orchestrator/ui/src/router.ts
  - orchestrator/ui/src/main.tsx
  - orchestrator/ui/src/state/groups.ts
  - orchestrator/ui/src/validation/groupParams.ts
  - orchestrator/ui/src/components/AppShell.tsx
  - orchestrator/ui/src/components/GroupList.tsx
  - orchestrator/ui/src/components/GroupListRow.tsx
  - orchestrator/ui/src/components/GroupsPage.tsx
  - orchestrator/ui/src/components/GroupEditorForm.tsx
  - orchestrator/ui/src/components/MemberPicker.tsx
  - orchestrator/ui/src/components/SensorSearchInput.tsx
  - orchestrator/ui/src/components/SensorList.tsx
  - orchestrator/ui/src/state/groups.test.ts
  - orchestrator/ui/src/validation/groupParams.test.ts
  - orchestrator/ui/src/components/SensorSearchInput.test.tsx
autonomous: true
requirements: [SRCH-01, SRCH-02, SRCH-03]
must_haves:
  truths:
    - "#/groups renders the group list (loaded/empty/loading), #/groups/new and #/groups/:id render the editor"
    - "SensorSearchInput matches friendly_name OR entity_id (superset of Phase 7 entity_id-only behavior)"
    - "SensorList renders a collapsible section per HA area (domain/Ungrouped fallback) when in area-grouping mode"
    - "MemberPicker multi-selects sensors and shows below-floor + peer unit-mismatch validation using client rules"
    - "Group save posts to api/groups/save and reflects success/validation/error via the reused SaveResultBanner"
    - "#/sensors and its entity flow are unchanged (zero regression)"
  artifacts:
    - path: "orchestrator/ui/src/state/groups.ts"
      provides: "groups signal, loadGroups (monotonic-seq guard), saveGroup (discriminated-union saveState), validation computed"
      contains: "signal"
    - path: "orchestrator/ui/src/validation/groupParams.ts"
      provides: "validateGroupMembers (floor 3) + validateUnitConsistency (peer mode) with verbatim UI-SPEC copy"
    - path: "orchestrator/ui/src/api/types.ts"
      provides: "GroupConfig, GroupSaveRequest, DetectorCatalog, GroupStatus DTOs matching 08-02 contracts"
      contains: "GroupConfig"
    - path: "orchestrator/ui/src/router.ts"
      provides: "#/groups, #/groups/new, #/groups/:id hash matching (hand-rolled, no router lib)"
  key_links:
    - from: "orchestrator/ui/src/state/groups.ts"
      to: "orchestrator/ui/src/api/client.ts"
      via: "apiGet('api/groups') / apiPost('api/groups/save') — relative, no leading slash"
      pattern: "api(Get|Post)\\('api/groups"
    - from: "orchestrator/ui/src/components/MemberPicker.tsx"
      to: "orchestrator/ui/src/validation/groupParams.ts"
      via: "validateGroupMembers + validateUnitConsistency"
      pattern: "validateGroupMembers"
  prohibitions:
    - "NO leading-slash fetch — relative apiGet/apiPost only (Ingress base-path safety)"
    - "NO router library (preact-router/preact-iso) — extend the hand-rolled hash matcher"
    - "NO new state library or class store — @preact/signals + pure mutators mirroring state/sensors.ts"
    - "NO reworded validation copy — messages match the UI-SPEC Copywriting Contract verbatim"
    - "NO regression to #/sensors — SensorSearchInput/SensorList extensions are additive (entity_id match still works)"
    - "NO auto-grouping — MemberPicker selection is always explicit operator action"
    - "NO new argus.css tokens/colors — reuse existing .argus-* classes"
---

<objective>
Build the SPA foundation for group authoring and sensor search/browse: the new routes and app-shell nav, the group DTOs/types matching the Plan 08-02 contracts, the `state/groups.ts` signals store, client-side group validation, the group list + editor + member-picker screens, and the SRCH-01/02/03 extensions to `SensorSearchInput` (friendly_name match) and `SensorList` (area/domain grouping). The algorithm chooser and attribution panel are Plan 08-04 — this plan wires their mount points (a chooser slot + a save flow) but ships the group authoring path end-to-end for peer/joint mode selection.

Purpose: Deliver group authoring + search/browse on the endpoint contracts fixed in Wave 1.
Output: New routes, group state/validation/types, list + editor + member-picker components, extended search/browse — all reusing Phase 7 components and argus.css.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-group-config-ui-algorithm-chooser/08-UI-SPEC.md
@.planning/phases/08-group-config-ui-algorithm-chooser/08-PATTERNS.md
@.planning/phases/08-group-config-ui-algorithm-chooser/08-02-SUMMARY.md

@orchestrator/ui/src/api/types.ts
@orchestrator/ui/src/api/client.ts
@orchestrator/ui/src/router.ts
@orchestrator/ui/src/main.tsx
@orchestrator/ui/src/state/sensors.ts
@orchestrator/ui/src/validation/detectorParams.ts
@orchestrator/ui/src/components/SensorsPage.tsx
@orchestrator/ui/src/components/SensorList.tsx
@orchestrator/ui/src/components/SensorSearchInput.tsx
@orchestrator/ui/src/components/AppShell.tsx
@orchestrator/ui/src/components/EmptyState.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types, router, app-shell nav, group state + validation</name>
  <files>orchestrator/ui/src/api/types.ts, orchestrator/ui/src/router.ts, orchestrator/ui/src/main.tsx, orchestrator/ui/src/components/AppShell.tsx, orchestrator/ui/src/state/groups.ts, orchestrator/ui/src/validation/groupParams.ts, orchestrator/ui/src/state/groups.test.ts, orchestrator/ui/src/validation/groupParams.test.ts</files>
  <read_first>
    - orchestrator/ui/src/state/sensors.ts (signal + monotonic-seq loadSensors guard + save discriminated-union + validation computed)
    - orchestrator/ui/src/validation/detectorParams.ts (message-constant convention, "messages are the parity spec — do not reword")
    - orchestrator/ui/src/router.ts + main.tsx (hash matcher + route render switch)
    - 08-02-SUMMARY.md (the AUTHORITATIVE JSON contracts as actually shipped)
    - 08-UI-SPEC.md (Copywriting Contract table for verbatim validation messages)
  </read_first>
  <behavior>
    - validateGroupMembers(['a','b']) returns "A group needs at least 3 members."; 3+ returns null
    - validateUnitConsistency for peer_divergence mode with mixed units returns the "Peer-divergence groups need members with the same unit. Found: {units}." message; same units returns null; joint mode always null
    - loadGroups populates the groups signal from api/groups and ignores a stale (superseded) response
    - router recognizes /groups, /groups/new, /groups/:id and extracts the :id segment
  </behavior>
  <action>
    types.ts: add GroupConfig, GroupSaveEntry/GroupSaveRequest, DetectorCatalogEntry/DetectorPreset/ParamFieldSchema/DetectorCatalog, GroupStatus/FeatureContribution, and extend SensorEntry with `areaName: string | null` and `domain: string` (matching 08-02's enriched /api/sensors payload). Shapes MUST match 08-02-SUMMARY contracts exactly.
    router.ts: extend normalizeHash/matching to parse /groups, /groups/new, /groups/:id manually (no router lib — honor the existing "hand-rolled" comment). Expose the parsed group id.
    main.tsx: switch on route.value to render GroupsPage for the /groups* family and SensorsPage for /sensors (keep the existing default).
    AppShell.tsx: add a nav row inside .argus-header with "Sensors" (#/sensors) and "Groups" (#/groups) links — reuse the flex header, no new nav component.
    state/groups.ts: mirror state/sensors.ts — `groups = signal<GroupConfig[]>([])`, `loading`, `saveState`, the group being edited (draft signals: name, members, mode, detector, params), `loadGroups()` with the monotonic-sequence stale-guard, `saveGroup()` with the try/catch → discriminated-union saveState, and `computed` validation flags using validation/groupParams.ts.
    validation/groupParams.ts: `validateGroupMembers(members)` and `validateUnitConsistency(members, mode)` returning the verbatim UI-SPEC copy (do not reword — mirrors detectorParams.ts header rule and EntitiesConfigLoader.ValidateGroups floor=3 / peer-unit rules).
    Add groups.test.ts + groupParams.test.ts (Vitest) covering the behaviors.
  </action>
  <verify>
    <automated>cd orchestrator/ui && npx vitest run src/state/groups.test.ts src/validation/groupParams.test.ts</automated>
  </verify>
  <done>Types match 08-02 contracts; router handles group routes; nav links present; group state loads/saves with the sensors.ts patterns; validation returns verbatim copy; tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Group list + editor + member picker; SRCH-01/02 extensions</name>
  <files>orchestrator/ui/src/components/GroupList.tsx, orchestrator/ui/src/components/GroupListRow.tsx, orchestrator/ui/src/components/GroupsPage.tsx, orchestrator/ui/src/components/GroupEditorForm.tsx, orchestrator/ui/src/components/MemberPicker.tsx, orchestrator/ui/src/components/SensorSearchInput.tsx, orchestrator/ui/src/components/SensorList.tsx, orchestrator/ui/src/components/SensorSearchInput.test.tsx</files>
  <read_first>
    - orchestrator/ui/src/components/SensorList.tsx + SensorListRow.tsx (row + list conventions, EmptyState branch)
    - orchestrator/ui/src/components/SensorsPage.tsx (page orchestration: load-on-mount, search-change, SaveBar + SaveResultBanner)
    - orchestrator/ui/src/components/SensorSearchInput.tsx (200ms debounce, placeholder, cleanup-on-unmount)
    - 08-UI-SPEC.md (Component Inventory, States tables, Copywriting Contract, Sensor Search & Browse Contract)
  </read_first>
  <behavior>
    - SensorSearchInput placeholder reads "Filter by name or entity ID…" and the consuming filter matches friendly_name OR entity_id case-insensitively (test: a query matching only friendly_name returns the row)
    - GroupList renders GroupListRow[] when non-empty, EmptyState copy "No groups configured." when empty
    - GroupEditorForm in /groups/new shows an empty form; in /groups/:id it pre-fills from the loaded group
    - MemberPicker toggles member selection and surfaces below-floor / peer unit-mismatch validation under the picker
  </behavior>
  <action>
    SensorSearchInput.tsx: change placeholder to "Filter by name or entity ID…" (applies everywhere incl. #/sensors — additive, entity_id still matches). Extend the match predicate (in the consuming filter, or add a shared predicate helper) to test friendly_name too. Keep the 200ms debounce + cleanup-on-unmount.
    SensorList.tsx: add an optional area-grouping render mode — wrap SensorListRow groups in `<details>` sections per HA area (alphabetical; a final domain/"Ungrouped" fallback section) reusing the existing `.argus-disclosure-toggle` class; section header shows "{Area} ({count})". Default (non-grouped) mode unchanged so #/sensors is untouched.
    GroupList.tsx / GroupListRow.tsx: GroupList renders `<ul class="argus-list">` of GroupListRow (name, mode badge peer/joint, member count, status pill, edit link to #/groups/:id) with the EmptyState branch. Row status pill sourced from GroupStatus is optional here (populated by 08-04); render a "no status yet" state when absent.
    GroupsPage.tsx: top-level page for the /groups* family — routes internally between GroupList (#/groups) and GroupEditorForm (#/groups/new, #/groups/:id) based on the router's parsed route/id, loads groups on mount, loads the edited group by id.
    GroupEditorForm.tsx: name field, mode select (peer_divergence / joint), a MemberPicker, a slot/placeholder where the AlgorithmChooser mounts (08-04 fills it), FieldValidationError for name-required/floor/unit-mismatch, reused SaveBar + SaveResultBanner wired to saveGroup(). Save disabled while invalid or saving.
    MemberPicker.tsx: wrap SensorList in multi-select mode; drive selection into the groups draft members; run validateGroupMembers + validateUnitConsistency (peer mode) and render the messages via FieldValidationError.
    Add SensorSearchInput.test.tsx assertions for the friendly_name match + new placeholder.
  </action>
  <verify>
    <automated>cd orchestrator/ui && npx vitest run src/components/SensorSearchInput.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Search matches friendly_name; area/domain browse renders; group list + editor + member picker work with client validation; #/sensors untouched; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (Ingress) | SPA fetches/posts under Supervisor's dynamic base path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Spoofing/EoP | SPA API calls under Ingress | mitigate | Relative apiGet/apiPost only (no leading slash) — server-side IsAuthorizedRequest (08-02) is the authority; client validation is UX-only |
| T-08-09 | Tampering | client-only validation bypass | accept | Backend re-validates on /api/groups/save (08-02) — client validation is fast-feedback, never the authority |
</threat_model>

<verification>
- `cd orchestrator/ui && npx vitest run` green
- `cd orchestrator/ui && npx tsc --noEmit` clean
- `cd orchestrator/ui && npx vite build` succeeds
- Manual: #/sensors still loads and saves (zero regression)
</verification>

<success_criteria>
Group authoring path works end-to-end (list → new/edit → member picker with client validation → save via hot-reload endpoint); search matches friendly_name; sensors browse by area/domain; #/sensors unchanged.
</success_criteria>

<output>
Create `.planning/phases/08-group-config-ui-algorithm-chooser/08-03-SUMMARY.md` when done.
</output>
