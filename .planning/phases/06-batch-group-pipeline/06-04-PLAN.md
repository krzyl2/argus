---
phase: 06-batch-group-pipeline
plan: 04
type: execute
wave: 2
depends_on: [06-01, 06-02, 06-03]
files_modified:
  - orchestrator/Argus.Orchestrator/Batch/BatchSchedulerWorker.cs
  - orchestrator/Argus.Orchestrator/Workers/MqttPublisherWorker.cs
  - orchestrator/Argus.Orchestrator/Logging/LogEvents.cs
  - orchestrator/Argus.Orchestrator/Program.cs
  - orchestrator/Argus.Orchestrator.Tests/GroupBatchSchedulerTests.cs
  - orchestrator/Argus.Orchestrator.Tests/BatchSchedulerWorkerTests.cs
autonomous: true
requirements: [GRP-02, GRP-08]
must_haves:
  truths:
    - "After the per-entity loop, RunBatchAsync scores every group in _liveConfig.Get().Groups per-cycle (CFG-04 live read, no caching) (GRP-02)"
    - "peer_divergence groups publish one binary_sensor+score PER member from GroupScoreResponse.PerMember; joint groups publish one group-level binary_sensor+score from GroupScoreResponse.GroupVerdict (GRP-08 layout branches on mode)"
    - "Nightly fit calls FitGroupAsync for joint groups only; peer_divergence groups are stateless and are skipped before the try (no FitGroup RPC for peer)"
    - "A joint group is skipped for the whole cycle when ANY member breaches the wall-clock staleness_cap; a peer group drops only the stale member(s) and still scores when >=3 fresh members remain, otherwise skips (GRP-02 staleness enforcement)"
    - "A pivot row where any member value is null is excluded from the scored matrix so the matrix stays rectangular (no forward-filled gap reaches the detector)"
    - "On ConfigChanged, removed members (oldMembers.Except(newMembers)) are retracted BEFORE the new group set is published, so no survivor flickers and no orphan remains (GRP-08 retraction ordering)"
    - "A per-group exception is caught and logged; OperationCanceledException is always rethrown; one failing group never stops the others or the entity loop (fault isolation)"
  artifacts:
    - path: "orchestrator/Argus.Orchestrator/Batch/BatchSchedulerWorker.cs"
      provides: "group scoring loop in RunBatchAsync + joint-only fit in RunNightlyFitAsync + staleness boundary policy"
      contains: "RunGroupBatchAsync"
    - path: "orchestrator/Argus.Orchestrator/Workers/MqttPublisherWorker.cs"
      provides: "group discovery publish at start + on ConfigChanged, with removed-members-first retraction using a stored previous-groups snapshot"
      contains: "RetractGroupAsync"
    - path: "orchestrator/Argus.Orchestrator/Program.cs"
      provides: "DI registration of IGroupInfluxDataSource/GroupInfluxReader inside the InfluxUrl-configured block; group services threaded into BatchSchedulerWorker"
      contains: "IGroupInfluxDataSource"
    - path: "orchestrator/Argus.Orchestrator.Tests/GroupBatchSchedulerTests.cs"
      provides: "joint-skip-on-stale vs peer-drop-member, per-member vs group-level publish, nightly fit mode-branch tests"
      contains: "GroupBatchSchedulerTests"
  key_links:
    - from: "orchestrator/Argus.Orchestrator/Batch/BatchSchedulerWorker.cs"
      to: "orchestrator/Argus.Orchestrator/Batch/IGroupInfluxDataSource.cs"
      via: "RunGroupBatchAsync/RunGroupFitAsync call QueryGroupAsync to obtain the aligned matrix + LastSeenUtc"
      pattern: "QueryGroupAsync"
    - from: "orchestrator/Argus.Orchestrator/Batch/BatchSchedulerWorker.cs"
      to: "orchestrator/Argus.Orchestrator/Batch/IBatchDetectorClient.cs"
      via: "ScoreGroupBatchAsync (both modes) + FitGroupAsync (joint only)"
      pattern: "(ScoreGroupBatch|FitGroup)Async"
    - from: "orchestrator/Argus.Orchestrator/Batch/BatchSchedulerWorker.cs"
      to: "orchestrator/Argus.Orchestrator/Mqtt/IStatePublisher.cs"
      via: "PublishGroupFlagAsync/PublishGroupScoreAsync per member (peer) or group-level (joint)"
      pattern: "PublishGroup(Flag|Score)Async"
    - from: "orchestrator/Argus.Orchestrator/Workers/MqttPublisherWorker.cs"
      to: "orchestrator/Argus.Orchestrator/Mqtt/DiscoveryPublisher.cs"
      via: "PublishGroupAsync (current set) + RetractGroupAsync (removed members) on start and ConfigChanged"
      pattern: "(PublishGroup|RetractGroup)Async"
  prohibitions:
    - "Do NOT call FitGroupAsync for peer_divergence groups — they are stateless (skip with continue before the try, mirroring RESEARCH Pattern 4)"
    - "Do NOT drop a single stale member from a JOINT group mid-cycle — a joint model has a fixed feature-column set; skip the whole group when any member is stale (RESEARCH Pitfall 3)"
    - "Do NOT score a peer group after staleness-dropping members if fewer than 3 fresh members remain — re-check the min-N floor"
    - "Do NOT blanket-retract a whole group on a partial membership change — retract only oldMembers.Except(newMembers) BEFORE republishing (RESEARCH Pitfall 5)"
    - "Do NOT cache the group list — read _liveConfig.Get().Groups fresh every cycle (CFG-04), exactly like the entity loop"
    - "Do NOT change the group Flux/staleness data-shaping (that is Plan 06-02); this plan only APPLIES the exclusion policy at the scoring boundary"
    - "Do NOT add any new NuGet package (RESEARCH: zero new packages this phase)"
---

<objective>
Wire the Wave-1 group building blocks (config schema 06-01, aligned Influx reader + group RPCs 06-02, MQTT group publish/retract 06-03) into the live pipeline: a group scoring loop in `BatchSchedulerWorker.RunBatchAsync` after the per-entity loop, a joint-only group Fit in `RunNightlyFitAsync`, the wall-clock staleness-cap exclusion policy applied at the scoring boundary, group discovery publish + removed-members-first retraction in `MqttPublisherWorker` on start and ConfigChanged, and the DI wiring in `Program.cs`.

Purpose: This is the integration plan that makes group anomalies actually appear in HA — it consumes every Wave-1 artifact and owns the two files no Wave-1 plan touches (BatchSchedulerWorker.cs, Program.cs). It is why 06-04 is Wave 2 and depends on all three Wave-1 plans.
Output: group loop + joint-only fit in the scheduler, staleness policy at the boundary, group discovery/retraction wiring in the MQTT worker, DI registration, and integration tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-batch-group-pipeline/06-CONTEXT.md
@.planning/phases/06-batch-group-pipeline/06-RESEARCH.md
@.planning/phases/06-batch-group-pipeline/06-PATTERNS.md
@.planning/phases/06-batch-group-pipeline/06-01-PLAN.md
@.planning/phases/06-batch-group-pipeline/06-02-PLAN.md
@.planning/phases/06-batch-group-pipeline/06-03-PLAN.md
</context>

<artifacts_this_phase_produces>
This plan consumes Wave-1 artifacts and produces the integration surface:

Consumed (must exist before this plan runs):
- `GroupConfig` (GroupId, FriendlyName, Members, Mode, Detector, Params, [YamlIgnore] ResolvedUnits) + `EntitiesConfig.Groups` — from 06-01
- `IGroupInfluxDataSource.QueryGroupAsync(members, every, aggFn, stalenessCap, ct)` → `GroupAlignedData(IReadOnlyList<GroupRow> Rows, IReadOnlyDictionary<string,DateTime> LastSeenUtc)` where `GroupRow(DateTime Timestamp, IReadOnlyDictionary<string,double?> MemberValues)`; `GroupInfluxReader` — from 06-02
- `IBatchDetectorClient.ScoreGroupBatchAsync(GroupScoreRequest, ct)` → GroupScoreResponse; `FitGroupAsync(FitGroupRequest, ct)` → FitGroupResponse — from 06-02
- `IStatePublisher.PublishGroupFlagAsync(groupId, memberId?, on, ct)` / `PublishGroupScoreAsync(groupId, memberId?, score, ct)` — from 06-03
- `DiscoveryPublisher.PublishGroupAsync(...)` (MqttConnection + delegate overloads) + `RetractGroupAsync(...)` (removed-members-only, MqttConnection + delegate overloads) — from 06-03

Produced (this plan):
- `BatchSchedulerWorker.RunGroupBatchAsync(GroupConfig, ct)` + `RunGroupFitAsync(GroupConfig, ct)` (private), group loop appended to `RunBatchAsync`/`RunNightlyFitAsync`; group-Influx-source field injected via both ctors
- Staleness-boundary helper on the worker: builds a rectangular per-member value matrix from `GroupAlignedData`, excludes null-cell rows, applies wall-clock `staleness_cap` (joint skip-group vs peer drop-member + min-3 floor)
- `MqttPublisherWorker` group discovery publish + removed-members-first retraction on ConfigChanged (stored previous-groups snapshot)
- `LogEvents` group scheduler events (5012–5015 range)
- `Program.cs` DI: `GroupInfluxReader`/`IGroupInfluxDataSource` inside the `InfluxUrl`-configured block; group source threaded into the `BatchSchedulerWorker` factory
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Group scoring loop + joint-only fit + staleness-cap boundary in BatchSchedulerWorker</name>
  <files>orchestrator/Argus.Orchestrator/Batch/BatchSchedulerWorker.cs, orchestrator/Argus.Orchestrator/Logging/LogEvents.cs</files>
  <read_first>
    - orchestrator/Argus.Orchestrator/Batch/BatchSchedulerWorker.cs (dual ctor, RunBatchAsync entity loop lines 125-145, RunEntityBatchAsync publish-last-verdict lines 169-180, RunNightlyFitAsync lines 212-254, BuildScoreBatchRequest lines 183-208, RunBatchForTestAsync/RunNightlyFitForTestAsync internal helpers)
    - orchestrator/Argus.Orchestrator/Batch/IGroupInfluxDataSource.cs + GroupAlignedData/GroupRow shapes (06-02 artifact — null MemberValues cell = gap; LastSeenUtc = per-member freshness)
    - orchestrator/Argus.Orchestrator/Batch/IBatchDetectorClient.cs (ScoreGroupBatchAsync/FitGroupAsync signatures — 06-02 artifact)
    - orchestrator/Argus.Orchestrator/Mqtt/IStatePublisher.cs (PublishGroupFlagAsync/PublishGroupScoreAsync — 06-03 artifact)
    - proto/argus.proto lines 74-109 (Series{MemberId,Values}; GroupScoreRequest{GroupId,Detector,Params,Series}; GroupScoreResponse{PerMember[],GroupVerdict,Contributions[],Ok,Error}; Verdict.Score is double?, Verdict.EntityId is the member id; FitGroupRequest{GroupId,Detector,Params,Series})
    - 06-RESEARCH.md Pattern 4 (group loop, peer skip-fit), Pitfall 3 (joint fixed feature vector — skip whole group on stale), Open Question 1 (per-member drop for peer, whole-row/whole-group skip for joint), Pitfall 2 (fn=mean is the safe default for null-on-gap)
    - 06-CONTEXT.md staleness_cap decision + specifics ("stale member beyond cap → excluded, not scored")
  </read_first>
  <behavior>
    - RunBatchAsync scores each group in _liveConfig.Get().Groups AFTER the existing per-entity loop, read fresh per-cycle (CFG-04)
    - Each group is wrapped in per-item try/catch; OperationCanceledException rethrown; other exceptions logged and the next group continues
    - every / fn / staleness_cap come from GroupConfig.Params with defaults every=5m, fn=mean, staleness_cap read as a duration (fall back to a sane default when absent); QueryGroupAsync is called with those values
    - Null-cell rows (any member value null) are excluded so the matrix passed to the detector is rectangular
    - Wall-clock staleness: a member is stale when (utcNow - LastSeenUtc[member]) > staleness_cap
    - JOINT + any member stale → the whole group is skipped this cycle (logged), no ScoreGroupBatch call
    - PEER + some members stale → those members are dropped from the matrix; if >=3 fresh members remain the group is scored on the fresh subset, else the group is skipped (min-N floor re-check)
    - peer_divergence response: publish PublishGroupScoreAsync/PublishGroupFlagAsync PER member from GroupScoreResponse.PerMember (verdict.EntityId is the member id, verdict.Score is double? → ?? 0.0)
    - joint response: publish one group-level PublishGroupScoreAsync/PublishGroupFlagAsync from GroupScoreResponse.GroupVerdict; carry GroupScoreResponse.Contributions through (log at debug/info — no HA surface this phase, GRP-09 is Phase 8)
    - response.Ok == false → log an error and publish nothing for that group
    - RunNightlyFitAsync: for each group, skip peer_divergence entirely (continue before the try — stateless); for joint call FitGroupAsync with the aligned Series (same per-item try/catch + OperationCanceledException rethrow)
  </behavior>
  <action>
    Inject an `IGroupInfluxDataSource` into BatchSchedulerWorker: add a `private readonly IGroupInfluxDataSource _groupInfluxReader;` field and a parameter to BOTH constructors (test ctor and production ctor) positioned consistently; the production ctor forwards to the test ctor via `: this(...)` as today. Keep the existing entity path untouched.

    In RunBatchAsync, after the existing `foreach (var entity in _liveConfig.Get().Entities)` loop, add a second loop `foreach (var group in _liveConfig.Get().Groups)` with the identical fault-isolation shape as the entity loop: `try { await RunGroupBatchAsync(group, ct); } catch (OperationCanceledException) { throw; } catch (Exception ex) { _logger.LogError(LogEvents.BatchSchedulerError, ex, "Group batch failed for {GroupId}", group.GroupId); }`.

    Add private `RunGroupBatchAsync(GroupConfig group, CancellationToken ct)`:
    1. Read `every`, `fn`, and the staleness cap from `group.Params` (keys as documented in 06-CONTEXT.md defaults; every default 5m, fn default mean, staleness_cap default a reasonable multiple of the window when absent). Parse the staleness_cap duration defensively (TryParse; default on failure).
    2. Call `await _groupInfluxReader.QueryGroupAsync(group.Members, every, fn, stalenessCap, ct)` to get `GroupAlignedData`.
    3. Apply the staleness/exclusion policy in a private helper `BuildGroupMatrix(group, data, utcNow, out droppedMembers, out skipWholeGroup)`: exclude any row where a member cell is null (rectangular guard); compute per-member staleness via `utcNow - data.LastSeenUtc[member] > stalenessCap`. Branch on mode using `string.Equals(group.Mode, "peer_divergence", StringComparison.OrdinalIgnoreCase)`: for JOINT, if any member is stale set skipWholeGroup=true and return; for PEER, remove stale members from the active member set and, if fewer than 3 fresh members remain, set skipWholeGroup=true. Produce an ordered `IReadOnlyList<string> activeMembers` and a parallel `IReadOnlyDictionary<string, List<double>> memberSeries` over the surviving rows/members.
    4. If skipWholeGroup or no rows survive, log at info/warning (GroupSkippedStale) and return without an RPC call.
    5. Build a `GroupScoreRequest { GroupId = group.GroupId, Detector = group.Detector }`, copy `group.Params` into `request.Params`, and add one `Series { MemberId = m }` per active member with `series.Values.AddRange(memberSeries[m])` (mirror BuildScoreBatchRequest's construction style).
    6. `var response = await _detectorClient.ScoreGroupBatchAsync(request, ct);` — if `!response.Ok`, log GroupSchedulerError with `response.Error` and return.
    7. Publish branching on mode: PEER → `foreach (var v in response.PerMember) { await _statePublisher.PublishGroupScoreAsync(group.GroupId, v.EntityId, v.Score ?? 0.0, ct); await _statePublisher.PublishGroupFlagAsync(group.GroupId, v.EntityId, v.IsAnomaly, ct); }`. JOINT → `var v = response.GroupVerdict; await _statePublisher.PublishGroupScoreAsync(group.GroupId, null, v.Score ?? 0.0, ct); await _statePublisher.PublishGroupFlagAsync(group.GroupId, null, v.IsAnomaly, ct);`. Log GroupScored with the score/anomaly and, for joint, the top-ranked entry of `response.Contributions` (carried through per GRP-09 note; no HA surface).

    In RunNightlyFitAsync, after the existing entity fit loop, add `foreach (var group in _liveConfig.Get().Groups)`: if `string.Equals(group.Mode, "peer_divergence", StringComparison.OrdinalIgnoreCase)` then `continue` (stateless — no FitGroup RPC for peer); else wrap `await RunGroupFitAsync(group, ct)` in the same per-item try/catch + OperationCanceledException rethrow. Add private `RunGroupFitAsync(GroupConfig group, CancellationToken ct)` that queries the aligned matrix (same QueryGroupAsync + rectangular-null exclusion; for joint fit, exclude stale members' rows the same way — skip fit if no rows survive), builds a `FitGroupRequest { GroupId, Detector }` with params + Series identically to the score path, calls `await _detectorClient.FitGroupAsync(request, ct)`, and logs GroupSchedulerError when `!response.Ok`.

    In LogEvents.cs add four group events in the batch-scheduler 5xxx region following the existing numbering convention: `GroupScored = new(5012, nameof(GroupScored))`, `GroupSkippedStale = new(5013, nameof(GroupSkippedStale))`, `GroupSchedulerError = new(5014, nameof(GroupSchedulerError))`, `GroupNoData = new(5015, nameof(GroupNoData))`. Use `GroupSchedulerError` for the response.Ok==false and per-group catch paths, `GroupSkippedStale` for staleness skips, `GroupNoData` for empty-matrix skips, `GroupScored` for successful publishes.

    Do not embed, in any comment or log message text, a verbatim copy of the acceptance-criteria literals a negative gate greps for (e.g. do not write the raw method name a `== 0` gate might match) — reference behavior conceptually in prose.
  </action>
  <verify>
    <automated>cd orchestrator &amp;&amp; dotnet build Argus.Orchestrator/Argus.Orchestrator.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - RunBatchAsync has a group loop after the entity loop with per-group try/catch + OperationCanceledException rethrow
    - RunGroupBatchAsync applies null-row exclusion + wall-clock staleness (joint skip-group, peer drop-member + min-3 floor) before any RPC
    - Peer publishes per-member via PublishGroupScoreAsync/PublishGroupFlagAsync(groupId, memberId, ...); joint publishes group-level via (groupId, null, ...)
    - RunNightlyFitAsync skips peer_divergence (continue) and calls FitGroupAsync only for joint
    - IGroupInfluxDataSource injected via both ctors; existing entity path unchanged
    - LogEvents.GroupScored/GroupSkippedStale/GroupSchedulerError/GroupNoData (5012–5015) defined
    - Build succeeds (Program.cs DI update lands in Task 2)
  </acceptance_criteria>
  <done>Group scoring loop, joint-only fit, and the staleness-cap boundary policy are wired into the scheduler; peer drops stale members with a min-3 floor, joint skips the whole group on any stale member.</done>
</task>

<task type="auto">
  <name>Task 2: Group discovery publish + removed-members-first retraction in MqttPublisherWorker; Program.cs DI</name>
  <files>orchestrator/Argus.Orchestrator/Workers/MqttPublisherWorker.cs, orchestrator/Argus.Orchestrator/Program.cs</files>
  <read_first>
    - orchestrator/Argus.Orchestrator/Workers/MqttPublisherWorker.cs (ExecuteAsync: ConnectAsync, StatePublisher.SetConnection, OnConfigChanged fire-and-forget republish, initial PublishAllAsync, _stoppingToken field, ConfigChanged subscribe/unsubscribe in finally)
    - orchestrator/Argus.Orchestrator/Mqtt/DiscoveryPublisher.cs (PublishGroupAsync / RetractGroupAsync MqttConnection overloads — 06-03 artifact; existing PublishAllAsync/RetractAsync signatures)
    - orchestrator/Argus.Orchestrator/Config/EntitiesConfig.cs (GroupConfig.Members/Mode/GroupId — 06-01 artifact)
    - orchestrator/Argus.Orchestrator/Program.cs (lines 137-159 — the `if (!string.IsNullOrWhiteSpace(connectionSettings.InfluxUrl))` block: InfluxDBClient/InfluxDbReader/IInfluxDataSource/IBatchDetectorClient registration + BatchSchedulerWorker factory)
    - 06-RESEARCH.md Pitfall 5 (retract removed members BEFORE republish; per-member granularity; no blanket retract)
    - 06-CONTEXT.md GRP-08 retraction ("retract removed members' discovery topics BEFORE publishing the new set")
  </read_first>
  <action>
    In MqttPublisherWorker, add group discovery to both the initial publish and the ConfigChanged handler, and add removed-members-first retraction on ConfigChanged. Store the previous group set so the diff can be computed:

    - Add a private field holding the last-published group snapshot: `private IReadOnlyList<GroupConfig> _lastGroups = System.Array.Empty<GroupConfig>();` (the worker is single-threaded through ExecuteAsync + the fire-and-forget handler; the handler runs sequentially enough for this snapshot to be the diff basis — assign it only at the end of each publish pass).
    - After the initial `await DiscoveryPublisher.PublishAllAsync(_mqtt, _liveConfig.Get().Entities, stoppingToken);`, publish group discovery for the current set: `foreach (var group in _liveConfig.Get().Groups) await DiscoveryPublisher.PublishGroupAsync(_mqtt, group, stoppingToken);` then set `_lastGroups = _liveConfig.Get().Groups;`.
    - In OnConfigChanged, before publishing the new set, compute per-group removed members and retract them FIRST. For each group_id present in `_lastGroups`, look up the same group_id in the new set: if the group_id is gone entirely, the removed set is all of its old members (peer) or the whole group (joint); if it still exists, `removed = oldMembers.Except(newMembers, StringComparer.OrdinalIgnoreCase)`. Call `await DiscoveryPublisher.RetractGroupAsync(_mqtt, oldGroup, removed, _stoppingToken);` (use the removed-members-only overload from 06-03) for each group with a non-empty removed set (or whole-group removal) — retraction happens BEFORE the republish of the current set. Then publish `PublishGroupAsync` for every group in the new set and update `_lastGroups`.
    - Keep the existing entity discovery/availability republish in OnConfigChanged unchanged; add the group publish/retract alongside it inside the same fire-and-forget Task.Run body, ordered: retract-removed-groups → republish-entities (existing) → republish-groups → update `_lastGroups`. Log a GroupSchedulerError (or the existing error path) on failure; swallow OperationCanceledException as the existing handler does.

    In Program.cs, inside the existing `if (!string.IsNullOrWhiteSpace(connectionSettings.InfluxUrl))` block (the batch path), register the group Influx source next to the per-entity one: `builder.Services.AddSingleton<GroupInfluxReader>();` and `builder.Services.AddSingleton<IGroupInfluxDataSource>(sp => sp.GetRequiredService<GroupInfluxReader>());` (GroupInfluxReader reuses the already-registered singleton InfluxDBClient via its production ctor — do not register a second InfluxDBClient). Update the `AddHostedService<BatchSchedulerWorker>` factory to pass `sp.GetRequiredService<IGroupInfluxDataSource>()` in the position matching the new constructor parameter added in Task 1. Because group scoring lives in the batch path, group services are only registered when InfluxDB is configured — consistent with the existing "no InfluxDB → streaming-only, no batch worker" degrade. MqttPublisherWorker's group discovery/retraction does not require InfluxDB, so it always runs; when no groups are configured the group loops are no-ops. Respect the config-load timing pitfall: 06-01 owns Validate()/ValidateGroups() with the nullable IHaSensorRegistry; this plan only wires the reload path — the UI-save reload at Program.cs POST /api/sensors/save already calls EntitiesConfigLoader.Load then liveCfg.Swap, which fires ConfigChanged that MqttPublisherWorker now handles for groups. Do not change the entity save handler's body beyond what is required; groups are authored via file edits this phase (no group UI until Phase 8), so the file-watcher reload path (ConfigFileWatcherService → Swap → ConfigChanged) is the primary group reload trigger.
  </action>
  <verify>
    <automated>cd orchestrator &amp;&amp; dotnet build Argus.Orchestrator/Argus.Orchestrator.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - MqttPublisherWorker publishes group discovery at start and on ConfigChanged
    - On ConfigChanged, removed members (oldMembers.Except(newMembers)) are retracted BEFORE the new set is republished; survivors are not touched
    - _lastGroups snapshot updated after each publish pass so the next diff is correct
    - Program.cs registers GroupInfluxReader + IGroupInfluxDataSource inside the InfluxUrl block and threads it into the BatchSchedulerWorker factory
    - No second InfluxDBClient registration; no new NuGet package
    - Build succeeds
  </acceptance_criteria>
  <done>Group discovery is published on start and reload with precise removed-members-first retraction; DI resolves the group source into the batch worker.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration tests — staleness branch, publish layout, nightly fit mode-branch</name>
  <files>orchestrator/Argus.Orchestrator.Tests/GroupBatchSchedulerTests.cs, orchestrator/Argus.Orchestrator.Tests/BatchSchedulerWorkerTests.cs</files>
  <read_first>
    - orchestrator/Argus.Orchestrator.Tests/BatchSchedulerWorkerTests.cs (FakeInfluxDbReader/FakeBatchDetectorClient/FakeStatePublisher shapes; MakeLive; RunBatchForTestAsync/RunNightlyFitForTestAsync usage; ScoreBatchEntityIds call-tracking pattern)
    - orchestrator/Argus.Orchestrator/Batch/IGroupInfluxDataSource.cs + GroupAlignedData/GroupRow (06-02 artifact — for the fake group source)
    - orchestrator/Argus.Orchestrator/Mqtt/IStatePublisher.cs (PublishGroupFlagAsync/PublishGroupScoreAsync added by 06-03 — the FakeStatePublisher MUST implement them)
    - proto/argus.proto lines 74-109 (GroupScoreResponse.PerMember/GroupVerdict/Contributions; Verdict shape)
    - 06-RESEARCH.md Pitfall 3 + Open Question 1 (joint skip-group vs peer drop-member)
  </read_first>
  <behavior>
    - joint group with one member stale beyond cap → ScoreGroupBatchAsync NOT called (whole group skipped), no group publish
    - peer group with one member stale (still >=3 fresh) → ScoreGroupBatchAsync called with only the fresh members in Series; scored
    - peer group dropping members below 3 fresh → skipped, no RPC
    - peer response.PerMember with N members → N score + N flag PublishGroup* calls, each with the member id
    - joint response.GroupVerdict → exactly one group-level score + one flag PublishGroup* call with null memberId
    - RunNightlyFit: FitGroupAsync called for joint groups, NEVER for peer_divergence groups
    - a group RPC exception is isolated (worker does not throw; other groups/entities proceed)
  </behavior>
  <action>
    First, update the existing `FakeStatePublisher` in BatchSchedulerWorkerTests.cs to implement the two new IStatePublisher members added by 06-03 (`PublishGroupFlagAsync(string groupId, string? memberId, bool on, CancellationToken)` and `PublishGroupScoreAsync(string groupId, string? memberId, double score, CancellationToken)`) with call recorders (e.g. a `List<(string GroupId, string? MemberId)> GroupFlagCalls` / `GroupScoreCalls`) — otherwise the test project will not compile against the extended interface. Keep existing entity tests green.

    Create GroupBatchSchedulerTests.cs mirroring BatchSchedulerWorkerTests structure. Add:
    - `FakeGroupInfluxDataSource : IGroupInfluxDataSource` returning a configurable `GroupAlignedData` (rows + LastSeenUtc) so tests control freshness/gaps; expose call tracking.
    - Extend the detector fake (either reuse a local fake or a shared one) with `ScoreGroupBatchAsync`/`FitGroupAsync`: track `ScoreGroupCallCount`, `FitGroupCallCount`, the last `GroupScoreRequest` (to assert Series member ids), and a configurable `GroupScoreResponse` (PerMember for peer, GroupVerdict for joint). Add a `ThrowOnScoreGroup` init flag for the fault-isolation test.
    - Helpers building a peer_divergence GroupConfig (4 members) and a joint GroupConfig (3 members), with `every`/`fn`/`staleness_cap` in Params.
    - Tests:
      1. joint + one member stale (LastSeenUtc older than cap) → assert ScoreGroupCallCount == 0 and no group publish.
      2. peer + one member stale, 3 fresh remain → assert ScoreGroupCallCount == 1 and the request Series contains only the 3 fresh member ids.
      3. peer + members stale leaving <3 fresh → assert ScoreGroupCallCount == 0.
      4. peer response with 3 PerMember verdicts → assert 3 GroupScore + 3 GroupFlag calls, each carrying the member id.
      5. joint response GroupVerdict → assert exactly 1 GroupScore + 1 GroupFlag call with null memberId.
      6. RunNightlyFit over one peer + one joint group → assert FitGroupCallCount == 1 (joint only), peer never fit.
      7. ScoreGroupBatchAsync throws for one group but a second group still scores (fault isolation) — worker does not rethrow.
    Use RunBatchForTestAsync/RunNightlyFitForTestAsync and MakeLive with an EntitiesConfig carrying Groups (and no entities, so the tests isolate the group path). Do not require live Influx/gRPC/MQTT.
  </action>
  <verify>
    <automated>cd orchestrator &amp;&amp; dotnet test Argus.Orchestrator.Tests/Argus.Orchestrator.Tests.csproj --filter "FullyQualifiedName~GroupBatchSchedulerTests|FullyQualifiedName~BatchSchedulerWorkerTests"</automated>
  </verify>
  <acceptance_criteria>
    - joint-skip-on-stale, peer-drop-member, peer-below-floor-skip tests pass
    - per-member (peer) vs group-level null-memberId (joint) publish tests pass
    - nightly-fit mode-branch test passes (joint fit only, peer never)
    - group RPC exception isolated (no rethrow) test passes
    - existing BatchSchedulerWorkerTests remain green after FakeStatePublisher extension
  </acceptance_criteria>
  <done>Integration behavior verified offline: staleness branching, mode-branched publish layout, joint-only nightly fit, and per-group fault isolation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config groups: → group scoring loop | operator-authored group_id/members/params drive Flux queries (via 06-02 reader) and MQTT topics (via 06-03) crossing into the live pipeline |
| InfluxDB → aligned matrix → detector (gRPC) | HA-sourced values + freshness cross into the scored feature matrix; staleness policy is the correctness gate |
| orchestrator → MQTT broker → HA | group discovery/retraction published; membership diff drives retraction scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Tampering | malformed group config (params, member ids) flowing into the Flux query built by GroupInfluxReader | mitigate | member ids / bucket / field are validated by `_safeFluxString` in 06-02 before interpolation; this plan passes the already-config-validated (06-01 ValidateGroups: floor, mode, units) GroupConfig through — no unvalidated string reaches Flux from the loop; per-group try/catch bounds any parse/RPC failure to one group |
| T-06-09 | Denial of Service | unbounded group size (huge members list) → large Series payload / long-running RPC per cycle | mitigate | member count is bounded by config which 06-01 ValidateGroups prunes; per-group try/catch + OperationCanceledException rethrow keeps one slow/failing group from stalling the tick beyond the group's own scope; group loop runs after (not interleaved with) the entity loop so entity scoring is never blocked by a group |
| T-06-10 | Tampering | orphaned/flickering HA entities on membership change | mitigate | removed-members-first retraction (oldMembers.Except(newMembers)) BEFORE republish, using the stored `_lastGroups` snapshot diff — no blanket retract of survivors (RESEARCH Pitfall 5) |
| T-06-11 | Information Disclosure | stale sensor value scored as live (correctness/trust failure) | mitigate | wall-clock staleness_cap enforced at the scoring boundary: joint skips the whole group on any stale member; peer drops stale members and re-checks the min-3 floor; null-cell rows excluded so no forward-filled gap reaches the detector (GRP-02) |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | none — this phase installs zero new packages (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `dotnet build` clean; `dotnet test --filter GroupBatchSchedulerTests` + `BatchSchedulerWorkerTests` green
- Manual (post-integration): with a peer group configured, three members appear in HA under one device and update within the batch cadence; with a joint group, one group entity appears; stopping one member's sensor beyond staleness_cap drops it (peer) or skips the group (joint); removing a member from a peer group retracts only its entities, survivors stay live
- Assumption A1 (06-02 RESEARCH): the aggregateWindow+pivot null-on-gap semantics underpinning the null-row exclusion were doc-verified, not live-verified — confirm against a live/Docker InfluxDB before production sign-off (non-blocking for this plan's offline unit verification)
</verification>

<success_criteria>
- Group anomalies surface in HA end-to-end: config → aligned matrix → group RPC → per-member (peer) or group-level (joint) MQTT entities under one device (GRP-08)
- Staleness cap enforced at the scoring boundary: joint skip-group, peer drop-member with min-3 floor, no forward-filled gap scored (GRP-02)
- Membership change retracts only removed members before republish; joint fit runs, peer fit never runs
</success_criteria>

<output>
Create `.planning/phases/06-batch-group-pipeline/06-04-SUMMARY.md` when done
</output>
</invoke>
