---
phase: 06-batch-group-pipeline
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - orchestrator/Argus.Orchestrator/Mqtt/UniqueId.cs
  - orchestrator/Argus.Orchestrator/Mqtt/StatePublisher.cs
  - orchestrator/Argus.Orchestrator/Mqtt/IStatePublisher.cs
  - orchestrator/Argus.Orchestrator/Mqtt/DiscoveryPublisher.cs
  - orchestrator/Argus.Orchestrator.Tests/MqttRetractionTests.cs
autonomous: true
requirements: [GRP-08]
must_haves:
  truths:
    - "Peer-divergence groups publish one binary_sensor + score PER member; joint groups publish one group-level binary_sensor + score (GRP-08 layout branches on mode)"
    - "All entities of a group share ONE HA device (device.identifiers = argus_group_{slug}), so HA groups them under one device"
    - "Group MQTT topics use the distinct argus/group/{slug}/... namespace, never colliding with per-entity argus/{slug}/... topics"
    - "Group discovery retraction retracts only the removed members' topics (oldMembers.Except(newMembers)), never blanket-retracting the whole group"
    - "unique_id follows argus_group_{group_slug}_{member_slug}_flag|score (peer) and argus_group_{group_slug}_flag|score (joint)"
  artifacts:
    - path: "orchestrator/Argus.Orchestrator/Mqtt/UniqueId.cs"
      provides: "GroupFlagId/GroupScoreId helpers"
      contains: "GroupFlagId"
    - path: "orchestrator/Argus.Orchestrator/Mqtt/DiscoveryPublisher.cs"
      provides: "BuildGroupBinarySensorConfig/BuildGroupSensorConfig + RetractGroupAsync (removed-members-only)"
      contains: "BuildGroupBinarySensorConfig"
    - path: "orchestrator/Argus.Orchestrator/Mqtt/StatePublisher.cs"
      provides: "GroupFlagTopic/GroupScoreTopic + PublishGroupFlagAsync/PublishGroupScoreAsync"
      contains: "PublishGroupFlagAsync"
  key_links:
    - from: "orchestrator/Argus.Orchestrator/Mqtt/DiscoveryPublisher.cs"
      to: "orchestrator/Argus.Orchestrator/Mqtt/UniqueId.cs"
      via: "group discovery payloads use UniqueId.GroupFlagId/GroupScoreId for unique_id/object_id"
      pattern: "UniqueId\\.Group(Flag|Score)Id"
    - from: "orchestrator/Argus.Orchestrator/Mqtt/StatePublisher.cs"
      to: "orchestrator/Argus.Orchestrator/Mqtt/UniqueId.cs"
      via: "GroupFlagTopic/GroupScoreTopic slug via UniqueId.Slug"
      pattern: "UniqueId\\.Slug"
  prohibitions:
    - "Do NOT copy the per-entity device.identifiers = new[] { slug } line into group builders — group entities MUST share one device.identifiers = argus_group_{slug} (RESEARCH critical deviation)"
    - "Do NOT reuse the per-entity argus/{slug}/... topic prefix for group state — use argus/group/{slug}/... (topic-collision guard)"
    - "Do NOT blanket-retract a whole group on partial membership change — retract only removed members' topics (Pitfall 5)"
---

<objective>
Add the MQTT group entity layer (GRP-08): unique_id helpers, distinct group state topics, discovery payload builders branching on mode, and a precise removed-members-only retraction. Peer mode emits N member pairs; joint mode emits one group pair; all share one HA device per group_id.

Purpose: Gives the scoring loop (Plan 06-04) the publish + retract surface that surfaces group anomalies as HA entities and cleans them up on membership change without orphaning.
Output: `UniqueId.GroupFlagId/GroupScoreId`, `StatePublisher` group topic + publish methods, `DiscoveryPublisher.BuildGroup*Config` + `RetractGroupAsync`, extended retraction tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-batch-group-pipeline/06-CONTEXT.md
@.planning/phases/06-batch-group-pipeline/06-RESEARCH.md
@.planning/phases/06-batch-group-pipeline/06-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan produces (consumed by Plan 06-04):
- `UniqueId.GroupFlagId(string groupId, string? memberId = null)` → `argus_group_{Slug(groupId)}_flag` (joint) or `argus_group_{Slug(groupId)}_{Slug(memberId)}_flag` (peer)
- `UniqueId.GroupScoreId(...)` analogous with `_score` suffix
- `StatePublisher.GroupFlagTopic(groupId, memberId?)` / `GroupScoreTopic(...)` → `argus/group/{slug}/[{memberSlug}/]flag|score/state`
- `StatePublisher.PublishGroupFlagAsync/PublishGroupScoreAsync` (added to IStatePublisher)
- `DiscoveryPublisher.BuildGroupBinarySensorConfig(GroupConfig, string? memberId=null)` / `BuildGroupSensorConfig(...)`
- `DiscoveryPublisher.PublishGroupAsync(...)` (publish current members) + `RetractGroupAsync(...)` (removed members only) — both with a testable publish-delegate overload mirroring RetractAsync
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UniqueId group helpers + StatePublisher group topics/publishers</name>
  <files>orchestrator/Argus.Orchestrator/Mqtt/UniqueId.cs, orchestrator/Argus.Orchestrator/Mqtt/StatePublisher.cs, orchestrator/Argus.Orchestrator/Mqtt/IStatePublisher.cs</files>
  <read_first>
    - orchestrator/Argus.Orchestrator/Mqtt/UniqueId.cs (Slug, AnomalyId, ScoreId formulas)
    - orchestrator/Argus.Orchestrator/Mqtt/StatePublisher.cs (FlagTopic/ScoreTopic, PublishFlagAsync/PublishScoreAsync, EnsureConnected, retain:false)
    - orchestrator/Argus.Orchestrator/Mqtt/IStatePublisher.cs (current 3-method interface)
    - 06-RESEARCH.md Code Examples "Publishing per-member Verdict data" (topic-namespace note); 06-PATTERNS.md UniqueId + StatePublisher sections
  </read_first>
  <behavior>
    - GroupFlagId with null memberId → argus_group_{slug}_flag; with memberId → argus_group_{slug}_{memberSlug}_flag
    - GroupScoreId mirrors with _score
    - GroupFlagTopic null memberId → argus/group/{slug}/flag/state; with memberId → argus/group/{slug}/{memberSlug}/flag/state
    - Group topics never share the per-entity argus/{slug}/... prefix
    - PublishGroupFlagAsync/PublishGroupScoreAsync publish retain:false via EnsureConnected, like the per-entity methods
  </behavior>
  <action>
    In UniqueId add `GroupFlagId(string groupId, string? memberId = null)` returning `$"argus_group_{Slug(groupId)}_flag"` when memberId is null else `$"argus_group_{Slug(groupId)}_{Slug(memberId)}_flag"`, and `GroupScoreId(...)` identically with the `_score` suffix; reuse the existing Slug helper for both groupId and memberId. In StatePublisher add `GroupFlagTopic(string groupId, string? memberId = null)` → `$"argus/group/{Slug(groupId)}/flag/state"` (joint) or `$"argus/group/{Slug(groupId)}/{Slug(memberId)}/flag/state"` (peer), and `GroupScoreTopic(...)` analogous with `score`. Add `PublishGroupFlagAsync(string groupId, string? memberId, bool on, CancellationToken ct)` and `PublishGroupScoreAsync(string groupId, string? memberId, double score, CancellationToken ct)` mirroring the existing PublishFlagAsync/PublishScoreAsync bodies exactly (EnsureConnected, ON/OFF payload for flag, invariant-culture "G" for score, retain:false). Add these two publish methods to IStatePublisher so fakes and the worker can call them. Keep the argus/group namespace distinct from argus/{slug}.
  </action>
  <verify>
    <automated>cd orchestrator &amp;&amp; dotnet build Argus.Orchestrator/Argus.Orchestrator.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - GroupFlagId/GroupScoreId branch on memberId presence
    - GroupFlagTopic/GroupScoreTopic use argus/group/{slug}/... namespace
    - PublishGroupFlagAsync/PublishGroupScoreAsync on both StatePublisher and IStatePublisher
    - Build succeeds
  </acceptance_criteria>
  <done>Group unique_id + distinct-namespace topics + group publish methods added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DiscoveryPublisher group builders (one device per group) + removed-members-only retraction</name>
  <files>orchestrator/Argus.Orchestrator/Mqtt/DiscoveryPublisher.cs</files>
  <read_first>
    - orchestrator/Argus.Orchestrator/Mqtt/DiscoveryPublisher.cs (BuildBinarySensorConfig/BuildSensorConfig payload shape, JsonOptions, PublishAllAsync/RetractAsync delegate-overload pattern, BridgeAvailabilityTopic)
    - orchestrator/Argus.Orchestrator/Config/EntitiesConfig.cs (GroupConfig — from Plan 06-01)
    - 06-RESEARCH.md Pattern 5 (group discovery payload, device-block deviation); 06-PATTERNS.md DiscoveryPublisher section
  </read_first>
  <behavior>
    - peer_divergence: BuildGroupBinarySensorConfig(group, memberId) uses GroupFlagId(groupId, memberId), state_topic argus/group/{slug}/{memberSlug}/flag/state, Polish member-scoped name
    - joint: BuildGroupBinarySensorConfig(group, null) uses GroupFlagId(groupId), state_topic argus/group/{slug}/flag/state
    - Every group entity's device.identifiers == argus_group_{groupSlug} (shared across all member pairs), device.name = Argus grupa {friendly_name}
    - device_class problem; payload_on ON / payload_off OFF; bridge availability included
    - RetractGroupAsync retracts exactly the removed members' binary_sensor+sensor config topics (2 per removed member for peer; the single group pair when the whole joint group / whole group_id is removed)
  </behavior>
  <action>
    Add `BuildGroupBinarySensorConfig(GroupConfig group, string? memberId = null)` and `BuildGroupSensorConfig(GroupConfig group, string? memberId = null)` modeled on the per-entity builders but with three deliberate deviations: (1) unique_id/object_id come from UniqueId.GroupFlagId/GroupScoreId(group.GroupId, memberId); (2) state_topic uses the argus/group/{slug}/... namespace matching StatePublisher.GroupFlagTopic/GroupScoreTopic; (3) the device block uses `identifiers = new[] { $"argus_group_{UniqueId.Slug(group.GroupId)}" }` — ONE shared value across all of a group's entities — with `name = $"Argus grupa {group.FriendlyName}"`, plus the existing Model/Manufacturer constants. Names follow the Polish friendly-name convention (D8): joint anomaly name `$"{group.FriendlyName} anomalia"`, peer `$"{group.FriendlyName} {memberId} anomalia"`; score names append " score". Determine peer vs joint by `string.Equals(group.Mode, "peer_divergence", StringComparison.OrdinalIgnoreCase)`. Add `PublishGroupAsync` (MqttConnection overload + testable delegate overload, mirroring PublishAllAsync) that, for a single group, publishes the binary_sensor + sensor config topics for each current member (peer) or the single group pair (joint) with retain:true to `homeassistant/binary_sensor/{id}/config` and `homeassistant/sensor/{id}/config`. Add `RetractGroupAsync` (MqttConnection + testable delegate overload, mirroring RetractAsync) that accepts the group_id, the group friendly context needed to build ids, and the collection of REMOVED member ids (for peer) — publishing empty retained payloads to only those removed members' two config topics; when a whole joint group (or an entire group_id) is removed, retract its single group pair. Do NOT touch non-removed members. Reuse JsonOptions and the delegate-overload structure verbatim.
  </action>
  <verify>
    <automated>cd orchestrator &amp;&amp; dotnet build Argus.Orchestrator/Argus.Orchestrator.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - Group discovery payloads branch on mode for unique_id/name/state_topic
    - device.identifiers is the single argus_group_{slug} for every group entity (not per-member)
    - state_topic uses argus/group/{slug}/... namespace
    - PublishGroupAsync + RetractGroupAsync exist with MqttConnection + delegate overloads
    - RetractGroupAsync scope limited to removed members
  </acceptance_criteria>
  <done>Group discovery builders + precise retraction added; one HA device per group; no per-member device split.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MqttRetractionTests — group membership-change retraction</name>
  <files>orchestrator/Argus.Orchestrator.Tests/MqttRetractionTests.cs</files>
  <read_first>
    - orchestrator/Argus.Orchestrator.Tests/MqttRetractionTests.cs (PublishCall recorder, MakeRecorder, RetractAsync_OnlyRetractsPassedEntities_NotOthers)
    - orchestrator/Argus.Orchestrator/Config/EntitiesConfig.cs (GroupConfig — Plan 06-01)
  </read_first>
  <behavior>
    - Peer group shrinking 4→3 members: RetractGroupAsync(removed = the 1 removed member) publishes exactly 2 empty retained messages to that member's binary_sensor+sensor config topics
    - No retraction touches the 3 surviving members' topics
    - Whole joint group removed: retracts exactly the single group pair (2 messages)
    - All retraction publishes use empty payload + retain:true
  </behavior>
  <action>
    Extend MqttRetractionTests with group cases using the existing PublishCall recorder + MakeRecorder delegate seam (no live broker). Add a helper building a GroupConfig (peer_divergence with 4 members; joint with 3 members). Test: RetractGroupAsync for a peer group with a single removed member yields 2 calls to that member's `homeassistant/binary_sensor/{GroupFlagId}/config` and `homeassistant/sensor/{GroupScoreId}/config`, all empty payload + retain true; assert no call topic contains any surviving member's slug (mirror RetractAsync_OnlyRetractsPassedEntities_NotOthers). Test: removing a whole joint group retracts exactly the single group pair. Assert empty-payload + retain-true across all group retraction calls.
  </action>
  <verify>
    <automated>cd orchestrator &amp;&amp; dotnet test Argus.Orchestrator.Tests/Argus.Orchestrator.Tests.csproj --filter "FullyQualifiedName~MqttRetractionTests"</automated>
  </verify>
  <acceptance_criteria>
    - Peer 4→3 shrink retracts only the removed member's 2 topics; survivors untouched
    - Joint whole-group removal retracts the single group pair
    - All group retraction publishes empty payload + retain:true
    - Full MqttRetractionTests class green
  </acceptance_criteria>
  <done>Group membership-change retraction verified: no orphaning, no blanket-retract of survivors (GRP-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config group_id / member id → MQTT topic + unique_id | operator-controlled strings become topic path + HA entity identity |
| orchestrator → MQTT broker → HA | discovery/state published to broker, surfaced by HA MQTT integration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Spoofing | group synthetic topic colliding with a real per-entity topic | mitigate | dedicated argus/group/{slug}/... namespace prefix, never reusing argus/{slug}/...; group_id slugified via UniqueId.Slug (dot→underscore) so path segments stay well-formed |
| T-06-07 | Tampering | orphaned/stale HA entities after membership change | mitigate | RetractGroupAsync publishes empty retained payloads to exactly the removed members' config topics before republish (Plan 06-04 ordering), diffed oldMembers.Except(newMembers) — no orphan, no survivor flicker (Pitfall 5) |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | none — zero new packages (RESEARCH audit N/A) |
</threat_model>

<verification>
- `dotnet build` clean; `dotnet test --filter MqttRetractionTests` green
- Manual (Plan 06-04 integration): a group appears in HA under one device; removing a member retracts only its entities
</verification>

<success_criteria>
- Peer per-member and joint group-level entities publish via MQTT discovery under one HA device (GRP-08)
- Membership change retracts only removed members, no orphaned HA entities (GRP-08)
- Group topics isolated in argus/group/... namespace
</success_criteria>

<output>
Create `.planning/phases/06-batch-group-pipeline/06-03-SUMMARY.md` when done
</output>
