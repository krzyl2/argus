---
phase: 07-spa-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - orchestrator/ui/package.json
  - orchestrator/ui/package-lock.json
  - orchestrator/ui/vite.config.ts
  - orchestrator/ui/tsconfig.json
  - orchestrator/ui/vitest.config.ts
  - orchestrator/ui/index.html
  - orchestrator/ui/public/css/argus.css
  - orchestrator/ui/src/main.tsx
  - orchestrator/ui/src/router.ts
  - orchestrator/ui/src/api/client.ts
  - orchestrator/ui/src/api/types.ts
  - orchestrator/ui/src/state/sensors.ts
  - orchestrator/ui/src/validation/detectorParams.ts
  - orchestrator/ui/src/components/AppShell.tsx
  - orchestrator/ui/src/components/SensorsPage.tsx
  - orchestrator/ui/src/components/SensorSearchInput.tsx
  - orchestrator/ui/src/components/SensorList.tsx
  - orchestrator/ui/src/components/SensorListRow.tsx
  - orchestrator/ui/src/components/DetectorDisclosure.tsx
  - orchestrator/ui/src/components/DetectorEntry.tsx
  - orchestrator/ui/src/components/DetectorParamGrid.tsx
  - orchestrator/ui/src/components/AddDetectorButton.tsx
  - orchestrator/ui/src/components/PatternFiltersPanel.tsx
  - orchestrator/ui/src/components/SaveBar.tsx
  - orchestrator/ui/src/components/SaveResultBanner.tsx
  - orchestrator/ui/src/components/EmptyState.tsx
  - orchestrator/ui/src/components/FieldValidationError.tsx
  - orchestrator/ui/src/validation/detectorParams.test.ts
  - orchestrator/ui/src/api/client.test.ts
  - orchestrator/ui/src/components/SensorListRow.test.tsx
  - orchestrator/ui/src/components/SaveResultBanner.test.tsx
autonomous: true
requirements: [UI-02, UI-04]

must_haves:
  truths:
    - "SPA boots at #/sensors: opening with no hash redirects to #/sensors client-side (parity with v3 GET / -> 302 /sensors)"
    - "User can search sensors by entity id (200ms debounce), see rows, toggle tracked checkbox"
    - "User can assign HST/MAD/STL detectors per entity, edit params, add/remove detectors, all client-side"
    - "Field validation runs client-side with exact v3 rules and messages; Save button disabled while any field is in error"
    - "Every /api/* fetch uses a relative path (no leading slash) via the apiGet/apiPost wrapper"
    - "vite build emits index.html + assets/*.js and copies css/argus.css into dist/"
    - "npm test (Vitest) passes: validation rules, relative-fetch enforcement, row rendering, banner variants"
  artifacts:
    - path: "orchestrator/ui/vite.config.ts"
      provides: "Vite config with base:'./', outDir -> ../Argus.Orchestrator/wwwroot, emptyOutDir, preact() plugin"
      contains: "base: './'"
    - path: "orchestrator/ui/src/api/client.ts"
      provides: "Relative-fetch wrapper (apiGet/apiPost) that throws on leading-slash paths"
      contains: "startsWith('/')"
    - path: "orchestrator/ui/src/router.ts"
      provides: "Hand-rolled @preact/signals hash router, root->#/sensors redirect"
      contains: "@preact/signals"
    - path: "orchestrator/ui/src/validation/detectorParams.ts"
      provides: "TypeScript port of v3 _validationScript rules (Vitest-testable)"
    - path: "orchestrator/ui/public/css/argus.css"
      provides: "argus.css carried forward (visual source of truth), served relative from index.html"
    - path: "orchestrator/ui/src/components/SensorsPage.tsx"
      provides: "Single-route entity picker + detector assignment + save"
  key_links:
    - from: "orchestrator/ui/src/state/sensors.ts"
      to: "orchestrator/ui/src/api/client.ts"
      via: "apiGet('api/sensors?q=') / apiPost('api/sensors/save', body)"
      pattern: "api(Get|Post)\\("
    - from: "orchestrator/ui/src/components/SaveResultBanner.tsx"
      to: "orchestrator/ui/src/api/types.ts"
      via: "branches on { ok, kind } discriminant, not string-sniffing"
      pattern: "kind"
  prohibitions:
    - "NO leading-slash fetch: never fetch('/api/...') — always relative fetch('api/...') via apiGet/apiPost"
    - "NO <base href> tag in index.html — base:'./' + hash routing supersedes it (RESEARCH anti-pattern)"
    - "NO preact-router / preact-iso dependency — hand-roll the ~25-line hash router"
    - "NO pushState/history-API routing mixed with hash routing"
    - "NO new colors/spacing/font-size tokens beyond argus.css; NO icon library; NO redesign"
    - "NO new feature UI (group config, algorithm chooser, friendly-name search = Phase 8)"
    - "NO confirm() dialog on Remove detector (v3 is instant, pre-save, undoable)"
    - "NO hardcoded absolute Ingress path anywhere"
---

<objective>
Zbuduj greenfield SPA (Preact + Vite + TypeScript) w `orchestrator/ui/`, odtwarzającą 1:1 zachowanie v3.0 serwer-renderowanego (htmx) UI konfiguracji: wyszukiwanie/selekcja sensorów, per-entity przypisanie detektorów (HST/MAD/STL), walidacja pól, zapis. Router hash-owy (jedna trasa `#/sensors`), relatywny `base:'./'`, wymuszony relatywny fetch.

Purpose: Zastępuje mechanizm dostarczania UI (htmx -> SPA) bez utraty żadnej zdolności v3.0 (UI-04) i z odpornością na dynamiczny prefix Ingress przez konstrukcję (UI-02).
Output: Kompletny projekt Vite w `orchestrator/ui/` (config, komponenty, router, fetch wrapper, walidacja, testy Vitest) budujący się do statycznych assetów w `wwwroot/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-spa-scaffolding/07-UI-SPEC.md
@.planning/phases/07-spa-scaffolding/07-RESEARCH.md
@.planning/phases/07-spa-scaffolding/07-PATTERNS.md

# argus.css jest wizualnym źródłem prawdy — skopiuj verbatim do orchestrator/ui/public/css/
@orchestrator/Argus.Orchestrator/wwwroot/css/argus.css
# Parity spec dla walidacji, defaultów, copy banerów, klas CSS — komponenty odtwarzają te metody 1:1
@orchestrator/Argus.Orchestrator/Web/EntityPickerPage.cs
</context>

<artifacts_produced>
Nowy projekt Vite `orchestrator/ui/`: package.json + package-lock.json, vite.config.ts, tsconfig.json, vitest.config.ts, index.html, public/css/argus.css (kopia), src/ (main.tsx, router.ts, api/client.ts, api/types.ts, state/sensors.ts, validation/detectorParams.ts, 13 komponentów w components/), oraz testy Vitest (detectorParams.test.ts, client.test.ts, SensorListRow.test.tsx, SaveResultBanner.test.tsx).
</artifacts_produced>

<package_legitimacy_note>
Pakiety vite / preact / @preact/signals / vitest zostały oznaczone SUS przez heurystykę "too-new" — to FALSE POSITIVE (data ostatniego patcha, nie wiek projektu). Wszystkie są dojrzałe (3–10 lat), wysokie pobrania (1.8M–141M/tydz.), licencje permisywne (MIT). Zatwierdzone w RESEARCH.md Package Legitimacy Audit — brak checkpointu, brak instalacji nieznanych pakietów. Wersje (zweryfikowane w RESEARCH.md): vite 8.1.3, preact 10.29.3, @preact/signals 2.9.2, @preact/preset-vite 2.10.5, typescript 6.0.3, vitest 4.1.9, @testing-library/preact 3.2.4, jsdom 29.1.1.
</package_legitimacy_note>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite/Preact projekt + config + fetch wrapper + hash router + argus.css</name>
  <files>orchestrator/ui/package.json, orchestrator/ui/package-lock.json, orchestrator/ui/vite.config.ts, orchestrator/ui/tsconfig.json, orchestrator/ui/vitest.config.ts, orchestrator/ui/index.html, orchestrator/ui/public/css/argus.css, orchestrator/ui/src/main.tsx, orchestrator/ui/src/router.ts, orchestrator/ui/src/api/client.ts, orchestrator/ui/src/api/types.ts, orchestrator/ui/src/api/client.test.ts</files>
  <read_first>
    - 07-RESEARCH.md "Code Examples" (Vite config, Vitest config), "Architecture Patterns Pattern 2" (hash router), "Pattern 3" (relative-fetch client)
    - 07-UI-SPEC.md "API Contract" (JSON shapes) + "Screens/Routes" (root->#/sensors)
    - orchestrator/Argus.Orchestrator/wwwroot/css/argus.css (kopiowany verbatim)
  </read_first>
  <action>
    Utwórz projekt Vite w orchestrator/ui/ ze stackiem: preact, @preact/signals (runtime); vite, @preact/preset-vite, typescript, vitest, @testing-library/preact, jsdom (dev). Użyj wersji z package_legitimacy_note. Wygeneruj i zacommituj package-lock.json (npm ci wymaga go w Docker).

    vite.config.ts: plugin preact(); base: './'; build.outDir = resolve(__dirname, '../Argus.Orchestrator/wwwroot'); build.emptyOutDir = true. UWAGA (RESEARCH Pitfall 3): potrójnie zweryfikuj że outDir rozwija się dokładnie do .../Argus.Orchestrator/wwwroot — błędna ścieżka + emptyOutDir skasuje pliki .cs. NIE ustawiaj outDir na katalog projektu.

    vitest.config.ts (lub sekcja test w vite.config.ts): plugin preact(); test.environment='jsdom'; test.globals=true.

    index.html: element montujący #app; skrypt src/main.tsx; link do arkusza stylów jako relatywny href czcionką ./css/argus.css. NIE dodawaj znacznika base href (zakaz z prohibitions).

    public/css/argus.css: skopiuj verbatim z orchestrator/Argus.Orchestrator/wwwroot/css/argus.css (Vite kopiuje public/ do korzenia dist/, zachowując ścieżkę css/argus.css; emptyOutDir kasuje wwwroot więc argus.css musi pochodzić z buildu). To wizualne źródło prawdy — nie modyfikuj tokenów.

    src/router.ts: hand-rolled router oparty na @preact/signals per RESEARCH Pattern 2 — signal route zainicjalizowany z location.hash, listener hashchange aktualizujący signal, oraz efekt ustawiający location.hash na trasę sensorów gdy hash pusty (odpowiednik serwerowego 302 w v3). Dokładnie jedna realna trasa. NIE dodawaj biblioteki routera (prohibitions).

    src/api/client.ts: apiGet<T>(path) i apiPost<T>(path, body) per RESEARCH Pattern 3. Każda funkcja rzuca Error gdy path zaczyna się od ukośnika (wymuszenie relatywnych ścieżek — UI-02). fetch rozwiązuje względem document.baseURI (który już zawiera prefix Ingress). apiPost ustawia Content-Type application/json i serializuje body; zwraca sparsowany JSON; wołający sprawdzają pole ok/kind, nie res.ok.

    src/api/types.ts: typy kontraktu z UI-SPEC "API Contract" — SensorEntry { entityId, friendlyName, currentValue, unitOfMeasurement, isTracked }; SensorsResponse { entries: SensorEntry[] }; SaveRequest { entities: { entityId, detectors: { name, params }[] }[], include, exclude } (naturalny zagnieżdżony kształt per RESEARCH Open Q2 — TEN kształt jest autorytatywny i musi zgadzać się z DTO w planie 07-02); SaveResponse jako unia { ok: true, count, hasHst } | { ok: false, kind: 'validation', errorCount } | { ok: false, kind: 'error', reason }; DetectorDefaults { name, params }.

    src/main.tsx: bootstrap Preact renderujący AppShell/SensorsPage w zależności od route.value; import router; montaż w #app.

    src/api/client.test.ts: test że apiGet i apiPost rzucają na ścieżkę z wiodącym ukośnikiem; test że apiGet buduje relatywny request i parsuje JSON (mock fetch).
  </action>
  <verify>
    <automated>cd orchestrator/ui && npm ci && npm run build && test -f ../Argus.Orchestrator/wwwroot/index.html && test -f ../Argus.Orchestrator/wwwroot/css/argus.css && ls ../Argus.Orchestrator/wwwroot/assets/*.js >/dev/null 2>&1 && echo BUILD_OK</automated>
  </verify>
  <done>npm ci + npm run build kończą się sukcesem; wwwroot zawiera index.html, css/argus.css i assets/*.js; client.test.ts przechodzi (rzucanie na leading slash + relatywny fetch); router redirectuje na #/sensors przy pustym hashu.</done>
</task>

<task type="auto">
  <name>Task 2: Walidacja + 13 komponentów parity + signals store</name>
  <files>orchestrator/ui/src/validation/detectorParams.ts, orchestrator/ui/src/validation/detectorParams.test.ts, orchestrator/ui/src/state/sensors.ts, orchestrator/ui/src/components/AppShell.tsx, orchestrator/ui/src/components/SensorsPage.tsx, orchestrator/ui/src/components/SensorSearchInput.tsx, orchestrator/ui/src/components/SensorList.tsx, orchestrator/ui/src/components/SensorListRow.tsx, orchestrator/ui/src/components/DetectorDisclosure.tsx, orchestrator/ui/src/components/DetectorEntry.tsx, orchestrator/ui/src/components/DetectorParamGrid.tsx, orchestrator/ui/src/components/AddDetectorButton.tsx, orchestrator/ui/src/components/PatternFiltersPanel.tsx, orchestrator/ui/src/components/SaveBar.tsx, orchestrator/ui/src/components/SaveResultBanner.tsx, orchestrator/ui/src/components/EmptyState.tsx, orchestrator/ui/src/components/FieldValidationError.tsx, orchestrator/ui/src/components/SensorListRow.test.tsx, orchestrator/ui/src/components/SaveResultBanner.test.tsx</files>
  <read_first>
    - 07-UI-SPEC.md "Component Inventory" (mapowanie 1:1 do metod EntityPickerPage.cs, dokładne klasy CSS), "Client-side field validation rules", "Detector default values", "States", "Copywriting Contract"
    - orchestrator/Argus.Orchestrator/Web/EntityPickerPage.cs (parity spec — te metody komponenty odtwarzają)
    - orchestrator/Argus.Orchestrator/Config/InputValidator.cs (server-side reguły walidacji — TS musi dawać te same werdykty, w tym cross-field high>low)
  </read_first>
  <action>
    detectorParams.ts: port reguł walidacji z tabeli UI-SPEC "Client-side field validation rules" (źródło logiki: EntityPickerPage.cs _validationScript oraz InputValidator.cs). Reguły dokładnie: window/n_trees/min_consecutive/frozen_window = liczba całkowita >= 1; period = całkowita >= 2; high_threshold = float 0<v<=1 i > low_threshold; low_threshold = float 0<=v<1 i < high_threshold; frozen_variance_threshold = float >= 0; threshold (MAD/STL) = float > 0; puste/nienumeryczne = błąd "must provide a value". Komunikaty błędów verbatim z tabeli UI-SPEC (angielski, nie tłumacz). Funkcja walidująca pojedyncze pole i funkcja agregująca "czy jakiekolwiek pole ma błąd" dla dezaktywacji Save. Zaimplementuj cross-field high>low tak jak InputValidator.cs (obie strony w indywidualnym zakresie, potem porównanie).

    detectorParams.test.ts: przypadki brzegowe z tabeli — window=0 błąd, window=1 ok, period=1 błąd period=2 ok, high=0.7/low=0.3 ok, high=0.3/low=0.7 błąd cross-field, threshold=0 błąd, wartość pusta błąd. Encoduj INTENCJĘ: walidacja musi blokować zapis dokładnie tam gdzie v3 blokował.

    state/sensors.ts: signals store (@preact/signals) — lista sensorów, query, per-entity detektory (edytowalny model in-memory), stan zapisu (idle/saving/wynik), agregat błędów walidacji. Ładowanie przez apiGet('api/sensors?q='); zapis przez apiPost('api/sensors/save', body) budujący SaveRequest z UI-SPEC kształtu. Tabela defaultów detektorów (HST/MAD/STL z UI-SPEC "Detector default values") jako stała client-side — AddDetectorButton konstruuje nowy DetectorEntry lokalnie z tych defaultów (bez round-tripu do serwera).

    13 komponentów per UI-SPEC "Component Inventory" — każdy używa DOKŁADNIE klas CSS z tabeli (argus-header, argus-list-row, argus-detector-entry, argus-param-grid, argus-btn--primary, argus-banner--success/error/validation/reloading, argus-empty, itd.). Kluczowe zachowania parity:
    - SensorSearchInput: debounce 200ms, placeholder "Filter by entity ID…", wywołuje apiGet('api/sensors?q=').
    - SensorListRow: friendly name renderowany TYLKO gdy obecny I różny od entity_id (dokładna reguła v3); pill "tracked", checkbox, wartość+jednostka.
    - DetectorDisclosure: natywne <details>/<summary> (nie reimplementuj przez useState o ile niepotrzebne dla add/remove).
    - DetectorEntry: <select> z opcjami HST/MAD/STL (dokładnie 3, ta kolejność); caption czasowania "streaming (live, ~2 s reload)" dla HST, "batch (runs every N min)" dla MAD/STL; przycisk Remove bez confirm().
    - DetectorParamGrid: warianty HST/MAD/STL, zestaw/kolejność/defaulty pól dokładnie wg UI-SPEC.
    - AddDetectorButton: dopisuje DetectorEntry z defaultami HST client-side.
    - PatternFiltersPanel: dwa textarea include/exclude, po jednym wzorcu na linię, placeholdery "e.g. sensor.*temp*"/"e.g. sensor.*test*", helper "One glob pattern per line. Manual selections override patterns."
    - SaveBar: przycisk "Save configuration" (dokładna etykieta); spinner tylko w trakcie POST; disabled gdy jakiekolwiek pole w stanie błędu.
    - SaveResultBanner: rozgałęzia po polach ok/kind (nie string-sniffing); copy verbatim z UI-SPEC "States"/"Copywriting Contract", w tym warunkowa nota warm-up HST.
    - EmptyState: dwa warianty (no-query first-boot vs search miss), copy verbatim.
    - FieldValidationError: klasy argus-param-field--error + argus-param-field__error-msg, komunikat z detectorParams.ts.

    SensorListRow.test.tsx: friendly name renderowany gdy różny od entity_id, ukryty gdy równy/pusty; klasa argus-list-row--tracked gdy isTracked.
    SaveResultBanner.test.tsx: wariant success ({ok:true}) renderuje argus-banner--success + copy sukcesu; {ok:false, kind:'validation'} renderuje argus-banner--validation; {ok:false, kind:'error'} renderuje argus-banner--error + reason; nota warm-up HST pojawia się tylko gdy hasHst.
  </action>
  <verify>
    <automated>cd orchestrator/ui && npm test -- --run 2>&1 | tail -20</automated>
  </verify>
  <done>Vitest przechodzi: reguły walidacji (w tym cross-field high>low i puste pole), rendering wiersza z regułą friendly-name, warianty banera po ok/kind + nota warm-up HST. Wszystkie 13 komponentów istnieją, używają klas CSS z UI-SPEC, copy verbatim; Save disabled gdy błąd pola; Add/Remove detector działa client-side.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (HA iframe via Ingress) -> Kestrel | Cały ruch SPA przechodzi przez proxy Ingress; SPA nie może zakładać znanego prefiksu ścieżki |
| build-time env -> SPA bundle | Zmienne build-time nie mogą wyciekać sekretów do statycznego bundla |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | SPA bundle | mitigate | Brak sekretów w kodzie SPA; wersja footera z /api lub build-time literal, NIE reflection sekretów; żadnych tokenów w types.ts/client.ts |
| T-07-02 | Spoofing/Tampering | fetch base path | mitigate | apiGet/apiPost rzucają na ścieżki z wiodącym ukośnikiem — wymusza rozwiązywanie względem prefiksu Ingress, uniemożliwia trafienie w origin root (UI-02) |
| T-07-03 | Tampering | npm deps | mitigate | package-lock.json zacommitowany (npm ci deterministyczny); pakiety zweryfikowane w RESEARCH Package Legitimacy Audit (SUS = false positive, cleared) |
</threat_model>

<verification>
- npm ci && npm run build produkuje statyczne assety w wwwroot (index.html + assets/*.js + css/argus.css)
- npm test (Vitest) przechodzi: client, detectorParams, SensorListRow, SaveResultBanner
- Grep: brak fetch z wiodącym ukośnikiem poza wrapperem; brak znacznika base href w index.html; brak preact-router/preact-iso w package.json
- Ręczna inspekcja: wszystkie 13 komponentów obecne, klasy CSS zgodne z UI-SPEC
</verification>

<success_criteria>
SPA buduje się do statycznych assetów odpornych na dynamiczny prefix Ingress (base:'./' + hash routing + relatywny fetch), odtwarza wszystkie zdolności v3.0 (UI-04), testy Vitest zielone.
</success_criteria>

<output>
Create `.planning/phases/07-spa-scaffolding/07-01-SUMMARY.md` when done
</output>
