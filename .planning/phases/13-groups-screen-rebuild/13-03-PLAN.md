---
phase: 13-groups-screen-rebuild
plan: 03
type: execute
wave: 2
depends_on: [13-01, 13-02]
files_modified:
  - orchestrator/ui/src/components/AttributionPanel.tsx
  - orchestrator/ui/src/components/AttributionBar.tsx
  - orchestrator/ui/src/components/AttributionBar.test.tsx
  - orchestrator/ui/src/components/AttributionPanel.test.tsx
autonomous: true
requirements: [GRP-14]

must_haves:
  truths:
    - "AttributionPanel preserves the 60s poll of GET /api/groups/{id}/status and its 4 states (loading / no-score-yet / unsupported when contributions.length===0 / ranked list), never client re-sorting contributions (D-05)"
    - "The panel body is wrapped in a Card with a raw .argus-section-label reading Member attribution · last result, refreshes ~60s (D-05)"
    - "The unsupported state renders via custom .argus-empty markup, NOT a fabricated query passed to the sensor-specific EmptyState (Pitfall 4)"
    - "AttributionBar renders ranked bars with --color-accent fill on the top-ranked row and neutral fill on the rest, with its prop contract (memberId/contribution/topContribution/topRank) unchanged (D-05 / GRP-14)"
    - "The full frontend build (tsc -b && vite build) and the full vitest suite are green with no changes to state/groups.ts, state/groupEditor.ts, validation/groupParams.ts, AlgorithmCard.tsx, or Input.tsx"
  artifacts:
    - path: "orchestrator/ui/src/components/AttributionPanel.tsx"
      provides: "Card + SectionLabel + EmptyState-style wrap around the unchanged 60s poll / 4-state logic (GRP-14 / D-05)"
      contains: "argus-section-label"
    - path: "orchestrator/ui/src/components/AttributionBar.test.tsx"
      provides: "New test — accent fill on topRank, neutral otherwise, width-percent calc"
    - path: "orchestrator/ui/src/components/AttributionPanel.test.tsx"
      provides: "Extended test — 4 states still render through the Card/SectionLabel/empty wrap"
  key_links:
    - from: "orchestrator/ui/src/components/AttributionPanel.tsx"
      to: "orchestrator/ui/src/components/AttributionBar.tsx"
      via: "ranked contributions map to AttributionBar rows (memberId/contribution/topContribution/topRank) — server pre-sort preserved"
      pattern: "AttributionBar"
  prohibitions:
    - "Do not client-re-sort attribution contributions — the server pre-sorts (D-05, flagged-unverified)"
    - "Do not pass a fabricated query string to the sensor-specific EmptyState for the unsupported state (Pitfall 4, flagged-unverified)"
    - "Do not rename AttributionBar's memberId/topRank props to the kit's label/top names (flagged-unverified)"
---

<objective>
Rebuild the attribution panel to the Design System (GRP-14): wrap AttributionPanel's four render
branches in a Card with a raw .argus-section-label, render the unsupported state via custom
.argus-empty markup (not the sensor-specific EmptyState), and verify AttributionBar's already
DS-compliant accent-vs-neutral fill against the spec — all around the completely unchanged 60s poll,
4-state, server-pre-sorted logic (D-05). This is the final wave and carries the phase regression gate.

Purpose: GRP-14 requires the attribution panel and its ranked contribution bars to match the DS spec
in both themes. D-05 preserves the live-polling, server-driven behavior verbatim: 60s poll, the four
states, and the server pre-sort (never client re-sort). AttributionBar already implements the
accent-vs-neutral fill contract (argus-attribution-bar__fill--top uses --color-accent) from its
Phase 8 build — this plan restyles only if the visual spec diverges (CSS-only), and does not rename
its prop contract. Depends on 13-01 and 13-02 so the phase-wide build + full vitest suite prove no
regression across the whole rebuilt Groups screen.

Output: restyled AttributionPanel (Card + SectionLabel + custom empty), verified AttributionBar, a
new AttributionBar test, an extended AttributionPanel test, and a green full build + full vitest
suite proving no save/validation/poll regression.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-groups-screen-rebuild/13-RESEARCH.md
@.planning/phases/13-groups-screen-rebuild/13-PATTERNS.md
@.planning/phases/13-groups-screen-rebuild/13-CONTEXT.md
@.planning/phases/13-groups-screen-rebuild/13-01-PLAN.md
@.planning/phases/13-groups-screen-rebuild/13-02-PLAN.md
</context>

<artifacts_produced>
Public-shape changes this plan introduces (all markup/restyle — no poll, state, or prop-contract change):

- New CSS classes: none required (reuses .argus-attribution-panel, .argus-attribution-bar +
  __label/__track/__fill/__fill--top, .argus-section-label, .argus-empty, .argus-body, .argus-label —
  all already in argus.css, including the --color-accent __fill--top rule from Phase 8).
- New test file: AttributionBar.test.tsx.
- Extended test file: AttributionPanel.test.tsx (adds coverage that the 4 states render through the
  new Card/SectionLabel/custom-empty wrapping; existing assertions kept).
- AttributionPanel.tsx: the four early-return branches gain Card wrapping + a raw .argus-section-label;
  the unsupported branch switches to custom .argus-empty markup. The useEffect poll (POLL_INTERVAL_MS,
  apiGet, setStatus/setLoaded, cleanup) and the contributions.map (no re-sort) are unchanged.
- AttributionBar.tsx: no prop or logic change; CSS-only touch only if the visual spec requires it.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wrap AttributionPanel's 4 states in Card + SectionLabel; custom empty for unsupported</name>
  <files>orchestrator/ui/src/components/AttributionPanel.tsx, orchestrator/ui/src/components/AttributionPanel.test.tsx</files>
  <read_first>
    - orchestrator/ui/src/components/AttributionPanel.tsx (current — full file; poll useEffect lines 24-48 UNCHANGED, the 4 render branches lines 50-80, contributions.map with topRank={idx===0} — no re-sort)
    - orchestrator/ui/src/components/AttributionPanel.test.tsx (existing — extend, do not rewrite)
    - orchestrator/ui/src/components/Card.tsx (padding="sm")
    - orchestrator/ui/src/components/EmptyState.tsx (its prop is {query:string} with sensor-search-specific copy — do NOT force-fit it here, Pitfall 4)
    - orchestrator/ui/src/components/DashboardPage.tsx (raw .argus-section-label usage convention — no SectionLabel component exists)
    - 13-RESEARCH.md Pattern 5 + Pitfall 4 + Assumption A3; 13-PATTERNS.md "AttributionPanel.tsx"; 13-CONTEXT.md D-05
  </read_first>
  <behavior>
    - Before the first successful poll the loading state renders (Card-wrapped) with a "Loading attribution…" label.
    - When status is null the no-score-yet message renders (Card-wrapped).
    - When status.contributions.length === 0 the unsupported state renders via custom .argus-empty markup (Card-wrapped), naming the detector — NOT via the sensor-specific EmptyState.
    - When there are contributions the ranked list renders inside a Card under a .argus-section-label ("Member attribution · last result, refreshes ~60s"), mapping contributions to AttributionBar rows in their existing (server-pre-sorted) order with topRank on the first row only.
    - The 60s poll cadence and error-into-last-known-state behavior are unchanged.
  </behavior>
  <action>
    In AttributionPanel.tsx: leave the entire poll useEffect (POLL_INTERVAL_MS, the async poll,
    setStatus/setLoaded, and the interval cleanup) byte-identical (D-05). Wrap each of the four render
    branches: the `!loaded` loading return and the `!status` no-score return each become
    `<Card padding="sm">`-wrapped (add Card import). For the `status.contributions.length === 0`
    branch, replace the raw `<p class="argus-body argus-attribution-panel__unsupported">` with a
    `<Card padding="sm">` containing custom `.argus-empty` markup (`<div class="argus-empty">` with a
    `<p class="argus-body">` headline and a `<p class="argus-label">` naming `status.detector`) — do
    NOT pass a fabricated `query` into EmptyState (Pitfall 4 / Assumption A3). For the ranked branch,
    wrap the existing `.argus-attribution-panel` map in `<Card padding="sm">` and prepend a raw
    `<p class="argus-section-label">Member attribution · last result, refreshes ~60s</p>` (Phase 11
    raw-class convention, no wrapper component). Keep the `status.contributions.map(...)` exactly as-is
    — the same order (server pre-sort), the same `topRank={idx === 0}`, and the same
    `topContribution={status.contributions[0].contribution}` derivation; do NOT add any client-side
    sort. Extend AttributionPanel.test.tsx (keep existing assertions; use fake timers for the poll)
    with cases that each of the four states renders correctly through the new wrapping, that the
    ranked branch shows the section label and one AttributionBar per contribution in input order, and
    that the unsupported branch does NOT render an EmptyState.
  </action>
  <verify>
    <automated>cd orchestrator/ui && npm run test -- --run src/components/AttributionPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The poll useEffect (POLL_INTERVAL_MS, apiGet, setStatus/setLoaded, cleanup) is unchanged.
    - All four branches are Card-wrapped; the ranked branch renders a `.argus-section-label` with the "refreshes ~60s" copy.
    - The unsupported branch renders custom `.argus-empty` markup and does NOT reference `EmptyState` (grep: no `EmptyState` in this file).
    - `status.contributions.map` order and `topRank={idx === 0}` are unchanged — no client-side `.sort(` added (D-05).
    - AttributionPanel.test.tsx asserts all four states render through the new wrapping and the unsupported branch has no EmptyState.
    - `npm run test -- --run src/components/AttributionPanel.test.tsx` passes.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify/restyle AttributionBar and add its regression test</name>
  <files>orchestrator/ui/src/components/AttributionBar.tsx, orchestrator/ui/src/components/AttributionBar.test.tsx</files>
  <read_first>
    - orchestrator/ui/src/components/AttributionBar.tsx (current — full file; props memberId/contribution/topContribution/topRank; widthPct calc; __fill--top class on topRank)
    - orchestrator/ui/public/css/argus.css (the .argus-attribution-bar + __fill/__fill--top rules — confirm __fill--top uses --color-accent, ~lines 899-909; do not duplicate/introduce a new token)
    - Argus Design System/HANDOFF_TO_CLAUDE_CODE.md (visual spec to confirm accent-vs-neutral fill; AttributionBar in components/*)
    - 13-RESEARCH.md Pattern 5 ("AttributionBar.tsx restyle") + Anti-Patterns (do not rename props); 13-PATTERNS.md "AttributionBar.tsx"
  </read_first>
  <behavior>
    - A bar with topRank=true renders the __fill--top class (accent fill); a bar with topRank=false renders the base __fill class (neutral).
    - The fill width is (contribution / topContribution) * 100, clamped to 100, and 0 when topContribution is 0.
    - The label shows memberId and the value shows contribution.toFixed(3).
  </behavior>
  <action>
    Read AttributionBar.tsx and confirm it already implements the D-05 accent-vs-neutral fill contract
    (the `argus-attribution-bar__fill--top` modifier applied when `topRank` is true, using
    `--color-accent`). Do NOT change its prop contract (keep memberId/contribution/topContribution/
    topRank — do not rename to the kit's label/top names, Anti-Pattern) and do NOT change the widthPct
    logic. Only if the DS visual spec (HANDOFF_TO_CLAUDE_CODE.md) diverges from the current CSS, make
    a CSS-only adjustment in argus.css to the existing `.argus-attribution-bar*` rules (reuse existing
    tokens; introduce no new class). If it already matches, make no production change and simply add
    the regression test. Create AttributionBar.test.tsx asserting: topRank=true renders the
    `argus-attribution-bar__fill--top` class and topRank=false does not; the fill width equals the
    expected percentage for a sample (contribution/topContribution) and is 0 when topContribution is 0;
    the label renders memberId and the value renders contribution.toFixed(3).
  </action>
  <verify>
    <automated>cd orchestrator/ui && npm run test -- --run src/components/AttributionBar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - AttributionBar.tsx keeps its four props (memberId/contribution/topContribution/topRank) — no rename.
    - The `argus-attribution-bar__fill--top` accent modifier appears only when `topRank` is true.
    - Any change is CSS-only against existing `.argus-attribution-bar*` rules (no new class/token); if the current CSS already matches the spec, no production change is made.
    - AttributionBar.test.tsx asserts the topRank fill class toggle, the width-percent calc (incl. the topContribution=0 case), and the label/value text.
    - `npm run test -- --run src/components/AttributionBar.test.tsx` passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Phase regression gate — full build + full vitest suite green</name>
  <files>orchestrator/ui/</files>
  <read_first>
    - 13-RESEARCH.md "Validation Architecture" (Sampling Rate: per-wave-merge = full suite + build; Phase gate)
    - orchestrator/ui/package.json (test/build scripts; Node 26 + vitest 4.1.9 — use `npm run test -- --run` and `npm run build` exactly)
    - orchestrator/ui/src/validation/groupParams.test.ts (existing regression guard — must still pass, unchanged, D-07)
    - orchestrator/ui/src/state/groups.test.ts + orchestrator/ui/src/state/groupEditor.test.ts (existing regression guards — must still pass, unchanged, D-07)
  </read_first>
  <action>
    With Plans 13-01, 13-02, and this plan's Tasks 1-2 merged, run the full frontend build and the full
    vitest suite to prove no regression in group CRUD, the chooser state machine, member/group
    validation, or the attribution poll across every rebuilt component and its call sites. Do NOT
    modify state/groups.ts, state/groupEditor.ts, validation/groupParams.ts, AlgorithmCard.tsx, or
    Input.tsx — their existing suites are the D-07 regression guard. If the build or any test fails,
    fix the offending file within this phase's markup scope (never by weakening groupParams.ts, the
    state layers, or the client-validation gating that drives SaveBar's disabled state — see Security
    Domain V5) and re-run until green; record any fix in the SUMMARY.
  </action>
  <verify>
    <automated>cd orchestrator/ui && npm run build && npm run test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` (tsc -b && vite build) is green.
    - `npm run test -- --run` (full suite) is green, including the unchanged groupParams.test.ts, state/groups.test.ts, and state/groupEditor.test.ts.
    - No modifications exist in state/groups.ts, state/groupEditor.ts, validation/groupParams.ts, AlgorithmCard.tsx, or Input.tsx across the phase (D-07 / Pitfall 3).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ← GET /api/groups/{id}/status (60s poll) | server-computed, server-pre-sorted contribution data (memberId/contribution/detector) is rendered into the panel + bars |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-05 | Info Disclosure | AttributionPanel/AttributionBar rendering memberId/detector strings | low | mitigate | Preact JSX auto-escapes text children; no raw innerHTML sink introduced by the Card/SectionLabel/empty-state wrap. |
| T-13-06 | Tampering | client ordering of attribution rows | low | accept | The server pre-sorts contributions and the panel never re-sorts (D-05); rendering order faithfully reflects the server verdict, preserving the integrity of the "top-ranked" accent signal. |

No package installs in this phase → no supply-chain (T-13-SC) checkpoint required.
</threat_model>

<verification>
- `npm run build` green (cross-file type check across the whole rebuilt Groups screen).
- `npm run test -- --run` full suite green, including the unchanged groupParams.test.ts + state/groups.test.ts + state/groupEditor.test.ts.
- Manual/UAT (deferred to /gsd-verify-work): a group with a joint verdict shows ranked bars with the top row accent-filled in both themes; a peer/no-attribution group shows the unsupported empty state; the panel refreshes on its ~60s cadence (GRP-14 / D-05).
</verification>

<success_criteria>
- GRP-14: the attribution panel renders ranked per-member/per-feature bars matching the DS spec in both themes — Card + SectionLabel wrap, accent-vs-neutral AttributionBar fill, custom empty state.
- D-05: 60s poll, 4 states, and server pre-sort preserved (no client re-sort).
- Phase gate: full build + full vitest suite green with the D-07 state/validation/primitive files unmodified.
</success_criteria>

<output>
Create `.planning/phases/13-groups-screen-rebuild/13-03-SUMMARY.md` when done.
</output>
