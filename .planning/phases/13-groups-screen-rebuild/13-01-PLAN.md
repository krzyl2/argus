---
phase: 13-groups-screen-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - orchestrator/ui/src/components/GroupsPage.tsx
  - orchestrator/ui/src/components/GroupList.tsx
  - orchestrator/ui/src/components/GroupListRow.tsx
  - orchestrator/ui/src/components/GroupEditorForm.tsx
  - orchestrator/ui/src/components/MemberPicker.tsx
  - orchestrator/ui/src/components/GroupList.test.tsx
  - orchestrator/ui/src/components/GroupListRow.test.tsx
  - orchestrator/ui/src/components/GroupEditorForm.test.tsx
  - orchestrator/ui/src/components/MemberPicker.test.tsx
autonomous: true
requirements: [GRP-12]

must_haves:
  truths:
    - "The Groups list view renders a DS .argus-page-header, a Create group link (hash-router preserved), and a Card-wrapped list of group rows (SC1)"
    - "GroupList renders a custom .argus-empty branch (NOT the sensor-specific EmptyState) when there are zero groups"
    - "Each GroupListRow renders a mode Badge AND a detector Badge, and preserves the two-step Delete group / Confirm delete 3s-armed confirm plus the status / no status yet right-aligned meta (D-02)"
    - "GroupEditorForm renders the group name via the shared Input (external label + FieldValidationError) and the mode selector via the shared Select, under a DS .argus-page-header with a Back to groups affordance that changes only location.hash (D-01/D-06)"
    - "MemberPicker gates row rendering behind MIN_QUERY_LENGTH=2, renders rows via the shared Checkbox + Badge wrapped in Card, and uses SensorSearchInput (D-06/D-07)"
    - "validateGroupMembers/validateUnitConsistency and the state/groups.ts draft+save+delete layer remain unchanged — regression guard (D-07)"
  artifacts:
    - path: "orchestrator/ui/src/components/GroupList.tsx"
      provides: "Card-wrapped group list with custom empty-state branch (GRP-12 / SC1)"
      contains: "Card"
    - path: "orchestrator/ui/src/components/GroupListRow.tsx"
      provides: "DS row: mode Badge + detector Badge, preserved two-step delete + status meta (D-02)"
      contains: "Badge"
    - path: "orchestrator/ui/src/components/GroupEditorForm.tsx"
      provides: "Editor shell: name Input + mode Select + DS page-header + Back affordance (D-01/D-06)"
      contains: "Select"
    - path: "orchestrator/ui/src/components/MemberPicker.tsx"
      provides: "Card + Checkbox + Badge member rows behind MIN_QUERY_LENGTH gate (D-06/D-07)"
      contains: "MIN_QUERY_LENGTH"
    - path: "orchestrator/ui/src/components/MemberPicker.test.tsx"
      provides: "New test — MIN_QUERY_LENGTH gate (both branches) + Checkbox/Badge row wiring"
  key_links:
    - from: "orchestrator/ui/src/components/GroupList.tsx"
      to: "orchestrator/ui/src/components/GroupListRow.tsx"
      via: "Card-wrapped <ul> mapping GroupConfig to GroupListRow (row-count preserved)"
      pattern: "GroupListRow"
    - from: "orchestrator/ui/src/components/GroupEditorForm.tsx"
      to: "orchestrator/ui/src/components/Select.tsx"
      via: "raw <select id=group-mode> replaced by shared Select with descriptive option labels"
      pattern: "Select"
    - from: "orchestrator/ui/src/components/MemberPicker.tsx"
      to: "orchestrator/ui/src/components/Checkbox.tsx"
      via: "raw <input type=checkbox> replaced by shared Checkbox; member pill -> Badge tone member"
      pattern: "Checkbox"
  prohibitions:
    - "Do not drop URL addressability or remove the GroupsPage route-branch between GroupList and GroupEditorForm (D-01, flagged-unverified)"
    - "Do not reword any English message in validation/groupParams.ts (D-07, flagged-unverified)"
    - "Do not revert the MemberPicker MIN_QUERY_LENGTH gate (D-07, flagged-unverified)"
---

<objective>
Rebuild the group editor's list, editor shell, and member picker to the Argus Design System (GRP-12):
Card-wrapped list rows with mode + detector Badges, the shared Input/Select for the name + mode
fields, a DS page-header with a "Back to groups" affordance, and a Card + Checkbox + Badge member
picker behind the locked MIN_QUERY_LENGTH gate. All business logic (state/groups.ts,
validation/groupParams.ts) stays byte-identical — this is a markup + shared-primitive-adoption
refactor only (mirrors Phase 12's Sensors rebuild exactly).

Purpose: SC1 requires the list, member picker, and save/delete UI to match the DS spec in both
themes. D-02 adopts the kit's Card row + two-Badge visual while explicitly preserving the two-step
delete and status meta the kit's mock omits. D-01 keeps the hash-router deep-linkability (visual
PageHeader + Back only, never the kit's in-place editing toggle). D-06 swaps the raw name input, raw
mode select, and raw member-picker checkbox/pill for the Phase 10 shared primitives. The uncommitted
MemberPicker MIN_QUERY_LENGTH diff (D-07-locked target behavior) is folded into this phase's first
commit, not reverted.

Output: rebuilt GroupsPage (list branch), GroupList, GroupListRow, GroupEditorForm (name/mode/header),
and MemberPicker, plus four new test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-groups-screen-rebuild/13-RESEARCH.md
@.planning/phases/13-groups-screen-rebuild/13-PATTERNS.md
@.planning/phases/13-groups-screen-rebuild/13-CONTEXT.md
</context>

<multi_source_coverage_audit>
Phase-level audit (this is the lead plan; audit covers all three plans 13-01/02/03).

| Source | Item | Covered by |
|--------|------|-----------|
| GOAL | Rebuild Groups screen to DS spec, markup refactor allowed | 13-01 (list/editor/member) + 13-02 (wizard) + 13-03 (attribution) |
| GOAL | Group CRUD + guided wizard + attribution preserved end-to-end | 13-01 (CRUD) + 13-02 (wizard) + 13-03 (attribution) |
| REQ | GRP-12 (group editor rebuilt to DS spec) | 13-01 |
| REQ | GRP-13 (algorithm creation wizard rebuilt to DS spec) | 13-02 |
| REQ | GRP-14 (attribution panel / AttributionBar rebuilt to DS spec) | 13-03 |
| RESEARCH | Pattern 1 — list row Card + two Badges + preserved delete/status | 13-01 |
| RESEARCH | Pattern 2 — name/mode two-column editor header (Input/Select) | 13-01 |
| RESEARCH | Pattern 3 — member picker full adoption (Card/Checkbox/Badge/SearchInput) | 13-01 |
| RESEARCH | Pattern 4 — wizard restyle around unchanged groupEditor.ts | 13-02 |
| RESEARCH | Pattern 5 — attribution Card/SectionLabel/EmptyState wrap | 13-03 |
| RESEARCH | MemberPicker reconciliation (fold uncommitted MIN_QUERY_LENGTH diff) | 13-01 |
| RESEARCH | Pitfall 1 — do NOT add stopPropagation to member rows (no competing onClick) | 13-01 |
| RESEARCH | Pitfall 2 — aria-describedby / FieldValidationError id gap (follow existing convention) | 13-01 + 13-02 |
| RESEARCH | Pitfall 3 — AlgorithmCard/Input already Phase-12-final, do NOT re-widen | 13-02 |
| RESEARCH | Pitfall 4 / Anti-Pattern — do NOT force-fit sensor-specific EmptyState | 13-01 (list empty) + 13-03 (attribution unsupported) |
| RESEARCH | Pitfall 5 — do NOT mode-filter the catalog / gate guided to joint | 13-02 |
| RESEARCH | Wave 0 test gaps (9 new + 2 extend) | distributed 13-01/02/03 |
| CONTEXT | D-01 keep hash-router, visual PageHeader + Back only | 13-01 |
| CONTEXT | D-02 Card row + two Badges, preserve delete + status | 13-01 |
| CONTEXT | D-03 full unfiltered catalog + always-available guided step | 13-02 |
| CONTEXT | D-04 wizard DS spec + radio-card 2px accent selection | 13-02 |
| CONTEXT | D-05 attribution 60s poll / 4 states / server pre-sort | 13-03 |
| CONTEXT | D-06 full Phase 10 primitive adoption (Card/Select/Checkbox/Badge/Input/Banner) | 13-01 (list/editor/member) + 13-02 (advanced params Input) |
| CONTEXT | D-07 preserve groupParams.ts / state layers / SensitivityPresetPicker / MIN_QUERY_LENGTH / poll / SaveBar | 13-01 (member/state) + 13-02 (state machine/preset) + 13-03 (poll) |

Exclusions (not gaps): Deferred Ideas in 13-CONTEXT.md (algorithm tester/simulator, live per-member
value column, mode-filtered catalog, in-place URL-less editing) are out of scope. No unplanned items
remain.
</multi_source_coverage_audit>

<artifacts_produced>
Public-shape changes this plan introduces (all additive/markup — no state or validation shape change):

- New CSS classes: none required (reuses .argus-page-header, .argus-list, .argus-list-row,
  .argus-row-content/meta, .argus-param-field, .argus-empty, .argus-member-picker — all already in
  argus.css). An optional .argus-member-picker scroll-region rule is Claude's discretion (Open
  Question 1); if omitted, no argus.css change is made by this plan.
- New test files: GroupList.test.tsx, GroupListRow.test.tsx, GroupEditorForm.test.tsx,
  MemberPicker.test.tsx.
- GroupList.tsx: list <ul> wrapped in shared Card; empty-state branch kept as custom .argus-empty
  markup. No prop-interface change.
- GroupListRow.tsx: adds a second Badge (detector) alongside the existing mode Badge; delete/status
  logic and props unchanged.
- GroupEditorForm.tsx: name field -> shared Input (external label + FieldValidationError); mode field
  -> shared Select; heading -> .argus-page-header + Back affordance. No prop-interface change; the
  MemberPicker / AlgorithmChooser / AttributionPanel / SaveBar / GroupSaveResultBanner call sites are
  preserved verbatim (those components are rebuilt in 13-02/13-03 without prop-shape change).
- MemberPicker.tsx: raw checkbox -> shared Checkbox; member pill -> shared Badge; results <ul>
  wrapped in Card. The MIN_QUERY_LENGTH gate + useMemberPickerValidation export are preserved
  verbatim.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebuild the Groups list — Card-wrapped GroupList + two-Badge GroupListRow + DS page-header on GroupsPage</name>
  <files>orchestrator/ui/src/components/GroupsPage.tsx, orchestrator/ui/src/components/GroupList.tsx, orchestrator/ui/src/components/GroupListRow.tsx, orchestrator/ui/src/components/GroupList.test.tsx, orchestrator/ui/src/components/GroupListRow.test.tsx</files>
  <read_first>
    - orchestrator/ui/src/components/GroupsPage.tsx (current — list branch heading block at lines 27-44; the route-branch at lines 20-24 must stay, D-01)
    - orchestrator/ui/src/components/GroupList.tsx (current — full file; empty-state branch + <ul class="argus-list">)
    - orchestrator/ui/src/components/GroupListRow.tsx (current — full file; two-step delete state machine lines 23-42, mode Badge line 48, status meta lines 50-64 — all preserved)
    - orchestrator/ui/src/components/SensorList.tsx (Phase 12 Card-wrap analog; excerpt in 13-PATTERNS.md "Card-wrapped lists")
    - orchestrator/ui/src/components/SensorsPage.tsx (Phase 12 .argus-page-header analog; excerpt in 13-PATTERNS.md "Page header")
    - orchestrator/ui/src/components/Card.tsx (padding="none" API)
    - orchestrator/ui/src/components/Badge.tsx (tone union: tracked|member|neutral|ok|warn|error|accent)
    - Argus Design System/ui_kits/admin/Groups.jsx (layout/visual reference ONLY — its no-delete/no-status row and in-place editing are NOT authoritative, D-01/D-02)
    - 13-RESEARCH.md Pattern 1 + Assumption A1; 13-PATTERNS.md "GroupList.tsx" / "GroupListRow.tsx"
  </read_first>
  <behavior>
    - The list view renders a .argus-page-header (title "Groups") and a Create group link that navigates to #/groups/new.
    - GroupList wraps its rows in a Card; with zero groups it renders the existing custom .argus-empty message instead of a row list.
    - Each row renders a mode Badge and a detector Badge (two badges).
    - The two-step delete still arms on first click ("Confirm delete" text), fires deleteGroup on a second click within the 3s window, and reverts to "Delete group" after the timeout.
    - A row with a status renders the status Badge; a row without renders "no status yet".
  </behavior>
  <action>
    In GroupsPage.tsx (list branch only): replace the raw `<div><p class="argus-heading">Groups</p>...</div>` block with the DS `<header class="argus-page-header">` pattern (title span + subtitle reusing the existing subtitle sentence verbatim — do not invent copy), mirroring SensorsPage. Keep the `AreaSuggestionBanner`, the Create group anchor (`<a href="#/groups/new">`, keep it a hash-router anchor for deep-linkability per D-01 — it already carries argus-btn--primary), and the `<GroupList groups={groups.value} />` render. Do NOT change the isEditor route-branch or the `<GroupEditorForm .../>` return (D-01). In GroupList.tsx: import the shared Card and wrap the `<ul class="argus-list">` in `<Card padding="none">`; keep the zero-length branch as the existing custom .argus-empty markup (do NOT swap it for the sensor-specific EmptyState — its prop shape is query-based, Anti-Pattern). In GroupListRow.tsx: add a second `<Badge tone="accent">{group.detector}</Badge>` immediately after the existing mode `<Badge tone="neutral">{modeLabel}</Badge>` in `.argus-row-content` (matches the DS two-badge row, D-02 / Assumption A1). Keep the entire two-step delete state machine (armed/timerRef/CONFIRM_WINDOW_MS/handleDeleteClick), the status branch, the Edit anchor, and the destructive-ghost delete Button verbatim. Whole-row click-to-navigate is NOT required (D-02 asks for Card + two Badges only); keep the existing Edit anchor as the navigation target (Claude's discretion per Assumption A4 — if a row-level onClick is added, wrap the Delete Button + Edit anchor in a stopPropagation handler per SensorListRow, otherwise leave as-is). Create GroupList.test.tsx (Card-wrap present, row count equals group count, empty-state branch renders for []) and GroupListRow.test.tsx (mode + detector Badge both render; using fake timers, first delete click arms and shows the confirm label, a second click within the window calls deleteGroup, and no second click reverts after the timeout; status Badge vs "no status yet" fallback).
  </action>
  <verify>
    <automated>cd orchestrator/ui && npm run test -- --run src/components/GroupList.test.tsx src/components/GroupListRow.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - GroupsPage.tsx renders `<header class="argus-page-header">` on the list branch (grep matches `argus-page-header`), and the `isEditor` route-branch returning `<GroupEditorForm` is still present (D-01).
    - GroupList.tsx imports and uses `Card` to wrap the `<ul class="argus-list">` (grep matches `Card`); the zero-length branch still renders `argus-empty` markup and does NOT reference `EmptyState`.
    - GroupListRow.tsx renders two `<Badge>` elements (mode + detector) and still declares `CONFIRM_WINDOW_MS` and `handleDeleteClick` unchanged.
    - GroupListRow.test.tsx asserts the arm/confirm/timeout delete flow (fake timers) and the status / no-status meta.
    - `npm run test -- --run src/components/GroupList.test.tsx src/components/GroupListRow.test.tsx` passes.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rebuild GroupEditorForm shell — name Input, mode Select, DS page-header + Back affordance</name>
  <files>orchestrator/ui/src/components/GroupEditorForm.tsx, orchestrator/ui/src/components/GroupEditorForm.test.tsx</files>
  <read_first>
    - orchestrator/ui/src/components/GroupEditorForm.tsx (current — full file; name field lines 74-92, mode field lines 94-109, headings lines 72/111/121; slugify + all state/groups.ts imports preserved verbatim)
    - orchestrator/ui/src/components/Input.tsx (already Phase-12-final: {value,onChange,type?,placeholder?,ariaLabel?,disabled?,invalid?,id?,step?,ariaDescribedby?} — renders NO internal label/error; do NOT modify, Pitfall 3)
    - orchestrator/ui/src/components/Select.tsx ({value,onChange,options:SelectOption[],ariaLabel?,disabled?})
    - orchestrator/ui/src/components/FieldValidationError.tsx (renders the inline error span; unchanged)
    - orchestrator/ui/src/components/SettingsPage.tsx (external-label + Input convention to mirror; also Phase 12 DetectorParamGrid)
    - orchestrator/ui/src/components/SensorsPage.tsx (.argus-page-header pattern)
    - orchestrator/ui/src/state/groups.ts (draftFriendlyName/draftGroupId/draftMode/saveGroup/saveState — DO NOT MODIFY, D-07; read to keep wiring correct)
    - 13-RESEARCH.md Pattern 2 + Pitfall 2; 13-PATTERNS.md "GroupEditorForm.tsx"; 13-CONTEXT.md D-01/D-06
  </read_first>
  <behavior>
    - The editor renders a .argus-page-header ("Edit group" when groupId, "Create group" otherwise) with a "Back to groups" affordance that sets location.hash to #/groups (router unchanged).
    - The name field renders as an external `<label>` + shared `<Input>` + `<FieldValidationError>`; typing updates draftFriendlyName and, for a new group, slugifies into draftGroupId (existing behavior preserved).
    - When the name is empty the Input is marked invalid and the required-name error renders.
    - The mode field renders the shared `<Select>` with two options; changing it updates draftMode.
    - The MemberPicker, AlgorithmChooser (in its slot), AttributionPanel (existing groups only), SaveBar, and GroupSaveResultBanner are still composed with identical props.
  </behavior>
  <action>
    In GroupEditorForm.tsx: replace the `<p class="argus-heading">{groupId ? 'Edit group' : 'Create group'}</p>` with a `<header class="argus-page-header">` (title reflecting create/edit) plus a Back affordance — a `<Button variant="ghost" size="sm" onClick={() => { location.hash = '#/groups'; }}>` labelled "Back to groups" (visual only; D-01 forbids changing the router mechanism, this only sets the hash). Replace the raw `<input id="group-name" class="argus-param-field__input" ...>` with the shared `<Input>` per 13-PATTERNS.md target: pass `id="group-name"`, `value={draftFriendlyName.value}`, an `onChange={(next) => { draftFriendlyName.value = next; if (!groupId) draftGroupId.value = slugify(next); }}` (Input's onChange yields the string value — preserve the exact draftFriendlyName + slugify-on-create behavior), `invalid={!!nameError}`, and `ariaDescribedby={nameError ? 'group-name-err' : undefined}`, keeping the surrounding `.argus-param-field` div + external `<label for="group-name">` + `<FieldValidationError message={nameError ?? undefined} />` (follow the existing FieldValidationError convention as-is per Pitfall 2 — fixing its missing id is optional out-of-scope polish, do NOT block on it). Replace the raw `<select id="group-mode">` with the shared `<Select>` passing `value={draftMode.value}`, `ariaLabel="Mode"`, `options` of the two modes with descriptive labels ("Peer-divergence — which sensor is diverging" / "Joint (multivariate) — unusual combination"), and `onChange={(v) => { draftMode.value = v as typeof draftMode.value; }}`. Convert the raw `<p class="argus-heading">Members</p>` / `<p class="argus-heading">Choose algorithm</p>` section headings to the raw `.argus-section-label` class convention (`<p class="argus-section-label">`) established in Phase 11 (no new wrapper component). Preserve verbatim: all state/groups.ts imports, the useEffect load/reset, useMemberPickerValidation, nameError/noAlgorithmError derivation, hasErrors, toggleMember, and the `<MemberPicker>`, `<AlgorithmChooser>`, `<AttributionPanel>`, `<SaveBar>`, and `<GroupSaveResultBanner>` render blocks (their internals are rebuilt in 13-02/13-03 without prop-shape change). Do NOT modify state/groups.ts. Create GroupEditorForm.test.tsx asserting the behaviors above (page-header title switches on groupId; name renders via Input with the required-name error when empty; mode renders via Select; the child slots are present).
  </action>
  <verify>
    <automated>cd orchestrator/ui && npm run test -- --run src/components/GroupEditorForm.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The raw `<input id="group-name">` and raw `<select id="group-mode">` are gone; the name field uses `<Input>` and the mode field uses `<Select>` (grep: `Input` and `Select` imported and used; no `class="argus-param-field__input"` on a bare input/select in this file).
    - The editor renders `<header class="argus-page-header">` and a "Back to groups" control that sets `location.hash` to `#/groups`.
    - draftFriendlyName + slugify-on-create wiring is preserved (new-group name change still assigns draftGroupId).
    - state/groups.ts is unmodified (D-07).
    - GroupEditorForm.test.tsx asserts the create/edit header, the name-required error via Input+FieldValidationError, and the mode Select.
    - `npm run test -- --run src/components/GroupEditorForm.test.tsx` passes.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rebuild MemberPicker — Card + Checkbox + Badge rows, fold the MIN_QUERY_LENGTH gate</name>
  <files>orchestrator/ui/src/components/MemberPicker.tsx, orchestrator/ui/src/components/MemberPicker.test.tsx</files>
  <read_first>
    - orchestrator/ui/src/components/MemberPicker.tsx (current WORKING-TREE state — already contains the uncommitted MIN_QUERY_LENGTH gate at line 23 + the queryTooShort branch; this on-disk state IS the correct starting point, not HEAD)
    - orchestrator/ui/src/components/Checkbox.tsx ({checked,onChange:(checked:boolean)=>void,ariaLabel?,disabled?} — no onClick prop)
    - orchestrator/ui/src/components/Badge.tsx (tone="member" already defined)
    - orchestrator/ui/src/components/Card.tsx (padding="none")
    - orchestrator/ui/src/components/SensorSearchInput.tsx (already wraps the shared SearchInput; reuse, do not re-derive debounce)
    - orchestrator/ui/src/components/SensorListRow.tsx (Phase 12 Checkbox/Badge row analog — but note it uses stopPropagation for a competing row onClick that does NOT exist here, Pitfall 1)
    - orchestrator/ui/src/validation/groupParams.ts (validateGroupMembers/validateUnitConsistency — DO NOT MODIFY, D-07)
    - 13-RESEARCH.md Pattern 3 + Pitfall 1 + MemberPicker reconciliation (Summary); 13-PATTERNS.md "MemberPicker.tsx"
  </read_first>
  <behavior>
    - When the trimmed query is shorter than MIN_QUERY_LENGTH the picker renders the "type at least N characters" gate and NO sensor rows.
    - When the query is long enough but matches nothing, the existing sensor EmptyState renders (this component's original behavior — unchanged).
    - When there are matches, each row renders a shared Checkbox (checked reflects membership), the entity id (and friendly name when distinct), the unit-of-measurement meta, and a Badge tone="member" when selected.
    - Toggling a row's checkbox calls onToggleMember(entityId, checked) with the new checked state.
    - The two FieldValidationError lines (member-floor + unit-mismatch) still render below the list, driven by the unchanged groupParams.ts validators.
  </behavior>
  <action>
    Fold the uncommitted MIN_QUERY_LENGTH diff into this phase's history as the starting point — the on-disk MemberPicker.tsx (with `const MIN_QUERY_LENGTH = 2`, the `queryTooShort` computation, and the "type at least N characters" branch) is the D-07-locked target; do NOT revert it while refactoring markup. Rewrite only the results-row markup: replace the raw `<input class="argus-checkbox" type="checkbox" ...>` with the shared `<Checkbox checked={checked} ariaLabel={entry.entityId} onChange={(next) => onToggleMember(entry.entityId, next)} />`, and replace the raw member pill span with `<Badge tone="member">member</Badge>` (add Card/Checkbox/Badge imports). Wrap the results `<ul class="argus-list">` in `<Card padding="none">`. KEEP the `<label style={{ display: 'contents' }}>` row wrapper — do NOT add a stopPropagation wrapper (Pitfall 1: there is no competing row-level onClick here, unlike SensorListRow; the native label toggle-anywhere-on-row behavior is desired). Preserve verbatim: the SensorSearchInput usage, the queryTooShort/filtered/selectedSet/selectedEntries derivation, the memberFloorError/unitMismatchError calls, both `<FieldValidationError>` lines, and the exported useMemberPickerValidation hook. Do NOT modify validation/groupParams.ts. Optionally add a `.argus-member-picker` max-height/overflow scroll-region rule to argus.css (Open Question 1 — Claude's discretion; skip if leaving height unconstrained, which is current behavior). Create MemberPicker.test.tsx asserting: the short-query gate renders no rows and the "type at least" message; a long-enough query renders one row per match with the Checkbox and, for selected entries, the member Badge; toggling a checkbox calls onToggleMember with the correct (entityId, checked) args.
  </action>
  <verify>
    <automated>cd orchestrator/ui && npm run test -- --run src/components/MemberPicker.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - MemberPicker.tsx still declares `MIN_QUERY_LENGTH` and the `queryTooShort` branch (gate preserved, not reverted).
    - Rows render via the shared `Checkbox` and a `Badge` for the member marker (grep: `Checkbox` and `Badge` imported and used; no bare `<input ... type="checkbox"` and no `argus-pill` in this file).
    - The results `<ul class="argus-list">` is wrapped in `<Card padding="none">`.
    - The `<label style={{ display: 'contents' }}>` wrapper is retained and there is no stopPropagation wrapper added (Pitfall 1).
    - validation/groupParams.ts is unmodified (D-07).
    - MemberPicker.test.tsx asserts the gate (both branches) and toggle wiring.
    - `npm run test -- --run src/components/MemberPicker.test.tsx` passes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser DOM ← group/sensor data | group friendlyName/groupId/detector and member entityId/friendlyName strings are rendered into row + picker markup |
| browser → POST /api/groups/save | client validation (groupParams.ts) gates SaveBar; the authoritative boundary is server-side GroupInputValidator.cs (untouched, out of scope) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Info Disclosure / Tampering | GroupListRow / MemberPicker rendering group + sensor strings | low | mitigate | Preact JSX auto-escapes text children; this refactor introduces no HTML-injection sink and no raw innerHTML API anywhere in the rebuilt rows. |
| T-13-02 | Tampering | client-side groupParams.ts validation gating SaveBar disabled state | low | accept | Client validation is a UX convenience only; server re-validates every /api/groups/save POST independently (GroupInputValidator.cs). This plan preserves the client gate verbatim (ASVS V5) — it is not weakened or bypassed. |

No package installs in this phase → no supply-chain (T-13-SC) checkpoint required (13-RESEARCH.md: no new packages; Package Legitimacy Audit not applicable).
</threat_model>

<verification>
- `npm run build` green (type-checks the new Input/Select/Card/Checkbox/Badge call sites across GroupsPage/GroupList/GroupListRow/GroupEditorForm/MemberPicker).
- `npm run test -- --run src/components/GroupList.test.tsx src/components/GroupListRow.test.tsx src/components/GroupEditorForm.test.tsx src/components/MemberPicker.test.tsx` green.
- Manual/UAT (deferred to /gsd-verify-work): list rows show mode + detector badges and the two-step delete in both themes; the member picker gate + Card/Checkbox/Badge render correctly (SC1).
</verification>

<success_criteria>
- SC1: the group editor (list, member picker, save/delete) matches the DS spec in both themes — Card-wrapped list, two Badges per row, shared Input/Select/Checkbox/Badge, DS page-header + Back affordance.
- D-01: hash-router deep-linkability preserved (route-branch + hash navigation intact).
- D-02: two-step delete + status meta preserved. D-07: groupParams.ts + state/groups.ts + MIN_QUERY_LENGTH gate unchanged.
</success_criteria>

<output>
Create `.planning/phases/13-groups-screen-rebuild/13-01-SUMMARY.md` when done.
</output>
