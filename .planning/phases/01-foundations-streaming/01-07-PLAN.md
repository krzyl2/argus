---
phase: 01-foundations-streaming
plan: 07
type: execute
wave: 5
depends_on: [04, 05]
files_modified:
  - orchestrator/Argus.Orchestrator/Mqtt/MqttConnection.cs
  - orchestrator/Argus.Orchestrator/Mqtt/DiscoveryPublisher.cs
  - orchestrator/Argus.Orchestrator/Mqtt/StatePublisher.cs
  - orchestrator/Argus.Orchestrator/Mqtt/UniqueId.cs
  - orchestrator/Argus.Orchestrator/Mqtt/FriendlyName.cs
  - orchestrator/Argus.Orchestrator/Workers/MqttPublisherWorker.cs
  - orchestrator/Argus.Orchestrator/Program.cs
  - orchestrator/Argus.Orchestrator.Tests/UniqueIdTests.cs
  - orchestrator/Argus.Orchestrator.Tests/DiscoveryPayloadTests.cs
autonomous: true
requirements: [MQTT-01, MQTT-02, MQTT-03, MQTT-04, MQTT-05, RES-01]
requirements_addressed: [MQTT-01, MQTT-02, MQTT-03, MQTT-04, MQTT-05, RES-01]

must_haves:
  truths:
    - "Each entity publishes one binary_sensor + one sensor discovery config under one HA device"
    - "unique_id is argus_{entity_slug}_{detector}_{suffix}, deterministic, stable across restarts"
    - "All discovery payloads published with retain: true"
    - "LWT 'offline' configured in connect options BEFORE any state publish; 'online' published after connect"
    - "Re-publishing discovery does not duplicate or orphan entities (idempotent)"
    - "Polish friendly-names follow '[Room] [SensorType] anomalia'"
  artifacts:
    - path: "orchestrator/Argus.Orchestrator/Mqtt/UniqueId.cs"
      provides: "Deterministic unique_id + object_id + entity_slug formula"
      contains: "argus_"
    - path: "orchestrator/Argus.Orchestrator/Mqtt/DiscoveryPublisher.cs"
      provides: "Retained binary_sensor + sensor discovery payloads grouped per device"
      contains: "homeassistant/"
    - path: "orchestrator/Argus.Orchestrator/Mqtt/MqttConnection.cs"
      provides: "MQTTnet 5 client with LWT-before-connect and reconnect"
      contains: "MqttClientFactory"
  key_links:
    - from: "orchestrator/Argus.Orchestrator/Mqtt/MqttConnection.cs"
      to: "availability topic LWT"
      via: "WithWillTopic/WithWillPayload before ConnectAsync"
      pattern: "WithWill"
    - from: "orchestrator/Argus.Orchestrator/Mqtt/DiscoveryPublisher.cs"
      to: "orchestrator/Argus.Orchestrator/Mqtt/UniqueId.cs"
      via: "unique_id + object_id derivation"
      pattern: "UniqueId"
---

<objective>
Implement MQTT discovery and state publishing with MQTTnet 5: deterministic unique_ids, retained binary_sensor + sensor configs grouped under one HA device per source entity, LWT 'offline' configured before connect (graceful degradation to unavailable), Polish friendly-names, and idempotent re-publish.

Purpose: MQTT-01..05 create the HA entities with zero manual entity creation (phase success criterion 1, 2). RES-01 requires unavailable (not off) on detector loss — the LWT mechanism is set up here. PITFALL 5 (unique_id instability) and PITFALL 6 (missing/late LWT) are both mitigated here.
Output: MQTT connection (LWT), discovery publisher, state publisher, unique_id + friendly-name helpers, MqttPublisherWorker, Program.cs registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundations-streaming/01-CONTEXT.md
@.planning/phases/01-foundations-streaming/01-RESEARCH.md
@.planning/phases/01-foundations-streaming/01-04-SUMMARY.md
@.planning/phases/01-foundations-streaming/01-05-SUMMARY.md

<interfaces>
<!-- From Plan 04: EntitiesConfig (entity_id, friendly_name), ConnectionSettings (MqttHost/Port/User/Password) -->

unique_id formula (D-13/D-14):
  entity_slug = entity_id.Replace(".", "_")
  unique_id_anomaly = $"argus_{entity_slug}_hst_anomaly"
  unique_id_score   = $"argus_{entity_slug}_hst_score"
  object_id = same as unique_id (D-14 — prevents Polish char mangling)

Topics:
  discovery: homeassistant/binary_sensor/{unique_id_anomaly}/config  (retain)
             homeassistant/sensor/{unique_id_score}/config            (retain)
  state:     argus/{entity_slug}/flag/state    → ON/OFF
             argus/{entity_slug}/score/state   → float string
  avail:     argus/bridge/availability         → online/offline (LWT, bridge-level)

Friendly-name (D-16): "[Room] [SensorType] anomalia" derived from entities.yaml friendly_name.

MQTTnet 5 (RESEARCH Pattern 6) — MqttClientFactory (NOT MqttFactory); LWT via WithWillTopic/WithWillPayload("offline")/WithWillRetain BEFORE ConnectAsync; publish "online" immediately after connect.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UniqueId + FriendlyName helpers and discovery payload builder</name>
  <read_first>
    - .planning/phases/01-foundations-streaming/01-CONTEXT.md (D-13, D-14, D-15, D-16)
    - .planning/phases/01-foundations-streaming/01-RESEARCH.md (Pattern 5 MQTT discovery payload; PITFALL 5 unique_id instability)
  </read_first>
  <files>orchestrator/Argus.Orchestrator/Mqtt/UniqueId.cs, orchestrator/Argus.Orchestrator/Mqtt/FriendlyName.cs, orchestrator/Argus.Orchestrator/Mqtt/DiscoveryPublisher.cs, orchestrator/Argus.Orchestrator.Tests/UniqueIdTests.cs, orchestrator/Argus.Orchestrator.Tests/DiscoveryPayloadTests.cs</files>
  <behavior>
    - Test: UniqueId.Slug("sensor.salon_temperatura") == "sensor_salon_temperatura"; AnomalyId(...,"hst") == "argus_sensor_salon_temperatura_hst_anomaly"; ScoreId == "argus_sensor_salon_temperatura_hst_score".
    - Test: the same entity_id+detector always yields the same unique_id across calls (determinism, no GUID).
    - Test: discovery payload for an entity contains unique_id == object_id, device.identifiers == [entity_slug], device_class "problem" on binary_sensor, state_topic correct, availability_topic == "argus/bridge/availability" (bridge-level, not per-entity), payload_available "online"/payload_not_available "offline".
    - Test: FriendlyName for friendly_name "Salon temperatura" produces "Salon temperatura anomalia" (D-16 pattern), preserving Polish characters.
  </behavior>
  <action>
    UniqueId.cs: static `Slug(entityId)` = `entityId.Replace(".", "_")`; `AnomalyId(entityId, detector)` = `$"argus_{Slug(entityId)}_{detector}_anomaly"`; `ScoreId(...)` similarly with `_score`. No randomness (PITFALL 5).
    FriendlyName.cs: `ForAnomaly(friendlyName)` = `$"{friendlyName} anomalia"` (D-16). Keep UTF-8/Polish chars intact.
    DiscoveryPublisher.cs: build two JSON discovery payloads per entity (binary_sensor + sensor) per RESEARCH Pattern 5: set unique_id, object_id (= unique_id, D-14), name (Polish friendly-name), state_topic, availability_topic = "argus/bridge/availability" (bridge-level — all entities share one LWT topic so orchestrator crash marks ALL sensors unavailable), payload_available/not_available, device {identifiers:[slug], name, model:"Argus Anomaly Detector", manufacturer:"Argus"}, binary_sensor device_class "problem", sensor with state_class/unit omitted (raw score). Expose `BuildBinarySensorConfig(entity)`, `BuildSensorConfig(entity)`, and `PublishAllAsync(mqtt, entities, ct)` publishing to `homeassistant/binary_sensor/{id}/config` and `homeassistant/sensor/{id}/config` with retain=true, QoS AtLeastOnce. Idempotency (MQTT-04) is inherent: stable unique_id + retain; document that republish is safe.
    Tests: implement the four <behavior> tests; the payload tests assert against the serialized JSON object (no live broker).
  </action>
  <verify>
    <automated>dotnet test orchestrator/Argus.Orchestrator.sln --filter "FullyQualifiedName~UniqueId|FullyQualifiedName~DiscoveryPayload"</automated>
  </verify>
  <acceptance_criteria>
    - UniqueId.cs contains "argus_" and "Replace(\".\", \"_\")"
    - UniqueId.cs does NOT contain "Guid" or "Random" (determinism, PITFALL 5)
    - DiscoveryPublisher.cs contains "homeassistant/binary_sensor/" and "homeassistant/sensor/"
    - DiscoveryPublisher.cs contains "object_id" and "identifiers" and "device_class"
    - FriendlyName.cs contains "anomalia"
    - `dotnet test --filter "FullyQualifiedName~UniqueId|FullyQualifiedName~DiscoveryPayload"` exits 0
  </acceptance_criteria>
  <done>Deterministic unique_id/object_id (MQTT-02), retained discovery payloads per device (MQTT-01), Polish friendly-names (MQTT-05); payload tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MqttConnection (LWT before connect) + StatePublisher + MqttPublisherWorker + Program.cs wiring</name>
  <read_first>
    - .planning/phases/01-foundations-streaming/01-RESEARCH.md (Pattern 6 MQTTnet 5 connect+LWT; PITFALL 6 LWT timing; Code Example: publish retained discovery; D-15, D-17 MqttClientFactory)
    - orchestrator/Argus.Orchestrator/Mqtt/DiscoveryPublisher.cs (Task 1)
    - orchestrator/Argus.Orchestrator/Config/ConnectionSettings.cs (Plan 04 — Mqtt* settings)
  </read_first>
  <files>orchestrator/Argus.Orchestrator/Mqtt/MqttConnection.cs, orchestrator/Argus.Orchestrator/Mqtt/StatePublisher.cs, orchestrator/Argus.Orchestrator/Workers/MqttPublisherWorker.cs, orchestrator/Argus.Orchestrator/Program.cs</files>
  <behavior>
    - Test: the MqttClientOptions built by MqttConnection has a non-null Will with topic ending "/availability" and payload "offline" and WillRetain true (assert on the options builder output — no live broker). This proves LWT is set BEFORE connect (PITFALL 6).
    - Test: StatePublisher.FlagTopic(entityId) == "argus/{slug}/flag/state"; ScoreTopic == "argus/{slug}/score/state"; bridge availability topic constant == "argus/bridge/availability".
  </behavior>
  <action>
    MqttConnection.cs: use `MqttClientFactory` (NOT MqttFactory — D-17/RESEARCH state-of-art). Build `MqttClientOptionsBuilder().WithTcpServer(host,port).WithCredentials(user,password)` with bridge-level LWT: `WithWillTopic("argus/bridge/availability")`, `WithWillPayload("offline")`, `WithWillRetain(true)`, QoS AtLeastOnce, configured in options BEFORE ConnectAsync. After connect, publish "online" retained to `argus/bridge/availability`. Implement reconnect with Polly-style exponential backoff + jitter (Claude's Discretion). Expose `PublishAsync(topic,payload,retain,ct)`. Bridge LWT covers orchestrator crash (all sensors → unavailable via shared availability_topic); per-entity detector-loss path (RES-01) is handled by Plan 08 publishing "offline" to `argus/bridge/availability` on RpcException.
    StatePublisher.cs: topic helpers (FlagTopic/ScoreTopic/AvailabilityTopic per entity) + `PublishFlagAsync(entityId, bool on)`, `PublishScoreAsync(entityId, double score)`, `PublishAvailabilityAsync(entityId, bool online)` — all via MqttConnection. Score formatted invariant culture.
    MqttPublisherWorker.cs: BackgroundService that on start connects MqttConnection, publishes bridge "online", then calls DiscoveryPublisher.PublishAllAsync for all configured entities (retained discovery), then publishes initial per-entity availability "online". Logs structured (OBS-01). Keep a public method/event so Plan 08 can publish state/availability through the same connection (inject StatePublisher).
    Program.cs: register MqttConnection (singleton), DiscoveryPublisher, StatePublisher (singletons), and `AddHostedService<MqttPublisherWorker>()`. Do not remove existing Plan 04/05 registrations.
  </action>
  <verify>
    <automated>dotnet test orchestrator/Argus.Orchestrator.sln --filter "FullyQualifiedName~Mqtt"</automated>
  </verify>
  <acceptance_criteria>
    - MqttConnection.cs contains "MqttClientFactory"
    - MqttConnection.cs does NOT contain "MqttFactory" without the "Client" (i.e., grep for "new MqttFactory" returns nothing)
    - MqttConnection.cs contains "WithWillTopic" and "WithWillPayload" and "offline"
    - StatePublisher.cs contains "/flag/state" and "/availability"
    - Program.cs contains "AddHostedService<MqttPublisherWorker>"
    - `dotnet build orchestrator/Argus.Orchestrator.sln` exits 0
    - `dotnet test --filter "FullyQualifiedName~Mqtt"` exits 0
  </acceptance_criteria>
  <done>MQTTnet 5 client with LWT-before-connect (PITFALL 6), retained discovery published on startup (MQTT-01/03), idempotent (MQTT-04); state/availability publish surface ready for Plan 08 (RES-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator → MQTT broker | Credentialed connection; publishes discovery + state |
| MQTT broker → HA | Retained discovery drives HA entity creation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Spoofing | MQTT client identity | mitigate | WithCredentials(user,password) from env (CONF-03); broker auth per Q4 (username/password default per RESEARCH A3) |
| T-07-02 | Tampering | duplicate/orphan HA entities | mitigate | Deterministic unique_id + object_id (no GUID, grep-gated) + retain; idempotent republish (MQTT-02/04, PITFALL 5) |
| T-07-03 | Availability | stale 'off' shown after crash | mitigate | LWT 'offline' configured in connect options before any publish (PITFALL 6); RES-01 unavailable path |
| T-07-04 | Information Disclosure | MQTT password in logs/source | mitigate | Credentials only from ConnectionSettings env (CONF-03); never logged |
</threat_model>

<verification>
- `dotnet test orchestrator/Argus.Orchestrator.sln` passes (UniqueId, DiscoveryPayload, Mqtt)
- grep confirms MqttClientFactory, WithWill* before connect, no GUID in unique_id
- `dotnet build` exits 0
</verification>

<success_criteria>
- Discovery creates binary_sensor + sensor per entity under one device (MQTT-01)
- unique_id deterministic + stable (MQTT-02, PITFALL 5)
- retain:true everywhere + LWT before first state (MQTT-03, PITFALL 6)
- Idempotent republish (MQTT-04); Polish friendly-names (MQTT-05)
- Availability/LWT plumbing ready for graceful degradation (RES-01)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-streaming/01-07-SUMMARY.md`
</output>
