---
phase: 10-design-system-foundation
plan: 07
type: execute
wave: 3
depends_on: ["10-02", "10-03", "10-04"]
files_modified:
  - orchestrator/ui/src/components/GroupListRow.tsx
  - orchestrator/ui/src/components/SaveResultBanner.tsx
  - orchestrator/ui/src/components/GroupSaveResultBanner.tsx
  - orchestrator/ui/src/components/AreaSuggestionBanner.tsx
autonomous: true
requirements: [COMP-01]
must_haves:
  truths:
    - "GroupListRow renders its status/mode pills via the shared Badge and its Delete/Edit actions via the shared Button, keeping the inline two-step arm/confirm delete (label swaps 'Delete group' ↔ 'Confirm delete')"
    - "SaveResultBanner, GroupSaveResultBanner, and AreaSuggestionBanner each render via the shared Banner with the correct tone (success/validation/error/info), preserving their exact copy and aria roles"
    - "The existing SaveResultBanner.test.tsx still passes after the retrofit (zero regression)"
  prohibitions:
    - "The two-step arm/confirm delete stays — never replaced by window.confirm() or a modal (the armed state stays owned by GroupListRow, Button just renders the given label)"
    - "Banner copy is preserved verbatim during the retrofit — no rewording (the D-06 CTA-copy exception does not extend to banners)"
    - "No raw .argus-banner / .argus-pill / .argus-btn markup left in these call sites (D-04)"
  artifacts:
    - path: "orchestrator/ui/src/components/GroupListRow.tsx"
      provides: "Retrofitted to shared Button + Badge, arm/confirm preserved (D-04)"
      exports: ["GroupListRow"]
    - path: "orchestrator/ui/src/components/SaveResultBanner.tsx"
      provides: "Retrofitted to shared Banner (D-04)"
      exports: ["SaveResultBanner"]
    - path: "orchestrator/ui/src/components/GroupSaveResultBanner.tsx"
      provides: "Retrofitted to shared Banner (D-04)"
      exports: ["GroupSaveResultBanner"]
    - path: "orchestrator/ui/src/components/AreaSuggestionBanner.tsx"
      provides: "Retrofitted to shared Banner (info tone) (D-04)"
      exports: ["AreaSuggestionBanner"]
  key_links:
    - from: "orchestrator/ui/src/components/GroupListRow.tsx"
      to: "orchestrator/ui/src/components/Button.tsx"
      via: "renders <Button variant=\"destructive-ghost\"> whose label the row swaps on arm/confirm"
      pattern: "Button"
---

<objective>
Retrofit the group/display + feedback call sites to the shared components (D-04): `GroupListRow`
consumes the shared `Button` (destructive-ghost, keeping its own inline arm/confirm state) and
`Badge` (mode/status pills), and the three existing banner components (`SaveResultBanner`,
`GroupSaveResultBanner`, `AreaSuggestionBanner`) consolidate onto the shared `Banner`. Behavior
and copy preserved; the two-step delete confirm stays (no `window.confirm`).

Purpose: Make the live Groups screen consume the shared library (Success Criteria #2, D-05
verification scope) without changing what it does.
Output: 4 retrofitted call-site files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-design-system-foundation/10-CONTEXT.md
@.planning/phases/10-design-system-foundation/10-UI-SPEC.md
@orchestrator/ui/src/components/GroupListRow.tsx
@orchestrator/ui/src/components/SaveResultBanner.tsx
</context>

<artifacts_this_phase_produces>
No new symbols — rewires existing call sites to consume `Button`, `Badge`, and `Banner`. Exports
of the four files are unchanged.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: GroupListRow → shared Button + Badge (arm/confirm preserved)</name>
  <files>orchestrator/ui/src/components/GroupListRow.tsx</files>
  <read_first>
    - orchestrator/ui/src/components/GroupListRow.tsx (current arm/confirm delete state lines 21-40; raw .argus-pill lines 46, 53; raw destructive-ghost button lines 62-68)
    - orchestrator/ui/src/components/Button.tsx (shared Button, destructive-ghost variant; renders the label it is given)
    - orchestrator/ui/src/components/Badge.tsx (shared Badge tones: neutral/tracked)
    - .planning/phases/10-design-system-foundation/10-UI-SPEC.md (Copywriting Contract — Destructive confirmation two-step arm/confirm, no window.confirm, lines 150)
  </read_first>
  <action>
    In `GroupListRow.tsx`, keep the existing `armed`/`timerRef`/`handleDeleteClick` state and the
    `CONFIRM_WINDOW_MS` logic exactly as-is (the row owns the arm/confirm state). Replace the raw
    `<button class="argus-btn argus-btn--destructive-ghost" onClick={handleDeleteClick}>` with
    `<Button variant="destructive-ghost" size="xs" onClick={handleDeleteClick}>{armed ? 'Confirm
    delete' : 'Delete group'}</Button>` — Button renders whatever label the row passes, so the
    label swap keeps working without moving state into Button. Replace the raw
    `<span class="argus-pill">{modeLabel}</span>` with `<Badge tone="neutral">{modeLabel}</Badge>`
    and the raw `<span class="argus-pill argus-pill--tracked">` status pill with `<Badge
    tone="tracked">{status.isAnomaly ? 'anomaly' : 'active'}</Badge>`. Keep the `Edit` link and the
    `no status yet` label and the `.argus-list-row`/`.argus-row-*` wrapper structure unchanged.
    Import `Button` and `Badge`. Do NOT introduce `window.confirm` or a modal.
  </action>
  <verify>
    <automated>cd orchestrator/ui && npx tsc -b && grep -q "Confirm delete" src/components/GroupListRow.tsx && grep -q "from './Badge'" src/components/GroupListRow.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `GroupListRow.tsx` imports `Button` and `Badge`; renders `<Button
      variant="destructive-ghost" ...>` whose children are `armed ? 'Confirm delete' : 'Delete
      group'`
    - `GroupListRow.tsx` retains the `armed` state + `CONFIRM_WINDOW_MS` timer (no `window.confirm`)
    - No raw `class="argus-pill"` or `class="argus-btn argus-btn--destructive-ghost"` string
      remains in the file
    - `cd orchestrator/ui && npx tsc -b` exits 0
  </acceptance_criteria>
  <done>GroupListRow routes actions/pills through shared Button+Badge; two-step delete preserved (D-04).</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate the three banner components onto shared Banner</name>
  <files>orchestrator/ui/src/components/SaveResultBanner.tsx, orchestrator/ui/src/components/GroupSaveResultBanner.tsx, orchestrator/ui/src/components/AreaSuggestionBanner.tsx</files>
  <read_first>
    - orchestrator/ui/src/components/SaveResultBanner.tsx (success/validation/error branch + aria roles + warmup note — copy to preserve)
    - orchestrator/ui/src/components/GroupSaveResultBanner.tsx (its tone/copy branches)
    - orchestrator/ui/src/components/AreaSuggestionBanner.tsx (subtle/info suggestion banner)
    - orchestrator/ui/src/components/SaveResultBanner.test.tsx (tests that MUST keep passing)
    - orchestrator/ui/src/components/Banner.tsx (shared Banner API: tone/children/action/onDismiss)
  </read_first>
  <action>
    Retrofit each of the three banner components to render via the shared `Banner`, preserving
    their exact copy, branching, and aria semantics: in `SaveResultBanner.tsx`, map the `ok`
    branch to `<Banner tone="success">` (keeping the warmup `<p class="argus-warmup-note">` child
    verbatim), the `kind === 'validation'` branch to `<Banner tone="validation">`, and the error
    branch to `<Banner tone="error">`, keeping the same message strings. In
    `GroupSaveResultBanner.tsx`, map its branches to the matching Banner tones with identical copy.
    In `AreaSuggestionBanner.tsx`, render `<Banner tone="info">` (the subtle
    surface variant that replaces `.argus-area-suggestion-banner`) preserving its content and any
    action affordance via Banner's `action`/`onDismiss` slots. Do not reword any banner copy — the
    D-06 CTA exception applies only to the primary Save button, not to banners. Import `Banner` in
    each. Ensure `SaveResultBanner.test.tsx` still passes (do not change the rendered text or the
    branch conditions it asserts).
  </action>
  <verify>
    <automated>cd orchestrator/ui && npx vitest run src/components/SaveResultBanner.test.tsx && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - All three files import `Banner` and render `<Banner tone=...>` instead of raw
      `class="argus-banner ..."` divs
    - `SaveResultBanner.tsx` maps success/validation/error branches to Banner tones
      success/validation/error with unchanged copy (warmup note preserved)
    - `AreaSuggestionBanner.tsx` uses `<Banner tone="info">`
    - `cd orchestrator/ui && npx vitest run src/components/SaveResultBanner.test.tsx` still passes
    - `cd orchestrator/ui && npx tsc -b` exits 0
  </acceptance_criteria>
  <done>All three banners consolidated onto the shared Banner with copy + tests preserved (D-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server result → banner text | Save/group results and area-suggestion copy render as text; delete action triggers an existing endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07-01 | Injection (XSS) | banners | accept | Banner content (save reasons, error strings, group names) renders as Preact text nodes; no `dangerouslySetInnerHTML`. Retrofit does not change what data is rendered, only the wrapper component. |
| T-10-07-02 | Tampering | delete action | accept | The two-step arm/confirm delete calls the existing `deleteGroup` path (full-list-replace save); server validation unchanged. The UI confirm is a UX guard, not the security control. |
| T-10-07-SC | Tampering | npm installs | accept | No new dependencies. |

Assessment: behavior-preserving component swaps on the Groups screen; no new attack surface.
</threat_model>

<verification>
- `cd orchestrator/ui && npx tsc -b` exits 0
- `cd orchestrator/ui && npx vitest run src/components/SaveResultBanner.test.tsx` passes
- Grep confirms no raw `.argus-banner`/`.argus-pill`/destructive `.argus-btn` markup left in these files
</verification>

<success_criteria>
The Groups-screen call sites consume the shared Button/Badge/Banner (D-04) with zero behavior or
copy change and the two-step delete confirm preserved.
</success_criteria>

<output>
Create `.planning/phases/10-design-system-foundation/10-07-SUMMARY.md` when done
</output>
