---
phase: 10-design-system-foundation
plan: 06
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - orchestrator/ui/src/components/SaveBar.tsx
  - orchestrator/ui/src/components/AddDetectorButton.tsx
  - orchestrator/ui/src/components/DetectorEntry.tsx
  - orchestrator/ui/src/components/SensorListRow.tsx
  - orchestrator/ui/src/components/SensorSearchInput.tsx
autonomous: true
requirements: [COMP-01, A11Y-01]
must_haves:
  truths:
    - "SaveBar renders the primary CTA via the shared Button component and keeps the exact label 'Save configuration' (D-06)"
    - "AddDetectorButton renders via the shared Button (secondary variant), keeping the '+ Add detector' label"
    - "DetectorEntry renders its detector-type <select> via the shared Select and its Remove button via the shared Button (destructive-ghost)"
    - "SensorListRow renders its tracked checkbox via the shared Checkbox"
    - "SensorSearchInput delegates to the shared SearchInput (same debounce + placeholder), preserving existing behavior and its passing tests"
  prohibitions:
    - "The primary CTA label is never reverted to a bare generic 'Save' — it stays 'Save configuration' (D-06)"
    - "No behavior regression — the existing SensorSearchInput.test.tsx must still pass unchanged"
    - "No raw .argus-btn / .argus-detector-select / .argus-checkbox markup left in these call sites — they route through the shared components (D-04)"
  artifacts:
    - path: "orchestrator/ui/src/components/SaveBar.tsx"
      provides: "Retrofitted to shared Button (D-04/D-06)"
      exports: ["SaveBar"]
    - path: "orchestrator/ui/src/components/DetectorEntry.tsx"
      provides: "Retrofitted to shared Select + Button (D-04)"
      exports: ["DetectorEntry"]
    - path: "orchestrator/ui/src/components/SensorSearchInput.tsx"
      provides: "Thin instantiation of shared SearchInput (D-04)"
      exports: ["SensorSearchInput"]
  key_links:
    - from: "orchestrator/ui/src/components/SaveBar.tsx"
      to: "orchestrator/ui/src/components/Button.tsx"
      via: "imports and renders <Button variant=\"primary\">Save configuration</Button>"
      pattern: "Button"
---

<objective>
Retrofit the form-consuming call sites to the shared components built in Plan 10-02 (D-04, the
user's explicit choice of full retrofit now over library-only). Swap raw `.argus-btn` /
`.argus-detector-select` / `.argus-checkbox` / `.argus-search` markup for the shared `Button`,
`Select`, `Checkbox`, and `SearchInput`, preserving every existing behavior and label — notably
the primary CTA copy "Save configuration" (D-06). No behavior change; this is an
implementation-swap retrofit.

Purpose: Make the live Sensors screen consume the shared library (Success Criteria #2, D-05
verification scope) without changing what the screen does.
Output: 5 retrofitted call-site files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-design-system-foundation/10-CONTEXT.md
@.planning/phases/10-design-system-foundation/10-UI-SPEC.md
@orchestrator/ui/src/components/SaveBar.tsx
@orchestrator/ui/src/components/DetectorEntry.tsx
@orchestrator/ui/src/components/SensorListRow.tsx
@orchestrator/ui/src/components/SensorSearchInput.tsx
@orchestrator/ui/src/components/AddDetectorButton.tsx
</context>

<artifacts_this_phase_produces>
No new symbols — this plan rewires existing call sites to consume `Button`, `Select`, `Checkbox`,
`SearchInput` from Plan 10-02. Exports of the five files are unchanged.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: SaveBar + AddDetectorButton → shared Button (D-06 label preserved)</name>
  <files>orchestrator/ui/src/components/SaveBar.tsx, orchestrator/ui/src/components/AddDetectorButton.tsx</files>
  <read_first>
    - orchestrator/ui/src/components/SaveBar.tsx (current raw .argus-btn--primary + "Save configuration" label + #argus-spinner)
    - orchestrator/ui/src/components/AddDetectorButton.tsx (current raw .argus-btn--add-detector + "+ Add detector")
    - orchestrator/ui/src/components/Button.tsx (shared Button API from Plan 10-02: variant/size/loading/disabled/onClick)
    - .planning/phases/10-design-system-foundation/10-CONTEXT.md (D-06 CTA label rule lines 50-56)
  </read_first>
  <action>
    In `SaveBar.tsx`, replace the raw `<button class="argus-btn argus-btn--primary">` with
    `<Button variant="primary" loading={saving} disabled={disabled} onClick={onSave}>Save
    configuration</Button>` — keep the label text exactly "Save configuration" (D-06; never revert
    to bare "Save"). Since Button now renders its own `.argus-btn__spinner` when `loading`, remove
    the separate `#argus-spinner` span. Keep the `.argus-save-bar` wrapper and the `SaveBarProps`
    interface. In `AddDetectorButton.tsx`, replace the raw `<button class="argus-btn
    argus-btn--add-detector">` with `<Button variant="secondary" onClick={onAdd} ariaLabel={`Add
    detector to ${entityId}`}>+ Add detector</Button>`, keeping the `.argus-add-detector-row`
    wrapper and the props. Import `Button` from `./Button` in both.
  </action>
  <verify>
    <automated>cd orchestrator/ui && npx tsc -b && grep -q "Save configuration" src/components/SaveBar.tsx && grep -q "from './Button'" src/components/SaveBar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `SaveBar.tsx` imports `Button` and renders `<Button variant="primary" ...>Save configuration</Button>`
    - `SaveBar.tsx` no longer contains a raw `class="argus-btn argus-btn--primary"` string or the
      `#argus-spinner` span
    - `AddDetectorButton.tsx` imports and renders `<Button variant="secondary" ...>`
    - `cd orchestrator/ui && npx tsc -b` exits 0
  </acceptance_criteria>
  <done>SaveBar + AddDetectorButton route through the shared Button; "Save configuration" preserved (D-06).</done>
</task>

<task type="auto">
  <name>Task 2: DetectorEntry → shared Select + Button</name>
  <files>orchestrator/ui/src/components/DetectorEntry.tsx</files>
  <read_first>
    - orchestrator/ui/src/components/DetectorEntry.tsx (current raw .argus-detector-select + destructive-ghost Remove button lines 26-47)
    - orchestrator/ui/src/components/Select.tsx (shared Select API: value/onChange/options/ariaLabel)
    - orchestrator/ui/src/components/Button.tsx (shared Button, destructive-ghost variant)
  </read_first>
  <action>
    In `DetectorEntry.tsx`, replace the raw `<select class="argus-detector-select">` with `<Select
    value={detector.name} ariaLabel={`Detector type for entity ${entityIdx}`} options={[{value:'hst',
    label:'HST'},{value:'mad',label:'MAD'},{value:'stl',label:'STL'}]} onChange={(v) =>
    onTypeChange(v as 'hst'|'mad'|'stl')} />`. Replace the raw `<button class="argus-btn
    argus-btn--destructive-ghost">Remove</button>` with `<Button variant="destructive-ghost"
    size="xs" ariaLabel="Remove this detector" onClick={onRemove}>Remove</Button>`. Keep the
    `.argus-detector-header` / `.argus-timing-caption` wrapper structure and the `DetectorParamGrid`
    child unchanged. Import `Select` and `Button`. Preserve the exact onTypeChange type cast to
    'hst' | 'mad' | 'stl'.
  </action>
  <verify>
    <automated>cd orchestrator/ui && npx tsc -b && grep -q "from './Select'" src/components/DetectorEntry.tsx && grep -q "from './Button'" src/components/DetectorEntry.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `DetectorEntry.tsx` imports `Select` and `Button`; renders `<Select ...>` with the three
      hst/mad/stl options and `<Button variant="destructive-ghost" ...>Remove</Button>`
    - `DetectorEntry.tsx` no longer contains a raw `class="argus-detector-select"` or
      `class="argus-btn argus-btn--destructive-ghost"` string
    - `cd orchestrator/ui && npx tsc -b` exits 0
  </acceptance_criteria>
  <done>DetectorEntry routes its select + remove button through the shared components (D-04).</done>
</task>

<task type="auto">
  <name>Task 3: SensorListRow → Checkbox; SensorSearchInput → SearchInput</name>
  <files>orchestrator/ui/src/components/SensorListRow.tsx, orchestrator/ui/src/components/SensorSearchInput.tsx</files>
  <read_first>
    - orchestrator/ui/src/components/SensorListRow.tsx (current raw .argus-checkbox lines 38-45)
    - orchestrator/ui/src/components/SensorSearchInput.tsx (current debounce implementation to replace with delegation)
    - orchestrator/ui/src/components/SensorSearchInput.test.tsx (the tests that MUST keep passing — placeholder string, debounce cleanup, onChange behavior)
    - orchestrator/ui/src/components/Checkbox.tsx (shared Checkbox API: checked/onChange/ariaLabel)
    - orchestrator/ui/src/components/SearchInput.tsx (shared SearchInput API: value/onChange/placeholder/ariaLabel/debounceMs)
  </read_first>
  <action>
    In `SensorListRow.tsx`, replace the raw `<input class="argus-checkbox" type="checkbox">` with
    `<Checkbox checked={isTracked} ariaLabel={entry.entityId} onChange={onToggleTracked} />`,
    keeping the surrounding `<label style={{ display: 'contents' }}>` and all other row markup
    unchanged. Import `Checkbox`. In `SensorSearchInput.tsx`, make it a thin instantiation of the
    shared `SearchInput`: render `<SearchInput value={value} onChange={onChange} placeholder="Filter
    by name or entity ID…" ariaLabel="Filter entities" debounceMs={200} />` and delete the local
    debounce/ref/useEffect logic (now owned by SearchInput). Keep the `SensorSearchInput` export
    name and its props so existing call sites and the test file are unaffected. The placeholder
    string must stay exactly "Filter by name or entity ID…" and the debounce 200ms so
    `SensorSearchInput.test.tsx` still passes verbatim.
  </action>
  <verify>
    <automated>cd orchestrator/ui && npx vitest run src/components/SensorSearchInput.test.tsx && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `SensorListRow.tsx` imports and renders `<Checkbox checked={isTracked} ...>`; no raw
      `class="argus-checkbox"` input remains
    - `SensorSearchInput.tsx` renders `<SearchInput ...>` with placeholder "Filter by name or
      entity ID…" and no longer contains its own `setTimeout` debounce
    - `cd orchestrator/ui && npx vitest run src/components/SensorSearchInput.test.tsx` still passes
      (zero regression)
    - `cd orchestrator/ui && npx tsc -b` exits 0
  </acceptance_criteria>
  <done>SensorListRow uses shared Checkbox; SensorSearchInput delegates to shared SearchInput with tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → existing handlers | Retrofit preserves the existing onChange/onSave data paths; no new boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06-01 | Tampering | detector-type / search values | accept | Retrofit is an implementation swap; the same values flow to the same handlers (`onTypeChange`, `onChange`, `onSave`). Server-side validation (`DetectorDefaults.cs`, existing save endpoint) is unchanged and remains the authority. |
| T-10-06-SC | Tampering | npm installs | accept | No new dependencies. |

Assessment: behavior-preserving component swaps; no new attack surface, no data-path change.
</threat_model>

<verification>
- `cd orchestrator/ui && npx tsc -b` exits 0
- `cd orchestrator/ui && npx vitest run src/components/SensorSearchInput.test.tsx` passes
- Grep confirms no raw `.argus-btn--primary`/`.argus-detector-select`/`.argus-checkbox` markup left in these files
</verification>

<success_criteria>
The Sensors-screen form call sites consume the shared Button/Select/Checkbox/SearchInput (D-04)
with zero behavior change and the D-06 "Save configuration" label preserved.
</success_criteria>

<output>
Create `.planning/phases/10-design-system-foundation/10-06-SUMMARY.md` when done
</output>
