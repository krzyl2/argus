---
phase: 10-design-system-foundation
plan: 04
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - orchestrator/ui/src/components/Banner.tsx
  - orchestrator/ui/src/components/EmptyState.tsx
  - orchestrator/ui/src/components/AlgorithmCard.tsx
  - orchestrator/ui/src/components/SensitivityPresetPicker.tsx
autonomous: true
requirements: [COMP-01, A11Y-02]
must_haves:
  truths:
    - "Banner renders .argus-banner + .argus-banner--{tone} for tones success/error/validation/reloading/info"
    - "Banner with an info tone is dismissable (renders a .argus-banner__dismiss ghost button that calls onDismiss)"
    - "EmptyState renders .argus-empty with a heading + imperative body, matching the readme voice rules"
    - "Selecting an AlgorithmCard applies .argus-algorithm-card--selected which sets border-width:2px + accent border-color (A11Y-02: selection distinguishable without color alone)"
    - "SensitivityPreset radios use accent-color and Med default; selection expands preset params into the draft"
  prohibitions:
    - "AlgorithmCard selected state never signalled by color alone — the 2px border thickening is required (A11Y-02)"
    - "No window.confirm used anywhere; destructive confirmation stays the inline arm/confirm pattern owned by call sites"
    - "Banner info tone uses --color-surface / --color-text-primary (subtle), never a full-strength accent/status background"
    - "No new argus.css classes authored here (--info + tones exist from Plan 10-01)"
  artifacts:
    - path: "orchestrator/ui/src/components/Banner.tsx"
      provides: "Consolidated tone-driven banner (replaces 3 existing banners in Wave 3)"
      exports: ["Banner"]
    - path: "orchestrator/ui/src/components/EmptyState.tsx"
      provides: "Retrofitted empty-state (in place)"
      exports: ["EmptyState"]
    - path: "orchestrator/ui/src/components/AlgorithmCard.tsx"
      provides: "D-03 in-place radio-card retrofit (2px accent selection border)"
      exports: ["AlgorithmCard"]
    - path: "orchestrator/ui/src/components/SensitivityPresetPicker.tsx"
      provides: "D-03 in-place preset picker retrofit (dark-mode token wiring)"
      exports: ["SensitivityPresetPicker", "isCustomized"]
  key_links:
    - from: "orchestrator/ui/src/components/AlgorithmCard.tsx"
      to: "orchestrator/ui/public/css/argus.css"
      via: ".argus-algorithm-card--selected sets border-width:2px + accent (A11Y-02)"
      pattern: "argus-algorithm-card--selected"
---

<objective>
Deliver the feedback + selection components: a consolidated `Banner` (that Wave 3 uses to replace
the three existing banner components), the in-place `EmptyState` retrofit, and the two D-03
radio-card selectors (`AlgorithmCard`, `SensitivityPresetPicker`) brought to spec in place.
Carries A11Y-02 (radio-card selection = 2px accent border, never color alone). Satisfies COMP-01
for the feedback + selection categories.

Purpose: Provide the shared notification + selection primitives; lock the A11Y-02 border rule into
AlgorithmCard so every future radio-card (Sensors detector picker, Groups wizard) inherits it.
Output: 1 new Banner + 3 in-place retrofits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-design-system-foundation/10-CONTEXT.md
@.planning/phases/10-design-system-foundation/10-UI-SPEC.md
@.planning/phases/10-design-system-foundation/10-PATTERNS.md
@orchestrator/ui/src/components/AlgorithmCard.tsx
@orchestrator/ui/src/components/SensitivityPresetPicker.tsx
@orchestrator/ui/src/components/EmptyState.tsx
@orchestrator/ui/src/components/SaveResultBanner.tsx
</context>

<artifacts_this_phase_produces>
- `Banner` (`BannerProps`: tone 'success'|'error'|'validation'|'reloading'|'info', children,
  action?, onDismiss?)
- `EmptyState` (retrofitted; existing `EmptyStateProps` preserved)
- `AlgorithmCard` (retrofitted in place, D-03; existing props preserved)
- `SensitivityPresetPicker` + `isCustomized` (retrofitted in place, D-03; existing exports preserved)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Banner (consolidated) + EmptyState retrofit</name>
  <files>orchestrator/ui/src/components/Banner.tsx, orchestrator/ui/src/components/EmptyState.tsx</files>
  <read_first>
    - orchestrator/ui/src/components/SaveResultBanner.tsx (existing success/validation/error banner shape + roles aria-live)
    - orchestrator/ui/src/components/GroupSaveResultBanner.tsx, AreaSuggestionBanner.tsx (the other two banners Wave 3 will consolidate — read for the info/subtle tone shape)
    - orchestrator/ui/src/components/EmptyState.tsx (existing file to retrofit in place)
    - orchestrator/ui/public/css/argus.css (.argus-banner family lines 411-428 + --reloading/--validation + --info/__dismiss from Plan 10-01; .argus-empty lines 431-438)
    - Argus Design System/components/feedback/Banner.jsx, EmptyState.jsx (API reference)
  </read_first>
  <action>
    Create `Banner.tsx` exporting `Banner` with props { tone?: 'success' | 'error' | 'validation'
    | 'reloading' | 'info' (default 'info'); children: ComponentChildren; action?:
    ComponentChildren; onDismiss?: () => void }, rendering `<div class="argus-banner
    argus-banner--{tone}" role="status">` with the children in a flex-1 span, an optional trailing
    `action` slot, and — when `onDismiss` is provided — a trailing `<button
    class="argus-banner__dismiss" type="button" aria-label="Dismiss">✕</button>` calling onDismiss.
    Keep `role="status"` (the existing SaveResultBanner distinguishes alert vs status per branch;
    Banner defaults to status and callers may pass through richer semantics later). No inline
    styles. For `EmptyState.tsx`, retrofit in place: keep the existing query-branching logic and
    copy verbatim, ensure it composes `.argus-empty` and the `.argus-body`/`.argus-label`
    typography classes only (it already does) — this is a D-04 in-place retrofit confirming
    spec-compliance and dark-mode token propagation, not a rewrite of the copy.
  </action>
  <verify>
    <automated>cd orchestrator/ui && npx tsc -b && grep -q "argus-banner--" src/components/Banner.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `Banner.tsx` exports `Banner`; renders `argus-banner argus-banner--{tone}` and a
      `argus-banner__dismiss` button only when `onDismiss` is passed
    - `Banner.tsx` supports all five tones success/error/validation/reloading/info
    - `EmptyState.tsx` still exports `EmptyState` and composes `.argus-empty` (no inline styles)
    - `cd orchestrator/ui && npx tsc -b` exits 0
  </acceptance_criteria>
  <done>Consolidated Banner (with dismissable info tone) exists; EmptyState verified in place.</done>
</task>

<task type="auto">
  <name>Task 2: AlgorithmCard + SensitivityPresetPicker retrofit in place (D-03, A11Y-02)</name>
  <files>orchestrator/ui/src/components/AlgorithmCard.tsx, orchestrator/ui/src/components/SensitivityPresetPicker.tsx</files>
  <read_first>
    - orchestrator/ui/src/components/AlgorithmCard.tsx (existing file — role="radio", aria-checked, .argus-algorithm-card--selected)
    - orchestrator/ui/src/components/SensitivityPresetPicker.tsx (existing file — native radios, accent-color, Med default, isCustomized export)
    - orchestrator/ui/public/css/argus.css (.argus-algorithm-card--selected border-width:2px lines 725-728; sensitivity-preset radios accent-color lines 763-767)
    - .planning/phases/10-design-system-foundation/10-CONTEXT.md (D-03 lines 32-35)
    - .planning/phases/10-design-system-foundation/10-PATTERNS.md (A11Y-02 rule lines 278-286; both components already compliant per lines 236-244)
  </read_first>
  <action>
    Both are D-03 in-place modifications "to match the Design System spec exactly (2px selection
    border, dark mode)". Per 10-PATTERNS.md the selected-state 2px accent border
    (`.argus-algorithm-card--selected` = `border-color: var(--color-accent); border-width: 2px`)
    already satisfies A11Y-02 and the SensitivityPreset radios already use `accent-color:
    var(--color-accent)` — so the required work is: (1) confirm `AlgorithmCard.tsx` keeps
    `role="radio"` + `aria-checked={selected}` and toggles `argus-algorithm-card--selected` on the
    selected card, keeping the border-thicken as the non-color selection cue (A11Y-02); (2) confirm
    `SensitivityPresetPicker.tsx` keeps its `role="radiogroup"`, Med default, and preset-expansion
    behavior and the exported `isCustomized`; (3) verify no local `outline: none` or hard-coded
    light color overrides block dark-mode token propagation (the dark tokens now come from Plan
    10-01's `[data-theme="dark"]` block). Make only the minimal change needed to bring either file
    to spec — if both already comply, document the retrofit as a verified no-op in the SUMMARY and
    do NOT gratuitously rewrite working markup (Rule 3, surgical changes).
  </action>
  <verify>
    <automated>cd orchestrator/ui && npx tsc -b && grep -q "argus-algorithm-card--selected" src/components/AlgorithmCard.tsx && grep -q "radiogroup" src/components/SensitivityPresetPicker.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `AlgorithmCard.tsx` retains `role="radio"`, `aria-checked`, and applies
      `argus-algorithm-card--selected` for the selected card (A11Y-02 border cue)
    - `SensitivityPresetPicker.tsx` retains `role="radiogroup"`, the Med default, and exports
      `SensitivityPresetPicker` and `isCustomized`
    - Neither file introduces `outline: none` or a hard-coded hex color that would defeat
      dark-mode tokens
    - `cd orchestrator/ui && npx tsc -b` exits 0
  </acceptance_criteria>
  <done>Both radio-card selectors confirmed/retrofitted to spec with the 2px accent border (A11Y-02) and dark-mode token wiring (D-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog/props → DOM text | Banner/EmptyState/AlgorithmCard render caller strings (catalog `bestFor`, error messages) as text nodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Injection (XSS) | Banner/AlgorithmCard | accept | All content (error reasons, catalog "best for" copy) renders as Preact text nodes; no `dangerouslySetInnerHTML`. Catalog copy originates from server-side `DetectorCatalog.cs`, not free user input. |
| T-10-04-SC | Tampering | npm installs | accept | No new dependencies. |

Assessment: feedback/selection components; no dynamic HTML, network, or storage surface added.
</threat_model>

<verification>
- `cd orchestrator/ui && npx tsc -b` exits 0
- AlgorithmCard selected state uses the 2px border class (A11Y-02)
- Banner renders all five tones; info tone dismissable
</verification>

<success_criteria>
Banner/EmptyState/AlgorithmCard/SensitivityPreset match their DS specs; A11Y-02 border-not-color
rule locked into AlgorithmCard; D-03 in-place retrofits complete.
</success_criteria>

<output>
Create `.planning/phases/10-design-system-foundation/10-04-SUMMARY.md` when done
</output>
