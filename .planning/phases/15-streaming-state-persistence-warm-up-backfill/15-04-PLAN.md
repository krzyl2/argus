---
phase: 15-streaming-state-persistence-warm-up-backfill
plan: 04
type: execute
wave: 4
depends_on: ["15-01", "15-02", "15-03"]
files_modified:
  - detector/tests/test_restart_resilience.py
  - orchestrator/Argus.Orchestrator.Tests/ScoreStreamPipelineTests.cs
  - argus/config.yaml
  - .planning/phases/15-streaming-state-persistence-warm-up-backfill/15-UAT.md
autonomous: false
requirements:
  - PERSIST-01
  - PERSIST-02
  - PERSIST-03
  - PERSIST-04
  - WARM-01
  - WARM-02
  - BACKFILL-01
  - BACKFILL-02
  - BACKFILL-03
  - BACKFILL-04
user_setup:
  - service: home-assistant
    why: "Live UAT of the nine ROADMAP success criteria requires the add-on running against the operator's real HA instance and real slow-reporting sensors"
    dashboard_config:
      - task: "Update the Argus add-on to the new version"
        location: "Home Assistant -> Settings -> Add-ons -> Argus -> Check for updates -> Update"
  - service: ghcr.io
    why: "The release image is built and pushed from the operator's workstation, not CI"
    dashboard_config:
      - task: "docker login ghcr.io with a write:packages PAT (one-time workstation prerequisite)"
        location: "operator workstation shell"

must_haves:
  truths:
    - "All nine ROADMAP success criteria are exercised: six by automated tests across the three implementation plans plus this one, three (SC-2, SC-5, SC-7 end-to-end) confirmed on live HA"
    - "SC-1: a hard-killed detector restores n_seen from its checkpoint, losing at most one interval"
    - "SC-6: a corrupt checkpoint for one entity does not stop the detector reaching SERVING for the others"
    - "SC-8: an orchestrator restart against a checkpointed detector does not re-run backfill"
    - "argus/config.yaml version equals the image tag pushed to GHCR — the iron rule from the deploy recipe"
  artifacts:
    - "detector/tests/test_restart_resilience.py — extended with the hard-kill restore, corrupt-checkpoint startup, and idle-no-write cross-cutting cases"
    - "orchestrator/Argus.Orchestrator.Tests/ScoreStreamPipelineTests.cs — extended with the no-re-backfill-after-restart case"
    - "argus/config.yaml — version bumped to 2.1.9"
    - ".planning/phases/15-streaming-state-persistence-warm-up-backfill/15-UAT.md — the nine-criterion live checklist with recorded outcomes"
  key_links:
    - "argus/config.yaml version -> the GHCR tag passed to deploy/build-push.ps1 -> HA update detection via repository.yaml on master"
    - "the nine ROADMAP success criteria -> 15-UAT.md rows -> the recorded pass/fail evidence"
---

<objective>
Prove the phase, then ship it. Adds the cross-cutting restart and crash cases that no single
implementation plan owns, bumps the add-on version, and runs the nine ROADMAP success criteria against
live Home Assistant before the phase is called done.

Tracer-first does not apply to this plan: it adds no production code path and touches no architectural
layer, so there is no thin end-to-end slice left to prove. The vertical slices were proven by the
leading tracer task in each of 15-01, 15-02, and 15-03.

Purpose: SC-1, SC-6, and SC-8 span plan boundaries (kill a process and restart it; corrupt one file and
boot; restart one side and check the other did not re-act), and the operator's real pain point is only
observable on real slow-reporting sensors.

Output: a green full test suite, add-on 2.1.9 on GHCR, and a filled-in `15-UAT.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-streaming-state-persistence-warm-up-backfill/15-CONTEXT.md
@.planning/phases/15-streaming-state-persistence-warm-up-backfill/15-01-SUMMARY.md
@.planning/phases/15-streaming-state-persistence-warm-up-backfill/15-02-SUMMARY.md
@.planning/phases/15-streaming-state-persistence-warm-up-backfill/15-03-SUMMARY.md
</context>

## Artifacts this phase produces (15-04 scope)

| Artifact | Detail |
|---|---|
| `detector/tests/test_restart_resilience.py` | Extended with hard-kill restore, corrupt-checkpoint startup, and idle-no-write cases |
| `orchestrator/Argus.Orchestrator.Tests/ScoreStreamPipelineTests.cs` | Extended with the no-re-backfill-after-restart case |
| `argus/config.yaml` | `version:` bumped from `2.1.8` to `2.1.9` |
| `.planning/phases/15-streaming-state-persistence-warm-up-backfill/15-UAT.md` | New: nine-row checklist mapped to the ROADMAP success criteria, with recorded outcomes |
| GHCR tags | `ghcr.io/krzyl2/argus:2.1.9` and `:latest` |

No new production symbols, env vars, proto fields, or file paths are introduced by this plan.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cross-plan restart and crash cases no single plan owns</name>
  <files>detector/tests/test_restart_resilience.py, orchestrator/Argus.Orchestrator.Tests/ScoreStreamPipelineTests.cs</files>

  <read_first>
    - detector/tests/test_restart_resilience.py (the file being modified; the whole 107-line file — `save_test_model` helper and the existing RES-02 `create_server` startup-gate tests are the shape to extend)
    - detector/tests/test_checkpoint.py (built in 15-01 — reuse its checkpoint-writing helpers rather than duplicating them)
    - detector/argus_detector/server.py (`create_server`'s NOT_SERVING to SERVING gate and the checkpoint-writer attachment from 15-01)
    - detector/argus_detector/model_store.py (`save_checkpoint`/`load_checkpoint`/`load_all_into` as built in 15-01)
    - orchestrator/Argus.Orchestrator.Tests/ScoreStreamPipelineTests.cs (the file being modified; the fakes added in 15-03 Task 3)
    - .planning/phases/15-streaming-state-persistence-warm-up-backfill/15-CONTEXT.md (the nine Success Criteria at the end of the specifics section)
  </read_first>

  <behavior>
    - SC-1 hard kill: a detector subprocess is started against a temp model root, an entity is scored past one checkpoint tick with a short interval, the process is killed with a non-catchable signal so no flush can run, a second `create_server` is built on the same root, and the restored `n_seen` is greater than 0 and no more than one interval's worth of readings behind the last value scored. The assertion is on the bound, not on an exact number — one interval of loss is the accepted design (D-08).
    - SC-6 corrupt checkpoint at startup: a model root holds three entities where one has a `checkpoint.pkl` truncated to garbage bytes, one has a valid checkpoint, and one has a valid versioned model. `create_server` returns without raising, the health service ends at SERVING, and the two healthy entities are present in the registry.
    - SC-6 variant: the same root where the corrupt entity has a valid pickle but a `checkpoint.json` naming a bogus `river_version` — the entity is skipped with a WARN and the others still load.
    - SC-4 idle: two checkpoint ticks with no intervening `score_one` leave the `checkpoint.pkl` mtime and byte length unchanged.
    - SC-8 no re-backfill: a pipeline whose fake detector client returns a `skipped` true `WarmupResponse` is run twice against the same fakes, and the second run's warmup call also reports skipped with no growth in any counter the fake exposes — the orchestrator does not attempt to compensate for a skip.
    - SC-8 variant: a pipeline run where the history source returns rows but the client reports skipped still opens the stream and still primes the frozen detector — the frozen priming is not conditional on the detector accepting the prime.
  </behavior>

  <action>
    Extend `detector/tests/test_restart_resilience.py` rather than creating a new module — that file
    already owns the "restart loads state before SERVING" contract (RES-02) and this phase is extending
    exactly that contract to streaming state. Reuse the existing `_slug` and model-saving helpers, and
    reuse 15-01's checkpoint helpers instead of writing a third way to produce a checkpoint on disk.

    For the hard-kill case, launch the detector as a subprocess the way 15-01's SIGTERM test does, but
    terminate it with a signal the process cannot handle so the flush path is definitively bypassed.
    Configure a short checkpoint interval through the environment so the test does not wait 300 seconds.
    Assert a bound rather than an exact count: the point being proven is that at most one interval is
    lost, not that a specific number survived.

    For the corrupt-checkpoint cases, build the multi-entity root explicitly so the test states which
    entity is expected to fail and which are expected to survive. Assert on the health service's final
    state, not just on the absence of an exception — reaching SERVING with a bad file present is the
    actual requirement (D-09/PERSIST-04).

    For the two orchestrator cases, reuse the fakes 15-03 Task 3 added rather than writing new ones.

    Add nothing to production code in this task. If a case cannot be written because a seam is missing,
    stop and report it rather than widening a production surface to satisfy a test — the phase's
    implementation plans are already merged and their acceptance criteria passed.
  </action>

  <verify>
    <automated>python detector/scripts/gen_proto.py</automated>
    <automated>cd detector; python -m pytest -q</automated>
    <automated>dotnet test orchestrator/Argus.Orchestrator.sln</automated>
  </verify>

  <acceptance_criteria>
    - `cd detector; python -m pytest -q` exits 0 with no skipped tests.
    - `dotnet test orchestrator/Argus.Orchestrator.sln` exits 0 with no skipped tests.
    - A test kills a detector subprocess with a non-catchable signal and asserts the restored `n_seen` is greater than 0 and within one configured interval of the last scored count.
    - A test builds a three-entity model root with one garbage `checkpoint.pkl` and asserts `create_server` returns, the health status for `argus.v1.DetectorService` is SERVING, and both healthy entities are registered.
    - A test builds a root with a valid pickle whose sidecar carries a bogus `river_version` and asserts that entity is absent from the registry while the others are present.
    - A test asserts two checkpoint ticks with no intervening scoring leave the checkpoint file's mtime and size unchanged.
    - A test asserts a `skipped` true `WarmupResponse` on a second pipeline run produces no additional priming attempts.
    - A test asserts the frozen detector is still primed on a run where the detector client reports skipped.
    - `git diff --stat` for this task shows changes only under `detector/tests/` and `orchestrator/Argus.Orchestrator.Tests/` — no production file was touched.
  </acceptance_criteria>

  <done>The three cross-plan criteria (SC-1, SC-6, SC-8) plus the SC-4 idle case have executable assertions, and both suites are green with zero skips.</done>
</task>

<task type="auto">
  <name>Task 2: Version bump and full-suite release gate</name>
  <files>argus/config.yaml, .planning/phases/15-streaming-state-persistence-warm-up-backfill/15-UAT.md</files>

  <read_first>
    - argus/config.yaml (the file being modified; line 3 holds the current version 2.1.8)
    - deploy/build-push.ps1 (the release script the operator runs; confirm the parameter name and that it also writes the version)
    - .planning/phases/15-streaming-state-persistence-warm-up-backfill/15-01-SUMMARY.md through 15-03-SUMMARY.md (recorded decisions to fold into the UAT checklist)
    - .planning/ROADMAP.md (the Phase 15 section — the nine Success Criteria are the authoritative UAT source)
  </read_first>

  <action>
    Bump `version:` in `argus/config.yaml` from `2.1.8` to `2.1.9`. The iron rule from the recorded deploy
    process is that this value must equal the GHCR image tag, because the repo is its own HA add-on
    repository and HA reads `master` for update detection. If `deploy/build-push.ps1` also writes this
    value from its `-Version` parameter, still commit the bumped file so the repository state HA reads is
    correct independent of when the script runs.

    Create `.planning/phases/15-streaming-state-persistence-warm-up-backfill/15-UAT.md` as a
    nine-row table, one row per ROADMAP success criterion, each row carrying: the criterion text, whether
    it is covered by an automated test (naming the test) or requires live observation, the exact steps to
    observe it, the expected result, and an empty outcome column for the operator to fill during Task 3.
    Cite the ROADMAP as the source and do not invent a tenth criterion or reword the nine.

    Mark SC-2, SC-5, and SC-7 as requiring live observation: SC-2 needs a real orchestrator-only restart
    with the UI open, SC-5 needs a real entity configured with a non-default window, and SC-7 needs an
    entity with real InfluxDB history — none of which a unit test can stand in for. Mark SC-1, SC-3,
    SC-4, SC-6, SC-8, and SC-9 as automated, naming the covering test from plans 15-01, 15-03, or Task 1
    above, and note that a live spot-check is still cheap for SC-3 and SC-9.

    Run the full gate before finishing: regenerate stubs, run the detector suite, run the orchestrator
    suite. Do not proceed to Task 3 with any failure or any skipped test — a skipped test is a failed gate
    for this plan.
  </action>

  <verify>
    <automated>python detector/scripts/gen_proto.py</automated>
    <automated>cd detector; python -m pytest -q</automated>
    <automated>dotnet test orchestrator/Argus.Orchestrator.sln</automated>
    <automated>grep -n "^version:" argus/config.yaml</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "^version:" argus/config.yaml` outputs a line containing `2.1.9`.
    - `cd detector; python -m pytest -q` exits 0 and reports 0 skipped.
    - `dotnet test orchestrator/Argus.Orchestrator.sln` exits 0 and reports 0 skipped.
    - `.planning/phases/15-streaming-state-persistence-warm-up-backfill/15-UAT.md` exists and contains exactly nine criterion rows whose text matches the ROADMAP Phase 15 Success Criteria list.
    - Each of the six automated rows names a real test that exists in the repo (verifiable by grepping the named test out of the test files).
    - Each of the three live rows carries concrete observation steps naming a screen or a log source, not a generic instruction.
  </acceptance_criteria>

  <reversibility rating="reversible">A version bump in a tracked file plus a new planning document; nothing published yet.</reversibility>

  <done>Version is 2.1.9, both suites are green with zero skips, and the nine-row UAT checklist is written and traceable to the ROADMAP.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Deploy 2.1.9 and run the nine-criterion live UAT</name>

  <precondition>The operator's workstation is already logged in to ghcr.io with a write:packages PAT, and buildx plus QEMU are installed — these are the one-time prerequisites recorded in the deploy process and this plan does not set them up.</precondition>

  <what-built>
    Streaming HST state now checkpoints to `/data/models/{slug}/hst/checkpoint.pkl` every 300 seconds
    when dirty, flushes on SIGTERM, and restores before the detector reports healthy. The orchestrator no
    longer counts readings — `warmed_up`, `n_seen`, and the window all arrive on the `Verdict`, so a
    configured `window: 50` now governs both real HST calibration and the UI's `x/N` denominator. A new
    `Warmup` RPC primes a cold entity from up to 30 days of InfluxDB history before its stream opens,
    gated detector-side on `n_seen == 0` so restarts never re-feed, and the same history fills the frozen
    detector's window. All four knobs are env-only and default correctly.
  </what-built>

  <how-to-verify>
    Step 1 — build and push. From the repo root on the workstation, run the release script with the
    version parameter set to 2.1.9 (`deploy/build-push.ps1`). The recorded recipe is: the script bumps
    `argus/config.yaml`, runs `dotnet publish` so the Web SDK bundles `wwwroot`, then
    `docker buildx build --platform linux/amd64,linux/arm64 --push` to `ghcr.io/krzyl2/argus:2.1.9` and
    `:latest`. Publish must happen before buildx or the image ships a stale or missing Ingress UI. The
    `-Dev` switch builds amd64 only and is the faster option if the target HA host is amd64.

    Step 2 — release. Commit and push `argus/config.yaml` on `master`, then in Home Assistant go to
    Settings, Add-ons, Argus, Check for updates, Update. Confirm the add-on starts and the Ingress UI
    loads.

    Step 3 — walk the nine rows of
    `.planning/phases/15-streaming-state-persistence-warm-up-backfill/15-UAT.md` and fill in the outcome
    column. The three live-only rows are:

    SC-2 — open the Detectors screen and note the `x/N` warm-up figure for a mid-warm-up entity. Restart
    only the orchestrator (not the whole add-on) and reload the screen. The figure must be unchanged or
    higher, never reset to 0.

    SC-5 — configure a tracked entity with `window: 50` on its `hst` detector, save, and let it take at
    least one reading. The Detectors screen must show a denominator of 50, and the detector log line for
    that entity must report the same window.

    SC-7 — add a tracked entity that has at least 250 points of InfluxDB history and is not already
    checkpointed. On its first live reading the Detectors screen must show it as warmed up, and the
    orchestrator log must carry one primed line naming the point count.

    Cheap live spot-checks worth doing while you are there: restart the whole add-on and confirm the
    warm-up figure did not move backwards at all (SC-3, zero readings lost); and confirm the log carries
    no backfill failure line unless InfluxDB is genuinely unconfigured (SC-9).

    Step 4 — if any row fails, record the observed behavior in the outcome column and report it rather
    than adjusting the criterion. A failed criterion is gap-closure input, not a documentation problem.
  </how-to-verify>

  <resume-signal>Type "approved" when all nine UAT rows are recorded as passing, or describe which rows failed and what you observed.</resume-signal>

  <reversibility rating="costly">Pushing `2.1.9` and moving `:latest` on GHCR, then updating the operator's live HA, is not cleanly undoable — a rollback means publishing a further version. This blocking human checkpoint is the gate; no additional decision checkpoint is inserted because the operator explicitly authorizes the push at this point.</reversibility>

  <done>All nine ROADMAP success criteria are recorded as passing in `15-UAT.md` against live Home Assistant on add-on 2.1.9, or the failing rows are reported with observed behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workstation → GHCR | A locally built multi-arch image is pushed under the operator's PAT |
| GHCR → Home Assistant Supervisor | HA pulls and runs the pushed image based on the version in `master` |
| test subprocesses → temp filesystem | Restart tests spawn real detector processes and write real checkpoint files |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-04-01 | Tampering | Version drift between `argus/config.yaml` and the pushed GHCR tag | medium | mitigate | Task 2 asserts the file reads `2.1.9` via grep before any push, and Task 3 passes the same version to the release script; the recorded iron rule is that the two must match or HA's update detection misfires. |
| T-15-04-02 | Denial of Service | Shipping an image whose UI assets are stale because publish was skipped | medium | mitigate | Task 3's step 1 states the publish-before-buildx ordering explicitly, and step 2 requires confirming the Ingress UI loads before proceeding to UAT. |
| T-15-04-03 | Spoofing | GHCR push credentials | medium | transfer | Handled by the operator's `docker login` with a scoped `write:packages` PAT on their own workstation; declared as a `<precondition>` rather than automated, and no credential is read or stored by this plan. |
| T-15-04-04 | Denial of Service | Restart tests leaving orphaned detector subprocesses or temp model roots | low | mitigate | Subprocess tests use bounded waits and pytest `tmp_path`, so a hung process fails the test rather than blocking the suite, and roots are cleaned up by the fixture. |
| T-15-04-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase (RESEARCH.md Package Legitimacy Audit found nothing to audit — no `[ASSUMED]`, `[SUS]`, or `[SLOP]` entries). |
</threat_model>

<verification>
- `cd detector; python -m pytest -q` exits 0 with 0 skipped.
- `dotnet test orchestrator/Argus.Orchestrator.sln` exits 0 with 0 skipped.
- `grep -n "^version:" argus/config.yaml` reads `2.1.9`.
- `15-UAT.md` has all nine outcome cells filled.
</verification>

<success_criteria>
- All ten Phase 15 requirement IDs are traceable to at least one passing automated test or a recorded live UAT row.
- The nine ROADMAP success criteria are each recorded as passing, or the failures are reported with observed behavior for gap closure.
- Add-on 2.1.9 is live in the operator's Home Assistant and the Ingress UI loads.
</success_criteria>

<output>
Create `.planning/phases/15-streaming-state-persistence-warm-up-backfill/15-04-SUMMARY.md` when done.
Record in it: the final test counts for both suites, the nine UAT outcomes, and anything observed live
that the automated tests did not catch.
</output>
