---
phase: 04-validation-ci-packaging-documentation
plan: 01
type: execute
wave: 2
depends_on: [04-02]
files_modified:
  - orchestrator/Argus.Orchestrator/Config/InputValidator.cs
  - orchestrator/Argus.Orchestrator/Program.cs
  - orchestrator/Argus.Orchestrator/Logging/LogEvents.cs
  - orchestrator/Argus.Orchestrator.Tests/InputValidatorTests.cs
autonomous: true
requirements: [UI-04]

must_haves:
  truths:
    - "A POST /api/sensors/save body with an entity_id that fails ^[a-z0-9_]+\\.[a-z0-9_]+$ is rejected before any write to entities.yaml"
    - "A POST with an out-of-range detector parameter (e.g. window=-5, low_threshold >= high_threshold) is rejected before any write"
    - "A POST with a detector name not in {hst,mad,stl} (case-insensitive) is rejected before any write"
    - "On validation failure the save handler returns the .argus-banner--validation fragment and does NOT call ConfigWriter.WriteAsync or liveCfg.Swap"
    - "User-submitted entity_id and detector-name values are HTML-encoded in InputValidator error strings"
  artifacts:
    - path: "orchestrator/Argus.Orchestrator/Config/InputValidator.cs"
      provides: "Static input validator (entity_id regex + per-type param ranges + unknown-detector allowlist)"
      contains: "public static class InputValidator"
    - path: "orchestrator/Argus.Orchestrator.Tests/InputValidatorTests.cs"
      provides: "Unit coverage for every validation rule in 04-UI-SPEC Validation Rules"
      contains: "public class InputValidatorTests"
  key_links:
    - from: "orchestrator/Argus.Orchestrator/Program.cs"
      to: "InputValidator.Validate"
      via: "call in POST /api/sensors/save after DetectorFieldParser.Parse, before entity-list build"
      pattern: "InputValidator\\.Validate"
  prohibitions:
    - "InputValidator.Validate MUST NOT be invoked on the defaulted entity list (after empty→HST defaulting); it validates the raw parsedDetectors output"
    - "No partial write: ConfigWriter.WriteAsync must not run when validationErrors.Count > 0"
    - "Do NOT replace or modify EntitiesConfigLoader.Validate() (structural validation stays separate)"
---

<objective>
Add server-side INPUT validation that gates the UI save endpoint before any write to
`/data/entities.yaml`. Rejects invalid entity_id format, out-of-range detector parameters,
and unknown detector names with a clear banner and no filesystem side effects (UI-04, SC1).

Purpose: The server is the authoritative validation boundary; a tampered or malformed POST
body must never reach disk or the live pipeline.
Output: `InputValidator` static class, its wiring in the save handler, a new LogEvent, and a
full xUnit suite covering every rule in the 04-UI-SPEC validation contract.
</objective>

<execution_context>
@$HOME/.claude-work-work/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-work-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-validation-ci-packaging-documentation/04-CONTEXT.md
@.planning/phases/04-validation-ci-packaging-documentation/04-RESEARCH.md
@.planning/phases/04-validation-ci-packaging-documentation/04-PATTERNS.md
@.planning/phases/04-validation-ci-packaging-documentation/04-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs. Do not re-explore the codebase for these. -->

From orchestrator/Argus.Orchestrator/Config/EntitiesConfig.cs:
```csharp
public class DetectorConfig
{
    public string Name { get; set; } = string.Empty;
    public Dictionary<string, string> Params { get; set; } = new();
}
```

From orchestrator/Argus.Orchestrator/Web/DetectorFieldParser.cs:
```csharp
public static Dictionary<int, List<DetectorConfig>> Parse(IEnumerable<KeyValuePair<string,string>> formPairs);
// key = entity index (int), value = ordered detector list for that entity
```

Project-standard parse idiom (copy — EntitiesConfig.cs HstParams.From, lines 63-68):
```csharp
int.TryParse(v, out var r)                                     // integer fields
double.TryParse(v, NumberStyles.Any, CultureInfo.InvariantCulture, out var r)  // numeric fields
```

Save handler insertion point — Program.cs line 315 (`var parsedDetectors = DetectorFieldParser.Parse(formPairs);`).
Insert validation AFTER line 315 and BEFORE line 317 (`var snapshotById = registry.GetAll()...`).
`resolvedIds` is already in scope (Program.cs line 306). `logger` is `ILogger<Program>` in scope.
htmx fragment return idiom (Program.cs lines 395, 403): `return Results.Content(html, "text/html");`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: InputValidator static class + LogEvent</name>
  <read_first>
    - orchestrator/Argus.Orchestrator/Config/EntitiesConfig.cs (DetectorConfig shape; HstParams.From lines 49-68 — the TryParse idiom to copy)
    - orchestrator/Argus.Orchestrator/Config/EntitiesConfigLoader.cs (static-class + error-accumulation analog, lines 39-62)
    - orchestrator/Argus.Orchestrator/Web/EntityPickerPage.cs lines 260-268 (WebUtility.HtmlEncode idiom for user strings)
    - orchestrator/Argus.Orchestrator/Logging/LogEvents.cs (7xxx block, lines 45-53 — EventId declaration style)
    - .planning/phases/04-validation-ci-packaging-documentation/04-UI-SPEC.md lines 138-188 (exact rules) and lines 494-521 (exact copy)
  </read_first>
  <behavior>
    - Valid HST config (window=250, n_trees=25, high_threshold=0.7, low_threshold=0.3, min_consecutive=3, frozen_window=10, frozen_variance_threshold=0.001) → no errors
    - entity_id "sensor.UPPER", "sensor", "sensor.bad id", "Sensor.x" → each yields an error
    - entity_id "sensor.salon_temperatura" → no error
    - HST window=0 / n_trees=0 / min_consecutive=0 / frozen_window=0 → error each (integer ≥ 1)
    - HST high_threshold=1.5 or low_threshold=-0.1 → error (out of [0,1])
    - HST high_threshold=0.3 with low_threshold=0.7 (low >= high) → error (cross-field)
    - HST high_threshold present, low_threshold absent → NO cross-field crash (TryGetValue skip; Pitfall 5)
    - HST frozen_variance_threshold=-0.001 → error (must be ≥ 0)
    - MAD threshold=0 or -1 → error (> 0); MAD window=0 → error (≥ 1)
    - STL period=1 → error (≥ 2); STL threshold=0 → error (> 0)
    - Detector name "xgboost" / "" → "Unknown detector type" error; param validation skipped for that detector
    - Detector name "HST"/"Mad"/"STL" (mixed case) → accepted (case-insensitive)
    - Error message for unknown detector contains the HTML-encoded submitted name
    - Locale independence: "0.7" parses identically regardless of machine culture (InvariantCulture)
  </behavior>
  <action>
    Create `orchestrator/Argus.Orchestrator/Config/InputValidator.cs` as `public static class InputValidator`
    in namespace `Argus.Orchestrator.Config`. Public method
    `List<string> Validate(IEnumerable<string> resolvedIds, Dictionary<int, List<DetectorConfig>> parsedDetectors)`.
    Private static readonly `EntityIdRegex` = `^[a-z0-9_]+\.[a-z0-9_]+$` (RegexOptions.Compiled) and
    `KnownDetectors = { "hst","mad","stl" }`. Implement private `TryGetDouble`/`TryGetInt` helpers using the
    HstParams.From TryParse idiom (NumberStyles.Any + CultureInfo.InvariantCulture for doubles). Per-type
    validators `ValidateHst`/`ValidateMad`/`ValidateStl` enforce the exact ranges in 04-UI-SPEC Validation
    Rules (integer ≥ N, number > 0, number ≥ 0, in [0,1], cross-field high > low). Cross-field high/low uses
    TryGetValue for BOTH keys and skips the comparison if either is absent (Pitfall 5). Every error string that
    interpolates a user value (entity_id, unknown detector name) MUST wrap it in `WebUtility.HtmlEncode(...)`.
    Error copy MUST match 04-UI-SPEC Copywriting Contract exactly. Add
    `public static readonly EventId UiValidationBlocked = new(7008, nameof(UiValidationBlocked));` to
    LogEvents.cs in the 7xxx block (7003 UiSaveFailed exists; use a new 7008 to distinguish input-validation
    rejection from generic save failure). Do NOT add logging inside InputValidator — logging is at the call site.
  </action>
  <verify>
    <automated>cd orchestrator && dotnet test Argus.Orchestrator.Tests/Argus.Orchestrator.Tests.csproj --filter "FullyQualifiedName~InputValidatorTests"</automated>
  </verify>
  <acceptance_criteria>
    - `InputValidator.cs` declares `public static class InputValidator` with the exact `Validate(IEnumerable<string>, Dictionary<int, List<DetectorConfig>>)` signature returning `List<string>`.
    - `InputValidatorTests.cs` exists in `Argus.Orchestrator.Tests` with at least one test per behavior bullet above; all pass.
    - `grep` for `WebUtility.HtmlEncode` in InputValidator.cs returns ≥ 2 matches (entity_id + detector name paths).
    - `LogEvents.cs` contains `UiValidationBlocked = new(7008, ...)`.
    - Cross-field test with only high_threshold present does NOT throw KeyNotFoundException.
  </acceptance_criteria>
  <done>
    InputValidator enforces every 04-UI-SPEC rule with exact copy; UiValidationBlocked LogEvent added;
    full test suite green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire InputValidator into the save handler</name>
  <read_first>
    - orchestrator/Argus.Orchestrator/Program.cs lines 279-405 (the POST /api/sensors/save handler; insertion point at line 315→317)
    - orchestrator/Argus.Orchestrator.Tests/SaveEndpointDetectorParsingTests.cs (existing save-endpoint test analog for a regression check pattern)
    - .planning/phases/04-validation-ci-packaging-documentation/04-PATTERNS.md lines 216-231 (call-site wiring pattern)
  </read_first>
  <action>
    In `Program.cs`, inside the `POST /api/sensors/save` handler, AFTER
    `var parsedDetectors = DetectorFieldParser.Parse(formPairs);` (line 315) and BEFORE the
    `snapshotById`/entity-list build (line 317), call
    `var validationErrors = InputValidator.Validate(resolvedIds, parsedDetectors);`. When
    `validationErrors.Count > 0`: log `logger.LogWarning(LogEvents.UiValidationBlocked, "UI save blocked: {ErrorCount} validation error(s)", validationErrors.Count);`
    and `return Results.Content(EntityPickerPage.BuildValidationBanner(validationErrors.Count), "text/html");`
    — returning BEFORE any `writer.WriteAsync`, lock-file write, or `liveCfg.Swap`. This is the input-validation
    gate; the existing structural Validate-before-Swap in EntitiesConfigLoader.Load stays untouched.
    `EntityPickerPage.BuildValidationBanner(int)` is authored in Plan 04-02 — if that plan has not yet landed at
    execution time, add a minimal local `BuildValidationBanner` stub is NOT permitted; instead this plan is Wave 1
    and 04-02 is Wave 1 too, so coordinate: reference `EntityPickerPage.BuildValidationBanner`. If the symbol is
    missing at compile time, that is the expected cross-plan contract from 04-02 (see key_links). Do not duplicate it.

    ALSO (success-path wiring — required for SC5's warm-up note to render): 04-02 changes
    `EntityPickerPage.BuildSuccessBanner` to signature `BuildSuccessBanner(int count, bool hasHst = false)`.
    Upgrade the EXISTING success-banner call site in this same handler (Program.cs ~line 395, currently
    `BuildSuccessBanner(entities.Count)` / the `html` returned at line 395) to pass the real hasHst value:
    `EntityPickerPage.BuildSuccessBanner(entities.Count, entities.Any(e => e.Detectors.Any(d => d.Name.Equals("hst", StringComparison.OrdinalIgnoreCase))))`.
    Without this, hasHst defaults to false and the ~4-minute HST warm-up note (04-02 must_have truth, SC5)
    silently never appears in the live UI.
  </action>
  <verify>
    <automated>cd orchestrator && dotnet build Argus.Orchestrator/Argus.Orchestrator.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "InputValidator.Validate" Program.cs` shows the call positioned before the `snapshotById`/`entities` list construction and before `writer.WriteAsync`.
    - The validation-failure branch returns `Results.Content(...BuildValidationBanner..., "text/html")` and contains no `WriteAsync`/`Swap`/lock-file call.
    - `grep -n "LogEvents.UiValidationBlocked" Program.cs` returns the warn call.
    - Existing SaveEndpoint tests still pass: `dotnet test --filter "FullyQualifiedName~SaveEndpoint"`.
    - `grep -n "BuildSuccessBanner" Program.cs` shows the call site passing a second `hasHst` argument (not the single-arg overload) — the success banner receives the real HST-present flag.
  </acceptance_criteria>
  <done>
    Save handler rejects invalid input via BuildValidationBanner with zero filesystem side effects; orchestrator
    compiles; existing save-endpoint tests remain green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /api/sensors/save | Untrusted form body (entity_ids, detector names, param values) crosses into server config write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | POST body entity_id | mitigate | `InputValidator` EntityIdRegex `^[a-z0-9_]+\.[a-z0-9_]+$` rejects malformed ids before write (Task 1) |
| T-04-02 | Tampering | POST body detector name | mitigate | `InputValidator` KnownDetectors allowlist {hst,mad,stl} rejects unknown before write (Task 1) |
| T-04-03 | Tampering | POST body param values | mitigate | `InputValidator` per-type numeric range checks reject out-of-range before write (Task 1) |
| T-04-04 | Information Disclosure / XSS | Error banner text | mitigate | `WebUtility.HtmlEncode` on all user-submitted strings in error messages (Task 1) |
| T-04-05 | Denial of Service | Partial/corrupt config write | mitigate | Validation gates BEFORE ConfigWriter.WriteAsync — no partial write reaches disk (Task 2) |
</threat_model>

<verification>
- Server rejects invalid entity_id / out-of-range params / unknown detector name before any write (SC1).
- No partial write on rejection (ConfigWriter.WriteAsync not reached).
- All new + existing save-endpoint and validator tests pass.
</verification>

<success_criteria>
- `InputValidator` enforces every 04-UI-SPEC rule with exact copy and HTML-encoded user strings.
- Save handler gates on validation before write; returns the validation banner fragment.
- `dotnet build` succeeds; `InputValidatorTests` and `SaveEndpoint*` tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/04-validation-ci-packaging-documentation/04-01-SUMMARY.md`
</output>
