---
phase: 04-validation-ci-packaging-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - orchestrator/Argus.Orchestrator/Web/EntityPickerPage.cs
  - orchestrator/Argus.Orchestrator/wwwroot/css/argus.css
  - orchestrator/Argus.Orchestrator.Tests/EntityPickerPageTests.cs
autonomous: true
requirements: [UI-04]

must_haves:
  truths:
    - "Every param input carries a sibling .argus-param-field__error-msg span (empty, hidden) and aria-describedby/aria-invalid attributes on initial page load and new-entry fragments"
    - "Client-side inline JS validates numeric param fields on blur/input/submit using PARAM_RULES that mirror the server ranges exactly"
    - "When any param field is invalid the Save button gains the disabled attribute; when all valid the disabled attribute is removed"
    - "The server returns BuildValidationBanner (.argus-banner--validation) with the invalid-field count as an htmx fragment"
    - "The save-success banner renders the .argus-warmup-note only when the saved config contains at least one HST detector"
  artifacts:
    - path: "orchestrator/Argus.Orchestrator/Web/EntityPickerPage.cs"
      provides: "BuildValidationBanner, BuildSuccessBanner(count,hasHst), error-msg spans, inline validation JS"
      contains: "public static string BuildValidationBanner"
    - path: "orchestrator/Argus.Orchestrator/wwwroot/css/argus.css"
      provides: "Phase 4 validation CSS: .argus-param-field--error, .argus-param-field__error-msg, .argus-banner--validation, .argus-warmup-note"
      contains: "argus-param-field--error"
  key_links:
    - from: "orchestrator/Argus.Orchestrator/Web/EntityPickerPage.cs"
      to: "#argus-picker-form"
      via: "inline <script> event delegation (focusout/input/submit) toggling .argus-param-field--error and Save disabled"
      pattern: "PARAM_RULES"
  prohibitions:
    - "No external JS/CSS files, no CDN, no eval/Function/dynamic import (air-gapped, no build step)"
    - "Do NOT redefine any existing --color-*/--space-*/--font-* token or existing BEM class; only ADD the four Phase 4 modifiers"
    - "Inline JS script must stay under 2 KB minified (04-UI-SPEC size budget)"
    - "Detector-name <select> and entity_id checkboxes are NOT freeform — do NOT add client-side entity_id or detector-name validation"
  non_inferable:
    - "Visual confirmation that invalid fields turn red, the inline message shows, and Save greys out in a live HA Ingress browser session (routed to human_needed)"
---

<objective>
Add the client-side validation layer and all UI error states: per-field error spans + ARIA on
every param input, an inline JS validator that mirrors the server ranges and disables Save while
any field is invalid, a server validation-rejection banner, and the HST warm-up disclosure on
save success (UI-04 SC2, warm-up disclosure).

Purpose: Give the user immediate, accessible feedback before submit while keeping the server the
authoritative gate. Discloses the ~4-minute HST warm-up so users understand the gradual resume.
Output: Extended `EntityPickerPage.cs` (banners, error spans, id-format migration, inline JS),
four new CSS modifiers in `argus.css`, and unit coverage for the new HTML builders.
</objective>

<execution_context>
@$HOME/.claude-work-work/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-work-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-validation-ci-packaging-documentation/04-CONTEXT.md
@.planning/phases/04-validation-ci-packaging-documentation/04-RESEARCH.md
@.planning/phases/04-validation-ci-packaging-documentation/04-PATTERNS.md
@.planning/phases/04-validation-ci-packaging-documentation/04-UI-SPEC.md

<interfaces>
<!-- Current builders to extend. Do not re-explore. -->

From orchestrator/Argus.Orchestrator/Web/EntityPickerPage.cs:
```csharp
public static string BuildSuccessBanner(int count);          // lines 233-241 — EXTEND to (int count, bool hasHst = false)
public static string BuildErrorBanner(string reason);        // lines 260-268 — copy structure for BuildValidationBanner
public static string BuildDetectorEntry(int entityIdx, int detIdx, DetectorConfig det);  // public static
private static string BuildHstParamGrid(int entityIdx, int detIdx, Dictionary<string,string> storedParams);  // lines 407-457
private static string BuildMadParamGrid(...);   // lines 459-479
private static string BuildStlParamGrid(...);   // lines 481-507
```

Current param input HTML (id/label format to MIGRATE) — EntityPickerPage.cs lines 420-424:
```
id="p_{entityIdx}_{detIdx}_{key}"   →   id="param-{entityIdx}-{detIdx}-{key}"
for="p_..."                         →   for="param-{entityIdx}-{detIdx}-{key}"
```
There are NO CSS/JS selectors targeting the old `p_` id (verified: argus.css has no `#p_` selector), so the rename is safe.

Exact error-span markup, PARAM_RULES object, event bindings, banner copy, and CSS rule values
are FULLY SPECIFIED in 04-UI-SPEC.md (New Component Classes; Inline JS Validation Contract;
Copywriting Contract) — follow them verbatim.

Success-banner save path: Program.cs line 395 calls `BuildSuccessBanner(entities.Count)`. The
`hasHst` argument (`entities.Any(e => e.Detectors.Any(d => d.Name.Equals("hst", StringComparison.OrdinalIgnoreCase)))`)
will be wired at the Program.cs call site in Plan 04-01 (Wave 2) OR here if this plan also edits
Program.cs — but Program.cs is owned by Plan 04-01/04-03. Therefore: make `hasHst` a defaulted
parameter (`bool hasHst = false`) so the existing single-arg call site still compiles; the
call-site upgrade to pass the real hasHst value is done in Plan 04-01 Task 2 (which already edits
Program.cs). Note this dependency in the SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Param error spans + ARIA + id-format migration + new CSS modifiers</name>
  <read_first>
    - orchestrator/Argus.Orchestrator/Web/EntityPickerPage.cs lines 407-507 (the three Build*ParamGrid methods to migrate)
    - orchestrator/Argus.Orchestrator/wwwroot/css/argus.css (existing .argus-param-field structure; .argus-banner--reloading modifier precedent; .argus-btn[disabled] rule; --color-status-error token)
    - .planning/phases/04-validation-ci-packaging-documentation/04-UI-SPEC.md lines 191-317 (exact HTML markup + CSS values for the four new classes)
    - .planning/phases/04-validation-ci-packaging-documentation/04-PATTERNS.md lines 271-376 (id-rename + CSS insertion patterns)
  </read_first>
  <action>
    In `EntityPickerPage.cs` `BuildHstParamGrid`/`BuildMadParamGrid`/`BuildStlParamGrid`: (1) migrate every
    param input `id` and its label `for` from `p_{ei}_{di}_{key}` to `param-{ei}-{di}-{key}`; (2) add
    `aria-describedby="param-{ei}-{di}-{key}-err"` and `aria-invalid="false"` to each
    `input.argus-param-field__input`; (3) after each input add
    `<span class="argus-param-field__error-msg" id="param-{ei}-{di}-{key}-err" role="alert" aria-live="assertive"></span>`
    (empty). These spans render on ALL param fields in every response (initial page, new-entry fragment via
    BuildDetectorEntry). In `argus.css`, append a `/* ── Phase 4: Validation Error States ── */` section with
    exactly the four rules from 04-UI-SPEC: `.argus-param-field__error-msg` (display:none default + label
    typography + --color-status-error + margin-top var(--space-xs)), `.argus-param-field--error
    .argus-param-field__input` (border-color --color-status-error, outline none) + its `:focus` override,
    `.argus-param-field--error .argus-param-field__error-msg` (display:block), `.argus-banner--validation`
    (background --color-status-error, color #fff), and `.argus-warmup-note` (label typography,
    --color-text-secondary, margin-top var(--space-xs)). Do NOT add a new disabled-button rule — the existing
    `.argus-btn[disabled]` covers it. Use only existing tokens.
  </action>
  <verify>
    <automated>cd orchestrator && dotnet build Argus.Orchestrator/Argus.Orchestrator.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - No `p_{` / `for="p_` occurrences remain in the three Build*ParamGrid methods: `grep -n 'id="p_\|for="p_' EntityPickerPage.cs` returns nothing in those methods.
    - Each param input has `aria-describedby="param-` and `aria-invalid="false"`; a sibling `argus-param-field__error-msg` span with matching `id="param-...-err"` follows it.
    - `argus.css` contains all four selectors: `.argus-param-field__error-msg`, `.argus-param-field--error .argus-param-field__input`, `.argus-banner--validation`, `.argus-warmup-note`.
    - No existing token or class definition is modified (only additions after the Phase 3 CSS block).
    - Project compiles.
  </acceptance_criteria>
  <done>
    Every param field emits an ARIA-linked error span with the migrated id format; four Phase 4 CSS modifiers
    added using only existing tokens; build green.
  </done>
</task>

<task type="auto">
  <name>Task 2: BuildValidationBanner, warm-up-aware BuildSuccessBanner, inline validation JS</name>
  <read_first>
    - orchestrator/Argus.Orchestrator/Web/EntityPickerPage.cs lines 233-268 (BuildSuccessBanner + BuildErrorBanner templates to copy/extend) and BuildFullPage end (script insertion point before </body>)
    - orchestrator/Argus.Orchestrator.Tests/EntityPickerPageTests.cs (existing builder-test analog)
    - .planning/phases/04-validation-ci-packaging-documentation/04-UI-SPEC.md lines 262-343 (banner + warm-up markup), 346-383 (inline JS contract + PARAM_RULES), 494-521 (exact copy)
    - .planning/phases/04-validation-ci-packaging-documentation/04-PATTERNS.md lines 233-319 (banner + JS block patterns; C# brace-escaping note)
  </read_first>
  <action>
    Add `public static string BuildValidationBanner(int errorCount)` returning the
    `<div class="argus-banner argus-banner--validation" role="alert" aria-live="assertive">` fragment with copy
    `Save blocked: {N} field(s) have invalid values. Correct the highlighted fields and try again.`. Change
    `BuildSuccessBanner(int count)` to `BuildSuccessBanner(int count, bool hasHst = false)`; when `hasHst` is
    true append a `<p class="argus-warmup-note">` with the exact warm-up copy from 04-UI-SPEC
    (`HST detectors need ~4 minutes of readings to warm up (window=250 at ~1 reading/s). Anomaly scores will be
    low until warm-up completes.`); when false render the unchanged success message. Add an inline `<script>`
    block at the very bottom of `BuildFullPage` (after `</footer>`, before `</body>`) implementing the
    04-UI-SPEC Inline JS Validation Contract: a `PARAM_RULES` object (window/n_trees/high_threshold/
    low_threshold/min_consecutive/frozen_window/frozen_variance_threshold/threshold/period) mirroring server
    ranges, event delegation on `#argus-picker-form` for `focusout` (validate the field), `input` (re-validate a
    field already in error), and `submit` (validate all; `event.preventDefault()` if any invalid — do NOT
    manually fire htmx), toggling `.argus-param-field--error` + `aria-invalid` + the error-msg text, and
    setting/removing the Save button `disabled` attribute based on the invalid-field count. Cross-field high/low
    handled per PARAM_RULES `cross` keys. Because the JS uses `{` literals, author the script segment as a
    non-interpolated raw string (or double-brace `{{`) to avoid C# interpolation conflicts. Keep the whole
    script under 2 KB. Do NOT validate entity_id or detector-name client-side (checkbox/select — not freeform).
  </action>
  <verify>
    <automated>cd orchestrator && dotnet test Argus.Orchestrator.Tests/Argus.Orchestrator.Tests.csproj --filter "FullyQualifiedName~EntityPickerPageTests"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "BuildValidationBanner" EntityPickerPage.cs` shows a public static method returning the `argus-banner--validation` fragment with the exact banner copy.
    - `BuildSuccessBanner` signature is `(int count, bool hasHst = false)`; a unit test asserts the warm-up note IS present when hasHst=true and ABSENT when hasHst=false.
    - A unit test asserts `BuildValidationBanner(2)` output contains `Save blocked: 2 field(s)` and `argus-banner--validation`.
    - The rendered full page contains `PARAM_RULES` and an event listener on `#argus-picker-form`; the `<script>` is inline (no `src=`).
    - Script body (rules + logic) is under 2 KB (inspect).
    - `EntityPickerPageTests` all pass.
  </acceptance_criteria>
  <done>
    Validation banner + warm-up-aware success banner + inline mirror-JS validator authored and unit-tested;
    Save disables while invalid; test suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server-rendered HTML → browser DOM | User-submitted strings (param values, counts) interpolated into error/success banners |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | XSS | BuildValidationBanner / warm-up note | mitigate | Banner interpolates only a server-computed integer count and static copy — no raw user string; any user string reused here goes through WebUtility.HtmlEncode (inherited T-02-07/T-03-11) |
| T-04-07 | Tampering (client bypass) | inline JS validator | accept | Client validation is a convenience mirror only; server (Plan 04-01) is the authoritative gate — a bypassed client check is caught server-side |
| T-04-08 | Injection via inline script | inline `<script>` block | mitigate | No `eval`/`Function`/dynamic import/network; static authored JS only (Registry Safety contract) |
</threat_model>

<verification>
- Error spans + ARIA present on all param fields; four CSS modifiers added with existing tokens only.
- BuildValidationBanner + warm-up-aware BuildSuccessBanner unit-tested.
- Inline JS mirrors server ranges, disables Save while invalid; under 2 KB; no external deps.
- Live-HA visual confirmation is a human_needed item (non_inferable).
</verification>

<success_criteria>
- Client validation mirrors server rules; Save disabled + fields highlighted while invalid (SC2).
- Warm-up disclosure appears on HST save success.
- `dotnet build` + `EntityPickerPageTests` pass.
</success_criteria>

<output>
After completion, create `.planning/phases/04-validation-ci-packaging-documentation/04-02-SUMMARY.md`
</output>
