---
phase: 04-validation-ci-packaging-documentation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/build.yml
  - argus/DOCS.md
autonomous: true
requirements: [DOCS-02]

must_haves:
  truths:
    - "The CI build fails if wwwroot/js/htmx.min.js or wwwroot/css/argus.css is missing from the dotnet publish output"
    - "The existing per-arch <2 GB compressed-size gate remains the single image-size gate (no second gate added)"
    - "DOCS.md has a top-level '## Using the Ingress UI' section covering opening the UI, selecting entities, and assigning detectors"
    - "DOCS.md documents 'apply without restart' including the ~4-minute HST warm-up (window=250 at ~1 reading/s/entity) and notes MAD/STL have no comparable warm-up"
    - "DOCS.md documents recovering a corrupted /data/entities.yaml including the .ui_config_present lock-file reset path"
  artifacts:
    - path: ".github/workflows/build.yml"
      provides: "CI assertion step: wwwroot assets present in publish output before docker build"
      contains: "wwwroot/js/htmx.min.js"
    - path: "argus/DOCS.md"
      provides: "Ingress UI workflow section (open/select/assign/apply/recover)"
      contains: "Using the Ingress UI"
  key_links:
    - from: ".github/workflows/build.yml"
      to: "orchestrator/publish/wwwroot"
      via: "test -f assertion step inserted after 'Publish orchestrator' and before docker build"
      pattern: "test -f orchestrator/publish/wwwroot"
  prohibitions:
    - "Do NOT add a second image-size gate — reuse the existing <2 GB per-arch gate (build.yml lines 117-141)"
    - "Do NOT remove or rewrite the existing YAML-based Configuration section — the Ingress UI section is ADDITIVE"
    - "Warm-up figure MUST state the ~4-minute derivation from window=250 at ~1 reading/s/entity"
  non_inferable:
    - "The CI wwwroot assertion actually passing on a real GitHub Actions run (dotnet publish emits wwwroot; and the resulting multi-arch image stays <2 GB) — routed to human_needed (no HA Supervisor / Actions runner in this environment)"
---

<objective>
Guard the UI assets in the image pipeline and document the full Ingress UI workflow (DOCS-02, SC3,
SC5). Add a CI assertion that `dotnet publish` emitted `wwwroot/js/htmx.min.js` +
`wwwroot/css/argus.css` before the Docker build, reuse the existing <2 GB gate, and add a DOCS.md
"Using the Ingress UI" section covering open/select/assign/apply-without-restart (with the ~4-minute
HST warm-up disclosure) and corrupted-config recovery.

Purpose: Ensure the multi-arch image always bundles the UI, and give users a complete
zero-YAML configuration guide.
Output: One CI assertion step in build.yml; an additive Ingress UI section in DOCS.md.
</objective>

<execution_context>
@$HOME/.claude-work-work/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-work-work/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-validation-ci-packaging-documentation/04-CONTEXT.md
@.planning/phases/04-validation-ci-packaging-documentation/04-RESEARCH.md
@.planning/phases/04-validation-ci-packaging-documentation/04-PATTERNS.md

<interfaces>
<!-- CI + docs facts the executor needs. -->

build.yml (verified):
- "Publish orchestrator" step (lines 29-33): `dotnet publish ... -o orchestrator/publish/`
- "Set up QEMU" step (line 34) immediately follows → INSERT the assertion between them
- Existing size gate: image-facts job "Gate — compressed image size < 2 GB per arch" (lines 117-141) — REUSE, do not duplicate
- argus/Dockerfile line 51: `COPY orchestrator/publish/ /opt/argus/orchestrator/` → picks up wwwroot automatically

DOCS.md (verified): 269 lines; sections in order include `## Installation`, `## Configuration`,
`## Troubleshooting`, `## Support`. Insert `## Using the Ingress UI` AFTER `## Installation` and
BEFORE `## Configuration`. Keep the existing YAML Configuration section intact.

Corrupted-config recovery facts (verified against code — 04-RESEARCH Q5):
- `.ui_config_present` lock file written after each successful UI save (Program.cs line 379-380)
- `gen-entities.py` skips overwriting entities.yaml when `.ui_config_present` exists
- `EntitiesConfigLoader.Load` throws FileNotFoundException if entities.yaml missing; InvalidOperationException on structural error → orchestrator crashes at startup → add-on restarts (s6)
- Empty (valid) entities.yaml does NOT crash — logs warning, empty pipeline (Phase 1 decision)
- Reset path: delete /data/entities.yaml AND /data/.ui_config_present, restart → empty pipeline → re-configure via UI

Warm-up derivation (04-UI-SPEC): ~4 minutes = River HST window=250 at ~1 reading/second per entity;
MAD/STL are batch detectors trained on historical data — no comparable warm-up.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CI wwwroot presence assertion (reuse existing size gate)</name>
  <read_first>
    - .github/workflows/build.yml lines 20-40 (Publish orchestrator + Set up QEMU steps — insertion point) and lines 117-141 (existing <2 GB gate — must remain the only size gate)
    - .planning/phases/04-validation-ci-packaging-documentation/04-PATTERNS.md lines 382-403 (CI step insertion pattern)
  </read_first>
  <action>
    In `.github/workflows/build.yml`, insert a new step named `Assert wwwroot assets present in publish output`
    immediately AFTER the `Publish orchestrator` step (line 33) and BEFORE `Set up QEMU` (line 34), matching the
    surrounding YAML indentation (step under `build-and-push.steps`). The step uses `shell: bash` and runs
    `test -f orchestrator/publish/wwwroot/js/htmx.min.js` and `test -f orchestrator/publish/wwwroot/css/argus.css`,
    each failing the build with `exit 1` and a clear `FAIL:` message if absent, ending with an `OK:` echo. Do NOT
    add or modify any size gate — the existing `Gate — compressed image size < 2 GB per arch` (lines 117-141)
    already covers the UI assets (~2.5 KB added; negligible). Leave all other steps unchanged.
  </action>
  <verify>
    <automated>python -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/build.yml')); steps=d['jobs']['build-and-push']['steps']; names=[s.get('name') for s in steps]; assert 'Assert wwwroot assets present in publish output' in names, names; i=names.index('Assert wwwroot assets present in publish output'); assert names[i-1]=='Publish orchestrator', names; assert names[i+1]=='Set up QEMU', names; print('OK: assertion step positioned correctly')"</automated>
  </verify>
  <acceptance_criteria>
    - build.yml is valid YAML and the new step sits between `Publish orchestrator` and `Set up QEMU`.
    - The step body contains `test -f orchestrator/publish/wwwroot/js/htmx.min.js` and `test -f orchestrator/publish/wwwroot/css/argus.css` with `exit 1` on failure.
    - Exactly one image-size gate exists: `grep -c "compressed image size < 2 GB" build.yml` returns 1.
  </acceptance_criteria>
  <done>
    CI fails fast when UI assets are missing from publish output; the single existing <2 GB gate is unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: DOCS.md Ingress UI section</name>
  <read_first>
    - argus/DOCS.md (full structure — Installation numbered-step style lines ~42-56; Troubleshooting bullet/code style lines ~217-264; Configuration section that must stay intact)
    - .planning/phases/04-validation-ci-packaging-documentation/04-PATTERNS.md lines 407-443 (DOCS section outline + copy patterns)
    - .planning/phases/04-validation-ci-packaging-documentation/04-RESEARCH.md lines 463-511 (verified recovery steps + warm-up derivation)
  </read_first>
  <action>
    In `argus/DOCS.md`, insert a new top-level `## Using the Ingress UI` section AFTER `## Installation` and
    BEFORE `## Configuration`, keeping the existing YAML Configuration section unchanged (additive only). Include
    a lead-in note that the UI replaces manual YAML editing and Argus can be configured entirely via UI with zero
    manual YAML. Add subsections: `### Opening the UI` (Home Assistant → the add-on → "Open Web UI"; Ingress-only,
    no exposed port); `### Selecting Entities` (the live numeric-sensor list, filtering, checkbox selection);
    `### Assigning Detectors` (per-entity detector disclosure, add/remove detectors, editable HST/MAD/STL
    parameters, validation blocking Save on invalid values); `### Applying Changes (No Restart Required)`
    explaining pipeline reload without add-on restart AND stating the ~4-minute HST warm-up derived from
    window=250 at ~1 reading/second per entity (anomaly scores low until warm-up completes), and noting MAD/STL
    are batch detectors with no comparable warm-up; `### Recovering a Corrupted Configuration` with the verified
    steps — read the add-on Log tab to identify the YAML problem; Option A fix the YAML via SSH and restart;
    Option B reset by deleting `/data/entities.yaml` AND `/data/.ui_config_present` then restart (orchestrator
    starts with an empty pipeline; re-configure via UI); explain the `.ui_config_present` lock file prevents
    gen-entities.py from overwriting UI config on restart. Match the existing DOCS.md numbered-step + bold
    HA-navigation-term style. English only (D8 applies to HA friendly-names, not docs).
  </action>
  <verify>
    <automated>grep -q "## Using the Ingress UI" argus/DOCS.md && grep -q "Open Web UI" argus/DOCS.md && grep -qi "warm.up" argus/DOCS.md && grep -q "window=250" argus/DOCS.md && grep -q ".ui_config_present" argus/DOCS.md && grep -q "## Configuration" argus/DOCS.md && echo "OK: DOCS.md Ingress UI section present and Configuration section retained"</automated>
  </verify>
  <acceptance_criteria>
    - `## Using the Ingress UI` heading exists, positioned after `## Installation` and before `## Configuration`.
    - The existing YAML `## Configuration` section is retained unchanged.
    - Subsections exist for opening the UI, selecting entities, assigning detectors, applying changes without restart, and recovering a corrupted configuration.
    - The apply-without-restart subsection states the ~4-minute HST warm-up with the `window=250` at ~1 reading/s derivation and notes MAD/STL have no comparable warm-up.
    - The recovery subsection references deleting `/data/entities.yaml` AND `/data/.ui_config_present`.
  </acceptance_criteria>
  <done>
    DOCS.md documents the complete zero-YAML Ingress UI workflow including warm-up disclosure and corrupted-config
    recovery; the YAML Configuration section is preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI build pipeline → published image | Missing UI assets would ship a broken image; docs guide user recovery of a corrupted config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Denial of Service | Image ships without UI assets | mitigate | CI `test -f` assertion fails the build if htmx.min.js/argus.css absent from publish output (Task 1) |
| T-04-14 | Denial of Service | Image exceeds size budget | mitigate | Existing per-arch <2 GB gate retained; UI assets add ~2.5 KB — negligible (Task 1) |
| T-04-15 | Information Disclosure | Docs recovery steps exposing internals | accept | DOCS.md describes only file paths under /data the operator already controls; no secrets disclosed |
</threat_model>

<verification>
- CI assertion fails the build when UI assets are missing from publish output.
- Single <2 GB per-arch gate retained (no duplicate).
- DOCS.md Ingress UI section covers open/select/assign/apply (with warm-up)/recover; YAML section intact.
- CI passing on a real Actions run and image <2 GB confirmation is a human_needed item (non_inferable).
</verification>

<success_criteria>
- build.yml asserts wwwroot assets present before docker build; existing size gate reused (SC3).
- DOCS.md documents the full Ingress UI workflow incl. warm-up + recovery (SC5, DOCS-02).
</success_criteria>

<output>
After completion, create `.planning/phases/04-validation-ci-packaging-documentation/04-04-SUMMARY.md`
</output>
